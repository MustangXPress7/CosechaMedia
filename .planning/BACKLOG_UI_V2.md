# Backlog de Refinamiento UI (v2.1)

Este documento registra la deuda técnica y de UX identificada tras la implementación de la reubicación de controles (v2). Estos ítems deben resolverse antes de dar por avanzar a nuevas funcionalidades.

## 🔴 Prioridad Alta (Bloqueantes de UX / Errores de Flujo)

| ID | Punto | Descripción | Acción Técnica Sugerida | Estado |
|----|--------|-------------|-------------------------|--------|
| B-01 | **Ubicación Volcado Selectivo** | El volcado selectivo está en "Post-ingesta", pero es una acción de *pre-ingesta* (filtrar archivos por fecha antes de copiar). | Mover el botón `btn_selective_dump` al flujo de preparación de ingesta. | ✅ Hecho |
| B-02 | **Nombre de Origen en Sesión** | Las sesiones muestran el ID/ruta técnica pero no el nombre legible del origen. | Modificar la carga de la tabla de sesiones para incluir el nombre del origen desde la DB. | ✅ Hecho |
| B-03 | **Visibilidad Descripción Proyecto** | La descripción del proyecto no es visible o no se refleja correctamente en la UI. | Verificar binding de `desc_input` y su persistencia/carga desde la base de datos. | ✅ Hecho |
| B-13 | **Menú conceptual del dispositivo en "Ruta de origen"** | Error de concepto: el QR (WiFi) / FTP se muestra en la columna "Contenido", en la misma zona que el "Volcado selectivo…" (`main_window.py:448-451`), pero el menú conceptual del dispositivo debería mostrarse en la columna "Ruta de origen" — como ya hace MTP, cuya caché sí aparece en esa columna (`source_path` = `device_cache_dir`, `main_window.py:250`). | Mover el menú conceptual del dispositivo (QR/FTP/config) a la columna 0 junto al path en `_refresh_source_list` (`main_window.py:2044`) y dejar "Contenido" solo para el filtro de contenido (`_build_content_button`, `main_window.py:2061-2088`). | ⏳ Pendiente |
| B-14 | **Modo delicado por dispositivo** | Error de concepto: "Modo delicado" se aplica por sesión (`chk_session_delicate`, `main_window.py:533-535`, guardado vía `_save_session_override`), pero el problema reside en el dispositivo, no en la sesión. | Columna dedicada por fila en `source_list` para activar/desactivar el modo delicado por dispositivo; persistir por `device_id` (hoy `delicate_mode` es por proyecto, `main_window.py:1156`). Requiere mínimo toque en core/schema si se quiere por sesión/device. | ⏳ Pendiente |

## 🟡 Prioridad Media (Usabilidad y Organización)

| ID | Punto | Descripción | Acción Técnica Sugerida | Estado |
|----|--------|-------------|-------------------------|--------|
| B-04 | **Caos en "Añadir Origen"** | El diálogo unificado es redundante, repite opciones y la gestión de "Eliminar" está escondida en una pestaña específica. | Rediseñar la jerarquía de `SourcePickerDialog`. Centralizar la administración de orígenes y simplificar el flujo de añadir. | ✅ Hecho |
| B-05 | **Estética Botón Eliminar** | El botón de eliminar origen en la fila principal es visualmente disruptivo/feo. | Migrar la acción a un menú contextual (`QMenu`) sobre la fila o integrarlo en la edición del origen. | ✅ Hecho |
| B-06 | **Expansión de Tabla Orígenes** | La tabla de orígenes no tiene un límite de expansión claro o presenta saltos bruscos de tamaño. | Implementar `QScrollArea` con límites definidos o ajustar el `sizePolicy` para evitar saltos. | ✅ Hecho |
| B-08 | **Anillo de focus visible** | No hay estilo `:focus` en el QSS (solo `border-color` en inputs, `app/ui/theme.py:173`): al navegar con teclado ningún botón indica dónde está el foco. | Añadir `:focus { border: 2px solid @accent; }` a botones y controles en `_QSS_TEMPLATE` (sin desplazar el layout). | ⏳ Pendiente |
| B-15 | **Generar proxies → configuración de proyecto** | El control "Generar proxies" vive en el panel de ingesta (`chk_generate_proxies` + `proxy_resolution`, `main_window.py:566-579`) aunque la decisión se conoce al configurar el proyecto. | Trasladarlo a la configuración del proyecto (metadata dialog, `_show_metadata_dialog`) persistido en `projects`; `start_ingest` ya lo consume en `main_window.py:1741`. | ⏳ Pendiente |
| B-16 | **Etiqueta de estado al pie de la aplicación** | La etiqueta de estado (`ingest_status_label`, `main_window.py:555-557`) —donde aparecen mensajes como "Archivos completados eliminados de la tabla de ingesta"— está bajo "INICIAR INGESTA", mezclada con los controles de ingesta. Mover **solo** esa etiqueta; el resto (barra de progreso, estadísticas) queda donde está. | Reubicar `ingest_status_label` como franja fija al pie de la interfaz (tras `two_col`, `main_window.py:379`) sin tocar `progress_bar`/`stats_row`. | ⏳ Pendiente |
| B-17 | **Ruta maestra invisible en la barra superior** | La ruta maestra del proyecto no se ve en la header bar (antes sí). `project_path_label` usa `QSizePolicy.Ignored` horizontal (hint 0, `main_window.py:314-319`) y `hb.addStretch()` (`main_window.py:321`) tras él → se queda a 0 px. | Cambiar a `Expanding` con `setTextElideMode(Qt.ElideMiddle)` y ancho acotado para que la ruta sea siempre visible. | ⏳ Pendiente |

## 🟢 Prioridad Baja (Pulido Visual)

| ID | Punto | Descripción | Acción Técnica Sugerida | Estado |
|----|--------|-------------|-------------------------|--------|
| B-07 | **Temas en WiFi (PairDrop)** | El diálogo de WiFi solo soporta tema oscuro, ignorando la configuración global. | Eliminar colores hardcodeados en `WifiMethodDialog` y vincularlo al sistema de temas de `app/ui/theme.py`. | ✅ Hecho |
| B-09 | **Tipografía con jerarquía** | Toda la app usa `Segoe UI` 11px plano; no hay jerarquía tipográfica clara entre títulos, secciones y contenido. | Definir tokens de tipo (títulos 16-18px semibold, cuerpo 12px, etiquetas 10-11px) y, opcionalmente, empaquetar una fuente propia (p. ej. IBM Plex Sans) en `app/ui/theme.py`. | ⏳ Pendiente |
| B-10 | **Espaciado y superficies** | Paddings de 2-6px arbitrarios; todo plano con bordes de 1px sin niveles de superficie. | Adoptar escala de espaciado 4/8/12/16/24 y niveles de superficie (bg → card → elevated) con `border-radius` 8-10px en `_QSS_TEMPLATE`. | ⏳ Pendiente |
| B-11 | **Microinteracciones y botón primario** | El CTA `PrimaryAction` es verde plano; los hover son instantáneos. | Añadir borde +1px más claro y glow sutil sobre el acento al CTA; evaluar `QPropertyAnimation` puntual para estados clave (Qt no soporta transiciones CSS). | ⏳ Pendiente |
| B-12 | **Auditoría estética formal** | Sin revisión visual sistemática tras los cambios estéticos. | Ejecutar `/gsd-ui-review` (auditoría de 6 pilares) y/o generar el design system con la skill ui-ux-pro-max (`--design-system --persist`) para validar el resultado. | ⏳ Pendiente |
| B-18 | **Sesiones en contenedor rectangular** | La sección "Sesiones" (`main_window.py:456-535`) son widgets sueltos en la columna izquierda, sin la coherencia visual de las acciones post-ingesta. | Agruparla en un `QGroupBox` con `objectName` `PostActionsBox` (mismo estilo que `main_window.py:595-599`). | ⏳ Pendiente |
| B-19 | **Botones de edición/origen demasiado a la derecha** | `btn_edit_description` (`main_window.py:362-368`) queda empujado al borde por el label Expanding de la descripción, y `_btn_browse_sess_src` (`main_window.py:497-503`) por `session_src_label` Expanding. | Anclar cada botón pegado a su control (mover el `addStretch()` a después del botón, no antes). | ⏳ Pendiente |
| B-20 | **Eliminar dispositivos guardados/desconectados** | No hay forma de borrar dispositivos MTP/FTP/WiFi que quedan registrados en la lista de orígenes tras desconectarse. El botón de eliminar (columna 4) solo borra la sesión, pero los dispositivos siguen apareciendo en el diálogo "Añadir origen" y en `sd_cards`/`device_settings`. | Añadir acción de "olvidar dispositivo" que limpie `sd_cards`, `device_settings`, `recent_paths` y cualquier caché de `device_cache/` asociada al `device_id` o `volume_serial`. Accesible desde menú contextual en la columna 0 o desde el diálogo de añadir origen. | ⏳ Pendiente |
| B-21 | **Limpiar y ordenar el panel de configuración** | El diálogo de configuración del proyecto (`_show_metadata_dialog`) tiene controles desordenados y posiblemente选项 mezcladas sin agrupación clara. | Reorganizar el diálogo: agrupar por secciones (Organización, Duración, Proxies, Metadatos), ordenar los controles lógicamente, revisar espaciado y jerarquía visual. | ⏳ Pendiente |
| B-22 | **Reordenar botones en diálogo "Añadir origen"** | El `SourcePickerDialog` tiene los botones en un orden determinado por el orden de código, no por flujo lógico del usuario. | Reordenar las opciones del diálogo siguiendo el flujo natural: primero orígenes locales (carpeta, SD), luego dispositivos conectados (MTP, FTP),最后 opciones de red (WiFi). Evaluar si algún botón debe renombrarse. | ⏳ Pendiente |

---
**Estado:** Completado (B-01 a B-07, B-13 a B-18). Pendientes: B-08 a B-12 (estética), B-19 (pulido), B-20 a B-22 (limpieza/reordenación). Restricción: cambios en `app/core/` solo si son mínimos y necesarios.
