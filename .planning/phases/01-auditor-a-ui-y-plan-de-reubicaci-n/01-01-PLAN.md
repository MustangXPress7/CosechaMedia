---
phase: 01-auditor-a-ui-y-plan-de-reubicaci-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-INVENTARIO.md"
  - ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/capture_ui.py"
  - ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaA_estado-inicial.png"
  - ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaA_configurado.png"
  - ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaB_estado-inicial.png"
  - ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaB_configurado.png"
  - ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaC_estado-inicial.png"
  - ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaC_configurado.png"
  - ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaD_estado-inicial.png"
  - ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaD_configurado.png"
autonomous: true
requirements:
  - UI-01
estimate:
  tokens: 30000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "El inventario de widgets (`01-INVENTARIO.md`) cubre las cuatro zonas — A ventana principal/dashboard, B pickers de fuente (MTP/FTP/WiFi), C asistentes y paneles (SelectiveDump, ShootInbox, About, ProjectWizard), D acciones post-ingesta — y cada control relevante incluye attr, tipo Qt, texto/label, ubicación `archivo:línea`, zona y flujo (UI-01, D-01, D-04)"
    - "Cada control del inventario cita su ubicación real con el formato `app/ui/<módulo>.py:<línea>` y usa los atributos públicos con los que los nombran los tests (`btn_start`, `table`, `source_list`)"
    - "Los hallazgos confirmados D-07..D-12 están anclados a filas del inventario (cada decisión confirmada tiene su control o controles inventariados)"
    - "Existen 8 capturas offscreen (2 por zona × 4 zonas) en `captures/`, PNG ≤ 1920px de ancho, con nombres según el contrato UI-SPEC: `zonaA_estado-inicial.png`, `zonaA_configurado.png`, `zonaB_estado-inicial.png`, `zonaB_configurado.png`, `zonaC_estado-inicial.png`, `zonaC_configurado.png`, `zonaD_estado-inicial.png`, `zonaD_configurado.png`"
    - "Zona auditada sin inventario = falta de cobertura: se registra como pendiente, nunca se omite en silencio (UI-SPEC empty-inventario)"
    - "Captura faltante: marcador 'captura pendiente — motivo' en la leyenda; nunca fila de evidencia vacía (UI-SPEC empty-capturas)"
    - "La app no arranca offscreen: se registra la razón en la columna evidencia y se usa captura manual si es posible (UI-SPEC loading-capturas)"
    - "Evidencia no verificable: copy 'Evidencia no verificable — registrar el motivo en la columna Evidencia y volver a capturar en la fase de ejecución.' (UI-SPEC error-capturas)"
    - "Las tablas del inventario usan máximo 5 columnas y texto abreviado en celdas con leyenda (zona A/B/C/D + flujo c/i/f/r); se evita el scroll horizontal del visor (UI-SPEC overflow)"
  artifacts:
    - path: ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-INVENTARIO.md"
      provides: "Inventario de widgets por zona + leyenda de capturas (evidencia estructural y visual de la auditoría)"
      contains: "## Zona"
    - path: ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaA_estado-inicial.png"
      provides: "Captura offscreen zona A (ventana principal) estado inicial"
    - path: ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaA_configurado.png"
      provides: "Captura offscreen zona A estado configurado/poblado"
    - path: ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaB_estado-inicial.png"
      provides: "Captura offscreen zona B (pickers de fuente) estado inicial"
    - path: ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaB_configurado.png"
      provides: "Captura offscreen zona B estado configurado/poblado"
    - path: ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaC_estado-inicial.png"
      provides: "Captura offscreen zona C (asistentes y paneles) estado inicial"
    - path: ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaC_configurado.png"
      provides: "Captura offscreen zona C estado configurado/poblado"
    - path: ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaD_estado-inicial.png"
      provides: "Captura offscreen zona D (acciones post-ingesta) estado inicial"
    - path: ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaD_configurado.png"
      provides: "Captura offscreen zona D estado configurado/poblado"
    - path: ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/capture_ui.py"
      provides: "Harness offscreen de captura (herramienta de evidencia en el directorio de fase, NO código de la app)"
  key_links:
    - from: "01-INVENTARIO.md"
      to: "app/ui/main_window.py"
      via: "citas `archivo:línea` por atributo público (p. ej. `btn_start`, `table`, `source_list`)"
      pattern: "app/ui/main_window\\.py:\\d+"
    - from: "01-INVENTARIO.md"
      to: "captures/*.png"
      via: "tabla de leyendas mapea cada captura a su zona y estado"
      pattern: "captures/zona[A-D]_"
  prohibitions:
    - requirement_id: UI-01
      category: values
      status: unverified
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT proponer ocultar, eliminar ni colapsar ningún control de la interfaz en las propuestas del informe o del plan de reubicación — todo control relevante sigue accesible y descubrible (D-03)"
    - requirement_id: UI-03
      category: values
      status: unverified
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT proponer cambios de código ni implementar reubicaciones en esta fase — los entregables son diagnóstico (informe + plan); la ejecución es v2 (UI-04/UI-05)"
    - requirement_id: UI-01
      category: safety
      status: unverified
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT proponer renombrar los atributos públicos usados por los tests (`btn_start`, `table`, `source_list`) — el plan marca cualquier renombrado como riesgo alto o propone migración + actualización de tests como riesgo explícito"
    - requirement_id: UI-02
      category: safety
      status: unverified
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT proponer reubicar controles destructivos (formateo, apagado, eliminación) sin exigir confirmación explícita con consecuencia en el estado objetivo (patrón D-09)"
---

<objective>
**As a** operador de cámara, **I want to** disponer de un diagnóstico completo de la interfaz —qué controles existen, dónde están, qué está mal ubicado y hacia dónde deberían moverse— **so that** la fase v2 pueda reubicar los controles de forma priorizada y sin esconder nada.

**Purpose:** Capturar la **evidencia** de la auditoría (D-04): el inventario estructurado de widgets por zona y las capturas offscreen de la app. Es la capa de datos del pipeline de diagnóstico (evidencia → hallazgo → propuesta → plan priorizado). Sin esta evidencia, los informes posteriores (plans 02 y 03) no tendrían anclas verificables.

**Output:**
- `01-INVENTARIO.md` — inventario de widgets por zona con attr, tipo Qt, label, `archivo:línea`, zona y flujo + leyenda de capturas
- `captures/*.png` — 8 capturas offscreen (2 por zona × 4 zonas), PNG ≤ 1920px
- `capture_ui.py` — harness offscreen de captura (herramienta de evidencia en el directorio de fase; no es código de la app y NO debe tocar `app/`)
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-CONTEXT.md
@.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-UI-SPEC.md
@.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-PATTERNS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONCERNS.md

Fuente primaria del inventario: `app/ui/main_window.py` (Zonas A y D) y los diálogos de `app/ui/` (Zonas B y C). El UI-SPEC (aprobado 2026-08-15) es el contrato de entregables: sus tablas de Target-State nombran los controles con los atributos públicos reales y sus líneas de referencia coinciden con el árbol de trabajo actual.
</context>

## Artifacts this phase produces

Todos los entregables de la fase (este plan crea los dos primeros):

| Artifact | Creado por | Ruta |
|----------|-----------|------|
| Inventario de widgets + leyenda de capturas | Plan 01 | `.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-INVENTARIO.md` |
| Capturas offscreen (8 PNG ≤1920px) | Plan 01 | `.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zona[A-D]_{estado-inicial,configurado}.png` |
| Harness offscreen de captura (evidencia reproducible) | Plan 01 | `.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/capture_ui.py` |
| Informe consolidado de hallazgos | Plan 02 | `.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-HALLAZGOS.md` |
| Plan de reubicación priorizado | Plan 03 | `.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-PLAN-REUBICACION.md` |

## Assumptions señaladas (flagged — nunca drops silenciosos)

- **A-EDGE-UI-01 (edge probe `unclassified`):** el requisito UI-01 salió `unclassified` del edge-probe (prosa en español, clasificador con pistas en inglés). No se auto-resuelve: su cobertura se garantiza con las truths goal-backward de este plan (inventario completo de las 4 zonas, controles con ubicación `archivo:línea`) y los acceptance criteria de cada tarea. El verifier confirma con evidencia explícita; nunca pase silencioso.
- **A-EDGE-UI-02 / A-EDGE-UI-03:** mismos rows `unclassified` para UI-02 y UI-03 — cubiertos respectivamente por el plan 02 (informe con evidencia) y el plan 03 (plan priorizado).
- **A-BASELINE (cambios preexistentes en `app/`):** el árbol de trabajo ya contiene cambios sin commitear **anteriores a esta fase**: `app/ui/main_window.py`, `app/i18n/cosechamedia_en.ts`, `app/i18n/cosechamedia_en.qm`, `tools/translate_en.py` (+ `.gitignore`). El criterio "sin diffs" (éxito 4) se interpreta como *la fase no introduce cambios nuevos*; el gate de igualdad de conjunto contra este baseline se ejecuta en el plan 03. La auditoría lee el árbol de trabajo tal cual — las líneas de referencia del UI-SPEC coinciden con él.
- **A-OFFSCREEN:** si la app o algún diálogo no puede instanciarse con `QT_QPA_PLATFORM=offscreen` (p. ej. diálogos MTP/WPD con COM), se registra el motivo en la leyenda y se usa la evidencia de lectura de widgets como respaldo (contrato UI-SPEC loading/error) — nunca una fila de evidencia vacía.
- **A-NO-RESEARCH:** RESEARCH.md no existe (research omitida por el usuario); la fuente de verdad de estructura y contrato es `01-UI-SPEC.md` + `01-PATTERNS.md` (PATTERNS.md:6).

<tasks>

<task type="auto">
  <name>Tarea 1: Inventario de widgets por zona (evidencia estructural D-04)</name>
  <files>.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-INVENTARIO.md</files>
  <read_first>
    - .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-PATTERNS.md (formato de tablas de inventario, citas `archivo:línea`, regla máx. 5 columnas)
    - .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-UI-SPEC.md (§ Deliverables Design Contract → Estructura/Evidencia; § Target-State tablas por zona con los attrs reales)
    - .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-CONTEXT.md (D-01, D-04, D-07..D-12)
    - .planning/codebase/STRUCTURE.md (naming de atributos `btn_*`, `_btn_*`, `chk_*`, `combo_*`, `lbl_*`)
    - app/ui/main_window.py (Zonas A y D — el god object; leer por rangos guiados por grep de `self.<attr> = Q`, no en lectura completa)
    - app/ui/source_picker.py, app/ui/device_picker.py, app/ui/ftp_picker.py, app/ui/wifi_picker.py (Zona B)
    - app/ui/wifi_panel.py, app/ui/selective_dump.py, app/ui/project_wizard.py, app/ui/about_dialog.py (Zona C)
  </read_first>
  <action>
    Crear `01-INVENTARIO.md` (net-new, español fuente) con el formato del contrato UI-SPEC §Deliverables y los patrones de 01-PATTERNS.md. Cabecera: `# Fase 1: Inventario de Widgets` + `**Elaborado:** 2026-08-15` + `**Estado:** draft`. Secciones:
    1. `## Método` — lectura de widgets (D-04): grep dirigido por atributo público (patrón `self\.<attr> = Q` en `app/ui/*.py`), citas `archivo:línea` con backticks y rango `inicio-fin` cuando aplique, método entre paréntesis si aporta (patrón CONCERNS.md:62).
    2. `## Zona A — Ventana principal / dashboard` — inventariar al menos: `source_input`, `btn_browse_source`, `btn_receive_wifi`, `source_list` (columnas Ruta/Cámara/Contenido), `btn_remove_source`, `sessions_combo`, `_btn_browse_sess_src`, `session_dest_combo`, `session_dest_path`, `_btn_browse_sess_dest`, `chk_session_delicate`, `btn_delete_session`, `btn_delete_project`, `project_combo`, `project_path_label`, `btn_start`, `btn_stop`, `progress_bar`, `lbl_files_processed`/`lbl_files_pending`/`lbl_files_errors` (o equivalente real de `stats_row`), `btn_clear_completed`, `chk_generate_proxies`, `proxy_resolution`.
    3. `## Zona B — Pickers de fuente` — `SourcePickerDialog` (lista de guardados + Examinar), `DevicePickerDialog` (MTP), `FtpPickerDialog` (perfil + escaneo), `WifiMethodDialog`.
    4. `## Zona C — Asistentes y paneles` — `ProjectWizard` (incl. `desc_input`), `SelectiveDumpAssistant` (4 páginas), `ShootInboxPanel` (QR + URL), `AboutDialog` (tabs).
    5. `## Zona D — Acciones post-ingesta` — `chk_format_sources`, `combo_format_mode`, `chk_shutdown`, `btn_reorganize`, `btn_clear_completed`, volcado selectivo (entrada menú contextual).
    6. `## Anclas de hallazgos confirmados` — tabla que mapea D-07 (zona post-ingesta), D-08 (panel sesiones), D-09 (botones de eliminar), D-10 (descripción del proyecto), D-11 (columnas de `source_list`), D-12 (flujo duplicado de guardar dispositivos) a las filas del inventario que los evidencian (1+ fila por decisión).
    Formato de fila (máximo 5 columnas, leyenda encima: zona A/B/C/D, flujo c=conectar/i=ingestar/f=formatear/r=reorganizar): `| ID | attr | tipo Qt | texto/label | ubicación (archivo:línea) |` — la zona/flujo van abreviadas en la columna ubicación o en una columna extra con leyenda; nunca >5 columnas. Los widgets públicos usados por tests se marcan con su nombre exacto (`btn_start`, `table`, `source_list`).
    Reglas del contrato: una zona sin controles inventariables NO puede omitirse — si una zona no tuviera inventario, escribir el bloque "Zona sin inventario = falta de cobertura — pendiente" (nunca omitir en silencio, UI-SPEC empty-inventario). Toda fila de evidencia lleva cita `archivo:línea`; fila sin cita no es válida.
  </action>
  <verify>
    <automated>python -c "import pathlib; t=pathlib.Path('.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-INVENTARIO.md').read_text(encoding='utf-8'); assert all(s in t for s in ['## Zona A','## Zona B','## Zona C','## Zona D','## Anclas']), 'faltan secciones'; attrs=['btn_start','btn_stop','source_list','btn_remove_source','btn_delete_session','btn_delete_project','btn_reorganize','chk_format_sources','combo_format_mode','chk_shutdown','btn_clear_completed','chk_generate_proxies','proxy_resolution','sessions_combo','chk_session_delicate','source_input','btn_browse_source','btn_receive_wifi','project_combo','desc_input','table']; missing=[a for a in attrs if a not in t]; assert not missing, f'faltan attrs: {missing}'; assert all(d in t for d in ['D-07','D-08','D-09','D-10','D-11','D-12']), 'faltan anclas D-07..D-12'"</automated>
  </verify>
  <acceptance_criteria>
    - `01-INVENTARIO.md` existe y contiene las secciones `## Zona A`, `## Zona B`, `## Zona C`, `## Zona D` y `## Anclas de hallazgos confirmados`
    - Los 21 atributos listados en el verify (btn_start … table) aparecen en el documento
    - Cada fila de inventario incluye cita que coincide con el patrón `app/ui/[a-z_]+\.py:\d+` (≥30 citas en total)
    - La sección de anclas mapea cada D-07..D-12 a 1+ fila del inventario
    - Ninguna tabla supera 5 columnas (abreviaturas de zona/flujo con leyenda explicada)
    - Ningún control público usado por tests (`btn_start`, `table`, `source_list`) aparece propuesto para renombre — prohibición P-RENAME
  </acceptance_criteria>
  <done>El inventario de las 4 zonas está documentado con citas `archivo:línea` verificables y las anclas D-07..D-12 mapeadas. Estado: draft.</done>
</task>

<task type="auto">
  <name>Tarea 2: Capturas offscreen de las 4 zonas (evidencia visual D-04)</name>
  <files>
    .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/capture_ui.py
    .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaA_estado-inicial.png
    .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaA_configurado.png
    .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaB_estado-inicial.png
    .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaB_configurado.png
    .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaC_estado-inicial.png
    .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaC_configurado.png
    .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaD_estado-inicial.png
    .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures/zonaD_configurado.png
  </files>
  <read_first>
    - .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-INVENTARIO.md (qué controles debe verse en cada escena)
    - .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-UI-SPEC.md (§ Evidencia: naming, dimensiones, fallbacks)
    - tests/test_e2e.py (patrón offscreen: `QT_QPA_PLATFORM=offscreen` + `QApplication` único, líneas 11-24)
    - app/ui/main_window.py (instanciación de `MainWindow` y de los diálogos por zona)
  </read_first>
  <action>
    Escribir `capture_ui.py` (net-new, en el directorio de fase — herramienta de evidencia; PROHIBIDO tocar `app/`, `tests/` o `tools/`). Comportamiento:
    - Fija `QT_QPA_PLATFORM=offscreen` y crea un `QApplication` único (patrón tests/test_e2e.py).
    - Para cada zona (A: `MainWindow` completo; B: un picker de fuente representativo, p. ej. `SourcePickerDialog` o `DevicePickerDialog`; C: un asistente/panel representativo, p. ej. `ProjectWizard` o `SelectiveDumpAssistant`; D: la zona post-ingesta de `MainWindow` bajo la barra de progreso), renderiza dos escenas: `_estado-inicial` (app vacía sin configuración) y `_configurado` (zona poblada con datos ficticios insertados programáticamente — p. ej. ítems en `source_list`, una sesión seleccionada, orígenes cargados) y guarda PNG con `widget.grab().save(...)` o `QPixmap`.
    - Nombres exactos: `captures/zonaA_estado-inicial.png`, `zonaA_configurado.png`, `zonaB_estado-inicial.png`, `zonaB_configurado.png`, `zonaC_estado-inicial.png`, `zonaC_configurado.png`, `zonaD_estado-inicial.png`, `zonaD_configurado.png` (UI-SPEC §Evidencia).
    - Ancho máximo 1920px; si la ventana excede, redimensionar la escena antes de guardar.
    - Si una escena no puede renderizarse offscreen (p. ej. diálogo MTP/WPD con COM): registrar el motivo en la leyenda como "captura pendiente — &lt;motivo&gt;" y continuar (contrato empty/loading/error UI-SPEC); nunca dejar la fila vacía. Si la app entera no arranca offscreen, registrar la razón en la columna evidencia del inventario y usar captura manual si es posible.
    - Cerrar los diálogos y la app al terminar (salida limpia, exit 0).
    Añadir al final de `01-INVENTARIO.md` la sección `## Leyenda de capturas` con tabla: captura → zona → escena → estado → archivo de origen (widget inv) → notas (motivo si falló).
  </action>
  <verify>
    <automated>python -c "import pathlib,glob; base=pathlib.Path('.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/captures'); inv=pathlib.Path('.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-INVENTARIO.md').read_text(encoding='utf-8'); names=['zonaA_estado-inicial','zonaA_configurado','zonaB_estado-inicial','zonaB_configurado','zonaC_estado-inicial','zonaC_configurado','zonaD_estado-inicial','zonaD_configurado']; uncovered=[n for n in names if not (base/(n+'.png')).exists() and n not in inv]; assert not uncovered, f'capturas sin archivo y sin motivo en leyenda: {uncovered}'; import PySide6.QtGui as G; [ (lambda i: (i.width()&lt;=1920 or (_ for _ in ()).throw(AssertionError('ancho &gt; 1920: '+p))))(G.QImage(p)) for p in glob.glob(str(base)+'/*.png')]"
    </automated>
  </verify>
  <acceptance_criteria>
    - `capture_ui.py` existe en el directorio de fase y fija `QT_QPA_PLATFORM=offscreen` (no toca `app/`, `tests/` ni `tools/`)
    - Las 8 capturas `captures/zona[A-D]_{estado-inicial,configurado}.png` existen, o su ausencia está documentada con marcador que incluye el nombre de la captura en `01-INVENTARIO.md` §Leyenda de capturas (nunca fila vacía)
    - Todas las PNG presentes tienen ancho ≤ 1920px (verificado por el comando automatizado con `QImage`)
    - `01-INVENTARIO.md` termina con `## Leyenda de capturas` que mapea cada captura a zona, escena, estado y widget de origen
    - El harness termina con salida limpia (exit 0); no se crearon archivos nuevos bajo `app/` ni `tests/` (comprobación con `git status --porcelain app/ tests/` no muestra rutas nuevas respecto al estado inicial)
  </acceptance_criteria>
  <done>Las 8 capturas offscreen de las 4 zonas están guardadas en `captures/` (o documentadas con motivo), la leyenda está en `01-INVENTARIO.md` y la evidencia visual cubre el mínimo de 2 por zona del contrato UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evidencia (inventario/capturas) → informe → plan → fase v2 | La evidencia capturada en este plan alimenta el informe (plan 02), el plan de reubicación (plan 03) y, en v2, la implementación. Evidencia fabricada o mal citada se propaga a la implementación |
| Harness offscreen → sistema de archivos | `capture_ui.py` instancia la app con datos ficticios; si escribiera en `app/` o `tests/` rompería el criterio de éxito 4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Inventario de widgets (citas `archivo:línea`) | high | mitigate | Cada fila exige cita con patrón `app/ui/[a-z_]+\.py:\d+`; el verify automatizado impone ≥30 citas y la presencia de los 21 attrs críticos (acceptance criteria tarea 1). La tarea 3 del plan 03 cruza las citas contra el código real |
| T-01-02 | Tampering | `capture_ui.py` (harness offscreen) | medium | mitigate | Harness confinado al directorio de fase; action prohíbe tocar `app/`/`tests/`/`tools/`; el gate `git status --porcelain app/ tests/ tools/` del plan 03 detecta cualquier escritura indebida |
| T-01-03 | Spoofing | Leyenda de capturas (captura atribuida a zona/estado equivocado) | medium | mitigate | Tabla de leyendas mapea captura → zona → escena → widget de origen; revisión humana en el checkpoint de aprobación (plan 04) verifica las capturas contra la app real |
| T-01-04 | DoS | Ejecución offscreen colgada | low | accept | Coste bajo y acotado: el harness es un script local con salida limpia obligatoria; si una escena no renderiza se registra el motivo (contrato loading/error UI-SPEC) en vez de bloquear |
| T-01-05 | Information Disclosure | Inventario documenta estructura interna de la UI | low | accept | Docs locales en `.planning/` (repo del proyecto, sin secretos); el conocimiento de estructura UI no es sensible |
| T-01-SC | — | Instalaciones de paquetes | — | — | No aplica: este plan no ejecuta pip/npm/ningún gestor de paquetes (`requirements.txt` no cambia, criterio de éxito 4) |
</threat_model>

<verification>
- El inventario cubre las 4 zonas y las citas `archivo:línea` verifican contra el código leído (tarea 1)
- Existen 8 capturas offscreen (2 por zona) o ausencias documentadas con motivo en la leyenda (tarea 2)
- Ningún cambio bajo `app/`, `tests/` o `tools/` introducido por este plan (precondition de la tarea 2: estado git inicial registrado)
</verification>

<success_criteria>
- `01-INVENTARIO.md` existe con las 6 secciones (Método, Zona A-D, Anclas D-07..D-12) y ≥30 citas de código
- Las 8 capturas PNG existen (o motivo documentado) y todas ≤1920px
- `git status --porcelain app/ tests/ tools/` no muestra rutas nuevas respecto al inicio del plan
</success_criteria>

<output>
Crear `.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-01-SUMMARY.md` cuando termines, siguiendo `templates/summary.md`: qué evidencia se capturó, qué controles quedaron inventariados, qué capturas se lograron/faltaron (con motivo), y el estado git de `app/` al cierre.
</output>