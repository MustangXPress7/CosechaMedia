---
phase: 01-auditor-a-ui-y-plan-de-reubicaci-n
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-PLAN-REUBICACION.md"
autonomous: true
requirements:
  - UI-03
estimate:
  tokens: 26000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "El plan de reubicación `01-PLAN-REUBICACION.md` puntúa cada hallazgo con `Score = Impacto − (Esfuerzo + Riesgo)/2` (D-06) y lo asigna a una banda P1 (≥2.5), P2 (1.0–2.4) o P3 (<1.0) (UI-03)"
    - "El resumen por bandas cubre los tres casos: sin P1 → se declara 'Zona sin ítems de reubicación' (copy literal UI-SPEC:263), P1 con 1-2 ítems (one), P1 con 3+ ítems (many) con copy singular/plural ('1 ítem P1' / '3 ítems P1'); ninguna banda se oculta ni se vacía en silencio (UI-03 zero-one-many)"
    - "Cada ítem del plan incluye: zona, controles afectados, String(s) nuevo(s) en ES con `tr()` listado para v2, Riesgo (tests/i18n/estética), Score y Banda; ordenado por flujo del operador (conectar → ingestar → formatear → reorganizar)"
    - "Cada hallazgo del informe (plan 02) tiene trazabilidad: aparece como ítem del plan o se marca explícitamente 'no reubicar — motivo' (UI-03)"
    - "Los 11 strings nuevos en ES se listan sin implementar (v2 los implementará con `tr()` en los módulos destino)"
    - "Plan sin ítems de reubicación: se declara explícitamente 'Zona sin ítems de reubicación' en §1 y §3 (copy literal UI-SPEC:263 / PATTERNS.md:166) en vez de omitir la sección (UI-03 empty-plan)"
    - "El gate de cero código pasa: `git status --porcelain app/ tests/ tools/` no muestra rutas nuevas respecto a `baseline_git.txt` (capturado en el plan 01, tarea 2) y `python -m unittest discover tests/` sale 0"
  artifacts:
    - path: ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-PLAN-REUBICACION.md"
      provides: "Plan de reubicación priorizado (entregable de la fase, insumo de v2)"
      contains: "## Resumen por bandas"
  key_links:
    - from: ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-HALLAZGOS.md"
      to: ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-PLAN-REUBICACION.md"
      via: "cada ítem referencia el H-NN que lo origina (trazabilidad)"
    - from: ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-PLAN-REUBICACION.md"
      to: "ROADMAP fases v2 (UI-04/UI-05)"
      via: "el plan de reubicación es el contrato de entrada de la ejecución v2"
    - from: "Decisión D-06 (fórmula) + D-03 (no ocultar) + D-09 (destructivos)"
      to: ".planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-PLAN-REUBICACION.md"
      via: "bandas y órdenes derivados de la fórmula; prohibiciones aplicadas a cada ítem"
  prohibitions:
    - "PROHIBIDO ocultar, eliminar, colapsar o deshabilitar controles en este plan (D-03) — la fase es 100% diagnóstico; la ejecución es v2"
    - "PROHIBIDO proponer o implementar cambios de código en este plan (la ejecución es v2 UI-04/UI-05)"
    - "PROHIBIDO renombrar widgets públicos acoplados a tests (`btn_start`, `table`, `source_list`, `combo_format_mode`, `chk_generate_proxies`, `proxy_resolution`, `sessions_combo`, `chk_session_delicate`, `project_combo`, `source_input`, `btn_browse_source`, `btn_receive_wifi`, `desc_input`, `btn_remove_source`, `btn_delete_session`, `btn_delete_project`, `btn_reorganize`, `chk_format_sources`, `chk_shutdown`, `btn_clear_completed`)"
    - "PROHIBIDO recomendar reubicar controles destructivos (formateo/apagado/eliminación) sin confirmación explícita del operador (patrón D-09); dichos ítems quedan pendientes de confirmación en el checkpoint del plan 04"
---

<objective>
Escribir el plan de reubicación priorizado `01-PLAN-REUBICACION.md` — cada hallazgo del informe puntuado con la fórmula D-06, asignado a banda P1/P2/P3 y ordenado por el flujo real del operador — y verificar que la fase no ha tocado una sola línea de código.

Purpose: Es el entregable de decisión de la fase; el operador lo aprueba en el plan 04 y la fase v2 lo ejecuta. El gate de cero código garantiza que el diagnóstico es limpio.
Output: `01-PLAN-REUBICACION.md` (plan priorizado, español) + verificación de `git status` y suite de tests sin cambios.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-02-SUMMARY.md
@.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-HALLAZGOS.md
@.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-CONTEXT.md
@.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-UI-SPEC.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
</context>

## Artifacts this phase produces

| Artifact | Quién lo consume | Formato |
|----------|------------------|---------|
| `01-PLAN-REUBICACION.md` | Plan 04 (aprobación del operador), fase v2 (ejecución UI-04/UI-05) | Markdown ES, secciones: Resumen por bandas, Matriz de puntuación, Plan por zona, Riesgos y dependencias, Estado de aprobación |

- Este plan NO crea código; solo produce el plan de reubicación y verifica la línea base git.
- No se tocan `app/`, `tests/` ni `tools/` (verificado con git, tarea 3).

<tasks>

<task type="auto">
  <name>Tarea 1: Matriz de puntuación (D-06) y borrador de 01-PLAN-REUBICACION.md (UI-03)</name>
  <files>.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-PLAN-REUBICACION.md</files>
  <read_first>
    - .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-HALLAZGOS.md (los H-NN a puntuar)
    - .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-CONTEXT.md (D-06 fórmula, D-07..D-12, D-09 patrón destructivos)
    - .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-UI-SPEC.md (§Reubicación: bandas P1/P2/P3, orden por flujo, strings nuevos, copy empty-plan línea 263)
    - .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-PATTERNS.md (§ 01-PLAN-REUBICACION.md: analog ROADMAP.md — bloque de ítem con labels en negrita `**Control:**`…`**Orden:**`, tabla de estado por banda, bloque de trazabilidad/cobertura; copy empty-plan línea 166)
    - .planning/ROADMAP.md (analog de plan con prioridad/orden: estructura de fase/ítem con labels en negrita y tablas de estado)
  </read_first>
  <action>
    Escribir `01-PLAN-REUBICACION.md` en español con la estructura de abajo, siguiendo el patrón de 01-PATTERNS.md §`01-PLAN-REUBICACION.md` (analog `.planning/ROADMAP.md` — PATTERNS.md:13,77-114): bloque de ítem con labels en negrita, tabla de estado/progreso por banda (patrón ROADMAP.md:37-39), bloque de trazabilidad/cobertura (patrón REQUIREMENTS.md:36-46 → "ítems P1: N, P2: N, P3: N, sin banda: 0 ✓"), checklist `- [ ]` y footer con fecha (PATTERNS.md Shared Patterns).
    - §1 Resumen por bandas: tabla banda → nº ítems → ejemplos; los tres casos explícitos: sin P1 → "Zona sin ítems de reubicación" (copy literal UI-SPEC:263 / PATTERNS.md:166); P1 con 1-2 ítems (one); P1 con 3+ (many), con copy singular/plural ("1 ítem P1" / "3 ítems P1", UI-SPEC:270). Nunca omitir una banda (UI-03 zero-one-many).
    - §2 Matriz de puntuación: por cada hallazgo H-NN una fila con las subpuntuaciones Impacto, Esfuerzo, Riesgo (1-5), el Score calculado con `Score = Impacto − (Esfuerzo + Riesgo)/2` (D-06), y la Banda: P1 ≥ 2.5, P2 1.0–2.4, P3 < 1.0. Mostrar el cálculo de al menos 3 filas a modo de ejemplo.
    - §3 Plan por zona (A-D): por cada ítem de reubicación (id `R-NN`), incluir: zona, controles afectados (attrs del inventario), String(s) nuevo(s) en ES con `tr()` (listado sin implementar, para v2), Riesgo (tests/i18n/estética), Score y Banda, Orden dentro de la banda según flujo del operador (conectar → ingestar → formatear → reorganizar). Cada ítem se formatea con el bloque de labels en negrita del analog ROADMAP.md (PATTERNS.md:91): `**Control:**` (attr+label), `**Ubicación actual:**` (archivo:línea), `**Destino propuesto:**`, `**Strings nuevos:**`, `**Riesgo:**`, `**Score:**`, `**Orden:**`. Los ítems destructivos (formateo/apagado/eliminación) se marcan "pendiente de confirmación D-09" y no reciben orden de ejecución.
    - §4 Riesgos y dependencias: tests acoplados a widgets (los 20 attrs públicos), i18n (strings ES literales, catálogo EN vía .ts/.qm — no regenerar), estética (tema/accentos), y dependencias entre reubicaciones (p. ej. D-12 precede a sus tabs).
    - §5 Estado de aprobación: checklist `- [ ]` vacío (pendientes) para "apruebo P1/P2/P3" y "apruebo ítems destructivos" — los rellena el operador en el plan 04.
    Si no hubiera ítems: declarar explícitamente "Zona sin ítems de reubicación" en §1 y §3 (UI-03 empty-plan; copy literal UI-SPEC:263), nunca omitir las secciones.
    Regla D-03: prohibido ocultar/eliminar/colapsar controles; el plan solo mueve/agrupa/reordena.
  </action>
  <verify>
    <automated>python -c "import pathlib,re; t=pathlib.Path('.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-PLAN-REUBICACION.md').read_text(encoding='utf-8'); assert '## Resumen por bandas' in t; assert '## Matriz de puntuación' in t;     assert '## Plan por zona' in t or 'Zona sin ítems de reubicaci' in t; assert '## Riesgos y dependencias' in t; assert '## Estado de aprobación' in t; assert re.search(r'P1|P2|P3', t); assert re.search(r'\bScore\b', t)"
    </automated>
  </verify>
  <acceptance_criteria>
    - Las secciones §1..§5 existen (o §1/§3 declaran "Zona sin ítems de reubicación" explícitamente — copy literal UI-SPEC:263)
    - Cada H-NN del informe tiene su fila en la matriz con Score calculado según D-06 y banda P1/P2/P3
    - Los ítems destructivos están marcados "pendiente de confirmación D-09" sin orden de ejecución
    - Los strings nuevos ES se listan con `tr()` sin implementar
    - El documento está en español
  </acceptance_criteria>
  <done>Matriz completa con Score y banda por cada hallazgo; plan por zona redactado con los ítems R-NN, strings nuevos y riesgos.</done>
</task>

<task type="auto">
  <name>Tarea 2: Trazabilidad hallazgo ↔ ítem del plan (UI-03)</name>
  <files>.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-PLAN-REUBICACION.md</files>
  <read_first>
    - .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-HALLAZGOS.md (lista completa de H-NN)
    - .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-INVENTARIO.md (attrs por zona para referencias cruzadas)
    - .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-PATTERNS.md (patrón de trazabilidad y bloque de cobertura del analog REQUIREMENTS.md:36-46: "ítems P1: N, P2: N, P3: N, sin banda: 0 ✓")
  </read_first>
  <action>
    Añadir al plan de reubicación una sección de trazabilidad (puede ser parte de §3 o §5-bis) siguiendo el patrón de tabla de trazabilidad + bloque de cobertura del analog REQUIREMENTS.md (PATTERNS.md:101-114): tabla con cada H-NN del informe y su destino — `R-NN` si se reubica, o "no reubicar — motivo" si se descarta (motivo explícito: ya bien ubicado / depende de v2 / requiere confirmación D-09), cerrada con el bloque de cobertura "ítems P1: N, P2: N, P3: N, sin banda: 0 ✓" (copy singular/plural). Verificar que la lista de H-NN del informe está completa en esta tabla (conteo), y que cada R-NN referenciado existe en la matriz. Prohibido descartar un hallazgo sin motivo escrito.
  </action>
  <verify>
    <automated>python -c "import pathlib,re; h=pathlib.Path('.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-HALLAZGOS.md').read_text(encoding='utf-8'); t=pathlib.Path('.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-PLAN-REUBICACION.md').read_text(encoding='utf-8'); hn=set(re.findall(r'H-\d+', h)); tn=set(re.findall(r'H-\d+', t)); missing=hn-tn; assert not missing, f'hallazgos sin trazabilidad: {missing}'; assert 'no reubicar' in t.lower() or len(re.findall(r'R-\d+', t))&gt;0, 'sin destino ni descartes'"
    </automated>
  </verify>
  <acceptance_criteria>
    - La tabla de trazabilidad cubre todos los H-NN del informe
    - Cada H-NN tiene destino (R-NN) o motivo de descarte escrito
    - Todos los R-NN referenciados existen en la matriz de puntuación
  </acceptance_criteria>
  <done>Todos los hallazgos del informe tienen trazabilidad explícita; no hay huérfanos.</done>
</task>

<task type="auto">
  <name>Tarea 3: Gate de cero código — línea base git y suite de tests (UI-03)</name>
  <files>.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-PLAN-REUBICACION.md</files>
  <precondition>`baseline_git.txt` existe en el directorio de fase (capturado en el plan 01, tarea 2, paso 1) con las rutas del estado git inicial de `app/ tests/ tools/` una por línea (estado previo con cambios no commiteados en app/ui/main_window.py, app/i18n/cosechamedia_en.ts, app/i18n/cosechamedia_en.qm, tools/translate_en.py). Si no existe, detenerse y capturarlo antes de continuar (sin modificar el árbol).</precondition>
  <read_first>
    - .planning/codebase/TESTING.md (comando canónico de tests)
    - .planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-02-SUMMARY.md (estado git al cierre del plan 02)
  </read_first>
  <action>
    Verificar que la fase no ha tocado código:
    1. Ejecutar `git status --porcelain app/ tests/ tools/` y comparar con la línea base documentada: extraer de cada línea el SEGUNDO token (el PATH — con `l.split(None,1)[1]`, nunca `l.split()[0]`, que devuelve el código de estado `M`/`??`) y comparar el conjunto de rutas contra `baseline_git.txt`; debe ser IDÉNTICO (igualdad de conjuntos, no solo ausencia de cambios). Cualquier ruta nueva o modificación extra en `app/`, `tests/` o `tools/` es una violación → detenerse, reportar y revertir antes de continuar.
    2. Ejecutar `python -m unittest discover tests/` (comando canónico, Qt offscreen): debe salir 0. Si falla, es violación → detenerse y reportar (no arreglar: la fase es diagnóstico).
    3. Anotar el resultado en una sección final "Gate de cero código" de `01-PLAN-REUBICACION.md`: conjuntos git comparados (base vs final, igualdad), salida de tests, fecha. Si algo falló, dejar el registro con la discrepancia y el estado "REVISA OPERADOR".
    No ejecutar `tools/update_translations.ps1` ni `tools/translate_en.py` (regeneran .ts/.qm y romperían la línea base i18n).
  </action>
  <verify>
    <automated>python -c "import subprocess,pathlib; r=subprocess.run(['git','status','--porcelain','app/','tests/','tools/'],capture_output=True,text=True); out=set(l.split(None,1)[1] for l in r.stdout.strip().splitlines() if l.strip()); base=set(pathlib.Path('.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/baseline_git.txt').read_text(encoding='utf-8').splitlines()) if pathlib.Path('.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/baseline_git.txt').exists() else None; assert base is not None, 'baseline_git.txt no existe'; assert out==base, f'DIFF git: base={base-base&out} extra={out-base}'; assert 'Gate de cero c' in pathlib.Path('.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-PLAN-REUBICACION.md').read_text(encoding='utf-8')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `baseline_git.txt` existe (capturado en el plan 01, tarea 2) con las rutas del estado git inicial de app/ tests/ tools/ una por línea; si no existiera, capturarlo aquí al inicio de la tarea y registrar la desviación en el SUMMARY
    - `git status --porcelain app/ tests/ tools/` final == línea base (igualdad de conjuntos)
    - `python -m unittest discover tests/` sale 0
    - El registro "Gate de cero código" está en el plan de reubicación
  </acceptance_criteria>
  <done>La fase no ha modificado ninguna ruta fuera de `.planning/` y los tests pasan; el resultado queda registrado en el plan de reubicación.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan de reubicación → v2 | El plan es el contrato de entrada de la ejecución v2; puntuaciones, bandas u órdenes erróneos se materializan en reubicaciones reales |
| Plan de reubicación → datos del operador | Recomendaciones sobre controles destructivos (formateo/apagado/eliminación) afectan a datos de rodaje si se ejecutan sin confirmación |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | Score/banda calculados mal (fórmula D-06 mal aplicada) | medium | mitigate | §2 muestra el cálculo de ≥3 filas a modo de ejemplo verificable; el acceptance_criteria exige que cada H-NN tenga Score y banda; el plan 04 (revisión humana) valida la consistencia banda ↔ score |
| T-03-02 | Tampering | Ítem destructivo con orden de ejecución sin confirmación D-09 | high | mitigate | Regla en tarea 1: los ítems destructivos se marcan "pendiente de confirmación D-09" y NO reciben orden; el §5 exige checkbox explícito del operador en el plan 04 antes de que v2 los ejecute |
| T-03-03 | Tampering | Ejecutar `tools/update_translations.ps1` / `translate_en.py` (regenera .ts/.qm y rompe línea base i18n) | high | mitigate | Prohibición explícita en tarea 3; el gate git compara conjuntos con la línea base y detectaría archivos i18n alterados |
| T-03-04 | Tampering | Modificación de `tests/` durante la fase (rompería el acople de tests a widgets) | medium | mitigate | `tests/` incluido en el gate git (tarea 3); el verify automatizado compara el conjunto completo app+tests+tools contra baseline_git.txt |
| T-03-05 | Information Disclosure | Descarte de hallazgos sin motivo (trazabilidad incompleta) | medium | mitigate | Tarea 2: tabla de trazabilidad exige destino o motivo escrito; verify automatizado cubre todos los H-NN del informe |
| T-03-06 | Repudiation | "Zona sin ítems de reubicación" declarado sin evidencia | low | mitigate | §1/§3 declaran el caso explícitamente (copy literal UI-SPEC:263) y la matriz sigue presente; el plan 04 revisa contra el informe |
| T-03-SC | — | Instalaciones de paquetes | — | — | No aplica: este plan no ejecuta gestores de paquetes |
</threat_model>

<verification>
- Matriz con Score (D-06) y banda P1/P2/P3 por cada hallazgo (tarea 1)
- Trazabilidad completa H-NN → R-NN / "no reubicar — motivo" (tarea 2)
- Gate de cero código: conjuntos git idénticos a línea base y suite de tests en verde (tarea 3)
</verification>

<success_criteria>
- `01-PLAN-REUBICACION.md` existe con §1 Resumen por bandas (casos zero/one/many explícitos), §2 Matriz, §3 Plan por zona, §4 Riesgos y dependencias, §5 Estado de aprobación
- Todos los hallazgos del informe tienen Score, banda y trazabilidad
- `git status --porcelain app/ tests/ tools/` == línea base y `python -m unittest discover tests/` exit 0
- No se ejecutaron herramientas de regeneración i18n
</success_criteria>

<output>
Crear `.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-03-SUMMARY.md` cuando termines, siguiendo `templates/summary.md`: bandas resultantes (nº de ítems por banda), ítems R-NN clave, hallazgos descartados con motivo, resultado del gate de cero código (conjuntos git comparados y salida de tests), y confirmación de que el plan 04 solo requiere la aprobación del operador.
</output>
