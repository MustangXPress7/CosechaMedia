---
phase: 01.5.0-consolidaci-n-y-bugs-del-flujo
plan: 03
type: execute
wave: 2
depends_on: ["01.5.0-01", "01.5.0-02"]
files_modified:
  - app/core/db.py
  - app/core/ingestor.py
  - app/core/watcher.py
  - tests/test_watcher_inventory.py
  - tests/test_db.py
  - tests/test_main_window.py
autonomous: true
requirements: [R2, R3, R5]
user_setup: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - TRUTH-W1: El inventario de archivos vistos por el watcher se persiste por fuente en una tabla `watcher_seen` (source_path, file_path, first_seen, last_verdict) creada aditivamente; el cap duro de 10k de `watcher.py` queda eliminado y sustituido por poda por antigüedad (30 días).
    - TRUTH-W2: El predicado de skip compartido `Ingestor.should_skip(path) -> bool` es usado por `handle_new_file` y por el watcher; un archivo con volcado previo se salta SOLO si el destino sigue en disco — un destino borrado ⇒ re-volcar (reparación F-02 intacta).
    - TRUTH-W3: skip por veredicto: `errored` siempre se re-maneja; `filtered` se salta (mismo content-filter); `copied` se salta solo si el destino persiste en disco.
    - TRUTH-W4: Las rutas se normalizan (`os.path.normpath`) al grabar/consultar el inventario, evitando dedupe fallida por variantes de ruta en Windows.
    - TRUTH-W5 (R3): `_resolve_db_path()` es independiente del CWD en dev — ya aplicado (commit 1cf4fd8); queda un test de regresión que lo demuestra.
    - TRUTH-W6 (R5): `_auto_sync_check` despacha `list_devices`/`is_reachable` fuera de la UI thread vía `_run_background` — ya aplicado (commit 84e89aa); queda un test de regresión que demuestra el despacho off-thread.
  artifacts:
    - app/core/db.py (tabla `watcher_seen` en `create_tables`; métodos `load_seen`, `save_seen`, `prune_seen`)
    - app/core/ingestor.py (método `should_skip` refactorizado desde el preámbulo de `handle_new_file` 338-365)
    - app/core/watcher.py (`_watch`: usa `should_skip`, carga/guarda inventario DB, elimina cap 10k, poda por antigüedad)
    - tests/test_watcher_inventory.py (NUEVO: F-02, cap eliminado, veredictos, poda, normalización, predicado compartido)
    - tests/test_db.py (regresión `_resolve_db_path` CWD-independent + tabla watcher_seen + load/save/prune)
    - tests/test_main_window.py (regresión auto-sync off-thread D-05)
  key_links:
    - db.watcher_seen (tabla) ↔ Ingestor.should_skip ↔ watcher._watch → handle_new_file (F-02)
    - _auto_sync_check (main_window.py:229-268) → _run_background (2153-2165) → _process_device_poll
  assumptions:
    - '[Edge R2 boundary] umbral de poda = 30 días, según RESEARCH.md; el cap 10k se elimina por completo (peso muerto — el watcher es single-pass, watcher.py:59-63). verified by backstop: test de poda con first_seen antiguo manipulado (UPDATE a fecha vieja) ⇒ prune_seen lo elimina; test de cap eliminado (sin `while len(scanned_files) > 10000`).'
    - '[Edge R2 precision] el skip del watcher debe pasar por el MISMO predicado que handle_new_file (destino presente en disco) para no romper F-02. verified by backstop: test F-02 sobre el skip del watcher con destino borrado (no solo handle_new_file), y el test existente `test_completed_file_with_missing_dest_is_recopied` sigue pasando.'
    - '[Edge R3] "unclassified — review manually": DB path en dev ya anclado (`data_dir` db.py:8-17); solo falta test de regresión. verified by backstop: test en test_db.py con os.chdir temporal.'
    - '[Edge R5] "unclassified — review manually": polling ya off-thread; el test de regresión verificará despacho vía `_run_background` y no-inline de `_process_device_poll`. verified by backstop: TestAutoSyncOffThread.'
    - 'Reversibility (costly, D-04): añadir `watcher_seen` es migración aditiva (CREATE TABLE IF NOT EXISTS) sin tocar filas existentes; revertir elimina tabla + métodos + skip. Se implementa con contendido aditivo para acotar el coste de reversión.'
  prohibitions:
    - El skip del watcher NO debe saltar un archivo solo por inventario sin comprobar la presencia del destino en disco (rompería F-02).
    - El veredicto `errored` NO debe saltarse; siempre se re-maneja.
    - NO se debe introducir un archivo JSON propio para el inventario (RESEARCH.md Don't Hand-Roll: tabla SQLite vía get_connection).
    - NO se debe modificar el flujo WiFi (`_ensure_wifi_ingestion`): el inventario solo aplica a fuentes watcher (SD/MTP/FTP staging).
---

<objective>
Resolver el bug de watcher re-ingesta (requisito R2, decisión D-04) persistiendo el inventario de archivos vistos por fuente en una tabla SQLite `watcher_seen` con poda por antigüedad, y eliminar el cap duro de 10k; además cubrir con tests de regresión los fixes ya aplicados de DB path CWD (R3) y polling de dispositivos fuera de la UI thread (R5).

Purpose: El cap de 10k era peso muerto (el watcher es single-pass) y el coste real era que en cada re-ingesta los archivos fuera de ventana volvían a sondearse con ffprobe (hasta 10 s cada uno) porque la LRU de metadata_engine es en memoria. La persistencia del inventario con veredicto evita ese re-sondeo preservando la reparación F-02 (destino borrado ⇒ re-volcar).

Output: `db.py` con tabla + métodos `load_seen`/`save_seen`/`prune_seen`; `ingestor.py` con `should_skip`; `watcher.py` sin cap 10k y con inventario persistente; `tests/test_watcher_inventory.py` (nuevo); regresión `test_db.py` y `test_main_window.py`.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Fase 1.5.0)
@.planning/phases/01.5.0-consolidaci-n-y-bugs-del-flujo/01.5.0-CONTEXT.md (D-04, D-05, discreciones)
@.planning/phases/01.5.0-consolidaci-n-y-bugs-del-flujo/01.5.0-RESEARCH.md (secciones "D-04", "D-05", "Discreción 1", "Discreción 2" + Pattern 3 + Pitfalls 1/4)
@.planning/phases/01.5.0-consolidaci-n-y-bugs-del-flujo/01.5.0-PATTERNS.md (secciones db.py, ingestor.should_skip, watcher.py, tests nuevos)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tabla watcher_seen + métodos load_seen/save_seen/prune_seen en db.py</name>
  <files>app/core/db.py, tests/test_db.py</files>
  <read_first>
    - app/core/db.py (líneas 1-21 imports + data_dir/_resolve_db_path, 26-35 get_connection, 37-313 create_tables, 291-296 analogo footage_folders, 569-581 get_files_by_session, 1089-1098 set_setting)
    - tests/test_db.py (setup/tearDown TestDatabaseManager 11-16)
  </read_first>
  <action>En `app/core/db.py`:
  1. Añade al final del `create_tables()` (tras `footage_folders`, ~línea 296) esta tabla aditiva (NO toces tablas existentes):
     `watcher_seen(source_path TEXT NOT NULL, file_path TEXT NOT NULL, first_seen TEXT DEFAULT CURRENT_TIMESTAMP, last_verdict TEXT CHECK (last_verdict IN ('copied','filtered','errored')), PRIMARY KEY (source_path, file_path))` — CREATE TABLE IF NOT EXISTS.
  2. Añade métodos en la clase `DatabaseManager` siguiendo `get_connection()` per-op + WAL + parametrización (patrón de `get_files_by_session` 569-581 y `set_setting` 1089-1098):
     - `load_seen(self, source_path: str) -> set`: SELECT file_path FROM watcher_seen WHERE source_path = ? (normaliza con `os.path.normpath` al construir el set); devuelve set de `file_path` normalizados.
     - `save_seen(self, source_path: str, paths, verdict: str = "copied")`: INSERT OR IGNORE INTO watcher_seen (source_path, file_path, last_verdict) VALUES (?, ?, ?) en una transacción batch (múltiples ejecuciones + un `conn.commit()`); normaliza `os.path.normpath` en ambas direcciones.
     - `prune_seen(self, source_path: str, days: int = 30)`: DELETE FROM watcher_seen WHERE source_path = ? AND first_seen < datetime('now', ?) (con parametrización `f'-{days} days'` — sin interpolar el número en SQL); `conn.commit()`.
  Importa `os` en db.py si no está ya (verifica los imports actuales; `from pathlib import Path` puede ya existir). Usa placeholders `?` en toda consulta nueva — jamás concatenar strings (V5 Input Validation: SQL parametrizado).
  En `tests/test_db.py`:
  - Test migración aditiva: crear `DatabaseManager(db_path=<tmp>/test.db)` → assert existe la tabla `watcher_seen` (`SELECT name FROM sqlite_master WHERE type='table' AND name='watcher_seen'`); re-abrir DatabaseManager sobre la misma DB → assert sigue existiendo (idempotente) y las tablas previas (p. ej. `projects`) intactas.
  - Test load/save/prune roundtrip: `save_seen("E:/src", {"E:/x/a.mp4", "E:/x/b.mp4"})` → `load_seen("E:/src")` devuelve el set (normalizado); `save_seen` repetido no duplica (INSERT OR IGNORE); insertar una fila con `first_seen` antiguo (UPDATE a fecha vieja) y `prune_seen(days=30)` → la fila antigua se elimina, la reciente no.
  - Test normalización: `save_seen("E:/src", {"E:/x/a.mp4"})` → `load_seen("E:\\x\\a.mp4")` (variante backslash Windows) devuelve el mismo path (Pitfall 4).</action>
  <acceptance_criteria>
    - `grep -n "watcher_seen" app/core/db.py` encuentra el CREATE TABLE (dentro de create_tables) y las definiciones de `load_seen`/`save_seen`/`prune_seen`.
    - `grep -n "INSERT OR IGNORE INTO watcher_seen" app/core/db.py` presente en save_seen.
    - Comando: `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_db -v` — 3+ tests nuevos (migración aditiva, load/save/prune roundtrip, normalización) pasan junto a los existentes.
  </acceptance_criteria>
  <verify>
    <automated>$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_db -v</automated>
  </verify>
  <done>La tabla `watcher_seen` existe aditivamente y es idempotente; `load_seen`/`save_seen`/`prune_seen` funcionan con transacción, normalización de rutas y poda por antigüedad; los tests de regresión DB pasan.</done>
</task>

<task type="auto">
  <name>Task 2: Predicado compartido Ingestor.should_skip + uso en watcher._watch (sin cap 10k)</name>
  <files>app/core/ingestor.py, app/core/watcher.py</files>
  <read_first>
    - app/core/ingestor.py (líneas 338-380 handle_new_file preámbulo, 382-400 _completed_dest_path, 311-333 begin_watching/watcher_completed)
    - app/core/watcher.py (líneas 26-67 _watch: single-pass, poda stale, cap 10k 52-54)
  </read_first>
  <action>En `app/core/ingestor.py`:
  1. Refactoriza el preámbulo de `handle_new_file` (338-365) a un método `should_skip(self, source_path: str) -> bool` con la MISMA semántica: normaliza `source_path` con `os.path.normpath`; si `self._completed_dest_path(source_path)` no es None y `os.path.isfile(known_dest)` → skip True (destino presente); si `known_dest` es None y `self._content_filter` y `NOT self._matches_filter(source_path)` → skip True (fuera de ventana); en caso contrario skip False (remanejar — preserva F-02: destino borrado ⇒ re-volcar). Incluye la lógica de veredicto por inventario: consulta el inventario (`db.load_seen`/veredicto) para resolver 'copied' (destino presente ⇒ skip), 'errored' (⇒ no skip, siempre se remaneja), 'filtered' (⇒ skip solo si el mismo content-filter). `handle_new_file` reutiliza `should_skip` como preámbulo (mismo resultado que antes del refactor) y registra el veredicto en el inventario cuando cortocircuita.
  2. Mantén `_completed_dest_path` intacto (382-400).
  En `app/core/watcher.py`, en `_watch` (26-67) D-04:
  - Reemplaza el `if path not in scanned_files and not _is_system_entry(file):` por un chequeo que use `self.ingestor.should_skip(path)` antes de `handle_new_file`: `if not self.ingestor.should_skip(path) and not _is_system_entry(file): self.ingestor.handle_new_file(path)`.
  - Elimina el bloque del cap duro (52-54: el `while len(scanned_files) > CAP:` con `popitem(last=False)` donde `CAP` es el literal del cap actual de diez mil entradas), reemplazándolo por la poda por antigüedad en DB. No dejes ese literal del cap duro en `watcher.py`.
  - Carga el inventario con `db.load_seen(source_dir)` al inicio de la pasada y guarda con `db.save_seen(source_dir, current_files)` al final; poda con `db.prune_seen(source_dir, days=30)`.
  - Mantén la semántica single-pass (guard `first_pass` 59-63) y la poda de `scanned_files` de archivos que ya no existen.
  NO interactúes con el flujo WiFi (`_ensure_wifi_ingestion`). NO añadas imports nuevos innecesarios. Respeta el estilo del archivo (docstrings/prints español).</action>
  <acceptance_criteria>
    - `grep -n "def should_skip" app/core/ingestor.py` presente.
    - `grep -n "handle_new_file" app/core/watcher.py` muestra el uso de `self.ingestor.should_skip(path)` en `_watch`.
    - `grep -c "10000" app/core/watcher.py` = 0 (cap 10k eliminado de watcher.py).
    - `grep -n "prune_seen\|load_seen\|save_seen" app/core/watcher.py` presente (inventario DB en _watch).
    - Comando: `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_ingestor -v` — el test F-02 existente (`test_completed_file_with_missing_dest_is_recopied`) sigue pasando (el refactor a `should_skip` no rompe la semántica).
  </acceptance_criteria>
  <verify>
    <automated>$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_ingestor -v</automated>
  </verify>
  <done>`should_skip` encapsula el predicado compartido (destino presente ⇒ skip; destino borrado ⇒ re-manejar F-02; veredicto 'errored' ⇒ siempre remanajar); `handle_new_file` lo reutiliza; el watcher elimina el cap 10k, usa `should_skip` y persiste/poda el inventario por fuente.</done>
</task>

<task type="auto">
  <name>Task 3: tests/test_watcher_inventory.py (nuevo) — F-02, cap eliminado, veredictos, poda, normalización</name>
  <files>tests/test_watcher_inventory.py</files>
  <read_first>
    - tests/test_ingestor.py (setup/tearDown 34-62, FakeMeta, patrón singleton swap)
    - tests/test_db.py (setup/tearDown TestDatabaseManager 11-16)
    - tests/test_watcher_inventory.py (archivo nuevo — modelarlo sobre los dos anteriores; ver PATTERNS.md sección test_watcher_inventory)
  </read_first>
  <action>Crea `tests/test_watcher_inventory.py` (nuevo) con `unittest`. Modelalo sobre `tests/test_ingestor.py` (FakeMeta + tempdir + singleton swap) y `tests/test_db.py` (DatabaseManager temp). No uses hilos del watcher real: ejercita `should_skip` y los métodos DB directamente con un `FakeIngestor` que exponga `should_skip`/`handle_new_file`/`watcher_completed` (PATTERNS.md caso FakeIngestor).
  Casos D-04 obligatorios:
  1. F-02 conservado en el SKIP del watcher (Pitfall 1): un archivo con fila 'completed' en `files` pero sin destino en disco ⇒ `should_skip` devuelve False (se remaneja); con destino presente ⇒ True.
  2. Cap duro eliminado: assert `_watch`/watcher ya no contiene la poda del cap duro (por concepto: el literal del cap actual, diez mil entradas) y que el inventario persiste en DB sin cap.
  3. Veredictos: 'copied' con destino presente ⇒ skip; 'filtered' (mismo content-filter) ⇒ skip; 'errored' ⇒ siempre se remaneja (skip False).
  4. Poda por antigüedad: insertar `first_seen` antiguo (UPDATE a fecha vieja) ⇒ `prune_seen(days=30)` lo elimina; límite 30 días.
  5. Normalización de rutas: grabar "E:/x/y.mp4" ⇒ consultar "E:\x\y.mp4" (Windows) ⇒ misma entrada (Pitfall 4).
  6. Predicado compartido: `handle_new_file` usa `should_skip` como preámbulo (mismo resultado que antes del refactor — los tests existentes de F-02 en test_ingestor.py siguen verdes, ya cubierto por Task 2).
  El archivo debe ser ejecutable con `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_watcher_inventory -v`.</action>
  <acceptance_criteria>
    - `tests/test_watcher_inventory.py` existe y contiene clases/métodos de los 6 casos D-04.
    - Comando: `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_watcher_inventory -v` — 6+ tests pasan.
    - `grep -n "should_skip\|prune_seen\|watcher_seen\|normpath" tests/test_watcher_inventory.py` — references presentes (cubre los casos 1/3/4/5).
  </acceptance_criteria>
  <verify>
    <automated>$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_watcher_inventory -v</automated>
  </verify>
  <done>`tests/test_watcher_inventory.py` cubre F-02 en el skip del watcher, cap 10k eliminado, veredictos, poda por antigüedad y normalización de rutas, y pasa en verde.</done>
</task>

<task type="auto">
  <name>Task 4: Tests de regresión R3 (DB path CWD) y R5 (auto-sync off-thread) + suite completa</name>
  <files>tests/test_db.py, tests/test_main_window.py</files>
  <read_first>
    - tests/test_db.py (setup/tearDown TestDatabaseManager 11-16)
    - tests/test_main_window.py (setup setUpClass/setUp 28-66, patrón offscreen + monkeypatch db)
    - app/core/db.py (líneas 8-17 data_dir/_resolve_db_path — ya anclado)
    - app/ui/main_window.py (líneas 229-268 _auto_sync_check, 2153-2165 _run_background)
  </read_first>
  <action>En `tests/test_db.py` añade un test de regresión de independencia del CWD (R3): en una clase con `os.chdir` a un directorio temporal, guarda el CWD original, haz `os.chdir(<tmp>)`, assert `db._resolve_db_path()` apunta a `<repo>/data/sd_import.db` (`endswith(os.path.join("data", "sd_import.db"))` y que el base es la raíz del repo), y restaura el CWD en `finally`/teardown.
  En `tests/test_main_window.py` añade una clase `TestAutoSyncOffThread` (R5) con:
  - (a) `window._auto_sync_check()` con `mock.patch.object(window, "_run_background")` ⇒ assert `_run_background` fue llamado con un callable + callback; `_process_device_poll` NO ejecutado inline (`assert not called`).
  - (b) `window._poll_in_progress = True` ⇒ `_run_background` NO se llama (guard).
  - (c) dentro del callable de fondo mockeado: `threading.get_ident()` del callable != `threading.get_ident()` del test (verifica no-UI-thread); `mtp.WpdBackend().list_devices` y `ftp.FtpBackend().is_reachable` patcheados invocables dentro del callable.
  Configura las sesiones del proyecto con `device_id` para que `_auto_sync_check` tenga sesiones que sondear (patrón del setup existente 28-66: inserta proyecto + sesiones). Luego ejecuta la suite completa: `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest discover -s tests -p "test_*.py"` y confirma green.</action>
  <acceptance_criteria>
    - `grep -n "def test.*cwd\|os.chdir" tests/test_db.py` presente (test R3 DB path CWD-independent).
    - `grep -n "class TestAutoSyncOffThread" tests/test_main_window.py` presente (test R5).
    - Comando: `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_db tests.test_main_window -v` — tests R3 + R5 pasan junto a los existentes.
    - Comando final: `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest discover -s tests -p "test_*.py"` — suite completa `OK` (gate de fase, Success Criterion 2).
  </acceptance_criteria>
  <verify>
    <automated>$env:QT_QPA_PLATFORM="offscreen"; python -m unittest discover -s tests -p "test_*.py"</automated>
  </verify>
  <done>Los tests de regresión R3 (DB path independiente del CWD) y R5 (auto-sync despacha off-thread vía _run_background) pasan; la suite completa está verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| watcher._watch (thread) → SQLite DB | Escritura/lectura del inventario en `watcher_seen` desde el hilo del watcher; conexión per-op con `check_same_thread=False` + WAL. |
| Ingestor.should_skip → DB (`_completed_dest_path`) | Consulta SQL parametrizada del destino previo. |
| main_window._auto_sync_check → worker (QThread) | `list_devices` (WPD COM) y `is_reachable` (FTP) despachados off-thread. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1.5-21 | Tampering | Inyección SQL en el inventario D-04 (`load_seen`/`save_seen`/`prune_seen`/`should_skip`) | medium | mitigate | Solo SQL parametrizado con placeholders `?` vía `db.get_connection()` (patrón `_completed_dest_path`, ingestor.py:389-393); ninguna concatenación de cadenas; `source_path`/`file_path` nunca interpolados como SQL. |
| T-1.5-22 | (integridad de datos) | Skip del watcher por inventario sin verificar destino → pérdida de copia (F-02 roto) | high | mitigate | `should_skip` exige destino presente en disco (`os.path.isfile(known_dest)`) antes de saltar; 'errored' siempre se remaneja; bloqueo ASVS L1: dato perdido sin copia en el archivo. |
| T-1.5-23 | (información) | Dedupe fallida por rutas no normalizadas → re-copia + filas MD5 nuevas | medium | mitigate | `os.path.normpath` en ambas direcciones (grabar/consultar inventario, Pitfall 4); test de normalización. |
| T-1.5-24 | Denial of Service | `list_devices`/`is_reachable` en UI thread congelan la UI (R5) | low | mitigate | Ya despachado off-thread vía `_run_background`+QThread (commit 84e89aa); test de regresión confirma el despacho y los guards `_poll_in_progress`/`_stage_thread.isRunning()`. |
| T-1.5-SC | Tampering | npm/pip/cargo installs | N/A | N/A | Esta plan NO instala paquetes. Sin gate de paquetes. |

Sin exposición de red nueva: el inventario y el polling operan sobre recursos locales existentes; el flujo FTP/WiFi inbox existente queda intacto y fuera del alcance de esta plan.
</threat_model>

<verification>
Chequeos de la plan (ver `01.5.0-VALIDATION.md`):
- `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_db -v` — migración aditiva watcher_seen + load/save/prune + normalización + CWD-independent (R3).
- `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_watcher_inventory -v` — F-02 en skip, cap eliminado, veredictos, poda, normalización (R2).
- `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_main_window -v` — TestAutoSyncOffThread off-thread (R5).
- `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest discover -s tests -p "test_*.py"` — suite completa verde (gate de fase, Success Criterion 2).
</verification>

<success_criteria>
R2/R3/R5 resueltos: el inventario del watcher se persiste por fuente con poda por antigüedad, el cap 10k queda eliminado y el skip compartido preserva F-02 (destino borrado ⇒ re-volcar); `_resolve_db_path` es independiente del CWD (regresión); el polling de dispositivos despacha off-thread (regresión); la suite completa pasa con los tests de regresión.
</success_criteria>

<output>
Create `.planning/phases/01.5.0-consolidaci-n-y-bugs-del-flujo/01.5.0-03-SUMMARY.md` when done.
</output>
