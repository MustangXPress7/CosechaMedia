# Phase 01.5.0: Consolidación y bugs del flujo - Pattern Map

**Mapped:** 2026-08-29
**Files analyzed:** 10 (5 producción modificados + 4 tests extendidos + 1 test nuevo)
**Analogs found:** 10 / 10

> Todos los archivos de esta fase son **modificaciones quirúrgicas de archivos existentes**
> (no hay módulos nuevos de producción). El mejor analogo de cada archivo es **el propio
> archivo**, más los patrones internos que ya contiene (dicts de metadata, DB per-op,
> worker QThread, predicado F-02). Para el único archivo nuevo (`test_watcher_inventory.py`),
> los analogos son `tests/test_ingestor.py` + `tests/test_db.py`.

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `app/core/metadata_engine.py` | service | transform (subprocess CLI → dict) | El propio archivo (rama éxito 171-190 / ramas fallo 250-292, `_finalize_dates` 119-137) | exact (self) |
| `app/core/ingestor.py` | service | file-I/O + CRUD (copia + DB) | El propio archivo (`copy_verified` 32-76, `handle_new_file` 338-365, `_completed_dest_path` 382-400) | exact (self) |
| `app/core/watcher.py` | service | event-driven (polling single-pass) | El propio archivo (`_watch` 26-67) + `Ingestor._completed_dest_path` (ingestor.py:382-400) | exact (self) |
| `app/core/db.py` | model | CRUD (SQLite, WAL) | El propio archivo (tabla `footage_folders` 291-296, `device_settings` 260-269, método `get_files_by_session` 569-581) | exact (self) |
| `app/ui/main_window.py` | component/controller | event-driven (Qt signals) | El propio archivo (`on_file_finished` 1863-1889, `_clear_completed_rows` 2188-2199) | exact (self) |
| `tests/test_metadata_engine.py` | test | unit (mock subprocess) | El propio archivo (`TestGetVideoMetadata` 119-180 con `patch("subprocess.run")`) | exact (self) |
| `tests/test_ingestor.py` | test | unit (tempdir + singleton monkeypatch) | El propio archivo (`TestIngestor` setUp/tearDown 34-62, tests 131-150 a re-escribir) | exact (self) |
| `tests/test_main_window.py` | test | unit (Qt offscreen, QApplication) | El propio archivo (`TestCameraDetectionToken` 28-100) | exact (self) |
| `tests/test_db.py` | test | unit (DatabaseManager temp DB) | El propio archivo (`TestDatabaseManager` 10-42) | exact (self) |
| `tests/test_watcher_inventory.py` | test (NUEVO) | unit (DB + watcher sin hilos) | `tests/test_ingestor.py` (FakeMeta + tempdir + singleton swap) + `tests/test_db.py` (tablas) | role-match |

---

## Pattern Assignments

### `app/core/metadata_engine.py` (service, transform) — D-01/D-02

**Analog:** `app/core/metadata_engine.py` (propio archivo)

**Imports pattern** (lines 1-9):
```python
import subprocess
import json
import os
import threading
from datetime import datetime
from typing import Optional, Dict, List
import sys

from app.core.db import db
```
Sin cambios de imports para D-01 — `subprocess`, `json`, `os` ya importados.

**Comando ffprobe actual (150-168) — punto de retry D-01:**
```python
cmd = [
    self.ffprobe_path,
    "-v", "quiet",
    "-print_format", "json",
    "-show_format",
    "-show_streams",
    "-read_intervals", "%+0.5",
    file_path
]
try:
    result = subprocess.run(
        cmd,
        capture_output=True,
        text=True,
        check=True,
        startupinfo=self.startupinfo,
        timeout=10
    )
    data = json.loads(result.stdout)
```

**Dict de éxito (171-190) — añadir `"metadata_verified": True` (D-01):**
```python
metadata = {
    "camera_model": "Unknown",
    "camera_make": "Unknown",
    "serial": None,
    "creation_date": None,
    "creation_dt": None,
    "date_source": None,
    "duration": 0,
    "bitrate": 0,
    "format": "",
    "width": 0,
    "height": 0,
    "fps": 0,
    "codec": "",
    "audio_codec": "",
    "file_size": os.path.getsize(file_path),
    "is_video": False,
    "is_audio": False,
    "is_image": False
}
```

**Ramas fallo duplicadas (250-270 TimeoutExpired, 271-292 Exception) — extraer a `_fallback_metadata(file_path, error)`:**
```python
# 250-255 (TimeoutExpired) y 273-277 (Exception) — dict idéntico:
metadata = {"camera_model": "Unknown_Camera", "camera_make": "Unknown", "serial": None,
        "creation_date": None, "creation_dt": None, "date_source": None,
        "duration": 0, "bitrate": 0, "format": "", "width": 0, "height": 0,
        "fps": 0, "codec": "", "audio_codec": "", "file_size": 0,
        "is_video": False, "is_audio": False, "is_image": False}
try:
    metadata["file_size"] = os.path.getsize(file_path)
except OSError:
    pass
# 260-269: fallback por extensión (image/raw → _camera_from_extension) + _finalize_dates
```
El helper nuevo fija `"metadata_verified": False` y `"metadata_error": str(error)`.

**Retry degradado (esqueleto de RESEARCH.md Pattern 1 — reemplaza el try/except actual):**
```python
try:
    result = subprocess.run(cmd, capture_output=True, text=True, check=True,
                            startupinfo=self.startupinfo, timeout=10)
except subprocess.TimeoutExpired:
    # Reintento degradado: solo duración (formato), timeout ampliado.
    retry_cmd = [self.ffprobe_path, "-v", "quiet", "-print_format", "json",
                 "-show_format", file_path]
    try:
        result = subprocess.run(retry_cmd, capture_output=True, text=True,
                                check=True, startupinfo=self.startupinfo,
                                timeout=30)
    except (subprocess.TimeoutExpired, subprocess.CalledProcessError, OSError) as e:
        print(f"Error metadata (timeout/retry) {file_path}: {e}")
        return self._fallback_metadata(file_path, "timeout")
except (subprocess.CalledProcessError, OSError, json.JSONDecodeError) as e:
    print(f"Error metadata {file_path}: {e}")
    return self._fallback_metadata(file_path, str(e))
```
Retry SOLO en `TimeoutExpired`, sin `-show_streams`, timeout 30 (a criterio del planner). No reintentar ramas genéricas (Pitfall 6).

**`detect_camera_batch` (308-316) — no contar "Unknown_Camera" ni no-verificados (D-02):**
```python
for file_path in file_paths[:10]:
    metadata = self.get_video_metadata(file_path)
    if metadata:
        camera = metadata.get("camera_model", "Unknown")
        if camera != "Unknown":
            camera_counts[camera] = camera_counts.get(camera, 0) + 1
```
Cambio: `if camera != "Unknown"` pasa a `if metadata.get("metadata_verified") is not False and camera not in ("Unknown", "Unknown_Camera")`.

---

### `app/core/ingestor.py` (service, file-I/O + CRUD) — D-03/D-04

**Analog:** `app/core/ingestor.py` (propio archivo)

**Imports** (lines 1-14):
```python
import glob
import json
import os
import shutil
import threading
import time
from datetime import datetime
from concurrent.futures import ThreadPoolExecutor
from typing import Dict, List, Optional, Set
from app.core.db import db, data_dir
from app.core.utils import create_folder_structure, calculate_md5
from app.core.metadata_engine import metadata_engine

from PySide6.QtCore import QObject, Signal
```
`calculate_md5` se conserva importado (sigue en `_handle_reference_file` 463).

**`copy_verified` (32-76) — pase único D-03, eliminar 61-68:**
```python
def copy_verified(source_path: str, dest_path: str, progress_cb=None):
    """Copia un archivo calculando el hash del origen durante la copia y
    comparándolo con el del destino. Devuelve el hash MD5 verificado
    en éxito, o None si falla.
    Elimina el destino parcial ante cualquier error o discrepancia.
    ``progress_cb(copied, total)`` se invoca por bloque copiado."""
    import hashlib
    src_md5 = hashlib.md5()
    total = 0
    try:
        total = os.path.getsize(source_path)
    except OSError:
        pass
    try:
        with open(source_path, 'rb') as src_f, open(dest_path, 'wb') as dst_f:
            copied = 0
            while True:
                chunk = src_f.read(8192)
                if not chunk:
                    break
                src_md5.update(chunk)
                dst_f.write(chunk)
                copied += len(chunk)
                if progress_cb:
                    progress_cb(copied, total)
        try:
            shutil.copystat(source_path, dest_path)
        except OSError:
            pass
        src_hash = src_md5.hexdigest()
        dest_hash = calculate_md5(dest_path)          # ← QUITAR (61-68)
        if src_hash and dest_hash and src_hash != dest_hash:
            try:
                os.remove(dest_path)                  # ← QUITAR
            except OSError:
                pass
            return None                               # ← QUITAR
        return src_hash                               # ← queda solo `return src_hash`
    except Exception as e:
        print(f"Error copying {source_path}: {e}")
        try:
            os.remove(dest_path)
        except OSError:
            pass
        return None                                   # ← rama 70-76 intacta
```
Garantía post-cambio: el destino contiene exactamente los bytes leídos+hasheados; la rama de borrado ante excepción se conserva.

**Wrapper `_copy_verified` (565-577) — sin cambios:**
```python
def _copy_verified(self, source_path: str, dest_path: str) -> bool:
    """Copia verificada por MD5 con progreso por archivo (emitido por %)."""
    state = {"last": -1}

    def _progress(copied: int, total: int):
        if not total:
            return
        pct = int(copied * 100.0 / total)
        if pct != state["last"]:
            state["last"] = pct
            self.copy_progress.emit(source_path, copied, total)

    return copy_verified(source_path, dest_path, progress_cb=_progress)
```
NOTA: la anotación `-> bool` es incorrecta (devuelve `str | None`); no cambiar en este fix salvo que el planner lo pida.

**Preámbulo de `handle_new_file` (338-365) → refactor a `should_skip` (D-04):**
```python
def handle_new_file(self, source_path: str):
    with self._processed_lock:
        if source_path in self.processed_files:
            return
        if self._stop_event.is_set():
            return

    # Dedupe con verificación real del destino: un volcado previo solo
    # exime de re-copiar si la copia sigue en disco. Si se borró del
    # archivo, la ingesta la vuelve a volcar desde el origen.
    known_dest = self._completed_dest_path(source_path)
    if known_dest is not None and os.path.isfile(known_dest):
        with self._processed_lock:
            self._copied_files.add(source_path)
        with self._stats_lock:
            self._stats["skipped"] += 1
        return

    # Reparación de copia borrada: un archivo con volcado previo (fila
    # completed/reference) cuya copia falta en el archivo se re-vuelca
    # SIEMPRE, aunque haya quedado fuera de la ventana actual («últimos
    # x días»). El filtro de contenido decide solo sobre contenido nuevo;
    # la ventana se recalcula desde el último volcado y esos archivos de
    # tandas anteriores quedarían excluidos sin esta salvedad.
    if known_dest is None and self._content_filter and not self._matches_filter(source_path):
        with self._stats_lock:
            self._stats["skipped"] += 1
        return
```
Este bloque (345-365) se encapsula en `should_skip(path) -> bool` con el mismo veredicto, conservando exactamente la semántica F-02. El watcher usa el predicado antes de llamar `handle_new_file`.

**`_completed_dest_path` (382-400) — patrón SQL parametrizado a reutilizar:**
```python
def _completed_dest_path(self, source_path: str) -> Optional[str]:
    """Última ruta destino registrada como volcada para un origen."""
    with self._db_lock:
        try:
            conn = db.get_connection()
            try:
                cursor = conn.cursor()
                cursor.execute(
                    "SELECT dest_path FROM files WHERE source_path = ? "
                    "AND status IN ('completed', 'reference') "
                    "ORDER BY id DESC LIMIT 1",
                    (source_path,)
                )
                row = cursor.fetchone()
            finally:
                conn.close()
            return row[0] if row else None
        except Exception:
            return None
```
Normalizar rutas con `os.path.normpath` al grabar/consultar inventario (Pitfall 4).

---

### `app/core/watcher.py` (service, event-driven polling) — D-04

**Analog:** `app/core/watcher.py` (propio archivo) + `db.py` métodos

**Archivo completo actual (1-67) — esqueleto a modificar:**
```python
import os
import threading
import time
from collections import OrderedDict
from typing import Callable, Optional
from app.core.ingestor import Ingestor
from app.core.metadata_engine import _is_system_entry

class FileSystemWatcher:
    def __init__(self, source_dir: str, ingestor: Ingestor, status_callback: Optional[Callable[[str], None]] = None):
        self.source_dir = source_dir
        self.ingestor = ingestor
        self.status_callback = status_callback
        self.running = False
        self._thread = None

    def start(self):
        if not self.running:
            self.running = True
            self._thread = threading.Thread(target=self._watch, daemon=True)
            self._thread.start()

    def stop(self):
        self.running = False
```

**`_watch` (26-67) — puntos de cambio:**
```python
def _watch(self):
    scanned_files = OrderedDict()
    print(f"Watcher started on: {self.source_dir}")

    first_pass = True
    while self.running:
        try:
            current_files = set()
            for root, dirs, files in os.walk(self.source_dir):
                dirs[:] = [d for d in dirs if not _is_system_entry(d)]
                for file in files:
                    path = os.path.join(root, file)
                    current_files.add(path)

                    # Ignora archivos del sistema/ocultos: el chequeo debe
                    # ser sobre el nombre, no sobre la ruta absoluta
                    # (path.startswith('.') nunca se cumple en Windows).
                    if path not in scanned_files and not _is_system_entry(file):
                        self.ingestor.handle_new_file(path)
                    scanned_files[path] = None

            # Evict entries for files that no longer exist on disk
            stale = [k for k in scanned_files if not os.path.exists(k)]
            for k in stale:
                del scanned_files[k]

            # Hard cap fallback: evict oldest entries if still over limit
            while len(scanned_files) > 10000:          # ← ELIMINAR (52-54)
                scanned_files.popitem(last=False)

            if self.status_callback:
                self.status_callback(f"Escaneados {len(current_files)} archivos en {self.source_dir}")

            if first_pass:
                first_pass = False
                self.ingestor.watcher_completed()
                self.running = False
                return

            time.sleep(1.0)
        except Exception as e:
            print(f"Watcher error: {e}")
```
Cambios D-04: (a) reemplazar el `if path not in scanned_files` por `not self.ingestor.should_skip(path)` (predicado compartido, F-02 intacto); (b) eliminar cap 10k (52-54); (c) cargar inventario con `db.load_seen(source_dir)` y guardar con `db.save_seen(source_dir, paths)` al final de la pasada; (d) poda por antigüedad `db.prune_seen(source_dir, days=30)` (a criterio del planner). Ver `db.py` para los métodos.

---

### `app/core/db.py` (model, CRUD SQLite) — D-04 tabla + métodos

**Analog:** `app/core/db.py` (propio archivo)

**`get_connection()` (31-35) — patrón per-op obligatorio:**
```python
def get_connection(self):
    conn = sqlite3.connect(self.db_path, check_same_thread=False, timeout=5)
    conn.execute("PRAGMA journal_mode=WAL")
    conn.row_factory = sqlite3.Row
    return conn
```

**Migración aditiva — analogo `footage_folders` (291-296) dentro de `create_tables` (37-313):**
```python
cursor.execute('''
    CREATE TABLE IF NOT EXISTS footage_folders (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        name TEXT NOT NULL UNIQUE
    )
''')
```
Nueva tabla (esquema de RESEARCH.md Pattern 3):
```sql
CREATE TABLE IF NOT EXISTS watcher_seen (
    source_path  TEXT NOT NULL,
    file_path    TEXT NOT NULL,
    first_seen   TEXT DEFAULT CURRENT_TIMESTAMP,
    last_verdict TEXT CHECK (last_verdict IN ('copied','filtered','errored')),
    PRIMARY KEY (source_path, file_path)
);
```
(Añadir al final de `create_tables`, tras footage_folders; sin tocar tablas existentes — migración aditiva.)

**Analogos de migración con `PRAGMA table_info` + `ALTER` (247-251) si se añade columna después:**
```python
cursor.execute("PRAGMA table_info(inbox_senders)")
sender_cols = [row[1] for row in cursor.fetchall()]
if "location" not in sender_cols:
    cursor.execute(
        "ALTER TABLE inbox_senders ADD COLUMN location TEXT DEFAULT ''")
```

**Método SELECT — analogo `get_files_by_session` (569-581):**
```python
def get_files_by_session(self, session_id: int):
    conn = self.get_connection()
    cursor = conn.cursor()
    cursor.execute(
        '''SELECT source_path, dest_path, file_size, md5_hash, status, verified_at
           FROM files WHERE session_id = ? ORDER BY id''',
        (session_id,)
    )
    rows = [{"source_path": r[0], "dest_path": r[1], "file_size": r[2],
             "md5_hash": r[3], "status": r[4], "verified_at": r[5]}
            for r in cursor.fetchall()]
    conn.close()
    return rows
```
`load_seen(source_path)` sigue este patrón (devuelve `set` de `file_path`). Normalizar rutas (`os.path.normpath`) en ambas direcciones (Pitfall 4).

**Método INSERT/UPDATE con commit — analogo `set_setting` (1089-1098):**
```python
def set_setting(self, key: str, value):
    """Guarda un valor de configuración general."""
    conn = self.get_connection()
    cursor = conn.cursor()
    cursor.execute(
        'INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)',
        (key, value)
    )
    conn.commit()
    conn.close()
```
`save_seen(source_path, paths)` usa `INSERT OR IGNORE` en una transacción batch (varias ejecuciones + un commit). `prune_seen(source_path, days)` usa `DELETE WHERE first_seen < datetime('now','-30 days')` + `conn.commit()`.

**Discreción DB path — YA CORREGIDO (8-17), solo test de regresión:**
```python
def data_dir() -> str:
    """Directorio de datos de la app (junto al ejecutable si frozen)."""
    if getattr(sys, "frozen", False):
        base_dir = os.path.dirname(sys.executable)
    else:
        from pathlib import Path
        base_dir = str(Path(__file__).resolve().parents[2])
    data_dir_path = os.path.join(base_dir, "data")
    os.makedirs(data_dir_path, exist_ok=True)
    return data_dir_path
```

**Singleton al final (1101):** `db = DatabaseManager()` — los tests monkeypachean `db` a nivel de módulo.

---

### `app/ui/main_window.py` (component/controller, event-driven) — D-02

**Analog:** `app/ui/main_window.py` (propio archivo)

**Imports relevantes para el marker (17-22 y 1-60, patrón):**
```python
import os
import sys
import time
# ...
from PySide6.QtCore import QTimer, QThread, Qt, QByteArray
from PySide6.QtWidgets import (QMainWindow, QWidget, QVBoxLayout, QTableWidgetItem, ...)
# ...
from app.core.db import db
from app.core import mtp
from app.core.ftp import FtpBackend
```

**`on_file_finished` (1863-1889) — punto del marker D-02 (célula cámara, columna 1):**
```python
def on_file_finished(self, source_path, dest_path, success, metadata=None, ingestor=None):
    item = self._file_row_map.get(self._file_row_key(source_path, ingestor))
    if item is not None:
        row = self.table.indexFromItem(item).row()
        if self.project_camera_detection_mode != "manual" and metadata and metadata.get("camera_model") != "Unknown":
            camera_item = QTableWidgetItem(metadata["camera_model"])
            self.table.setItem(row, 1, camera_item)

        if success:
            status = self.tr("Completado")
            text_color = QColor(theme.color("success"))
        else:
            status = self.tr("Error")
            text_color = QColor(theme.color("danger"))

        status_item = QTableWidgetItem(status)
        status_item.setForeground(text_color)
        self.table.setItem(row, 2, status_item)
```

**Patrón D-02 a aplicar en la condición de la línea 1867 (RESEARCH.md Pattern 1):**
```python
if self.project_camera_detection_mode != "manual" and metadata:
    if metadata.get("metadata_verified") is False:
        camera_item = QTableWidgetItem(self.tr("⚠ Metadatos no verificados"))
    elif metadata.get("camera_model") != "Unknown":
        camera_item = QTableWidgetItem(metadata["camera_model"])
    if metadata.get("metadata_verified") is False:
        camera_item.setToolTip(self.tr("ffprobe no respondió; metadatos no verificados"))
    self.table.setItem(row, 1, camera_item)
```
**CRÍTICO (Pitfall 3):** la columna de estado (2) mantiene el texto exacto `self.tr("Completado")` — el marker vive en la célula de cámara (columna 1) + tooltip. `_clear_completed_rows` (2188-2199) borra solo `status_item.text() == done_text`.

**Relacionado — `_clear_completed_rows` (2188-2199) — NO cambiar:**
```python
def _clear_completed_rows(self):
    """Quita de la tabla de ingesta las filas cuyo estado es «Completado»."""
    done_text = self.tr("Completado")
    was_sorted = self.table.isSortingEnabled()
    self.table.setSortingEnabled(False)
    try:
        for r in range(self.table.rowCount() - 1, -1, -1):
            status_item = self.table.item(r, 2)
            if status_item and status_item.text() == done_text:
                self.table.removeRow(r)
    finally:
        self.table.setSortingEnabled(was_sorted)
```

**D-05 ya aplicado — patrón existente a NO duplicar:**
- `_sync_timer` QTimer 5000 ms (224-227) → `_auto_sync_check` (229-268) → `_run_background(_probe_devices, _on_poll_done)` (268)
- `_run_background` (2153-2165) lanza `_TaskWorker` (130-147) en QThread; guardas `_stage_thread.isRunning()` (232) y `_poll_in_progress` (239).
- Trabajo restante D-05: SOLO test de regresión en `test_main_window.py`. Hardening opcional: envolver `_disconnected_devices` (3531-3551, `mtp.WpdBackend().list_devices()` en línea 3537) en `_run_background`.

**Worker pattern de referencia — `_TaskWorker` (130-147):**
```python
class _TaskWorker(QObject):
    """Ejecuta una función en un QThread y notifica por señales Qt (cola segura)."""
    progress = Signal(str)
    finished = Signal(bool, object)

    def __init__(self, fn, *args, **kwargs):
        super().__init__()
        self._fn = fn
        self._args = args
        self._kwargs = kwargs

    def run(self):
        try:
            result = self._fn(self.progress, *self._args, **self._kwargs)
            self.finished.emit(True, result)
        except Exception as e:
            print(f"Background task error: {e}")
            self.finished.emit(False, e)
```

---

### `tests/test_metadata_engine.py` (test, unit) — extender D-01/D-02

**Analog:** el propio archivo — `TestGetVideoMetadata` (119-180)

**Patrón de mock `subprocess.run` (152-153):**
```python
with patch("subprocess.run") as mock_run:
    mock_run.return_value = type("R", (), {"stdout": self._payload(), "returncode": 0})()
    meta = self.engine.get_video_metadata(self.file)
```

**Setup (119-129):**
```python
def setUp(self):
    self.engine = MetadataEngine()
    self.engine._cache.clear()
    self.tmp = tempfile.mkdtemp()
    self.file = os.path.join(self.tmp, "clip.mp4")
    with open(self.file, "wb") as f:
        f.write(b"fakemp4data")

def tearDown(self):
    shutil.rmtree(self.tmp, ignore_errors=True)
```

**Tests nuevos D-01 (a añadir en `TestGetVideoMetadata`):**
- Retry: `mock_run.side_effect = [subprocess.TimeoutExpired("ffprobe", 10), success_result]`; assert 2 llamadas, la 2ª cmd sin `-show_streams`, timeout=30; resultado verificado.
- 2º fallo: `mock_run.side_effect = [subprocess.TimeoutExpired(...), subprocess.TimeoutExpired(...)]`; assert `metadata["metadata_verified"] is False`, `metadata["metadata_error"]` presente, `file_size` real (vía getsize), sin raise.
- **Pitfall 6:** excepción genérica (p. ej. `subprocess.CalledProcessError`) → fallback con flag, SIN retry (assert `mock_run.call_count == 1`).
- D-02 en `detect_camera_batch`: `patch.object(engine, "get_video_metadata", return_value={"camera_model": "Unknown_Camera", "metadata_verified": False})` → no se cuenta como cámara (`primary_camera == "Unknown"`).

---

### `tests/test_ingestor.py` (test, unit) — re-escribir 131-150 D-03

**Analog:** el propio archivo — `TestIngestor` setUp/tearDown (34-62)

**SetUp/tearDown (35-62) — patrón de monkeypatch de singletons:**
```python
def setUp(self):
    self.tmp = tempfile.mkdtemp(prefix="sdimport_ing_")
    self.src_dir = os.path.join(self.tmp, "src")
    self.dst_dir = os.path.join(self.tmp, "dst")
    self.resume_dir = os.path.join(self.tmp, "resume")
    os.makedirs(self.src_dir)
    os.makedirs(self.dst_dir)
    os.makedirs(self.resume_dir)

    self.db = DatabaseManager(db_path=os.path.join(self.tmp, "ingest.db"))
    self._orig_db = ingestor_module.db
    self._orig_meta = ingestor_module.metadata_engine
    self._orig_calc = ingestor_module.calculate_md5
    self._orig_data_dir = ingestor_module.data_dir
    ingestor_module.db = self.db
    ingestor_module.metadata_engine = FakeMeta()
    ingestor_module.data_dir = lambda: self.resume_dir

    self.ing = Ingestor(1, self.dst_dir, session_id=1)

def tearDown(self):
    self.ing.stop()
    self.ing.executor.shutdown(wait=True)
    ingestor_module.db = self._orig_db
    ingestor_module.metadata_engine = self._orig_meta
    ingestor_module.calculate_md5 = self._orig_calc
    ingestor_module.data_dir = self._orig_data_dir
    shutil.rmtree(self.tmp, ignore_errors=True)
```

**Tests a RE-ESCRIBIR (131-150) — ya no producen mismatch (Pitfall 5):**
- `test_mismatch_detected_and_dest_removed` (131-140): sustituir por test que verifique `calculate_md5` NO se llama durante `_copy_verified` (`ingestor_module.calculate_md5 = mock.Mock(return_value="")`; assert `not called`) + destino íntegro en disco + hash devuelto == hash real del contenido del destino (leído en el test con `self._orig_calc(dest)`).
- `test_copy_error_removes_partial` (142-150): sustituir por simulación de excepción de escritura (p. ej. patch de `open` en escritura o dest en dir inexistente con permisos) → assert destino eliminado / `None`.
- Los tests de progreso (106-129) y `test_verified_copy_success` (74-91, assert `row["md5_hash"] == self._orig_calc(dest)` en línea 90) **siguen pasando sin cambios** — el assert de la línea 90 es precisamente la propiedad D-03.

---

### `tests/test_main_window.py` (test, unit Qt offscreen) — extender D-05/D-02

**Analog:** el propio archivo — `TestCameraDetectionToken` (28-100)

**Setup (31-66) — patrón offscreen + monkeypatch de `db` en 3 módulos:**
```python
@classmethod
def setUpClass(cls):
    cls.app = QApplication.instance() or QApplication([])

def setUp(self):
    self.tmp = tempfile.mkdtemp(prefix="sdimport_camtest_")
    self._orig_db = mw.db
    self._orig_ing_db = ingestor_module.db
    self._orig_me_db = me_module.db
    self.db = DatabaseManager(db_path=os.path.join(self.tmp, "cam.db"))
    mw.db = self.db
    ingestor_module.db = self.db
    me_module.db = self.db

    conn = self.db.get_connection()
    conn.execute(
        "INSERT INTO projects (name, root_path) VALUES ('Test', ?)", (self.tmp,)
    )
    self.pid = conn.execute("SELECT last_insert_rowid()").fetchone()[0]
    conn.commit()
    conn.close()

    self.window = mw.MainWindow()
    self.window.current_project_id = self.pid

def tearDown(self):
    if hasattr(self.window, '_sync_timer') and self.window._sync_timer:
        self.window._sync_timer.stop()
    if hasattr(self.window, '_cam_timer') and self.window._cam_timer:
        self.window._cam_timer.stop()
    self.window.close()
    mw.db = self._orig_db
    ingestor_module.db = self._orig_ing_db
    me_module.db = self._orig_me_db
    shutil.rmtree(self.tmp, ignore_errors=True)
```
NOTA D-05: el `_sync_timer` (5000 ms) provoca polling en los tests — el teardown ya lo para.

**Test D-05 (nueva clase, p. ej. `TestAutoSyncOffThread`):**
- (a) `window._auto_sync_check()` con `mock.patch.object(window, "_run_background")` → assert llamado con callable + callback; `_process_device_poll` NO ejecutado inline (`assert not called`).
- (b) `window._poll_in_progress = True` → `_run_background` NO se llama.
- (c) dentro del callable de fondo mockeado: `threading.get_ident()` del callable != `threading.get_ident()` del test (verifica no-UI-thread); `mtp.WpdBackend().list_devices` y `FtpBackend.is_reachable` patcheados dentro del callable.

**Test D-02 (marker):** crear sesión activa, llamar `window.on_file_finished(src, dest, True, {"camera_model": "Unknown_Camera", "metadata_verified": False})` con una fila previa en `_file_row_map`, assert `self.table.item(row, 1).text()` contiene "no verificados" y columna 2 sigue siendo `self.tr("Completado")`. Ejercitar `_clear_completed_rows` sobre esa fila → la fila SÍ se limpia (Pitfall 3).

---

### `tests/test_db.py` (test, unit) — extender discreción DB path + D-04 tabla

**Analog:** el propio archivo — `TestDatabaseManager` (10-16)

**Setup (11-16):**
```python
def setUp(self):
    self.tmp = tempfile.mkdtemp(prefix="sdimport_db_")
    self.db = DatabaseManager(db_path=os.path.join(self.tmp, "test.db"))

def tearDown(self):
    shutil.rmtree(self.tmp, ignore_errors=True)
```

**Test regresión DB path (discreción):** en una clase con `os.chdir` a dir temporal: guardar CWD original, `os.chdir(self.tmp)`, assert `db._resolve_db_path().endswith(os.path.join("data", "sd_import.db"))` y que apunta a `<repo>/data/sd_import.db`, restaurar CWD en teardown con `try/finally`.

**Test D-04 migración aditiva:** crear DB nueva y assert la tabla `watcher_seen` existe (`SELECT name FROM sqlite_master`); re-crear DatabaseManager sobre la misma DB y assert sigue existiendo (idempotente) y tablas previas intactas. Tests de `load_seen`/`save_seen`/`prune_seen` (roundtrip, INSERT OR IGNORE duplicados, poda por antigüedad con `first_seen` manipulado, normalización `os.path.normpath`).

---

### `tests/test_watcher_inventory.py` (test NUEVO, unit DB + watcher sin hilos) — D-04

**Analogos:** `tests/test_ingestor.py` (FakeMeta, singleton swap, tempdir) + `tests/test_db.py` (DatabaseManager temp).

**Estructura a copiar (sin hilos — el watcher en tests se ejercita con `_watch` en modo síncrono o con un FakeIngestor):**
```python
import os
import tempfile
import unittest
import shutil

import app.core.watcher as watcher_module
from app.core.db import DatabaseManager
from app.core.watcher import FileSystemWatcher


class FakeIngestor:
    """Captura los paths que handle_new_file recibe y sus veredictos."""
    def __init__(self):
        self.handled = []
        self.skipped = []
    def should_skip(self, path):
        # espejo del predicado real (fila completed + dest presente → skip)
        return False
    def handle_new_file(self, path):
        self.handled.append(path)
    def watcher_completed(self):
        pass
```

**Patrón setUp (de test_ingestor.py:35-51 y test_db.py:11-13):**
```python
def setUp(self):
    self.tmp = tempfile.mkdtemp(prefix="sdimport_watcher_")
    self.src_dir = os.path.join(self.tmp, "src")
    os.makedirs(self.src_dir)
    self.db = DatabaseManager(db_path=os.path.join(self.tmp, "watcher.db"))
    self._orig_db = watcher_module.db    # si watcher importa db directamente
    watcher_module.db = self.db

def tearDown(self):
    watcher_module.db = self._orig_db
    shutil.rmtree(self.tmp, ignore_errors=True)
```

**Casos D-04 (según RESEARCH.md):**
1. **F-02 conservado:** con un archivo con fila completed sin destino en disco, `should_skip` → False → re-volcar (cubrir el skip del watcher con destino borrado, no solo `handle_new_file` — Pitfall 1).
2. **Cap 10k eliminado:** crear > 10k archivos ficticios en memoria (no en disco) o verificar que `_watch` ya no contiene la poda (el test puede instrumentar `scanned_files`), o directamente: el inventario persiste en DB sin cap.
3. **Veredictos:** 'copied' con destino presente → skip; 'filtered' (mismo content-filter) → skip; 'errored' → siempre se re-maneja.
4. **Poda por antigüedad:** insertar `first_seen` antiguo (UPDATE a fecha vieja) → `prune_seen` lo elimina; límite 30 días.
5. **Normalización de rutas:** grabar "E:/x/y.mp4" → consultar "E:\\x\\y.mp4" (Windows) → misma entrada (Pitfall 4).
6. **Predicado compartido:** `handle_new_file` usa `should_skip` como preámbulo (mismo resultado que antes del refactor — los tests existentes de F-02 en test_ingestor.py siguen verdes).

---

## Shared Patterns

### SQLite per-op con `get_connection()` + WAL
**Source:** `app/core/db.py:31-35` (`get_connection`), usado en `_completed_dest_path` (ingestor.py:386-398), `get_files_by_session` (db.py:569-581)
**Apply to:** `db.py` (métodos nuevos `load_seen`/`save_seen`/`prune_seen`), `ingestor.py` (`should_skip` si consulta DB), `watcher.py` (inventario)
```python
conn = db.get_connection()
try:
    cursor = conn.cursor()
    cursor.execute("... WHERE x = ?", (param,))
    row = cursor.fetchone()   # o fetchall()
finally:
    conn.close()
# en escritura: conn.commit() antes de conn.close()
```

### Migración aditiva de esquema
**Source:** `app/core/db.py:291-296` (`footage_folders`), `:247-251` (ALTER con PRAGMA table_info)
**Apply to:** `db.py` — nueva tabla `watcher_seen` al final de `create_tables()`
```python
cursor.execute('''
    CREATE TABLE IF NOT EXISTS <tabla> (
        ...
    )
''')
```

### Predicado de skip que preserva F-02
**Source:** `app/core/ingestor.py:345-365` (`handle_new_file` preámbulo) + `_completed_dest_path` (382-400)
**Apply to:** `ingestor.py` (`should_skip`) y `watcher.py` (`_watch`)
Regla: un archivo con volcado previo se salta SOLO si el destino sigue en disco (`os.path.isfile(known_dest)`). Nunca saltar por inventario solo (Pitfall 1).

### "No esconder nada" — aviso visible (D-02)
**Source:** `app/ui/main_window.py:1867-1869` (célula cámara) + `:1872` (`self.tr("Completado")` exacto)
**Apply to:** `main_window.py` `on_file_finished`
Marker en célula de cámara (columna 1) + tooltip; **nunca** en columna de estado (rompe `_clear_completed_rows`, main_window.py:2188-2199).

### Worker QThread con señales
**Source:** `app/ui/main_window.py:130-147` (`_TaskWorker`), `:2153-2165` (`_run_background`), `:229-268` (`_auto_sync_check` ya despacha off-thread)
**Apply to:** (D-05 ya aplicado — solo tests). Si se mueve `_disconnected_devices` (3531-3551), envolver la llamada COM en un callable pasado a `_run_background`.

### Logging con `print(f"...")`
**Source:** `app/core/ingestor.py:71` (`print(f"Error copying {source_path}: {e}")`), `app/core/metadata_engine.py:272`
**Apply to:** todas las ramas de error nuevas (retry fallback, save_seen, prune_seen). Sin framework de logging.

### Singletons reemplazables a nivel de módulo
**Source:** `app/core/db.py:1101` (`db = DatabaseManager()`), tests swap: `ingestor_module.db = self.db` (test_ingestor.py:49), `mw.db = self.db` (test_main_window.py:41)
**Apply to:** si `watcher.py` importa `db`, mantener el acceso vía módulo para que los tests puedan monkeypachear (`watcher_module.db = ...`).

### i18n con `tr()` para texto nuevo
**Source:** `app/ui/main_window.py:1872` (`self.tr("Completado")`), interpolar con `.arg()`: `main_window.py:67` estilo `translator.tr("Formateando %1 (%2/%3)...").arg(path)...`
**Apply to:** todo string nuevo de UI (`"⚠ Metadatos no verificados"`, tooltip) — literales ES vía `self.tr()`; el catálogo EN se compila aparte.

---

## No Analog Found

| File | Role | Data Flow | Reason |
|------|------|-----------|--------|
| — | — | — | Todos los archivos (10/10) tienen analogo: 9 son modificaciones de archivos existentes (analogo = sí mismo) y `test_watcher_inventory.py` se modela sobre `test_ingestor.py` + `test_db.py`. Ningún paquete nuevo (RESEARCH.md Standard Stack: sin dependencias). |

---

## Metadata

**Analog search scope:** `app/core/` (12 módulos), `app/ui/` (main_window.py), `tests/` (17 suites)
**Files scanned:** 12 de producción + 17 tests (lecturas dirigidas a las líneas citadas por CONTEXT/RESEARCH)
**Pattern extraction date:** 2026-08-29
**Fuentes primarias citadas:** RESEARCH.md líneas 36-59 (D-01), 70-93 (D-03), 97-141 (D-04), 145-196 (D-05), 198-220 (discreciones); CONTEXT.md decisiones D-01..D-05.