---
phase: 1.5.0
slug: consolidaci-n-y-bugs-del-flujo
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
# No hay plan Wave 0 en esta fase (fase ligera): los archivos de test nuevos se crean in-task
# (test_watcher_inventory.py se crea en 01.5.0-03 Task 3; la verificacion / registro in-task en 03 Task 2 y 04).
wave_0_complete: true
created: 2026-08-29
---

# Phase 1.5.0 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Revisión 2026-08-29 (revision): mapa por plan/tarea real, archivos de test correctos, sin referencias a un plan Wave 0 inexistente.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | unittest (stdlib) — ejecutable también vía pytest (runner, no dependencia) |
| **Config file** | none — Qt tests corren con `QT_QPA_PLATFORM=offscreen` |
| **Quick run command** | `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest discover -s tests -p "test_*.py" -q` |
| **Full suite command** | `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest discover -s tests -p "test_*.py"` |
| **Estimated runtime** | ~90 segundos |

---

## Sampling Rate

- **After every task commit:** Run targeted test for that task (per the Per-Task map below)
- **After every plan wave:** Run targeted tests for all plans in the wave
- **Wave-merge / phase gate (W#2):** Full suite must be green — runs in plan **01.5.0-04** (last wave), NOT per plan
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement / Decision | Threat Ref | Secure Behavior | Test Type | Automated Command | Test File | Exists |
|---------|------|------|------------------------|------------|-----------------|-----------|-------------------|-----------|--------|
| 1.5.0-01-T1 | 01 | 1 | R1 / D-01 (retry degradado ffprobe + flag `metadata_verified`) | T-1.5-01/02 | Retry solo en `TimeoutExpired`; 2.º fallo/genérica → fallback sin raise, nunca silencioso | unit (mock ffprobe) | `python -m unittest tests.test_metadata_engine -v` | `tests/test_metadata_engine.py` | ✅ existente (se extiende) |
| 1.5.0-01-T2 | 01 | 1 | R1 / D-02 (marker "no verificados" en celda cámara, nunca columna estado) | T-1.5-03 | `_clear_completed_rows` intacto; marker solo columna 1 + tooltip | unit (Qt offscreen) | `python -m unittest tests.test_main_window -v` | `tests/test_main_window.py` | ✅ existente (se extiende) |
| 1.5.0-01-T3 | 01 | 1 | R1 (regresión objetivo de la plan) | — | No gate de suite completa aquí (W#2 → 01.5.0-04) | unit | `python -m unittest tests.test_metadata_engine tests.test_main_window -v` | `tests/test_metadata_engine.py`, `tests/test_main_window.py` | ✅ existentes |
| 1.5.0-02-T1 | 02 | 1 | R4 / D-03 (pase único de hash, sin relectura destino) | T-1.5-11/12 | Destino borrado ante excepción; hash bro stream | unit | `python -m unittest tests.test_ingestor.TestIngestor.test_verified_copy_success -v` + suite plan | `tests/test_ingestor.py` | ✅ existente (se reescribe 131-150) |
| 1.5.0-02-T2 | 02 | 1 | R4 / D-03 (reescritura tests mismatch) | T-1.5-11/12 | `test_copy_verified_single_pass_no_dest_reread`, `test_copy_error_removes_partial_dest` | unit | `python -m unittest tests.test_ingestor -v` | `tests/test_ingestor.py` | ✅ existente (re-escribir) |
| 1.5.0-02-T3 | 02 | 1 | R4 (regresión objetivo de la plan) | — | No gate de suite completa aquí (W#2 → 01.5.0-04) | unit | `python -m unittest tests.test_ingestor -v` | `tests/test_ingestor.py` | ✅ existente |
| 1.5.0-03-T1 | 03 | 2 | R2 / D-04 (tabla `watcher_seen` con `last_verdict`+`filter_key`; load→dict, save por veredicto, prune) | T-1.5-21/23 | SQL parametrizado; `os.path.normpath`; migración aditiva | unit (sqlite tmp) | `python -m unittest tests.test_db -v` | `tests/test_db.py` | ✅ existente (se extiende) |
| 1.5.0-03-T2 | 03 | 2 | R2 / D-04 (`should_skip` + watcher, cap 10k eliminado; registro de veredictos) | T-1.5-22/25 | F-02 (destino borrado ⇒ re-manejar); 'errored' siempre re-maneja; 'filtered' solo si `filter_key` coincide | unit | `python -m unittest tests.test_ingestor -v` (smoke `test_should_skip_resolves_verdict` in-task, W#3) | `tests/test_ingestor.py` | ✅ existente (se extiende) |
| 1.5.0-03-T3 | 03 | 2 | R2 / D-04 (test_watcher_inventory nuevo) | T-1.5-22/25 | F-02 en skip, cap eliminado, veredictos con filter_key, poda, normalización | unit (sin hilos; FakeIngestor) | `python -m unittest tests.test_watcher_inventory -v` | `tests/test_watcher_inventory.py` | ❌ crear en 03-T3 (in-task) |
| 1.5.0-04-T1 | 04 | 3 | R3 (regresión `_resolve_db_path` CWD-independent) | — | Restaura CWD en `finally`/teardown | unit | `python -m unittest tests.test_db -v` | `tests/test_db.py` | ✅ existente (se extiende) |
| 1.5.0-04-T2 | 04 | 3 | R5 (regresión auto-sync off-thread `_run_background`) | T-1.5-24 | Despacho off-thread; `_process_device_poll` no inline; guard `_poll_in_progress`; **gate suite completa** | unit (Qt offscreen) | `python -m unittest tests.test_main_window -v` + `python -m unittest discover -s tests -p "test_*.py"` | `tests/test_main_window.py` | ✅ existente (se extiende) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> **Esta fase no tiene plan Wave 0.** Es ligera (quick tasks) y todos los archivos de test se crean in-task por Nyquist (ninguna `<verify>` referencia un archivo que no exista):
>
> - `tests/test_watcher_inventory.py` — se crea dentro de **01.5.0-03 Task 3** (no en un plan Wave 0 previo).
> - Las extensiones de `test_db.py`, `test_ingestor.py`, `test_metadata_engine.py`, `test_main_window.py` se hacen dentro de cada plan que las toca (01/02/03/04).
> - Verificación in-task del predicado `should_skip` (W#3) vive en 01.5.0-03 Task 2 (smoke en `test_ingestor.py`).

*Si no: "Infraestructura existente cubre todos los requisitos de la fase; los tests nuevos se crean in-task."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| N/A | — | — | — |

*If none: "All phase behaviors have automated verification."*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (N/A — scaffolding in-task)
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
