---
phase: 1.5.0
slug: consolidaci-n-y-bugs-del-flujo
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-29
---

# Phase 1.5.0 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | unittest (stdlib) — disejutable también vía pytest (runner, no dependencia) |
| **Config file** | none — Qt tests corren con `QT_QPA_PLATFORM=offscreen` |
| **Quick run command** | `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest discover -s tests -p "test_*.py" -q` |
| **Full suite command** | `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest discover -s tests -p "test_*.py"` |
| **Estimated runtime** | ~90 segundos |

---

## Sampling Rate

- **After every task commit:** Run `quick run command`
- **After every plan wave:** Run `full suite command`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 1.5.0-01 | 01 | 1 | D-01 (FFprobe timeout + estado "no verificados") | — | N/A | unit + fake ffprobe | `unittest test_metadata_engine.py` | ✅ | ⬜ pending |
| 1.5.0-02 | 01 | 1 | D-03 (un solo hash MD5 stream-through) | — | N/A | unit + fixtures | `unittest test_ingestor.py` | ✅ (rewrite) | ⬜ pending |
| 1.5.0-03 | 01 | 1 | D-04 (inventario watcher en DB, poda por antigüedad) | — | N/A | unit + sqlite tmp | `unittest test_watcher.py test_db.py` | ✅ | ⬜ pending |
| 1.5.0-04 | 01 | 1 | D-05 (polling a worker/QThread — ya aplicado; regresión) | — | N/A | QT offscreen | `unittest test_e2e.py` | ✅ | ⬜ pending |
| 1.5.0-05 | 01 | 1 | DB path anclado al proyecto (ya aplicado; regresión) | — | N/A | unit | `unittest test_db.py` | ✅ | ⬜ pending |
| 1.5.0-06 | 01 | 1 | Race camara con uuid (ya aplicado; regresión) | — | N/A | QT offscreen | `unittest test_e2e.py` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_ingestor.py` — reescribir tests que monkeypatchean `calculate_md5` para el hash stream-through (D-03)
- [ ] `tests/test_watcher.py` + `tests/test_db.py` — stubs para la tabla `watcher_seen` y el predicado de skip (D-04)

*Si no: "Infraestructura existente cubre todos los requisitos de la fase."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| N/A | — | — | — |

*If none: "All phase behaviors have automated verification."*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending