---
phase: 01.5.0-consolidaci-n-y-bugs-del-flujo
plan: 04
type: execute
wave: 3
depends_on: ["01.5.0-01", "01.5.0-02", "01.5.0-03"]
files_modified:
  - tests/test_db.py
  - tests/test_main_window.py
autonomous: true
requirements: [R3, R5]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - TRUTH-Q1 (R3): `_resolve_db_path()` es independiente del CWD en dev — ya aplicado (commit 1cf4fd8); el test de regresión lo demuestra con `os.chdir` temporal.
    - TRUTH-Q2 (R5): `_auto_sync_check` despacha `list_devices`/`is_reachable` fuera de la UI thread vía `_run_background` — ya aplicado (commit 84e89aa); el test de regresión demuestra el despacho off-thread y los guards.
    - TRUTH-Q3: La suite completa del proyecto pasa en verde tras todos los fixes (gate de wave-merge / fase, Success Criterion 2).
  artifacts:
    - tests/test_db.py (test de regresión `_resolve_db_path` CWD-independent)
    - tests/test_main_window.py (clase `TestAutoSyncOffThread` — despacho off-thread y guards)
  key_links:
    - `_auto_sync_check` (main_window.py:229-268) → `_run_background` (2153-2165) → `_process_device_poll` (no inline)
    - `db._resolve_db_path()` (db.py:8-17 `data_dir`) independiente del CWD
  assumptions:
    - '[Edge R3] "unclassified — review manually": DB path en dev ya anclado (`data_dir` db.py:8-17); solo falta test de regresión. verified by backstop: test en test_db.py con os.chdir temporal y teardown que restaura el CWD.'
    - '[Edge R5] "unclassified — review manually": polling ya off-thread; el test de regresión verifica despacho vía `_run_background` (no-inline de `_process_device_poll`) y guard `_poll_in_progress`. verified by backstop: TestAutoSyncOffThread.'
    - 'Esta plan NO reintroduce la lógica de polling ni del DB path: solo regresión (R3/R5) + el gate de wave-merge de la suite completa. No hay token de cámara aquí (ya cubierto en 01).'
  prohibitions:
    - El test R3 debe restaurar el CWD original en `finally`/teardown (no dejar el proceso con CWD alterado).
    - El test R5 no debe ejecutar `_process_device_poll` inline; verifica el despacho al worker (mock de `_run_background`).
    - NO modificar la lógica de producción de `main_window.py` ni de `db.py` en esta plan (R3/R5 ya están aplicados en commits 1cf4fd8 y 84e89aa); solo se añaden/ajustan tests.
---

<objective>
Cubrir con tests de regresión los fixes ya aplicados de DB path CWD (requisito R3, commit 1cf4fd8) y polling de dispositivos fuera de la UI thread (requisito R5, commit 84e89aa), y ejecutar el gate de wave-merge de la suite completa (Success Criterion 2).

Purpose: R3 y R5 ya están implementados en el código (documentado en RESEARCH.md Discreción 1 y D-05); el único trabajo que falta es la cobertura de regresión que demuestre que el fix se mantiene. Además, al ser esta la última ola, ejecuta la suite completa tras todos los planes (gate de fase) — el race de la suite completa concurrente entre planes de la primera ola queda resuelto moviendo ese gate aquí (W#2).

Output: test de regresión R3 en `tests/test_db.py`; clase de regresión R5 `TestAutoSyncOffThread` en `tests/test_main_window.py`; suite completa en verde.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Fase 1.5.0)
@.planning/phases/01.5.0-consolidaci-n-y-bugs-del-flujo/01.5.0-CONTEXT.md (D-05, discreciones)
@.planning/phases/01.5.0-consolidaci-n-y-bugs-del-flujo/01.5.0-RESEARCH.md (secciones "D-05" + "Discreción 1" + Pitfall 4)
@.planning/phases/01.5.0-consolidaci-n-y-bugs-del-flujo/01.5.0-PATTERNS.md (secciones db.py, main_window auto-sync, test_main_window)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test de regresión R3 — `_resolve_db_path()` independiente del CWD</name>
  <files>tests/test_db.py</files>
  <read_first>
    - tests/test_db.py (setup/tearDown TestDatabaseManager 11-16)
    - app/core/db.py (líneas 8-17 data_dir/_resolve_db_path — ya anclado a la raíz del repo en dev)
  </read_first>
  <action>En `tests/test_db.py` añade un test de regresión de independencia del CWD (R3): dentro de una clase con unittest, guarda el CWD original (`os.getcwd()`), haz `os.chdir(<tempfile.mkdtemp()>)`, ejecuta el assert y restaura el CWD en `finally`/`tearDown`. El assert verifica que `db._resolve_db_path()` sigue apuntando a `<repo>/data/sd_import.db`: `str(db._resolve_db_path()).endswith(os.path.join("data", "sd_import.db"))` y que el base de `data_dir()` es la raíz del repo (no el CWD temporal). Usa `unittest.mock.patch` o un try/finally para restaurar el CWD de forma robusta (evitar CWD alterado en tests posteriores).</action>
  <acceptance_criteria>
    - `grep -n "def test.*cwd\|os.chdir" tests/test_db.py` presente (test R3 DB path CWD-independent).
    - Comando: `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_db -v` — el test R3 nuevo pasa junto a los existentes y los de la plan 03 (watcher_seen).
  </acceptance_criteria>
  <verify>
    <automated>$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_db -v</automated>
  </verify>
  <done>Un test de regresión demuestra que `_resolve_db_path()`/`data_dir()` es independiente del CWD en dev (R3), restaurando el CWD en teardown; test_db.py pasa en verde.</done>
</task>

<task type="auto">
  <name>Task 2: Test de regresión R5 — `_auto_sync_check` despacha off-thread + gate de suite completa</name>
  <files>tests/test_main_window.py</files>
  <read_first>
    - tests/test_main_window.py (setup setUpClass/setUp 28-66, patrón offscreen + monkeypatch db en 3 módulos)
    - app/ui/main_window.py (líneas 229-268 _auto_sync_check, 2153-2165 _run_background, 270-295 _process_device_poll)
  </read_first>
  <action>En `tests/test_main_window.py` añade una clase `TestAutoSyncOffThread` (R5) con:
  - (a) `window._auto_sync_check()` con `mock.patch.object(window, "_run_background")` ⇒ assert `_run_background` fue llamado con un callable + callback; `_process_device_poll` NO ejecutado inline (`assert not called`).
  - (b) `window._poll_in_progress = True` ⇒ `_run_background` NO se llama (guard).
  - (c) dentro del callable de fondo mockeado: `threading.get_ident()` del callable != `threading.get_ident()` del test (verifica no-UI-thread); `mtp.WpdBackend().list_devices` y `ftp.FtpBackend().is_reachable` patcheados invocables dentro del callable.
  Configura las sesiones del proyecto con `device_id` para que `_auto_sync_check` tenga sesiones que sondear (patrón del setup existente 28-66: inserta proyecto + sesiones; usa el mock de `db` en los 3 módulos del setup). NO modifiques la lógica de producción de `main_window.py` (R5 ya aplicado en 84e89aa).
  Tras este test, ejecuta la **suite completa** como gate de wave-merge/fase (W#2): `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest discover -s tests -p "test_*.py"`. Si algún test ajeno falla por cambios de los planes 01/02/03, corrígelo quirúrgicamente documentando por qué en el commit (nunca alteres la lógica de producción ya verificada en sus planes).</action>
  <acceptance_criteria>
    - `grep -n "class TestAutoSyncOffThread" tests/test_main_window.py` presente (test R5).
    - Comando: `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_main_window -v` — los tests R5 pasan junto a los existentes (marker D-02 de la plan 01 y TestCameraDetectionToken).
    - Comando final (gate de fase): `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest discover -s tests -p "test_*.py"` — suite completa `OK` (failure/error count = 0).
  </acceptance_criteria>
  <verify>
    <automated>$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_main_window -v</automated>
    <automated>$env:QT_QPA_PLATFORM="offscreen"; python -m unittest discover -s tests -p "test_*.py"</automated>
  </verify>
  <done>La clase `TestAutoSyncOffThread` demuestra que `_auto_sync_check` despacha off-thread vía `_run_background` (R5, guards incluidos); la suite completa del proyecto pasa en verde (gate de wave-merge/fase).</done>
</task>

</tasks>

## Artifacts this phase produces

- `tests/test_db.py` — test de regresión `_resolve_db_path` CWD-independent (R3)
- `tests/test_main_window.py` — clase `TestAutoSyncOffThread` (R5: despacho off-thread vía `_run_background`, guard `_poll_in_progress`)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main_window._auto_sync_check → worker (QThread) | `list_devices` (WPD COM) y `is_reachable` (FTP) despachados off-thread vía `_run_background`; el test verifica el despacho (no corre en UI thread). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1.5-24 | Denial of Service | `list_devices`/`is_reachable` en UI thread congelan la UI (R5) | low | mitigate | Ya despachado off-thread vía `_run_background`+QThread (commit 84e89aa); el test de regresión `TestAutoSyncOffThread` confirma el despacho y los guards `_poll_in_progress`/`_stage_thread.isRunning()` — no-inline de `_process_device_poll`. |
| T-1.5-SC | Tampering | npm/pip/cargo installs | N/A | N/A | Esta plan NO instala paquetes. Sin gate de paquetes. |

Sin exposición de red nueva: los tests de regresión ejercitan código local existente; no se añade ni modifica superficie de red.
</threat_model>

<verification>
Chequeos de la plan (ver `01.5.0-VALIDATION.md`):
- `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_db -v` — regresión R3 (CWD-independent) + tests watcher_seen de la plan 03.
- `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_main_window -v` — regresión R5 (auto-sync off-thread) + marker D-02 + TestCameraDetectionToken.
- `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest discover -s tests -p "test_*.py"` — **gate de wave-merge / fase** (W#2): suite completa verde.
</verification>

<success_criteria>
R3 resuelto (regresión): `_resolve_db_path()` independiente del CWD en dev; R5 resuelto (regresión): `_auto_sync_check` despacha `list_devices`/`is_reachable` off-thread vía `_run_background` con guards; la suite completa del proyecto pasa en verde.
</success_criteria>

<output>
Create `.planning/phases/01.5.0-consolidaci-n-y-bugs-del-flujo/01.5.0-04-SUMMARY.md` when done.
</output>
