---
phase: 01.5.0-consolidaci-n-y-bugs-del-flujo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/core/metadata_engine.py
  - app/ui/main_window.py
  - tests/test_metadata_engine.py
  - tests/test_main_window.py
autonomous: true
requirements: [R1]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - TRUTH-E1: Ante un timeout de ffprobe (10 s) el flujo reintenta UNA vez con probe degradado format-only y timeout ampliado (30 s); si el reintento responde, la metadata se marca como verificada.
    - TRUTH-E2: Si el reintento degradado también falla (o la excepción es genérica, no timeout), se devuelve un dict de fallback con `metadata_verified=False` y `metadata_error` poblado — nunca se lanza la excepción ni se deja una `file_size=0` sin aviso.
    - TRUTH-E3: El dict de éxito lleva `metadata_verified=True`; los dict de fallo son idénticos en forma al de éxito (mismas keys), con `file_size` real vía `os.path.getsize` cuando sea legible.
    - TRUTH-E4: El estado "metadatos no verificados" es visible en la celda de cámara (columna 1) + tooltip, y NUNCA altera la columna de estado (2) — `_clear_completed_rows` sigue limpiando las filas "Completado".
    - TRUTH-E5: `detect_camera_batch` deja de contar como cámara real los fallos (`metadata_verified is False` o `camera_model in ("Unknown", "Unknown_Camera")`).
  artifacts:
    - app/core/metadata_engine.py (helper privado `_fallback_metadata`; rama retry degradado en `get_video_metadata`; flag `metadata_verified` en dict éxito/fallo; filtro en `detect_camera_batch`)
    - app/ui/main_window.py (marker "Metadatos no verificados" en `on_file_finished`, columna cámara + tooltip)
    - tests/test_metadata_engine.py (regresión retry/2.º fallo/excepción genérica/`detect_camera_batch`)
    - tests/test_main_window.py (regresión marker D-02 + `_clear_completed_rows` intacto)
  key_links:
    - get_video_metadata (metadata_engine.py) → on_file_finished/main_window.py:1867 (celda cámara) → detect_camera_batch (metadata_engine.py:314-316)
    - Ejecución de la suite fuera de la rama "metadatos no verificados" = no romper `_clear_completed_rows` (main_window.py:2188-2199)
  assumptions:
    - '[Edge R1] "unclassified — review manually": el dict de metadata debe seguir consumido por `_run_single_file`, `_get_dispositivo_for_file` y `detect_camera_batch` sin cambios de contrato (keys nuevas solo añadidas). verified by backstop: la suite `tests/test_ingestor.py` y `tests/test_main_window.py` pasan con los dicts nuevos.'
    - '[Edge R2 boundary] umbrales D-01: retry timeout 30 s y probe format-only sin `-show_streams` (RESEARCH.md A1); la rama genérica `Exception` NO reintenta (Pitfall 6). verified by backstop: test de retry assert 2 llamadas; test de excepción genérica assert 1 llamada.'
    - '[Edge R2 precision] el retry debe ser SOLO en `TimeoutExpired`; reintentar en ramas genéricas duplicaría coste sin esperanza de éxito. verified by backstop: `mock_run.call_count == 1` en el test de `CalledProcessError`.'
  prohibitions:
    - El marker "no verificados" NO debe escribirse en la columna de estado (2) — rompería `_clear_completed_rows` (descriptorless; disposition flagged-unverified).
    - El fallo de metadata NO debe engrosar `file_size=0` silencioso sin `metadata_verified=False` y `metadata_error`.
    - NO se debe añadir ningún paquete nuevo ni framework de retry para D-01 (RESEARCH.md Standard Stack: sin dependencias).
---

<objective>
Resolver de forma fiable el bug "FFprobe timeout → metadata 'Unknown'/file_size=0" (requisito R1, decisiones D-01/D-02 de CONTEXT.md): ante timeout de ffprobe (10 s) se hace un reintento con probe degradado format-only y timeout ampliado; si sigue fallando, la fila se marca como "metadatos no verificados" con aviso visible — nunca silencioso.

Purpose: El core value del proyecto es la fiabilidad de la copia verificada; un fallback de metadata que silenciosamente categoriza como "Unknown_Camera" o deja `file_size=0` corrompe la organización posterior y los informes sin que el operador lo sepa (regla heredada D-03 de la fase 1 "no esconder nada en la interfaz").

Output: `metadata_engine.py` con reintento degradado + flag `metadata_verified`; `main_window.py` con el marker visible en la celda de cámara; tests de regresión en `test_metadata_engine.py` y `test_main_window.py`.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Fase 1.5.0)
@.planning/phases/01.5.0-consolidaci-n-y-bugs-del-flujo/01.5.0-CONTEXT.md (D-01/D-02)
@.planning/phases/01.5.0-consolidaci-n-y-bugs-del-flujo/01.5.0-RESEARCH.md (sección "D-01 / D-02" + Pattern 1 + Pitfalls 2/3/6)
@.planning/phases/01.5.0-consolidaci-n-y-bugs-del-flujo/01.5.0-PATTERNS.md (sección metadata_engine + test_metadata_engine)
@.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-CONTEXT.md (D-03 "no esconder nada")
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reintento degradado de ffprobe + flag metadata_verified en MetadataEngine</name>
  <files>app/core/metadata_engine.py, tests/test_metadata_engine.py</files>
  <read_first>
    - app/core/metadata_engine.py (líneas 1-190 get_video_metadata, 250-292 ramas fallo duplicadas, 298-330 detect_camera_batch)
    - app/core/metadata_engine.py línea 119-137 (_finalize_dates)
    - app/core/metadata_engine.py línea 314-316 (detect_camera_batch filtro camera)
    - tests/test_metadata_engine.py (clase TestGetVideoMetadata, setup 119-129, patrón de mock subprocess.run 152-153)
  </read_first>
  <action>En `app/core/metadata_engine.py`:
  1. Extrae el dict-fallback duplicado (líneas 250-292, ambos ramas) a un helper privado `_fallback_metadata(self, file_path: str, error: str) -> dict` que devuelve el dict idéntico en forma al de éxito (mismas keys: camera_model, camera_make, serial, creation_date, creation_dt, date_source, duration, bitrate, format, width, height, fps, codec, audio_codec, file_size, is_video, is_audio, is_image) pero con `camera_model="Unknown_Camera"`, `"metadata_verified": False` y `"metadata_error": str(error)`; conserva el intento de `os.path.getsize(file_path)` para `file_size` real dentro de try/except OSError, y el fallback por extensión (`_camera_from_extension`) + `_finalize_dates` igual que el bloque actual 260-269.
  2. Sustituye el `try/except` actual de `get_video_metadata` (150-168 + 250-292) por: (a) intento inicial con el comando existente (format+streams, `-read_intervals %+0.5`, timeout 10); (b) en `except subprocess.TimeoutExpired` reintenta UNA vez con `retry_cmd = [self.ffprobe_path, "-v", "quiet", "-print_format", "json", "-show_format", file_path]` (sin `-show_streams`, probe format-only) y `timeout=30`, con `startupinfo=self.startupinfo`; si el retry responde, parsea y continúa al parseo normal (verificado); si el retry lanza `(subprocess.TimeoutExpired, subprocess.CalledProcessError, OSError)` hace `print(f"Error metadata (timeout/retry) {file_path}: {e}")` y `return self._fallback_metadata(file_path, "timeout")`; (c) en `except (subprocess.CalledProcessError, OSError, json.JSONDecodeError)` (rama genérica) NO reintenta, hace `print(f"Error metadata {file_path}: {e}")` y `return self._fallback_metadata(file_path, str(e))`.
  3. Añade `"metadata_verified": True` al dict de éxito (17x-190).
  4. En `detect_camera_batch` (314-316) cambia la condición `if camera != "Unknown":` por `if metadata.get("metadata_verified") is not False and camera not in ("Unknown", "Unknown_Camera"):` (no contar fallos ni Unknown_Camera como cámara real).
  NO añadas imports nuevos (subprocess/json/os ya importados en metadata_engine.py:1-3) y NO añadas ningún paquete ni framework de retry. Respeta el estilo del archivo (docstrings/prints en español, 4-espacios, dicts mutables).
  En `tests/test_metadata_engine.py`, dentro de `TestGetVideoMetadata`, añade (usando `unittest.mock.patch("subprocess.run")` con el patrón existente 152-153):
  - test retry degradado: `mock_run.side_effect = [subprocess.TimeoutExpired("ffprobe", 10), <resultado éxito>]`; assert `mock_run.call_count == 2`; el 2.º call command no contiene `"-show_streams"` y su `timeout == 30`; resultado con `metadata_verified is True`.
  - test 2.º fallo: `mock_run.side_effect = [subprocess.TimeoutExpired("ffprobe", 10), subprocess.TimeoutExpired("ffprobe", 30)]`; assert `metadata["metadata_verified"] is False`, `"metadata_error" in metadata`, no raise, y `file_size` es el real (vía getsize de un archivo existente).
  - test excepción genérica: `mock_run.side_effect = subprocess.CalledProcessError(1, "ffprobe")`; assert `mock_run.call_count == 1` (NO reintenta, Pitfall 6) y `metadata_verified is False`.
  - test detect_camera_batch: `patch.object(engine, "get_video_metadata", return_value={"camera_model": "Unknown_Camera", "metadata_verified": False})`; assert `primary_camera == "Unknown"` (no se cuenta como cámara).</action>
  <acceptance_criteria>
    - `grep -n "metadata_verified" app/core/metadata_engine.py` encuentra el flag en el dict de éxito, en `_fallback_metadata` y en `detect_camera_batch`.
    - `grep -n "_fallback_metadata" app/core/metadata_engine.py` encuentra definición + al menos 2 usos (ramas TimeoutExpired y genérica).
    - `grep -n "retry_cmd" app/core/metadata_engine.py` presente y el retry se llama solo en la rama `except subprocess.TimeoutExpired`.
    - Comando: `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_metadata_engine -v` — 5+ tests (existentes + los 4 nuevos de retry/2.º fallo/genérica/detect_camera_batch) pasan.
  </acceptance_criteria>
  <verify>
    <automated>$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_metadata_engine -v</automated>
  </verify>
  <done>get_video_metadata reintenta UNA vez con probe format-only (timeout 30 s) ante timeout; el 2.º fallo o excepción genérica devuelve fallback con `metadata_verified=False` + `metadata_error`, sin lanzar; el dict de éxito lleva `metadata_verified=True`; detect_camera_batch no cuenta fallos ni Unknown_Camera. Los 4 tests de regresión nuevos pasan.</done>
</task>

<task type="auto">
  <name>Task 2: Marker visible "Metadatos no verificados" en la celda de cámara (on_file_finished)</name>
  <files>app/ui/main_window.py, tests/test_main_window.py</files>
  <read_first>
    - app/ui/main_window.py (líneas 1863-1889 on_file_finished, 2188-2199 _clear_completed_rows)
    - app/ui/main_window.py línea 1867 (condición actual `metadata.get("camera_model") != "Unknown"`)
    - tests/test_main_window.py (setup setUpClass/setUp 28-66, patrón offscreen + monkeypatch db en 3 módulos)
  </read_first>
  <action>En `app/ui/main_window.py`, dentro de `on_file_finished` (1863-1889), reemplaza el bloque de la celda de cámara (la condición `self.project_camera_detection_mode != "manual" and metadata and metadata.get("camera_model") != "Unknown"` en 1866-1869) por el patrón:
  - si `self.project_camera_detection_mode != "manual" and metadata`:
    - si `metadata.get("metadata_verified") is False`: crea `QTableWidgetItem(self.tr("⛔ Metadatos no verificados"))` (usar la cadena literal ES vía self.tr; el catálogo EN se compila aparte).
    - elif `metadata.get("camera_model") != "Unknown"`: `QTableWidgetItem(metadata["camera_model"])`.
    - si `metadata.get("metadata_verified") is False`: `camera_item.setToolTip(self.tr("ffprobe no respondió; metadatos no verificados"))`.
    - `self.table.setItem(row, 1, camera_item)`.
  CRÍTICO (Pitfall 3): la columna de estado (2) mantiene exactamente `status = self.tr("Completado")` en el caso success — NO cambies el texto de estado ni la lógica de `_clear_completed_rows` (2188-2199), que borra solo filas con `status_item.text() == done_text`.
  En `tests/test_main_window.py` añade un test (misma clase o nueva) de regresión D-02: crea una fila en `_file_row_map` (patrón de `self._file_row_key(source_path, ingestor)`), llama `window.on_file_finished(src, dest, True, {"camera_model": "Unknown_Camera", "metadata_verified": False})`, assert `self.window.table.item(row, 1).text()` contiene "no verificados", y que `self.window.table.item(row, 2).text() == self.window.tr("Completado")`. Luego llama `window._clear_completed_rows()` y assert la fila SÍ se elimina (Pitfall 3: el marker en columna 1 rompería el limpiado si se hubiera puesto en columna 2).</action>
  <acceptance_criteria>
    - `grep -n "Metadatos no verificados" app/ui/main_window.py` encuentra la cadena ES vía self.tr y el tooltip.
    - `grep -n 'tr("Completado")' app/ui/main_window.py` sigue presente sin alteración en el bloque status de `on_file_finished`.
    - Comando: `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_main_window -v` — el test D-02 nuevo pasa y los demás tests de TestCameraDetectionToken siguen verdes.
  </acceptance_criteria>
  <verify>
    <automated>$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_main_window -v</automated>
  </verify>
  <done>La celda de cámara (columna 1) muestra "⛔ Metadatos no verificados" + tooltip cuando `metadata_verified is False`; la columna de estado (2) mantiene "Completado" y `_clear_completed_rows` limpia correctamente la fila; el test de regresión D-02 pasa.</done>
</task>

<task type="auto">
  <name>Task 3: Suite completa de regresión tras los fixes de metadata</name>
  <files>tests/test_metadata_engine.py, tests/test_main_window.py</files>
  <read_first>- Sin lectura adicional: ejecutar la suite completa declarada en 01.5.0-VALIDATION.md.</read_first>
  <action>Ejecuta la suite completa de tests del proyecto con Qt offscreen: `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest discover -s tests -p "test_*.py"`. Si algún test ajeno a esta plan falla por los cambios de metadata (p. ej. un test que asuma `camera_model=="Unknown_Camera"` como cámara, o que espere `metadata_verified` ausente), corrígelo quirúrgicamente — documenta por qué en el commit. No modifiques el comportamiento implementado en Task 1/2 para hacer pasar un test; el test es el que debe reflejar el contrato nuevo. Confirma que `TestCameraDetectionToken` (test_main_window.py:28-100) sigue pasando (la carrera de cámara está fuera del alcance de esta plan; RESEARCH.md la da por aplicada).</action>
  <acceptance_criteria>
    - Comando: `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest discover -s tests -p "test_*.py"` termina con `OK` o `Ran N tests ... OK` (failure/error count = 0).
    - Si se modificó algún test existente por el contrato nuevo, el diff de ese test solo cambia asserts/expectativas, no la lógica de producción.
  </acceptance_criteria>
  <verify>
    <automated>$env:QT_QPA_PLATFORM="offscreen"; python -m unittest discover -s tests -p "test_*.py"</automated>
  </verify>
  <done>La suite completa pasa en verde tras los fixes D-01/D-02; no hay regresiones colaterales.</done>
</task>

</tasks>

## Artifacts this phase produces

- `app/core/metadata_engine.py`:
  - `_fallback_metadata(self, file_path, error)` — nuevo método privado (helper del dict de fallback)
  - key `metadata_verified` (bool) en el dict de metadata (True en éxito / False en fallback)
  - key `metadata_error` (str) en el dict de fallback
  - rama de retry en `get_video_metadata` (`except subprocess.TimeoutExpired` → `retry_cmd` format-only sin `-show_streams`, timeout 30)
  - cambio de filtro en `detect_camera_batch`: `metadata_verified is not False and camera not in ("Unknown", "Unknown_Camera")`
- `app/ui/main_window.py`:
  - `on_file_finished` → marker "Metadatos no verificados" en la celda de cámara (columna 1) + tooltip
- tests:
  - `tests/test_metadata_engine.py` — 4 métodos nuevos de regresión (retry degradado, 2.º fallo, excepción genérica sin retry, `detect_camera_batch` no cuenta fallos)
  - `tests/test_main_window.py` — test nuevo del marker D-02 + `_clear_completed_rows` intacto

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ffprobe CLI (external tool) → metadata_engine | La entrada es la ruta del archivo local, pasada como elemento de lista de args (no shell); el binario ffprobe viene de PATH (trusted per README). |
| metadata_engine → main_window (dict de metadata) | El dict viaja del core a la UI; el flag `metadata_verified` es producido por core y consumido por la UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1.5-01 | Tampering | `metadata_engine.get_video_metadata` (ffprobe subprocess) | low | mitigate | `file_path` se pasa como elemento de lista de args (`subprocess.run([...], file_path)`, metadata_engine.py:150-158) — sin interpolación en shell; el retry degradado mantiene el mismo patrón de lista de args, nunca `shell=True`. |
| T-1.5-02 | (información) | Fallback de metadata "Unknown_Camera" camuflado como cámara | low | mitigate | D-02 fuerza presentación visible (celda cámara + tooltip); `detect_camera_batch` (314-316) deja de contar `metadata_verified is False` o `Unknown`/`Unknown_Camera`. |
| T-1.5-03 | (información) | Aviso "no verificados" suprimido por ponerse en la columna de estado | high | mitigate | `_clear_completed_rows` (2188-2199) borra solo `text()=="Completado"`; el marker vive SOLO en la celda de cámara (columna 1) + tooltip, la columna de estado (2) mantiene el texto exacto (bloqueo ASVS L1: threat de integridad de info del operador). |
| T-1.5-SC | Tampering | npm/pip/cargo installs | N/A | N/A | Esta plan NO instala paquetes (RESEARCH.md Standard Stack: sin dependencias). Sin gate de paquetes. |

Todas las tareas de esta plan son N/A en cuanto a superficie de red: no hay exposición de red nueva; el flujo FTP/WiFi inbox existente queda intacto y fuera de alcance.
</threat_model>

<verification>
Chequeos de la plan (ver `01.5.0-VALIDATION.md`):
- `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_metadata_engine -v` — retry degradado, 2.º fallo, excepción genérica (no-retry), detect_camera_batch.
- `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest tests.test_main_window -v` — marker D-02 + _clear_completed_rows intacto + TestCameraDetectionToken.
- `$env:QT_QPA_PLATFORM="offscreen"; python -m unittest discover -s tests -p "test_*.py"` — suite completa verde (gate de fase, Success Criterion 2).
</verification>

<success_criteria>
R1 resuelto: ante timeout de ffprobe el flujo reintenta con probe degradado; el 2.º fallo produce un estado "metadatos no verificados" visible y nunca silencioso; `file_size=0` mudo queda eliminado (`metadata_verified=False` + `metadata_error`); `detect_camera_batch` no trata fallos como cámara real; la suite completa pasa con los tests de regresión D-01/D-02.
</success_criteria>

<output>
Create `.planning/phases/01.5.0-consolidaci-n-y-bugs-del-flujo/01.5.0-01-SUMMARY.md` when done.
</output>
