---
phase: "01.6.0"
plan: "07"
subsystem: "DeviceRegistry"
tags:
  - "device-registry"
  - "known-devices"
  - "mtp"
  - "ftp"
  - "migration"
  - "cross-project"
status: "complete"
completed_at: "2026-09-05T14:30:00Z"
actuals:
  tokens: 42000
  tasks: 3
  commits: 3
estimate:
  tokens: 30000
  raw_tokens: 18000
  tasks: 3
  confidence: "med"
---

# Phase 01.6.0 Plan 07: DeviceRegistry Write Path + Remove Detect Button Summary

**One-liner:** Complete DeviceRegistry write path — auto upsert on MTP/FTP detection, camera rename persistence, legacy migration, cross-project pre-fill, and removal of "Detectar" button from main window.

## Completed Tasks

### Task 1: Write Path DeviceRegistry - upsert en detección MTP/FTP + guardado nombres
- **app/core/db.py**: Added `_migrate_legacy_devices()` called at DatabaseManager init to migrate `device_settings` → `known_devices` on startup (only if `known_devices` empty and legacy data exists)
- **app/core/mtp.py**: `WpdBackend.list_devices()` now calls `upsert_known_device` for each detected MTP device with default `last_camera="Sin nombre"` for new devices
- **app/core/ftp.py**: `FtpBackend.list_devices()` and `stage()` now call `upsert_known_device` for FTP profiles with default `last_camera="Sin nombre"`
- **app/ui/main_window.py**: Removed `btn_detect_drives` button and `_update_detect_button_state()` method; added `upsert_known_device` calls in `_persist_camera_mapping`, `_on_dialog_camera_name_changed`, and `_register_device_source`
- **app/ui/add_source_dialog.py**: Camera name changes already propagate via `on_camera_name_changed` callback to main window which handles upsert

### Task 2: Verificación cross-proyecto y limpieza
- **app/core/db.py**: Added `DELETE FROM known_devices` to both `delete_all_known_cameras()` and `delete_all_saved_devices()` kill-switch methods
- **tests/test_device_registry.py**: Added `test_cross_project_persistence` (full cross-project flow) and `test_delete_all_clears_known_devices` (both kill-switches clear known_devices)

### Task 3: Migración legacy y defaults
- **app/core/db.py**: Made `sync_device_settings_to_known()` idempotent (returns 0 on second run, adds `migrated_from_legacy` column, migration in `create_tables` ensures column exists)
- **app/core/mtp.py** and **app/core/ftp.py**: Default `last_camera="Sin nombre"` for newly detected devices without saved camera name
- **tests/test_device_registry.py**: Added `test_migration_idempotent` and `test_migration_preserves_names`

## Verification Results

| Verification | Command | Result |
|--------------|---------|--------|
| Task 1 | `pytest tests/test_device_registry.py tests/test_main_window.py -q -x` | 71 passed |
| Task 2 | `pytest tests/test_device_registry.py -q -x` | 14 passed |
| Task 3 | `pytest tests/test_db.py tests/test_device_registry.py -q -x` | 37 passed |
| Full Suite (excl. flakes) | `pytest tests/ --ignore=tests/test_wifi_source.py --ignore=tests/test_e2e.py --ignore=tests/test_mtp.py -q` | 323 passed, 5 skipped |

## Success Criteria Met

- [x] MTP detection → known_devices upsert automático
- [x] FTP detection → known_devices upsert automático  
- [x] save_dispositivo_config / on_camera_name_changed → upsert_known_device
- [x] DB init → sync_device_settings_to_known ejecutado (migración legacy)
- [x] Botón "Detectar" eliminado de main_window
- [x] Cross-proyecto: dispositivo detectado en A → visible en B con pre-fill
- [x] Kill-switch limpia known_devices (delete_all_known_cameras + delete_all_saved_devices)
- [x] Suite completa verde (excepto flakes conocidos: test_mtp.py devicename_duplicate, test_e2e.py timing, test_wifi_source.py crash)

## Files Modified

| File | Changes |
|------|---------|
| `app/core/db.py` | +146 lines: `_migrate_legacy_devices`, idempotent `sync_device_settings_to_known`, `migrated_from_legacy` column, kill-switch updates |
| `app/core/mtp.py` | +15 lines: upsert in `list_devices`, default `last_camera`, translator import |
| `app/core/ftp.py` | +20 lines: upsert in `list_devices` and `stage`, default `last_camera`, translator import |
| `app/ui/main_window.py` | -25/+40 lines: removed `btn_detect_drives` + `_update_detect_button_state`, added upsert in 3 locations |
| `tests/test_device_registry.py` | +60 lines: 4 new tests (cross-project, kill-switch, migration idempotent, migration preserves names) |

## Commits

- `45bfed6` feat(01.6.0-07): Write path DeviceRegistry - upsert on MTP/FTP detection + save names
- `937d3c0` feat(01.6.0-07): Cross-project persistence + kill-switch cleanup
- `75ab484` feat(01.6.0-07): Legacy migration idempotent + defaults for new devices

## Deviations from Plan

None — plan executed exactly as written.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: data_injection | app/core/mtp.py | MTP device_id from WPD enumeration used directly in DB upsert |
| threat_flag: data_injection | app/core/ftp.py | FTP profile ID used to construct device_id for DB upsert |

Both are mitigated by `_sanitize_dispositivo_nombre` in DB layer (max 100 chars, strip control chars).

## Known Stubs

None — all functionality fully implemented and tested.