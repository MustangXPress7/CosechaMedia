---
phase: 01.6.0
plan: 03
type: execute
wave: 3
depends_on: ["01.6.0-01", "01.6.0-02"]
files_modified:
  - app/ui/reorganize_dialog.py
  - app/ui/main_window.py
  - tests/test_reorganize_dialog.py
autonomous: true
requirements: [REQ-06]

estimate:
  tokens: 40000
  raw_tokens: 25000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - El reorganizador existe y se accede desde "Reorganizar footage..." en la zona post-ingesta y desde la barra de menú (D-22)
    - El diálogo tiene selector de carpeta raíz, resumen de archivos por cámara/fecha, y confirmación antes de mover (D-17)
    - Los archivos se mueven de SinClasificar/ a cámara/fecha usando create_folder_structure (D-16, D-17)
    - Los archivos que no se pueden clasificar permanecen en SinClasificar/ y el resumen lo indica (D-18)
    - Se recalcula el MD5 de cada archivo movido y se re-registra en la DB files (D-19, D-20)
    - Sin reporte CSV — el resumen en el diálogo basta (D-21)
    - El reorganizador es independiente de ingesta activa (no requiere self._ingestors) (D-22)
  artifacts:
    - app/ui/reorganize_dialog.py (nuevo)
    - tests/test_reorganize_dialog.py (nuevo)
    - app/ui/main_window.py (modificado: btn_reorganize → reorganize_dialog + menú)
  key_links:
    - ReorganizeDialog._scan_folder ↔ metadata_engine.get_video_metadata()
    - ReorganizeDialog._execute_move ↔ shutil.move + calculate_md5 + db.get_connection()
    - main_window.btn_reorganize ↔ ReorganizeDialog
    - main_window menú → ReorganizeDialog
---

<objective>
Crear el Reorganizador de footage "Reorganizar footage..." que mueve archivos de SinClasificar/ a cámara/fecha con verificación MD5 y re-registro en DB (D-16..D-22, REQ-06).

Purpose: Sustituir el botón "Reorganizar por metadatos" (Unknown_Camera) por un diálogo funcional con SinClasificar como carpeta fuente, verificación MD5, y acceso tanto post-ingesta como desde el menú.
Output: reorganize_dialog.py nuevo + main_window.py integrado.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/gsd-core/templates/summary.md
</context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.6.0-registro-known-devices-req-09/01.6.0-CONTEXT.md
@.planning/phases/01.6.0-registro-known-devices-req-09/01.6.0-RESEARCH.md
@.planning/phases/01.6.0-registro-known-devices-req-09/01.6.0-PATTERNS.md
@.planning/phases/01.6.0-registro-known-devices-req-09/01.6.0-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Crear tests/test_reorganize_dialog.py (Wave 0 scaffold)</name>
  <files>tests/test_reorganize_dialog.py</files>
  <action>
Crear el archivo de tests siguiendo el patrón de tests existentes (unittest + Qt offscreen).

**Tests a implementar:**

1. **test_dialog_title**: Verificar que `ReorganizeDialog` tiene `windowTitle` "Reorganizar footage…"
2. **test_folder_picker_exists**: Verificar que hay un botón/selector para elegir carpeta raíz
3. **test_empty_sinclasificar**: Verificar que si no existe SinClasificar/, se muestra mensaje informativo
4. **test_scan_groups_by_camera_date**: Crear directorio temporal con `SinClasificar/cam1/file.mp4` y `SinClasificar/cam1/file2.mp4`; mockear `metadata_engine.get_video_metadata()` para devolver `{"camera_model": "Canon C300"}`; verificar que el resumen agrupa correctamente
5. **test_unclassified_stays_in_folder (D-18)**: Crear archivos que no se pueden clasificar (metadata_engine devuelve camera_model vacío); verificar que permanecen en SinClasificar/
6. **test_move_and_md5_recalc (D-19, D-20)**: Mockear `shutil.move`, `calculate_md5`, y `db.get_connection`; ejecutar el movimiento; verificar que se llamó `calculate_md5` y `UPDATE files SET dest_path=?, md5_hash=? WHERE dest_path LIKE ?`
7. **test_collision_suffix**: Crear archivo destino existente; verificar que se aplica sufijo `(n)`
8. **test_independent_of_ingestors (D-22)**: Verificar que el diálogo funciona sin `self._ingestors` activo (no requiere instancia de Ingestor)

**Mocks necesarios:**
- `app.core.metadata_engine.get_video_metadata` → devolver dict con camera_model y creation_time
- `app.core.db.get_connection` → mock con cursor que registra llamadas
- `app.core.utils.create_folder_structure` → devolver path de destino fake
- `shutil.move` → mock que no mueve realmente
- `app.core.utils.calculate_md5` → devolver hash fake
  </action>
  <verify>
    <automated>python -m pytest tests/test_reorganize_dialog.py -q -p no:cacheprovider -x</automated>
  </verify>
  <done>
    - tests/test_reorganize_dialog.py existe con al menos 8 tests
    - Todos los tests pasan con mocks
    - D-18 verificado (archivos sin clasificar permanecen en SinClasificar/)
    - D-22 verificado (funciona sin ingestors activos)
  </done>
  <precondition>Plan 1 completado (FALLBACK_CAMERA_NAME = SinClasificar definido, reorganize_by_metadata eliminado).</precondition>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: Crear ReorganizeDialog — diálogo SinClasificar + MD5 (D-16..D-22)</name>
  <files>app/ui/reorganize_dialog.py</files>
  <behavior>
    - test_folder_picker_returns_selected_path: el selector de carpeta devuelve el path seleccionado
    - test_scan_summary_groups_correctly: los archivos se agrupan por cámara y fecha en el resumen
    - test_unclassified_stays: archivos sin metadata permanecen en SinClasificar/
    - test_move_updates_db: tras el movimiento, la DB tiene dest_path y md5_hash actualizados
    - test_collision_gets_suffix: archivo con mismo nombre recibe sufijo (n)
    - test_cancel_doesnt_move: cancelar no mueve ningún archivo
  </behavior>
  <action>
Crear `app/ui/reorganize_dialog.py`:

**Clase ReorganizeDialog(QDialog):**
- Hereda QDialog con `tr()` method (patrón QtString)
- Constructor: `__init__(self, parent=None, project_root=None)` — project_root es la raíz del volcado del proyecto
- `setMinimumSize(700, 500)`, `setWindowTitle(self.tr("Reorganizar footage…"))`

**Estructura de UI con QStackedWidget (patrón SelectiveDumpAssistant):**

**Página 1 — Selección de carpeta:**
- QLabel explicativo: "Seleccione la carpeta raíz del volcado que contiene la subcarpeta SinClasificar/"
- QPushButton "Examinar…" que abre QFileDialog.getExistingDirectory
- Si project_root tiene SinClasificar/ detectada: mostrar resumen rápido ("Se encontraron X archivos sin clasificar en SinClasificar/")
- Botón "Escanear" para pasar a la página 2

**Página 2 — Resumen del escaneo:**
- Ejecutar escaneo off-thread (QThread worker) para no bloquear la UI
- El worker recorre `SinClasificar/` con `os.walk()`, para cada archivo:
  - Llama a `metadata_engine.get_video_metadata(path)` para extraer `camera_model` y `creation_time`
  - Si camera_model está vacío o es "SinClasificar": el archivo queda en "SinClasificar" (D-18)
  - Si creation_time está disponible: agrupar por cámara + fecha (formato YYYY-MM-DD)
  - Si no hay fecha: usar "sin-fecha"
- Mostrar resumen en QTableWidget: Cámara | Fecha | Nº archivos | Tamaño total
- Fila especial "SinClasificar (sin clasificar)" al final si hay archivos que no se pudieron clasificar (D-18)
- QLabel: "X archivos se moverán a sus carpetas correspondientes"
- Botones: "Reorganizar" (habilitado solo si hay archivos a mover) + "Cancelar"

**Página 3 — Ejecución y resultado:**
- Ejecutar movimiento off-thread (QThread worker):
  - Para cada archivo a mover:
    1. `dest_dir = create_folder_structure(root_path, camera, date)` (utils.py:15)
    2. `new_path = os.path.join(dest_dir, os.path.basename(file_path))`
    3. Manejar colisión: si `os.path.exists(new_path)`, aplicar sufijo `(n)` (patrón selective_dump.py:902)
    4. `shutil.move(file_path, new_path)` — cross-filesystem safe
    5. **D-19/D-20:** `md5_hash = calculate_md5(new_path)` (utils.py:42) — si falla (archivo corrupto), el usuario es informado y se reintenta el MD5 una vez; si sigue fallando, se registra `md5_hash = NULL`
    6. **D-20:** `UPDATE files SET dest_path = ?, md5_hash = ? WHERE dest_path LIKE ?` vía `db.get_connection()` (patrón DB de PATTERNS.md) — **CRÍTICO:** `dest_path` se actualiza SIEMPRE tras un `shutil.move` exitoso (independientemente de que el MD5 tenga éxito o no), para que la DB nunca apunte a una ruta inexistente. Solo el campo `md5_hash` queda a NULL si `calculate_md5` falla. Nunca mover físicamente sin actualizar `dest_path` en la DB.
  - Emitir progreso via Signal(str) para actualizar QProgressBar indeterminada
- Mostrar resumen final: "X archivos reorganizados correctamente, Y archivos permanecen en SinClasificar"
- **Sin CSV (D-21):** basta el resumen en el diálogo
- Botón "Cerrar"

**Worker QThread pattern (de selective_dump.py:1007-1024):**
- Clase `_ScanWorker(QObject)`: `progress = Signal(str)`, `finished = Signal(bool, object)`
- Clase `_MoveWorker(QObject)`: `progress = Signal(str)`, `finished = Signal(bool, object)`
- Ambos con try/except → `finished.emit(False, str(e))` en error

** Independencia de ingesta activa (D-22):**
- El diálogo NO importa ni usa `Ingestor` ni `self._ingestors`
- Funciona con cualquier carpeta elegida; no requiere sesión activa
- Se puede abrir desde la barra de menú en cualquier momento

**Colisión de nombres (Discreción del agente):**
- Aplicar sufijo `(n)` como el actual `reorganize_by_metadata`: `base (1).ext`, `base (2).ext`, etc.

**Manejo de errores:**
- Si `os.walk()` encuentra un archivo con permisos denegados: registrar con `print(f"Error reorganizando {path}: {e}")` y continuar (patrón de error handling del proyecto)
- Si `shutil.move` falla: registrar y continuar con el siguiente archivo (NO actualizar la DB de ese archivo, ya que no se movió)
- Si `calculate_md5` falla (archivo corrupto): registrar, mover ya no pendiente (el move ya ocurrió), actualizar `dest_path` en la DB y dejar `md5_hash = NULL`; el usuario es informado en el resumen. Nunca dejar la DB apuntando a la ruta antigua tras un move exitoso.
  </action>
  <verify>
    <automated>python -m pytest tests/test_reorganize_dialog.py -q -p no:cacheprovider -x</automated>
  </verify>
  <done>
    - app/ui/reorganize_dialog.py creado con QStackedWidget de 3 páginas
    - Escaneo off-thread de SinClasificar/ agrupa por cámara/fecha
    - Movimiento de archivos con shutil.move + calculate_md5 + DB update
    - Archivos sin clasificar permanecen en SinClasificar/ y se indican en el resumen
    - Colisión de nombres manejada con sufijo (n)
    - Independiente de ingesta activa
    - Todos los tests del scaffold pasan con la implementación
  </done>
  <precondition>tests/test_reorganize_dialog.py existe (Tarea 1 completada). Plan 1 completado (FALLBACK_CAMERA_NAME = SinClasificar).</precondition>
  <reversibility rating="costly">ReorganizeDialog es un componente nuevo; eliminarlo requiere restaurar reorganize_by_metadata (ya eliminado en Plan 1) o dejar la funcionalidad sin reemplazo.</reversibility>
</task>

<task type="auto">
  <name>Tarea 3: Integrar ReorganizeDialog en main_window + sustituir btn_reorganize (D-22)</name>
  <files>app/ui/main_window.py</files>
  <action>
**Sustituir "_reorganize_by_metadata" (D-22):**

En `app/ui/main_window.py`:
1. Localizar `_reorganize_by_metadata()` (~L4704-4730) — es la función que actualmente llama a `ingestor.reorganize_by_metadata()` con Unknown_Camera.
2. Reemplazar su cuerpo para que abra `ReorganizeDialog`:
   - Importar `from app.ui.reorganize_dialog import ReorganizeDialog` dentro de la función (lazy import, patrón del proyecto)
   - Extraer `project_root` de `self.current_project` (la carpeta raíz del proyecto actual)
   - Instanciar `ReorganizeDialog(self, project_root=project_root)`
   - Llamar `dialog.exec()` — el diálogo maneja todo el flujo internamente
   - No hay retorno; el diálogo es independiente de ingesta activa

3. Verificar que el `btn_reorganize` (~L627-629) ahora llama a `_reorganize_by_metadata` que abre el nuevo diálogo. El botón se mantiene en la zona post-ingesta. Renombrar internamente a `_open_reorganize_dialog` para mayor claridad (opcional, a discreción del agente).

**Añadir opción en barra de menú (D-22):**
- En la barra de menú principal, añadir una entrada "Reorganizar footage…" en el menú de proyecto/herramientas
- Esta opción está siempre disponible (no depende de ingesta activa)
- Al pulsar, abre `ReorganizeDialog` con el mismo path del proyecto actual

**Verificar que `_reorganize_by_metadata` no llama a métodos eliminados (Plan 1):**
- Plan 1 eliminó `ingestor.reorganize_by_metadata()`. El nuevo `_reorganize_by_metadata` en main_window NO debe importar ni llamar a ese método eliminado. Debe usar exclusivamente `ReorganizeDialog`.

**Verificar que el botón se actualiza:**
- Si el texto del botón dice "Reorganizar por metadatos", cambiarlo a "Reorganizar footage…" (D-22)
- El texto debe pasar por `self.tr(...)` para i18n
  </action>
  <verify>
    <automated>python -m pytest tests/test_reorganize_dialog.py tests/test_main_window.py -q -p no:cacheprovider -x</automated>
  </verify>
  <done>
    - _reorganize_by_metadata abre ReorganizeDialog en lugar de llamar al método eliminado del ingestor
    - "Reorganizar footage…" aparece tanto en el botón post-ingesta como en la barra de menú
    - El reorganizador funciona sin ingestors activos (D-22)
    - No hay referencias a `ingestor.reorganize_by_metadata` en main_window.py
    - Todos los tests pasan
    - Suite completa verde
  </done>
  <precondition>Plan 1 completado (reorganize_by_metadata eliminado de ingestor.py). Tarea 2 completada (ReorganizeDialog existe).</precondition>
  <reversibility rating="reversible">Los cambios en main_window.py son reversibles restaurando la función original (que llame a reorganize_by_metadata restaurado).</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ReorganizeDialog → filesystem | Movimiento de archivos en disco; requiere permisos de escritura |
| ReorganizeDialog → DB (files) | Actualización de dest_path y md5_hash en la tabla files |
| ReorganizeDialog → metadata_engine | Llamada a ffprobe para detectar cámara/fecha |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.6.0-08 | Tampering | shutil.move corruption | high | mitigate | calculate_md5 tras cada movimiento; dest_path se actualiza SIEMPRE tras un move exitoso (md5_hash a NULL si MD5 falla); shutil.move es atómico intra-filesystem |
| T-01.6.0-09 | Info Disclosure | print() logs | low | accept | print() con path y error es consistente con el patrón existente del proyecto; no hay secrets en paths |
| T-01.6.0-10 | Elevation | DB update sin permisos | medium | mitigate | get_connection() verifica que DB existe; SQLite WAL mode previene corrupción concurrente |
| T-01.6.0-SC | Tampering | npm/pip/cargo installs | high | mitigate | No se instalan paquetes nuevos; shutil.move y hashlib son stdlib |
</threat_model>

<verification>
- Tras Tarea 1: `python -m pytest tests/test_reorganize_dialog.py -q -p no:cacheprovider -x` pasa
- Tras Tarea 2: `python -m pytest tests/test_reorganize_dialog.py -q -p no:cacheprovider -x` pasa con la implementación
- Tras Tarea 3: `python -m pytest tests/test_reorganize_dialog.py tests/test_main_window.py -q -p no:cacheprovider -x` pasa
- Gate de ola: `python -m pytest tests/ -v -p no:cacheprovider` — suite completa verde
- Gate de eliminación: `grep -c "reorganize_by_metadata" app/core/ingestor.py` = 0 (Plan 1)
- Gate de integración: `grep -c "ReorganizeDialog" app/ui/main_window.py` >= 1
</verification>

<success_criteria>
- [ ] ReorganizeDialog existe con selector de carpeta, resumen, y movimiento + MD5
- [ ] Archivos se mueven de SinClasificar/ a cámara/fecha correctamente
- [ ] MD5 recalculado y re-registrado en DB para cada archivo movido
- [ ] Archivos sin clasificar permanecen en SinClasificar/ y se indican
- [ ] "Reorganizar footage…" aparece en el botón post-ingesta y en la barra de menú
- [ ] Independiente de ingesta activa
- [ ] reorganize_by_metadata eliminado de main_window (sin referencias)
- [ ] Suite completa de tests pasa
</success_criteria>

<output>
Create `.planning/phases/01.6.0-registro-known-devices-req-09/01.6.0-03-SUMMARY.md` when done
</output>
