---
phase: 01.6.0
plan: 07
type: execute
wave: 6
depends_on: ["01.6.0-06"]
files_modified:
  - app/core/db.py
  - app/ui/main_window.py
  - app/ui/add_source_dialog.py
  - app/core/mtp.py
  - app/core/ftp.py
  - tests/test_device_registry.py
  - tests/test_main_window.py
autonomous: true
requirements: [REQ-09, B-20]

estimate:
  tokens: 30000
  raw_tokens: 18000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - DeviceRegistry persiste dispositivos known_devices cross-proyecto (transversal a toda la app)
    - Al detectar MTP/FTP/WiFi -> upsert a known_devices automaticamente
    - Al guardar/editar nombre de camara en tabla/AddSourceDialog -> upsert a known_devices
    - La migracion device_settings -> known_devices se ejecuta al arrancar si hay datos legacy
    - Eliminado boton "Detectar" de main_window (reemplazado por auto-deteccion D-09)
  artifacts:
    - app/core/db.py (sync_device_settings_to_known en init + upsert helpers)
    - app/core/mtp.py (upsert al detectar)
    - app/core/ftp.py (upsert al detectar/listar)
    - app/ui/main_window.py (eliminar boton Detectar + upsert en save_dispositivo_config)
    - app/ui/add_source_dialog.py (upsert al editar nombre)
  key_links:
    - MTP/FTP detection -> known_devices upsert
    - save_dispositivo_config -> known_devices upsert
    - AddSourceDialog camera edit -> known_devices upsert
    - known_devices -> pre-fill cross-proyecto
---
<objective>
Completar DeviceRegistry: write path (upsert_known_device) en deteccion MTP/FTP, guardado de nombres y sync legacy; eliminar boton "Detectar" de main_window.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.6.0-registro-known-devices-req-09/01.6.0-CONTEXT.md
@.planning/phases/01.6.0-registro-known-devices-req-09/01.6.0-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Write path DeviceRegistry - upsert en deteccion MTP/FTP + guardado nombres</name>
  <files>app/core/mtp.py, app/core/ftp.py, app/ui/main_window.py, app/ui/add_source_dialog.py, app/core/db.py, tests/test_device_registry.py, tests/test_main_window.py</files>
  <action>
**Problema:** `get_dispositivo_for_device` lee de `known_devices`, pero `upsert_known_device` no se llama en:
- Deteccion MTP (`mtp.py` / `WpdBackend`)
- Deteccion FTP (`ftp.py` / `FtpBackend`)
- Guardado de nombre de camara (`save_dispositivo_config`, `on_camera_name_changed`)
- Inicio de app (migracion legacy `device_settings` -> `known_devices`)

**Fix en app/core/db.py (__init__ / create_tables):**
1. Al final de `create_tables()`, llamar a `self.sync_device_settings_to_known()` para migrar datos legacy una sola vez.

**Fix en app/core/mtp.py (WpdBackend / _WpdSession):**
1. En `_enum_children` o donde se enumeran dispositivos, tras obtener `device_id` y `name`:
   ```python
   db.upsert_known_device(device_id, "mtp", name=name, serial=serial, last_camera=saved_camera, metadata=meta)
   ```

**Fix en app/core/ftp.py (FtpBackend):**
1. En `list_devices` y `stage_device_folder`, tras resolver `profile_id`/`device_id`:
   ```python
   device_id = f"ftp:{profile_id}"
   db.upsert_known_device(device_id, "ftp", name=profile_name, last_camera=saved_camera)
   ```

**Fix en app/ui/main_window.py:**
1. En `save_dispositivo_config` (o `_on_dialog_camera_name_changed`): tras guardar en `device_settings`, hacer:
   ```python
   device_type = "mtp" if not device_id.startswith("ftp:") else "ftp"
   db.upsert_known_device(device_id, device_type, name=nombre_dispositivo, last_camera=nombre_dispositivo)
   ```
2. **Eliminar boton "Detectar"** (D-09: auto-deteccion off-thread ya existe):
   - Quitar `btn_detect_drives` del layout y su conexion
   - Quitar `_on_detect_drives` y logica asociada
   - La deteccion MTP/USB ya es automatica via auto-sync (timer 5s + `_maybe_check_device_connectivity`)

**Fix en app/ui/add_source_dialog.py:**
1. En `_on_camera_name_changed` (callback `on_camera_name_changed`): tras `save_dispositivo_config`, hacer upsert:
   ```python
   device_type = "mtp" if kind == "device" else "ftp"
   db.upsert_known_device(device_id, device_type, name=new_name, last_camera=new_name)
   ```

**Tests (tests/test_device_registry.py):**
- `test_upsert_on_mtp_detection`: mock MTP detection -> known_devices poblado
- `test_upsert_on_ftp_detection`: mock FTP list -> known_devices poblado
- `test_upsert_on_camera_rename`: editar nombre en AddSourceDialog -> known_devices actualizado
- `test_legacy_migration_on_startup`: device_settings legacy -> known_devices migrado al init DB
- `test_detect_button_removed`: verificar que `btn_detect_drives` no existe en main_window
</action>
  <verify>
    <automated>python -m pytest tests/test_device_registry.py tests/test_main_window.py -q -p no:cacheprovider -x</automated>
  </verify>
  <done>
    - MTP detection -> upsert_known_device llamado
    - FTP detection -> upsert_known_device llamado
    - save_dispositivo_config / on_camera_name_changed -> upsert_known_device
    - DB init -> sync_device_settings_to_known ejecutado
    - Boton "Detectar" eliminado de main_window
    - Tests pasan
  </done>
  <precondition>Plan 06 completado (DeviceRegistry UI + known_devices tabla existe).</precondition>
  <reversibility rating="reversible">Anadir llamadas upsert y quitar boton; revertible quitando lineas.</reversibility>
</task>

<task type="auto">
  <name>Tarea 2: Verificacion cross-proyecto y limpieza</name>
  <files>app/ui/main_window.py, app/ui/device_registry.py, tests/test_device_registry.py</files>
  <action>
**Verificar persistencia cross-proyecto real:**
1. Crear proyecto A, detectar dispositivo -> known_devices poblado
2. Cambiar a proyecto B, abrir AddSourceDialog -> dispositivo aparece con nombre pre-fill
3. Editar nombre en proyecto B -> persiste en known_devices
4. Volver a proyecto A -> nombre actualizado visible

**Limpiar codigo zombie:**
- Revisar `_refresh_source_list` / `_build_path_widget` para usar `known_devices` como fuente unica
- Eliminar referencias obsoletas a `device_settings` como fuente primaria
- Verificar que `delete_all_known_cameras` limpia `known_devices` (no solo `device_settings`)

**Tests:**
- `test_cross_project_persistence`: flujo completo cross-proyecto
- `test_delete_all_clears_known_devices`: kill-switch limpia known_devices
</action>
  <verify>
    <automated>python -m pytest tests/test_device_registry.py -q -p no:cacheprovider -x</automated>
  </verify>
  <done>
    - Cross-proyecto verificado end-to-end
    - Kill-switch limpia known_devices
    - Codigo zombie eliminado
  </done>
  <precondition>Tarea 1 completada.</precondition>
  <reversibility rating="reversible">Limpieza de codigo y verificacion; sin cambios de esquema.</reversibility>
</task>

<task type="auto">
  <name>Tarea 3: Migracion legacy y defaults</name>
  <files>app/core/db.py, tests/test_db.py</files>
  <action>
**Migracion device_settings -> known_devices robusta:**
1. `sync_device_settings_to_known`: iterar device_settings, upsert en known_devices con device_type inferido (ftp: -> ftp, wifi: -> wifi, otro -> mtp)
2. Ejecutar en `DatabaseManager.__init__` tras `create_tables()` (solo si known_devices vacio y device_settings tiene datos)
3. Anadir columna `migrated_from_legacy` a known_devices para trazabilidad (opcional, ALTER TABLE)

**Defaults para dispositivos nuevos:**
- Al detectar MTP sin nombre -> upsert con name=device_id, last_camera="Sin nombre"
- Al detectar FTP sin nombre -> name=profile_name

**Tests:**
- `test_migration_idempotent`: ejecutar sync 2 veces -> no duplica
- `test_migration_preserves_names`: nombres en device_settings -> known_devices
</action>
  <verify>
    <automated>python -m pytest tests/test_db.py tests/test_device_registry.py -q -p no:cacheprovider -x</automated>
  </verify>
  <done>
    - Migracion legacy idempotente al arranque
    - Dispositivos nuevos tienen defaults sensatos
    - Tests pasan
  </done>
  <precondition>Tareas 1-2 completadas.</precondition>
  <reversibility rating="reversible">Migracion solo anade datos; reversible con DELETE FROM known_devices.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MTP/FTP detection -> DB | Dispositivos fisicos enumerados; upsert automatico |
| User input (camera name) -> DB | Nombre editable; sanitizado en `_sanitize_dispositivo_nombre` |
| Cross-project known_devices | Tabla global; cambios afectan a todos los proyectos |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.6.0-22 | Tampering | Device name injection | low | mitigate | `_sanitize_dispositivo_nombre` (max 100 chars, strip, no control chars) |
| T-01.6.0-23 | Info Disclosure | Cross-project device names | low | accept | Nombres de camara no son sensibles; operador espera verlos |
| T-01.6.0-SC | Tampering | npm/pip/cargo installs | high | mitigate | No paquetes nuevos |
</threat_model>

<verification>
- Tras Tarea 1: `python -m pytest tests/test_device_registry.py tests/test_main_window.py -q -p no:cacheprovider -x` pasa
- Tras Tarea 2: `python -m pytest tests/test_device_registry.py -q -p no:cacheprovider -x` pasa
- Tras Tarea 3: `python -m pytest tests/test_db.py tests/test_device_registry.py -q -p no:cacheprovider -x` pasa
- Gate: `python -m pytest tests/ -q -p no:cacheprovider` - suite verde (excepto flakes conocidos)
</verification>

<success_criteria>
- [ ] MTP detection -> known_devices upsert automatico
- [ ] FTP detection -> known_devices upsert automatico
- [ ] save_dispositivo_config / on_camera_name_changed -> upsert_known_device
- [ ] DB init -> sync_device_settings_to_known ejecutado (migracion legacy)
- [ ] Boton "Detectar" eliminado de main_window
- [ ] Cross-proyecto: dispositivo detectado en A -> visible en B con pre-fill
- [ ] Kill-switch limpia known_devices
- [ ] Suite completa verde (excepto flakes)
</success_criteria>

<output>
Create `.planning/phases/01.6.0-registro-known-devices-req-09/01.6.0-07-SUMMARY.md` when done
</output>