---
phase: 01.6.0
plan: 02
type: execute
wave: 2
depends_on: ["01.6.0-01"]
files_modified:
  - app/ui/add_source_dialog.py
  - app/ui/main_window.py
  - app/ui/source_picker.py
  - tests/test_add_source_dialog.py
  - tests/test_source_picker.py
autonomous: false
requirements: [B-13, B-20]

estimate:
  tokens: 45000
  raw_tokens: 28000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - AddSourceDialog.renderiza tabla plana con 3 secciones visuales (física, WiFi, FTP) y sin pestañas (D-01, D-04)
    - La tabla tiene 5 columnas: checkbox selección | ubicación/ruta | nombre cámara editable | estado activo/online | papelera borrar (D-02)
    - Los dispositivos GUARDADOS y DETECTADOS aparecen integrados, no transitorios (D-03)
    - SourcePickerDialog y source_picker.py fueron eliminados; el botón "+ Origen" abre AddSourceDialog (D-04)
    - El menú dispositivo (QR) aparece solo en la columna "Ruta de origen", solo para WiFi (D-05, D-06)
    - Un origen WiFi no desaparece de la tabla al abrir el QR (D-07)
    - Se distingu entre MTP y USB masivo con flujo correcto por tipo (D-13, D-14)
    - Un fallo WPD/COM muestra aviso non-bloqueante, nunca derriba la app (D-15)
    - La detección de cámara funciona al seleccionar dispositivo sin nombre (D-08, D-09)
  artifacts:
    - app/ui/add_source_dialog.py (nuevo, ~400-600 líneas)
    - tests/test_add_source_dialog.py (nuevo)
    - app/ui/main_window.py (modificado: integración dialog + quitar botones + menú QR)
  key_links:
    - AddSourceDialog.exec() ↔ main_window._pick_source_entry / _add_source_entry
    - MtpDevicePane embebido en AddSourceDialog ↔ WpdBackend.list_devices()
    - FtpDevicePane embebido en AddSourceDialog ↔ FtpBackend
    - "_show_wifi_qr_for_sender" ↔ tabla de orígenes (no debe provocar refresh destructivo)
---

<objective>
Reemplazar SourcePickerDialog por el nuevo AddSourceDialog — tabla plana con 3 secciones, 5 columnas, menú QR-only y detección de cámara integrada (D-01..D-09, D-13..D-15).

Purpose: Centralizar la gestión de orígenes en un único diálogo claro que sustituye el diálogo antiguo con pestañas.
Output: add_source_dialog.py nuevo + main_window.py integrado + source_picker.py eliminado.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/gsd-core/templates/summary.md
</context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.6.0-registro-known-devices-req-09/01.6.0-CONTEXT.md
@.planning/phases/01.6.0-registro-known-devices-req-09/01.6.0-RESEARCH.md
@.planning/phases/01.6.0-registro-known-devices-req-09/01.6.0-PATTERNS.md
@.planning/phases/01.6.0-registro-known-devices-req-09/01.6.0-UI-REVIEW.md
@.planning/phases/01.6.0-registro-known-devices-req-09/01.6.0-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Crear tests/test_add_source_dialog.py (Wave 0 scaffold)</name>
  <files>tests/test_add_source_dialog.py</files>
  <action>
Crear el archivo de tests siguiendo el patrón de tests existentes (unittest + Qt offscreen). Configurar `QT_QPA_PLATFORM=offscreen` y `QApplication.instance() or QApplication([])` en setUpClass.

**Tests a implementar (adaptar con mocks):**

1. **test_dialog_title**: Verificar que `AddSourceDialog` tiene `windowTitle` "Añadir origen"
2. **test_flat_structure_no_tabs**: Verificar que NO hay `QTabWidget` en el layout; sí hay `QTableWidget` con 3 secciones (headers separadores)
3. **test_five_columns**: Verificar que la tabla tiene 5 columnas (checkbox, ruta, nombre, estado, papelera)
4. **test_accept_returns_sources**: Verificar que al marcar filas y aceptar, `result_sources()` devuelve la lista correcta
5. **test_reject_returns_none**: Verificar que cancelar devuelve lista vacía
6. **test_disabled_row_dimmed**: Verificar que filas con `enabled=False` tienen estilo atenuado (D-12)
7. **test_delete_triggers_callback**: Verificar que la columna papelera invoca `on_delete` callback
8. **test_mtp_vs_usb_mass**: Mockear `is_removable_drive` para verificar que MTP y USB masivo se clasifican correctamente (D-13)
9. **test_wpd_error_non_blocking**: Mockear `mtp.WpdBackend.list_devices()` para lanzar excepción; verificar que se muestra aviso sin crash (D-15)
10. **test_camera_detection_worker**: Verificar que la detección de cámara lanza un worker/QThread y no bloquea la UI (D-09)

**CRÍTICO (Anti-patrón documentado):** NO usar `.exec()` real de SenderEditDialog en tests. Mockear cualquier diálogo modal con un fake que devuelva el nombre directamente. Usar el patrón de `FakeDeviceDialog` / `FakeFtpDialog` de test_source_picker.py como referencia.

**Mocks necesarios:**
- `app.core.mtp.WpdBackend.list_devices` → devolver lista de DeviceInfo mocks
- `app.core.ftp.FtpBackend` → mockear conexión
- `app.core.utils.is_removable_drive` → True/False según test
- `app.core.utils.get_mounted_drives` → devolver lista fake
  </action>
  <verify>
    <automated>python -m pytest tests/test_add_source_dialog.py -q -p no:cacheprovider -x</automated>
  </verify>
  <done>
    - tests/test_add_source_dialog.py existe con al menos 8 tests
    - Todos los tests pasan con mocks (no requiere hardware real)
    - Ningún test usa `.exec()` sin mock
  </done>
  <precondition>Plan 1 completado (SinClasificar rename hecho, COM fixes en sitio).</precondition>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: Crear AddSourceDialog — tabla plana 3 secciones (D-01..D-04, D-05..D-07, D-13..D-15)</name>
  <files>app/ui/add_source_dialog.py</files>
  <behavior>
    - test_flat_table_structure: el diálogo muestra un QTableWidget con 3 secciones separadas visualmente (headers), sin QTabWidget
    - test_five_columns: la tabla tiene checkbox, ruta, nombre (editable), estado, papelera como columnas
    - test_mtp_vs_usb_section: los dispositivos MTP aparecen en sección "Conexión física", USB masivos en la misma sección pero con tipo diferenciado
    - test_qr_button_only_for_wifi: el botón QR solo aparece en filas WiFi en la columna ruta
    - test_disabled_rows_dimmed: filas con enabled=False tienen estilo atenuado
    - test_delete_callback: pulsar papelera invoca on_delete(kind, value)
  </behavior>
  <action>
Crear `app/ui/add_source_dialog.py` con la siguiente estructura:

**Clase AddSourceDialog(QDialog):**
- Hereda de QDialog, define `tr()` method a nivel de clase (patrón QtString de source_picker.py:39)
- Constructor: `__init__(self, parent=None, folders=(), senders=(), devices_missing=(), devices_connected=(), mtp_backend=None, ftp_backend=None, on_delete=None, on_detect=None)`
- `setMinimumSize(800, 500)`, `setWindowTitle(self.tr("Añadir origen"))`

**Tabla QTableWidget con 3 secciones (D-01):**
- Usar headers de sección como filas QTableWidget no editables con fondo distinto (separadores visuales). Las 3 secciones en orden:
  1. "Conexión física (MTP/USB/SD)" — dispositivos MTP (WPD/COM) + unidades USB masivas removibles
  2. "WiFi / PairDrop" — remitentes WiFi guardados
  3. "FTP" — perfiles FTP guardados
- Cada fila de sección es un separador visual (fondo `theme.color("bg_elevated")`, texto en negrita, span de columnas vía `setSpan`)

**5 columnas (D-02):**
1. **checkbox** (QCheckBox en celda) — selecciona el origen como activo en el proyecto
2. **ubicación/ruta** — para físicas: path o "MTP: device_name"; para WiFi: alias del remitente; para FTP: dirección del servidor
3. **nombre de cámara** (editable, QLineEdit en celda) — pre-rellenado con el nombre del dispositivo o "Sin nombre"
4. **estado activo/online** — texto con color: verde si conectado/online, rojo si desconectado
5. **papelera borrar** (QPushButton "borrar" en celda) — invoca `on_delete(kind, value)` (D-11)

**Población de filas (D-03):**
- Integrar dispositivos GUARDADOS (de la DB) y DETECTADOS (de `devices_connected` del backend)
- Los detectados se guardan junto a los demás al pulsar "Detectar"; no son transitorios
- Dispositivo detectado sin nombre: mostrar "detectando..." hasta que la detección (D-08/D-09) lo rellene

**Botones de acción debajo de la tabla:**
- "Examinar..." (abre QFileDialog para seleccionar carpeta local)
- "Detectar" (lanza enumeración WPD + auto-detección de cámara — D-08)
- "Nuevo WiFi" (abre inline para nombre WiFi)
- "Nuevo FTP" (abre inline o FtpDevicePane embebido, NO SenderEditDialog.exec())

**MTP vs USB masivo (D-13, D-14):**
- Importar `is_removable_drive` y `get_mounted_drives` de `app.core.utils`
- MTP (teléfonos/cámaras WPD): se marcan con etiqueta "MTP" en la columna ruta; al seleccionar abren el selector de carpetas WPD embebido (MtpDevicePane de device_picker.py)
- USB masivo (lectores SD, pendrives): se marcan con etiqueta "USB"; se tratan como carpeta local (usan el path del drive)
- La distinción es visible en la columna ruta: "[MTP] Nombre del dispositivo" vs "[USB] E:\DCIM"

**Detección de cámara (D-08, D-09):**
- Botón "Detectar" en la zona de acciones de la sección física
- Al seleccionar un dispositivo sin nombre de cámara: lanzar un QThread worker que llame a `metadata_engine.get_video_metadata()` para extraer `camera_model`
- Mientras se detecta: mostrar "detectando..." en la celda de nombre (editable, se rellena al completar)
- Si falla: continuar con el nombre actual (fallback manual, el usuario puede escribirlo)
- El popup non-blocking es el propio texto "detectando..." inline; no un QMessageBox modal

**Fallo WPD no-bloqueante (D-15):**
- Si `WpdBackend.list_devices()` lanza excepción: mostrar aviso no-bloqueante en la fila correspondiente ("No se pudo acceder al dispositivo") y continuar con las demás filas
- NO usar QMessageBox modal que bloquee la enumeración completa

**Acerca de SenderEditDialog.exec() (Anti-patrón RESEARCH):**
- Para el flujo FTP que pide nombre del servidor: NO usar `SenderEditDialog.exec()`. Usar un `QLineEdit` inline en la fila FTP de la tabla, o un widget embebido en la celda. El usuario escribe el nombre directamente en la tabla.
- Esto evita el cuelgue documentado en test_source_picker.py:266

**Botón Aceptar:**
- Deshabilitado por defecto (`setEnabled(False)`)
- Se habilita cuando hay al menos 1 origen seleccionado Y el nombre de cámara no está vacío
- `setDefault(True)` para shortcut Enter (hallazgo UI-REVIEW)

**Resultado del diálogo:**
- `result_sources()` devuelve una lista de dicts: `[{"kind": "folder"|"ftp_profile"|"sender"|"device", "value": ..., "camera": ..., "enabled": True}]`
- Aceptar → `self.accept()`, Cancelar → `self.reject()`

Seguir TODOS los patrones de PATTERNS.md: tr() vía QtString, theme.color() para colores, QSS template para fuentes, señales Qt para workers off-thread.
  </action>
  <verify>
    <automated>python -m pytest tests/test_add_source_dialog.py -q -p no:cacheprovider -x</automated>
  </verify>
  <done>
    - app/ui/add_source_dialog.py creado con tabla plana de 3 secciones y 5 columnas
    - MTP vs USB masivo distinguidos visualmente y con flujos correctos
    - Botón QR solo para WiFi en columna ruta
    - Detección de cámara funciona al seleccionar dispositivo sin nombre
    - Fallo WPD muestra aviso non-bloqueante
    - Todos los tests del scaffold pasan
    - Ningún `.exec()` modal sin mock en tests
  </done>
  <precondition>tests/test_add_source_dialog.py existe (Tarea 1 completada). Plan 1 completado.</precondition>
  <reversibility rating="costly">El nuevo AddSourceDialog reemplaza a SourcePickerDialog; mantener ambos es más costoso que uno solo. La eliminación de source_picker.py se hace en Tarea 3.</reversibility>
</task>

<task type="auto">
  <name>Tarea 3: Integrar AddSourceDialog en main_window + limpiar SourcePickerDialog (D-04, D-05..D-07, D-10)</name>
  <files>app/ui/main_window.py, app/ui/source_picker.py, tests/test_source_picker.py</files>
  <action>
**Integrar AddSourceDialog en main_window.py (D-04):**

1. En `_pick_source_entry()` (~L3459-3489): reemplazar la importación y uso de `SourcePickerDialog` por `AddSourceDialog`. El nuevo diálogo recibe `devices_connected` (D-03) además de los parámetros actuales.
   - Cambiar `from app.ui.source_picker import SourcePickerDialog` → `from app.ui.add_source_dialog import AddSourceDialog`
   - Cambiar la construcción: pasar `folders=, senders=, devices_missing=, devices_connected=, on_delete=, on_detect=`
   - Leer `dialog.result_sources()` en lugar de `(dialog.kind, dialog.value)`

2. En `_add_source_entry()` (~L3428): adaptar para manejar la lista de resultados del nuevo diálogo (una lista de orígenes, no uno solo). Para cada origen seleccionado, llamar a `_apply_source_choice()`.

3. En `_refresh_source_list()` (~L2373): quitar la generación de botones USB/MTP y FTP de la tabla de orígenes en la ventana principal (D-06). La columna "Ruta de origen" solo muestra el path/alias del dispositivo, sin botones.

4. En `_build_path_widget()` / `_build_remote_source_button()` (~L2417-2538): eliminar `_build_remote_source_button` y simplificar `_build_path_widget` para que solo muestre el path + checkbox + botón QR (para WiFi). Los botones USB/MTP y FTP se quitan de aquí. El QR se abre sin provocar `_refresh_source_list()` (D-07 fix — el bug WiFi desaparece al abrir QR).

5. Añadir callback `_detect_camera_for_source()` que lanza la detección off-thread (llama a `metadata_engine.get_video_metadata`) y devuelve el nombre detectado.

**Quitar "Escanear cámaras" (D-08):**
- Eliminar `btn_scan_cameras` de la ventana principal (~L479). El botón "Detectar" ahora vive en AddSourceDialog.

**Inhabilitar vs borrar (D-10):**
- Verificar que en la tabla de orígenes de la ventana principal, el checkbox de inhabilitar pone `enabled=0` en la sesión sin borrarla. El bóton de borrar (papelera) en el AddSourceDialog invoca `on_delete`.

**Eliminar source_picker.py y su test (D-04):**
- Eliminar `app/ui/source_picker.py` completamente
- Eliminar `tests/test_source_picker.py` completamente
- Verificar que no hay referencias a `SourcePickerDialog` o `source_picker` en ningún otro archivo (`grep -r "source_picker\|SourcePickerDialog" app/ tests/` debe dar 0 resultados)
  </action>
  <verify>
    <automated>python -m pytest tests/test_add_source_dialog.py tests/test_main_window.py -q -p no:cacheprovider -x</automated>
  </verify>
  <done>
    - _pick_source_entry usa AddSourceDialog y lee result_sources()
    - _build_path_widget no genera botones USB/MTP/FTP; solo QR para WiFi
    - "Escanear cámaras" (btn_scan_cameras) eliminado de la ventana principal
    - source_picker.py eliminado del repo
    - test_source_picker.py eliminado del repo
    - grep -r "SourcePickerDialog\|source_picker" app/ tests/ = 0 resultados
    - El botón "+ Origen" abre AddSourceDialog sin crashear
    - Un origen WiFi no desaparece de la tabla al abrir el QR (D-07)
    - Todos los tests pasan
  </done>
  <precondition>Plan 1 completado. Tarea 2 completada (AddSourceDialog existe).</precondition>
  <reversibility rating="one-way">Eliminar source_picker.py es one-way: restaurar requiere re-crear el archivo desde git. La integración en main_window.py es reversible pero costosa.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AddSourceDialog → main_window | El diálogo devuelve orígenes; main_window los persiste en la DB |
| MtpDevicePane → WPD COM | Selección de carpeta WPD desde el diálogo embebido |
| FtpDevicePane → FTP server | Conexión FTP desde el diálogo embebido |
| Camera detection → metadata_engine | Llamada off-thread a ffprobe para detectar cámara |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.6.0-04 | Tampering | Source elimination (D-04) | medium | mitigate | Verificar grep sin referencias huérfanas antes de commit; tests de integración |
| T-01.6.0-05 | Elevation | WPD error crash (D-15) | high | mitigate | Aviso non-bloqueante en fila; nunca QMessageBox modal que detenga la UI |
| T-01.6.0-06 | Info Disclosure | WiFi QR bug (D-07) | low | mitigate | Asegurar que _show_wifi_qr_for_sender no provoca _refresh_source_list destructivo |
| T-01.6.0-07 | DoS | SenderEditDialog.exec() hang | high | mitigate | NO usar .exec() modal en el nuevo diálogo; usar QLineEdit inline para nombre FTP |
| T-01.6.0-SC | Tampering | npm/pip/cargo installs | high | mitigate | No se instalan paquetes nuevos |
</threat_model>

<verification>
- Tras Tarea 1: `python -m pytest tests/test_add_source_dialog.py -q -p no:cacheprovider -x` pasa
- Tras Tarea 2: `python -m pytest tests/test_add_source_dialog.py -q -p no:cacheprovider -x` pasa (tests del scaffold ahora pasan con la implementación)
- Tras Tarea 3: `python -m pytest tests/test_add_source_dialog.py tests/test_main_window.py -q -p no:cacheprovider -x` pasa
- Gate de ola: `python -m pytest tests/ -v -p no:cacheprovider` — suite completa verde
- Gate de eliminación: `grep -r "SourcePickerDialog\|source_picker" app/ tests/ | wc -l` = 0
</verification>

<success_criteria>
- [ ] AddSourceDialog renderiza correctamente con 3 secciones y 5 columnas
- [ ] MTP y USB masivo distinguibles con flujos correctos
- [ ] QR solo para WiFi en la columna ruta de la tabla principal
- [ ] WiFi no desaparece al abrir QR (D-07)
- [ ] Detección de cámara funciona off-thread
- [ ] SourcePickerDialog y test_source_picker.py eliminados
- [ ] Referencias huérfanas: 0
- [ ] Suite completa de tests pasa
</success_criteria>

<output>
Create `.planning/phases/01.6.0-registro-known-devices-req-09/01.6.0-02-SUMMARY.md` when done
</output>
