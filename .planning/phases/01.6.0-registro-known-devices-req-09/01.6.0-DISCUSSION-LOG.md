# Phase 1.6.0: Añadir origen + Reorganizador + Bugs - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-03
**Phase:** 1.6.0-Añadir origen + Reorganizador + Bugs
**Areas discussed:** Eliminar vs inhabilitar (SC2); Detección de cámara; Menú dispositivo (SC1); Reorganizador _SinClasificar (SC3); Diseño dialogo plano nuevo; Lanzador Reorganizar en post-ingesta; Distinción MTP vs USB masivo; Thread-local COM (SC4)

---

## Eliminar vs inhabilitar (SC2)

| Option | Description | Selected |
|--------|-------------|----------|
| Check=inhabilitar, basura=borrar | Check deselecciona/inhabilita; la papelera borra del proyecto | ✓ |
| Nada borra en la tabla | Ningún control borraría desde la tabla | |
| Otra semantica | (no elegida) | |

**User's choice:** Check=inhabilitar, basura=borrar
**Notes:**

- **Alcance del borrado** (papelera): borrar del **proyecto**, no de los guardados.
- **Botón "borrar dispositivos guardados"**: vive como **columna papelera por fila/dispositivo** en el diálogo añadir origen (obligatoria) + **kill-switch opcional en la barra de menú** que borra todos los datos de dispositivos con confirmación.
- **Representación del inhabilitado**: fila atenuada visible (no oculta).

---

## Detección de cámara

| Option | Description | Selected |
|--------|-------------|----------|
| Mover Detectar, quitar Escanear | Se quita "Escanear cámaras" y se mueve "Detectar" al diálogo añadir origen | ✓ |
| Solo detección automática | (no elegida) | |
| Mantener en ambos | (no elegida) | |

**User's choice:** Mover Detectar, quitar Escanear
**Notes:**

- **Disparo**: automático al **seleccionar** un dispositivo sin nombre de cámara, con popup non-blocking (proceso + tiempo) y fallback al popup manual clásico.
- **Aplicación del nombre**: rellenar editable + confirmación.
- **Función de "Detectar"**: refresca la lista de orígenes detectados; los dispositivos detectados **se guardan** junto a los demás (no transitorios). La detección de cámara es mecanismo separado disparado por selección.

---

## Menú dispositivo (SC1)

| Option | Description | Selected |
|--------|-------------|----------|
| En cada fila del diálogo | El menú dispositivo (QR/FTP) vive en cada fila del diálogo | ✓ |
| Solo en tabla principal | (no elegida) | |
| Otro | (no elegida) | |

**User's choice:** En cada fila del diálogo
**Notes:**

- **Bug WiFi**: fix — **no desaparece al abrir el QR** (QR en panel/acción aparte, el origen permanece en la lista).
- **Regla SC1 (columna Ruta, no Contenido)**: mantener **solo el botón QR** para WiFi (acción runtime: emparejado) en la columna Ruta/origen. **Quitar USB/MTP y FTP** de la tabla; reconfigurar = **borrar+crear**. Confirmación final: solo QR; USB/FTP vía borrar+crear.

---

## Reorganizador _SinClasificar (SC3)

| Option | Description | Selected |
|--------|-------------|----------|
| Carpeta nueva SinClasificar | Los no clasificables pasan a una carpeta nueva `SinClasificar/` (sustituye Unknown_Camera) | ✓ |
| Reutilizar Unknown_Camera | (no elegida) | |
| Otro | (no elegida) | |

**User's choice:** Carpeta nueva SinClasificar
**Notes:**

- **Flujo**: "Reorganizar footage..." con **diálogo + selector de carpeta + resumen** antes de confirmar.
- **Tras reorganizar**: los que siguen sin clasificar **quedan en SinClasificar y se avisa**; el "Reorganizar por metadatos"/Unknown_Camera **se sustituye** por este flujo.
- **MD5 (override del roadmap "sin MD5")**: **con verificación MD5**, override explícito confirmado por el operador.
- **Qué significa verificar MD5** (origen no disponible): **recalcular y re-registrar** (actualiza crypto hash en DB `files` junto al movimiento).
- **Reporte**: solo resumen, **sin CSV**.

---

## Diseño dialogo plano nuevo

| Option | Description | Selected |
|--------|-------------|----------|
| Tabla única 3 secciones | Física (MTP/USB/SD) + WiFi/PairDrop + FTP, sin pestañas | ✓ |
| Pestañas + tabla por pestaña | (no elegida) | |
| Otro | (no elegida) | |

**User's choice:** Tabla única 3 secciones
**Notes:**

- **Columnas**: checkbox de selección (marca origen activo) | ubicación | nombre (editable) | estado activo/online | papelera borrar. El checkbox ≠ columna "activo".
- **Poblado**: guardados + detectados **integrados** (los detectados se añaden y guardan; sin nombre muestran `detectando...`/nombre por defecto).
- **Integración**: el nuevo `AddSourceDialog` **reemplaza** a `SourcePickerDialog`; cambiar carpeta = borrar+re-añadir.

---

## Lanzador Reorganizar en post-ingesta

| Option | Description | Selected |
|--------|-------------|----------|
| Sustituye a Reorganizar por metadatos | El botón "Reorganizar footage..." sustituye al actual en la zona post-ingesta | ✓ |
| Coexisten ambos | (no elegida) | |
| Otro | (no elegida) | |

**User's choice:** Sustituye a Reorganizar por metadatos
**Notes:**

- **Respuesta libre del operador**: el reorganizador es **independiente de ingesta activa** (funciona sobre cualquier carpeta), y además **se añade como opción en la barra de menú** para resaltar su carácter opcional.

---

## Distinción MTP vs USB masivo

| Option | Description | Selected |
|--------|-------------|----------|
| Distinguir MTP vs USB masivo | En la sección física se distingue MTP (WPD) y USB masivo (SD/pendrive), cada uno con su flujo correcto | ✓ |
| Manejo de fallo MTP sin cerrar | (no elegida) | |
| Ambas | (no elegida) | |

**User's choice:** Distinguir MTP vs USB masivo
**Notes:**

- **Presentación**: columna tipo + flujo por tipo (MTP abre selector WPD; USB masivo se trata como carpeta local; ambos guardables).
- **Fallo WPD/COM**: aviso no bloqueante en esa fila + nunca crashear; el flujo sigue para las demás; reutiliza manejo de errores de `mtp.py`.

---

## Thread-local COM (SC4)

| Option | Description | Selected |
|--------|-------------|----------|
| Limpiar tlocal + esperar staging | Reemplazar `_manager_local` Y esperar/detener el `_stage_thread`/worker | ✓ |
| Solo thread-local | (no elegida) | |
| Otro | (no elegida) | |

**User's choice:** Limpiar tlocal + esperar staging
**Notes:**

- **list_devices CoInit/CoUninit**: **patrón balanceado** (solo CoUninitialize si esta llamada hizo el CoInitialize, respetando el conteo de refs).
- **Triggers de la limpieza**: en **todos los resets/handoffs** de proyecto (borrar, cambiar, y al detener/reiniciar sesión de ingesta) + detener timers (auto-sync/detección) de forma fiable.

---

## the agent's Discretion

- Columnas exactas y orden de las 3 secciones de la tabla plana; cómo mostrar "detectando...".
- Ubicación exacta del kill-switch "borrar todos los dispositivos" y su confirmación.
- Widget del popup non-blocking de detección de cámara (barra de progreso, etc.).
- Regla de nombres de colisión/destino en el reorganizador (sufijo `(n)` como el actual).

## Deferred Ideas

- Volcado por orden de dispositivo (rotar tarjetas con un solo lector) — futuro.
- Registro devices / `known_devices` + DeviceRegistry pre-fill + notificadores SMTP/Telegram — Fase 1.7.0.
- Verificación avanzada XXH64 + ASC MHL + política de hash — Fase 1.8.0.
- "Revisar awesome-python y curar candidatos" — Fase 1.8.0 (arrastrado desde 1.5.0).
