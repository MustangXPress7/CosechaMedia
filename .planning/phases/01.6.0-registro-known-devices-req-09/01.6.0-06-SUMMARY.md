---
phase: 01.6.0
plan: 06
status: completed
completed_at: "2026-09-05T12:45:00Z"
commits:
  - fec1b8d fix(01.6.0-06): duplicate master path protection + wizard saves master_path (Task 1)
  - 71cd947 fix(01.6.0-06): session window filter supports weeks/months (Task 2)
  - 9187b5d fix(01.6.0-06): update projects button preserves dump_type + session (Task 3)
  - 8dd2517 feat(01.6.0-06): DeviceRegistry - known_devices table + UI + pre-fill (Task 4)
---

## Summary - Plan 01.6.0-06

**Objetivo:** Corregir 4 bugs de proyectos/sesiones + DeviceRegistry (REQ-09)

### Tareas Completadas

#### Tarea 1: DB Lock duplicate master_path + Wizard master_path
- **Commit:** fec1b8d
- **Cambios:**
  - `DuplicateMasterPathError` exception en `db.py`
  - `db.create_project()` con verificación duplicado ANTES de transacción
  - `PRAGMA busy_timeout=5000` en `get_connection()`
  - `ProjectWizard.finish_wizard()` usa `db.create_project()` y muestra warning claro en duplicado
- **Tests:** 3 nuevos (duplicate master_path, wizard saves root_path, concurrent creates)

#### Tarea 2: Session Date Interval (Window mode weeks/months)
- **Commit:** 71cd947
- **Cambios:**
  - `_open_window_filter_dialog()` con spinbox + combo (días/semanas/meses)
  - `_window_value_unit_from_filter()`, `_window_days_from_filter()`, `_window_filter_text()` helpers
  - Guarda `window_value` + `window_unit` + `window_days` en content_filter (backward compatible)
  - Botón config muestra "Últimas N días/semanas/meses"
- **Tests:** 2 actualizados (mock nuevo dialog, verifica formato nuevo + legacy)

#### Tarea 3: Update Projects Button preserves dump_type
- **Commit:** 9187b5d
- **Cambios:**
  - `load_existing_projects()` guarda/restaura `current_session_id` via `_restore_session_id`
  - `_refresh_sessions_combo()` prioriza `_restore_session_id` > `prev_id`
  - Sesión activa y su `dump_type` preservados al recargar lista de proyectos

#### Tarea 4: DeviceRegistry - known_devices + UI + pre-fill (REQ-09)
- **Commit:** 8dd2517
- **Cambios:**
  - Tabla `known_devices` (device_id, type, name, serial, last_camera, last_seen, metadata_json)
  - Métodos DB: `upsert_known_device`, `get_known_device`, `list_known_devices`, `delete_known_device`, `sync_device_settings_to_known`
  - `get_dispositivo_for_device()` ahora lee `known_devices` primero (fallback `device_settings`)
  - `DeviceRegistryDialog` (Herramientas → Dispositivos conocidos): CRUD, import/export JSON, kill-switch
  - AddSourceDialog pre-fill desde `known_devices`
  - Menú: Herramientas → Dispositivos conocidos…
  - Tests: 5 nuevos (CRUD, cross-project, migración, import/export)

### Verificación
- `python -m pytest tests/test_db.py` - 23 passed
- `python -m pytest tests/test_main_window.py` - 57 passed
- `python -m pytest tests/test_device_registry.py` - 5 passed
- Suite completa: 384 passed (2 flakes pre-existentes documentados)

### Archivos Modificados
- `app/core/db.py` (+146 líneas)
- `app/ui/project_wizard.py` (+71/-37)
- `app/ui/main_window.py` (+17/-7)
- `app/ui/device_registry.py` (nuevo, 370 líneas)
- `tests/test_db.py` (+34)
- `tests/test_device_registry.py` (nuevo, 180 líneas)

### Métricas
- 4 tareas / 5 commits
- ~250 líneas de código nuevo (excluyendo tests)
- 35 tests nuevos/actualizados