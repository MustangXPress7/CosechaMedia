# RESEARCH.md — Phase 1.6.1

No se requiere investigación externa. La reestructuración es puramente de UI, sin nuevas dependencias ni integraciones.

**Stack verificado:**
- PySide6 >=6.5,<7: QAction, QMenu, QMenuBar, QActionGroup se usan tal cual en menu_mixin.py.
- `tr()` / `self.tr()` para i18n ES – patrón existente.
- QSettings para auto-detect flag – mantener.

**No hay decisiones arquitectónicas nuevas.** La tarea es reubicar acciones existentes sin cambiar wiring.

**Riesgos:**
- Strings duplicados al mover acciones entre menús → validar que cada QAction solo se añade una vez.
- Menú Idioma dentro de Ayuda → QActionGroup debe permanecer exclusivo.
- Tests que reference menu names (no existentes).

**Conclusión:** No se necesita discovery adicional. Proceder con plan de implementación UI.
