---
phase: 01.6.0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ui/mixins/devices_mixin.py
  - app/ui/main_window.py
  - tests/test_main_window.py
  - tests/test_wifi_source.py
  - tests/test_source_content.py
  - tests/test_session_content_modes.py
  - tests/test_e2e.py
autonomous: true
requirements: []
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 3
  confidence: high
must_haves:
  truths:
    - DevicesMixin module exists at app/ui/mixins/devices_mixin.py with all 18 device methods verbatim
    - MainWindow composes DevicesMixin (class MainWindow(QMainWindow, WifiMixin, CameraMixin, DevicesMixin))
    - All 18 extracted methods removed from MainWindow without behavior change
    - Test suite patches devices_mixin_module.db in all 5 test files (parallel singleton patch)
    - Full test suite passes (393 tests)
  artifacts:
    - app/ui/mixins/devices_mixin.py
    - Modified app/ui/main_window.py
    - Modified tests/test_main_window.py
    - Modified tests/test_wifi_source.py
    - Modified tests/test_source_content.py
    - Modified tests/test_session_content_modes.py
    - Modified tests/test_e2e.py
  key_links:
    - DevicesMixin._register_device_source calls _stage_device_in_background
    - DevicesMixin._stage_device_in_background uses _StageWorker from mixins.workers
    - DevicesMixin depends on db, mtp, ftp, translator, theme, QMessageBox, QThread
    - Tests must patch devices_mixin_module.db alongside mw.db, camera_mixin_module.db, etc.
---

<objective>
Extract add-source/registry + MTP/FTP staging methods from MainWindow to DevicesMixin (Bloque 6).

Purpose: Reduce MainWindow god object by ~480 lines (18 methods) following canonical mixin pattern established in Bloque 1 (dialogs), Bloque 3 (CameraMixin), Bloque 4 (SourcesMixin), Bloque 5 (WifiMixin).
Output: DevicesMixin module, updated MainWindow composition, patched test suite.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/ui/mixins/wifi_mixin.py
@app/ui/mixins/camera_mixin.py
@app/ui/mixins/workers.py
@app/ui/main_window.py
@tests/test_main_window.py
@tests/test_wifi_source.py
@tests/test_source_content.py
@tests/test_session_content_modes.py
@tests/test_e2e.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DevicesMixin module with verbatim extraction</name>
  <files>app/ui/mixins/devices_mixin.py</files>
  <action>
Create app/ui/mixins/devices_mixin.py with the 18 methods extracted verbatim from MainWindow (lines 2965-3320 approx). Methods to extract:

1. _delete_all_saved_devices (2965-2982)
2. _delete_all_known_cameras (2984-2998)
3. _open_known_devices (3000-3006)
4. _disconnected_devices (3008-3043)
5. _register_device_source_from_picker (3045-3061)
6. _assign_folder_source (3063-3093)
7. _assign_session_folder (3095-3117)
8. _repair_folder_device_id (3119-3140)
9. _warn_managed_source (3142-3156)
10. _show_ftp_status (3158-3160)
11. _show_ftp_status_for_device (3162-3164)
12. _reset_mtp_thread_local (3166-3181)
13. _reset_ingestors (3183-3202)
14. _pick_ftp_source (3205-3227)
15. _register_device_source (3229-3273)
16. _stage_device_in_background (3275-3291)
17. _on_stage_progress (3293-3294)
18. _on_stage_done (3296-3319)

Required imports (matching wifi_mixin.py / camera_mixin.py style):
- os, json
- PySide6.QtWidgets: QMessageBox, QDialog, QFileDialog, QInputDialog
- PySide6.QtCore: QThread, QTimer
- app.core.db: db, WIFI_DEVICE_ID
- app.core: translator, mtp, ftp
- app.core.metadata_engine: metadata_engine
- app.core.sd_reader: sd_reader
- app.core.utils: is_removable_drive
- app.ui: theme, icons
- app.ui.ftp_picker: FtpPickerDialog
- app.ui.ftp_status: FtpStatusDialog
- app.ui.add_source_dialog: AddSourceDialog (for DeviceInfo type hint if needed)
- app.core.mtp: DeviceInfo
- app.ui.mixins.workers: _StageWorker

Class signature: class DevicesMixin: (no __init__, no super().__init__)
All strings wrapped in self.tr() or translator.tr() as in source.
Single quotes. Spanish comments/docs preserved verbatim.
  </action>
  <verify>
<automated>python -c "import app.ui.mixins.devices_mixin; print('DevicesMixin imported OK')" && grep -c "def _" app/ui/mixins/devices_mixin.py | grep -q "18"</automated>
  </verify>
  <done>DevicesMixin module created with 18 methods, imports correct, no syntax errors</done>
</task>

<task type="auto">
  <name>Task 2: Update MainWindow to compose DevicesMixin and remove extracted methods</name>
  <files>app/ui/main_window.py</files>
  <action>
Modify app/ui/main_window.py:

1. Add import: from app.ui.mixins.devices_mixin import DevicesMixin (after line 44 import CameraMixin)

2. Update class declaration (line ~149):
   class MainWindow(QMainWindow, WifiMixin, CameraMixin, DevicesMixin):

3. Delete the 18 extracted methods from MainWindow (lines 2965-3320 approx):
   - _delete_all_saved_devices
   - _delete_all_known_cameras
   - _open_known_devices
   - _disconnected_devices
   - _register_device_source_from_picker
   - _assign_folder_source
   - _assign_session_folder
   - _repair_folder_device_id
   - _warn_managed_source
   - _show_ftp_status
   - _show_ftp_status_for_device
   - _reset_mtp_thread_local
   - _reset_ingestors
   - _pick_ftp_source
   - _register_device_source
   - _stage_device_in_background
   - _on_stage_progress
   - _on_stage_done

4. Verify select_dest_path and _save_project_root remain in MainWindow (they are destination path management, not device registry/staging).

5. Ensure any internal references to these methods (e.g., self._stage_thread, self._stage_worker) still work — they are instance attributes set by _stage_device_in_background, so no change needed.

6. Run quick syntax check: python -m py_compile app/ui/main_window.py
  </action>
  <verify>
<automated>python -m py_compile app/ui/main_window.py && python -c "import app.ui.main_window; w = app.ui.main_window.MainWindow(); assert hasattr(w, '_delete_all_saved_devices'); assert hasattr(w, '_stage_device_in_background'); print('MainWindow has DevicesMixin methods')"</automated>
  </verify>
  <done>MainWindow composes DevicesMixin, 18 methods removed, syntax valid, methods accessible via mixin</done>
</task>

<task type="auto">
  <name>Task 3: Patch test suite for devices_mixin_module.db singleton</name>
  <files>tests/test_main_window.py, tests/test_wifi_source.py, tests/test_source_content.py, tests/test_session_content_modes.py, tests/test_e2e.py</files>
  <action>
Update all 5 test files to patch devices_mixin_module.db in parallel with existing singleton patches (mw.db, camera_mixin_module.db, ingestor_module.db, me_module.db). Pattern per test class setUp/tearDown:

1. Add import: import app.ui.mixins.devices_mixin as devices_mixin_module

2. In setUp: save original, patch:
   self._orig_devices_db = devices_mixin_module.db
   devices_mixin_module.db = self.db

3. In tearDown: restore:
   devices_mixin_module.db = self._orig_devices_db

Files to update (each has multiple test classes with setUp/tearDown):
- tests/test_main_window.py: 18 test classes → 18 setUp/tearDown pairs
- tests/test_wifi_source.py: check classes
- tests/test_source_content.py: check classes
- tests/test_session_content_modes.py: check classes
- tests/test_e2e.py: check classes

Apply the exact same pattern used for camera_mixin_module.db in each file. Use search/replace for consistency.

After patching all files, run full test suite:
python -m pytest tests/ -x -q
  </action>
  <verify>
<automated>python -m pytest tests/ -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 test files patched with devices_mixin_module.db, full test suite passes (393 tests)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MainWindow ↔ DevicesMixin | Internal composition, same process, trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.6.0-01 | Tampering | devices_mixin.py | low | accept | Pure refactor, no new logic; verbatim extraction preserves behavior |
| T-01.6.0-02 | Information Disclosure | test db patches | low | accept | Test-only singleton replacement, no production impact |

</threat_model>

<verification>
1. DevicesMixin module loads without error
2. MainWindow instantiates and has all 18 mixin methods
3. All 393 tests pass
4. MainWindow line count reduced by ~480 lines
5. No behavior changes — pure verbatim refactor
</verification>

<success_criteria>
- DevicesMixin created with 18 methods verbatim
- MainWindow composes DevicesMixin, methods removed
- Test suite patches devices_mixin_module.db in 5 files
- Full suite: 393 passed
- MainWindow ~3750 → ~3270 lines
</success_criteria>

<output>
Create .planning/quick/260906-bloque6-devicesmixin/260906-01-SUMMARY.md when done
</output>