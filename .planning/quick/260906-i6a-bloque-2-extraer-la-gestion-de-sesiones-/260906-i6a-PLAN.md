---
phase: 260906-i6a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ui/mixins/sessions_mixin.py
  - app/ui/main_window.py
  - app/ui/mixins/__init__.py
  - tests/test_main_window.py
  - tests/test_source_content.py
  - tests/test_e2e.py
  - tests/test_wifi_source.py
  - tests/test_session_content_modes.py
  - tests/test_selective_dump.py
autonomous: true
requirements: []
user_setup: []
estimate:
  tokens: 25000
  raw_tokens: 12500
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "SessionsMixin class exists in app/ui/mixins/sessions_mixin.py with all session methods moved VERBATIM"
    - "MainWindow inherits from SessionsMixin via MRO composition (after WifiMixin, CameraMixin)"
    - "All session methods in MainWindow are removed and delegate to SessionsMixin"
    - "Tests patch sessions_mixin_module.db singleton (mirror CameraMixin pattern)"
  artifacts:
    - "app/ui/mixins/sessions_mixin.py"
    - "app/ui/main_window.py (updated imports and MRO)"
    - "app/ui/mixins/__init__.py (export SessionsMixin)"
    - "Test files updated with sessions_mixin_module.db patch"
  key_links:
    - "MRO order: MainWindow(QMainWindow, WifiMixin, CameraMixin, SessionsMixin)"
    - "Singleton db patch in tests: sessions_mixin_module.db = test_db"
    - "No __init__ or super().__init__ in SessionsMixin"
---

<objective>
Extract session management methods from MainWindow into a new SessionsMixin following the exact pattern of WifiMixin/CameraMixin: VERBATIM method moves, MRO composition, singleton db patch in tests.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/ui/main_window.py
@app/ui/mixins/camera_mixin.py
@app/ui/mixins/wifi_mixin.py
@app/ui/mixins/__init__.py
@tests/test_main_window.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SessionsMixin with all session methods VERBATIM</name>
  <files>app/ui/mixins/sessions_mixin.py, app/ui/mixins/__init__.py</files>
  <action>
Create app/ui/mixins/sessions_mixin.py with the SessionsMixin class containing ALL session methods moved VERBATIM from main_window.py. Source line ranges:
- Lines 2255-2605 (contiguous block): _refresh_sessions_combo through _browse_session_src
- Line 2802-2810: _populate_source_paths_from_sessions
- Lines 3642-3665: _open_content_filter

Required imports in sessions_mixin.py:
  from PySide6.QtCore import QSettings
  from PySide6.QtWidgets import QMessageBox, QInputDialog, QFileDialog, QDialog, QSpinBox, QComboBox, QDialogButtonBox, QFormLayout
  from datetime import datetime
  import json
  import os
  from app.core.db import db
  from app.core.metadata_engine import metadata_engine
  from app.core import translator
  from app.ui import icons
  from app.ui.selective_dump import SelectiveDumpAssistant, content_summary

Rules:
- NO __init__, NO super().__init__
- Class docstring in Spanish: "Métodos de gestión de sesiones extraídos de MainWindow (quick 260906-i6a)."
- Preserve single quotes, tr() calls, Spanish comments/docstrings exactly
- Keep @staticmethod decorators on _window_value_unit_from_filter and _window_days_from_filter
- Methods reference self. for all instance attributes (db, current_project_id, sessions_combo, etc.) — these exist on MainWindow via MRO
- Update app/ui/mixins/__init__.py to export SessionsMixin
  </action>
  <verify>
python -c "from app.ui.mixins.sessions_mixin import SessionsMixin; print('SessionsMixin imported OK')"
  </verify>
  <done>
SessionsMixin class created with all session methods VERBATIM, proper imports, no __init__, Spanish docstring, exported from __init__.py
  </done>
</task>

<task type="auto">
  <name>Task 2: Update MainWindow to use SessionsMixin via MRO</name>
  <files>app/ui/main_window.py</files>
  <action>
Update app/ui/main_window.py:
1. Add import: `from app.ui.mixins.sessions_mixin import SessionsMixin` (after CameraMixin import line 44)
2. Update class declaration line 128 to: `class MainWindow(QMainWindow, WifiMixin, CameraMixin, SessionsMixin):`
3. DELETE all session methods from MainWindow (they now come from SessionsMixin via MRO):
   - Lines 2255-2605 (contiguous block): _refresh_sessions_combo through _browse_session_src
   - Lines 2802-2810: _populate_source_paths_from_sessions
   - Lines 3642-3665: _open_content_filter
4. Ensure no duplicate methods remain (grep for method names to verify)

Critical: The methods in SessionsMixin use self. to access MainWindow attributes (current_project_id, sessions_combo, btn_delete_session, session_src_label, _btn_browse_sess_src, session_dest_label, _btn_browse_sess_dest, btn_session_dump_mode, btn_session_dump_config, ingest_status_label, _source_paths, _wifi_ingestors, project_camera_detection_mode, project_folder_name, project_organization_type, project_duration_type, project_default_dispositivo, project_use_metadata_date, dest_root, project_date, _current_camera_map, _restore_session_id, _unknown_cameras, _cam_detection_id, _cam_timer, _cam_scan_scheduled, _cam_detected, project_camera_detection_timeout). These attributes exist on MainWindow and are accessible via MRO.
  </action>
  <verify>
python -c "from app.ui.main_window import MainWindow; print('MainWindow imports OK'); assert 'SessionsMixin' in str(MainWindow.__mro__)"
python -m pytest tests/test_main_window.py::TestCameraDetectionToken -x -v 2>&1 | head -30
  </verify>
  <done>
MainWindow imports SessionsMixin, MRO includes SessionsMixin, all session methods removed from MainWindow, basic import test passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Update test files to patch sessions_mixin_module.db singleton</name>
  <files>tests/test_main_window.py, tests/test_source_content.py, tests/test_e2e.py, tests/test_wifi_source.py, tests/test_session_content_modes.py, tests/test_selective_dump.py</files>
  <action>
For each test file that sets `mw.db = self.db` in setUp (mirror CameraMixin pattern at test_main_window.py lines 40-48), add parallel patch for sessions_mixin_module.db:

Pattern to add in each setUp:
  import app.ui.mixins.sessions_mixin as sessions_mixin_module
  self._orig_sessions_db = sessions_mixin_module.db
  sessions_mixin_module.db = self.db

Pattern to add in each tearDown:
  sessions_mixin_module.db = self._orig_sessions_db

Files to update (per context): test_main_window.py, test_source_content.py, test_e2e.py, test_wifi_source.py, test_session_content_modes.py, test_selective_dump.py

Each file likely has ~12-14 setUp/tearDown pairs needing the patch. Follow the exact pattern from test_main_window.py CameraMixin lines 22, 41-42, 68-69.
  </action>
  <verify>
python -m pytest tests/test_main_window.py -x -v 2>&1 | head -50
python -m pytest tests/test_source_content.py -x -v 2>&1 | head -30
python -m pytest tests/test_e2e.py -x -v 2>&1 | head -30
python -m pytest tests/test_wifi_source.py -x -v 2>&1 | head -30
python -m pytest tests/test_session_content_modes.py -x -v 2>&1 | head -30
python -m pytest tests/test_selective_dump.py -x -v 2>&1 | head -30
  </verify>
  <done>
All test files patched with sessions_mixin_module.db singleton patch in setUp/tearDown mirroring CameraMixin pattern; test suites pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MainWindow → SessionsMixin | Internal MRO composition; no external trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260906-i6a-01 | Tampering | sessions_mixin.py | low | accept | Pure refactor, no new logic; VERBATIM move verified by tests |
| T-260906-i6a-02 | Information Disclosure | Test singleton patches | low | mitigate | Test-only patches; production code unaffected |
</threat_model>

<verification>
- All session methods present in SessionsMixin (grep method names)
- MainWindow MRO includes SessionsMixin
- No session methods remain in MainWindow (grep confirms)
- All test suites pass with db singleton patches
- No runtime regressions in session management flows
</verification>

<success_criteria>
- SessionsMixin created with 18 methods VERBATIM from MainWindow
- MainWindow inherits SessionsMixin via MRO (order: QMainWindow, WifiMixin, CameraMixin, SessionsMixin)
- 6 test files updated with sessions_mixin_module.db patch
- All existing tests pass
- Zero functional change — pure structural refactor
</success_criteria>

<output>
Create .planning/quick/260906-i6a-bloque-2-extraer-la-gestion-de-sesiones-/260906-i6a-01-SUMMARY.md when done
</output>