---
phase: 260906-i6a
plan: 01
subsystem: app/ui
tags: [refactor, mixin, sessions, mro]
requires: []
provides: [SessionsMixin]
affects: [MainWindow]
tech_stack:
  added: []
  patterns: [mixin-composition, mro, singleton-db-patch]
key_files:
  created:
    - app/ui/mixins/sessions_mixin.py
  modified:
    - app/ui/main_window.py
    - app/ui/mixins/__init__.py
    - tests/test_main_window.py
    - tests/test_source_content.py
    - tests/test_e2e.py
    - tests/test_wifi_source.py
    - tests/test_session_content_modes.py
decisions:
  - "Extracted 18 session management methods from MainWindow to SessionsMixin VERBATIM"
  - "MainWindow MRO updated to: QMainWindow, WifiMixin, CameraMixin, SessionsMixin"
  - "Test files updated to patch sessions_mixin_module.db singleton (mirroring CameraMixin pattern)"
  - "Session methods delegate via MRO to SessionsMixin — zero behavior change"
metrics:
  duration: "~45 minutes"
  completed: "2026-09-06"
  tasks: 3
  commits: 1
actuals:
  tokens: 74000
  tasks: 3
  commits: 1
status: complete
---

# Phase 260906-i6a Plan 01: Extract Session Management to SessionsMixin

**One-liner:** Extracted 18 session management methods from MainWindow to SessionsMixin via MRO composition — pure structural refactor, zero behavior change.

## Tasks Completed

| Task | Description | Status |
|------|-------------|--------|
| 1 | Create SessionsMixin with 18 session methods VERBATIM from MainWindow | ✅ |
| 2 | Update MainWindow to use SessionsMixin via MRO, delete session methods | ✅ |
| 3 | Patch sessions_mixin_module.db in 6 test files | ✅ |

## Changes Summary

### Files Created
- **app/ui/mixins/sessions_mixin.py** — New mixin class with 18 session methods moved VERBATIM from MainWindow (lines 2255-2605, 2802-2810, 3642-3665). No `__init__`, Spanish docstring, proper imports, preserves `tr()` calls and `@staticmethod` decorators.

### Files Modified
- **app/ui/main_window.py** — Added SessionsMixin import, updated class declaration MRO to `MainWindow(QMainWindow, WifiMixin, CameraMixin, SessionsMixin)`, deleted all 18 session methods from MainWindow (verified none remain in `MainWindow.__dict__`).
- **app/ui/mixins/__init__.py** — Exported SessionsMixin alongside WifiMixin and CameraMixin.
- **tests/test_main_window.py** — Added `sessions_mixin_module.db` patch in 14 test class `setUp`/`tearDown` methods (mirroring CameraMixin pattern).
- **tests/test_source_content.py** — Added sessions_mixin_module.db patch in setUp/tearDown.
- **tests/test_e2e.py** — Added sessions_mixin_module.db patch in setUp/tearDown.
- **tests/test_wifi_source.py** — Added sessions_mixin_module.db patch in setUp/tearDown.
- **tests/test_session_content_modes.py** — Added sessions_mixin_module.db patch in setUp/tearDown for both test classes; fixed mock target from `mw.SelectiveDumpAssistant` to `sessions_mixin_module.SelectiveDumpAssistant`.

### Files NOT Modified
- **tests/test_selective_dump.py** — Does not use MainWindow or mixins; no changes needed.

## Verification Results

- **Import test:** `SessionsMixin` imports successfully
- **MRO check:** `SessionsMixin` present in `MainWindow.__mro__` at correct position (after CameraMixin)
- **Method removal:** All 18 session methods confirmed removed from `MainWindow.__dict__` and inherited via MRO
- **Test suite baseline:** 393 passed, 5 skipped, 28 subtests, 1 pre-existing failure (test_mtp order-dependent) — matches expected baseline exactly

## Deviations from Plan

**None** — Plan executed exactly as written. Pure structural refactor with zero functional change.

## Known Stubs

None — all methods fully implemented and tested.

## Threat Flags

None — no new security surface introduced. Test-only singleton patches are internal to test execution.

## Commits

Single atomic commit: `refactor(ui): extraer gestion de sesiones de MainWindow a SessionsMixin`