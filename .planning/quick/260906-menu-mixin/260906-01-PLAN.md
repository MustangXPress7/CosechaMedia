---
phase: quick
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ui/mixins/menu_mixin.py
  - app/ui/main_window.py
  - tests/test_main_window.py
autonomous: true
requirements: []
user_setup: []

estimate:
  tokens: 25000
  raw_tokens: 12500
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "build_menu se ejecuta y construye la barra de menús completa (Archivo, Ingesta, Configuración, Herramientas, Vista, Ayuda, Idioma)"
    - "_switch_language cambia el idioma vía translator.set_language y muestra mensaje de reinicio"
    - "MainWindow hereda de MenuMixin y mantiene idéntico comportamiento de menú e idioma"
  artifacts:
    - "app/ui/mixins/menu_mixin.py (nuevo)"
    - "app/ui/main_window.py (métodos build_menu y _switch_language eliminados, hereda MenuMixin)"
  key_links:
    - "MenuMixin.compose → MainWindow(QMainWindow, WifiMixin, CameraMixin, MenuMixin)"
    - "build_menu usa translator, theme, wheat_field, QSettings → imports necesarios en mixin"
    - "_switch_language usa translator, QMessageBox → imports necesarios en mixin"
---

<objective>
Extraer build_menu + _switch_language de MainWindow a MenuMixin en app/ui/mixins/menu_mixin.py (patrón canónico: composición múltiple, sin __init__, verbatim).

Purpose: Reducir MainWindow (bloque 7 del plan de reducción god object). build_menu ~162 líneas + _switch_language ~8 líneas = ~170 líneas totales.

Output:
- Nuevo mixin: app/ui/mixins/menu_mixin.py con build_menu y _switch_language verbatim
- MainWindow actualizado: hereda MenuMixin, elimina los dos métodos
- Tests adaptados: parcheo paralelo del singleton db en menu_mixin_module si hace falta
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/ui/mixins/camera_mixin.py
@app/ui/mixins/wifi_mixin.py
@app/ui/main_window.py
@tests/test_main_window.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear MenuMixin con build_menu y _switch_language verbatim</name>
  <files>
    app/ui/mixins/menu_mixin.py
  </files>
  <action>
    Crear app/ui/mixins/menu_mixin.py siguiendo el patrón canónico de camera_mixin.py y wifi_mixin.py:
    - Docstring en español: "Métodos de menú e idioma extraídos de MainWindow (quick 260906-menu-mixin)."
    - Importar: QAction, QActionGroup, QMessageBox, QSettings desde PySide6; translator, theme, wheat_field desde app.core/app.ui
    - Clase MenuMixin SIN __init__, SIN super().__init__()
    - Copiar VERBATIM build_menu (líneas 2631-2792 de main_window.py actual) y _switch_language (2793-2800)
    - Mantener tr() intacto, comillas simples, español en comentarios/docs
    - NO añadir lógica nueva, SOLO extracción fiel
  </action>
  <verify>
    <automated>python -c "import app.ui.mixins.menu_mixin; m = app.ui.mixins.menu_mixin.MenuMixin(); print('MenuMixin OK:', hasattr(m, 'build_menu'), hasattr(m, '_switch_language'))"</automated>
  </verify>
  <done>
    menu_mixin.py existe con build_menu y _switch_language verbatim; importable sin errores
  </done>
</task>

<task type="auto">
  <name>Task 2: Actualizar MainWindow para heredar MenuMixin y eliminar métodos extraídos</name>
  <files>
    app/ui/main_window.py
  </files>
  <action>
    Editar app/ui/main_window.py:
    1. Añadir import: from app.ui.mixins.menu_mixin import MenuMixin
    2. Cambiar declaración de clase: class MainWindow(QMainWindow, WifiMixin, CameraMixin, MenuMixin):
    3. ELIMINAR build_menu (líneas ~2631-2792) y _switch_language (líneas ~2793-2800) del cuerpo de MainWindow
    4. Verificar que no queden referencias huérfanas a estos métodos en MainWindow
    5. Mantener todos los demás métodos que build_menu conecta (show_about, _switch_theme, _switch_accent, _toggle_wheat_background, _detect_sd_card, etc.) — siguen en MainWindow
  </action>
  <verify>
    <automated>python -c "import app.ui.main_window as mw; w = mw.MainWindow(); print('build_menu:', hasattr(w, 'build_menu')); print('_switch_language:', hasattr(w, '_switch_language')); w.close()"</automated>
  </verify>
  <done>
    MainWindow hereda MenuMixin; build_menu y _switch_language ya no están en main_window.py; menú funciona idéntico
  </done>
</task>

<task type="auto">
  <name>Task 3: Adaptar tests — parcheo paralelo del singleton db en menu_mixin_module</name>
  <files>
    tests/test_main_window.py
  </files>
  <action>
    Editar tests/test_main_window.py siguiendo el patrón establecido en Bloque 3 (camera_mixin) y WiFi mixin:
    - Importar app.ui.mixins.menu_mixin as menu_mixin_module
    - En setUp: guardar _orig_menu_db = menu_mixin_module.db; asignar menu_mixin_module.db = self.db
    - En tearDown: restaurar menu_mixin_module.db = _orig_menu_db
    - Añadir db al setUp/tearDown de cada clase de test que use MainWindow (TestCameraDetectionToken, TestMetadataUnverifiedMarker, etc.)
    - NO crear tests nuevos; solo parcheo para que los tests existentes sigan pasando
  </action>
  <verify>
    <automated>cd H:/Proyectos Software/SD Import && python -m pytest tests/test_main_window.py -x -v 2>&1 | head -50</automated>
  </verify>
  <done>
    Suite test_main_window.py pasa (393 tests); parcheo db en menu_mixin_module funcionando
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Usuario → UI | Usuario interactúa con menús; acciones disparan slots internos |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Tampering | menu_mixin.py imports | low | accept | Extracción verbatim; sin lógica nueva; imports estándar Qt/app |
| T-quick-02 | Spoofing | Herencia múltiple | low | accept | Patrón ya validado en WifiMixin/CameraMixin; MRO lineal sin conflictos |
</threat_model>

<verification>
1. python -c "import app.ui.mixins.menu_mixin; m = app.ui.mixins.menu_mixin.MenuMixin(); m.build_menu(); m._switch_language('en')" (no debe lanzar)
2. python -c "import app.ui.main_window as mw; w = mw.MainWindow(); w.build_menu(); w._switch_language('en'); w.close()" (no debe lanzar)
3. pytest tests/test_main_window.py -x (suite completa pasa)
4. git diff --stat app/ui/main_window.py (reducción ~170 líneas)
</verification>

<success_criteria>
- MenuMixin creado con build_menu + _switch_language verbatim
- MainWindow hereda MenuMixin y elimina los dos métodos (~170 líneas menos)
- Tests adaptados con parcheo db paralelo
- Suite completa pasa (393 tests)
- Comportamiento idéntico: menús, cambio de idioma, shortcuts, todo funcional
</success_criteria>

<output>
Create .planning/quick/260906-menu-mixin/260906-01-SUMMARY.md when done
</output>