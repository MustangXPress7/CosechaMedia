---
phase: quick
plan: "01"
status: complete
subsystem: ui
tags:
  - refactor
  - mixin
  - menu
  - god-object-reduction
requires: []
provides: []
affects:
  - app/ui/main_window.py
  - app/ui/mixins/menu_mixin.py
  - tests/test_main_window.py
tech_stack:
  added:
    - app/ui/mixins/menu_mixin.py (new)
  patterns:
    - Multiple inheritance composition (MenuMixin added to MainWindow MRO)
    - Verbatim extraction of methods to mixin (no behavior change)
    - Test singleton patching for new mixin module
key_files:
  created:
    - app/ui/mixins/menu_mixin.py
  modified:
    - app/ui/main_window.py
    - tests/test_main_window.py
decisions:
  - "MenuMixin follows canonical pattern: no __init__, no super().__init__(), verbatim extraction"
  - "MRO order: QMainWindow, WifiMixin, CameraMixin, MenuMixin, DevicesMixin, SourcesMixin, ProjectMixin, SessionsMixin"
  - "Test fix: TestCleanupMenu now imports QAction from PySide6.QtGui directly since main_window no longer exports it"
metrics:
  duration_seconds: 420
  tasks_completed: 3
  files_changed: 3
  lines_added: 190
  lines_removed: 174
  main_window_reduction: 173
actuals:
  tokens: 74000
  tasks: 3
  commits: 1
---

# Phase Quick Plan 01: MenuMixin Extraction Summary

## One-Liner

Extracción verbatim de `build_menu` (~162 líneas) y `_switch_language` (~8 líneas) de `MainWindow` a `MenuMixin` en `app/ui/mixins/menu_mixin.py` — MainWindow reducido 173 líneas, herencia múltiple preserva comportamiento idéntico.

## Tasks Completed

| Task | Description | Status |
|------|-------------|--------|
| 1 | Crear `MenuMixin` con `build_menu` y `_switch_language` verbatim en `app/ui/mixins/menu_mixin.py` | ✅ |
| 2 | Actualizar `MainWindow`: importar `MenuMixin`, heredar en MRO, eliminar los dos métodos | ✅ |
| 3 | Adaptar tests: parcheo singleton `db` en `menu_mixin_module` + fix `TestCleanupMenu` import | ✅ |

## Verification

- **Import test**: `MenuMixin` importable, `build_menu` y `_switch_language` presentes
- **MainWindow test**: `MainWindow` tiene `build_menu` y `_switch_language` via herencia, no en `__dict__`
- **MRO verified**: `['MainWindow', 'QMainWindow', ..., 'WifiMixin', 'CameraMixin', 'MenuMixin', 'DevicesMixin', ...]`
- **Test suite**: `test_main_window.py` 57 passed; full suite 393 passed, 5 skipped, 28 subtests, 1 pre-existing MTP failure only
- **Line reduction**: `main_window.py` -173 lines (176 net change: 3 insertions, 173 deletions)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Import Fix] QAction/QActionGroup imports corrected in MenuMixin**
- **Found during**: Task 1 verification
- **Issue**: `QAction` and `QActionGroup` are in `PySide6.QtGui`, not `PySide6.QtWidgets`
- **Fix**: Changed imports in `menu_mixin.py` to `from PySide6.QtGui import QAction, QActionGroup`
- **Files modified**: `app/ui/mixins/menu_mixin.py`
- **Commit**: 4dcabaf

**2. [Rule 1 - Test Fix] TestCleanupMenu QAction import updated**
- **Found during**: Task 3 test run
- **Issue**: Test used `mw.QAction` but `main_window.py` no longer exports `QAction`
- **Fix**: Added local import `from PySide6.QtGui import QAction` in test method
- **Files modified**: `tests/test_main_window.py`
- **Commit**: 4dcabaf

**3. [Rule 2 - Cleanup] Removed unused QAction/QActionGroup imports from main_window.py**
- **Found during**: Post-extraction review
- **Issue**: `QAction` and `QActionGroup` only used in extracted `build_menu`
- **Fix**: Removed from imports in `main_window.py`
- **Files modified**: `app/ui/main_window.py`
- **Commit**: 4dcabaf

## Auth Gates

None — pure refactor, no external dependencies.

## Threat Flags

None — no new attack surface introduced. Extraction is verbatim with same imports.

## Known Stubs

None — all methods fully implemented, no placeholders.

## Self-Check: PASSED

- [x] `app/ui/mixins/menu_mixin.py` exists with both methods verbatim
- [x] `app/ui/main_window.py` imports MenuMixin, inherits it, methods removed
- [x] `tests/test_main_window.py` patches `menu_mixin_module.db` (existing pattern)
- [x] Commit hash `4dcabaf` recorded
- [x] Full test suite passes at baseline (393 passed, 1 pre-existing failure)