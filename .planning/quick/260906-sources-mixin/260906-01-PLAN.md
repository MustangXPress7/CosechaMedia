---
phase: quick-260906
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ui/mixins/sources_mixin.py
  - app/ui/main_window.py
  - tests/test_main_window.py
  - tests/test_wifi_source.py
  - tests/test_source_content.py
  - tests/test_session_content_modes.py
  - tests/test_e2e.py
autonomous: true
requirements: []
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 3
  confidence: high
must_haves:
  truths:
    - SourcesMixin existe en app/ui/mixins/sources_mixin.py con 30 métodos source verbatim
    - MainWindow hereda de SourcesMixin (MRO: QMainWindow, WifiMixin, CameraMixin, SourcesMixin)
    - Los 30 métodos source ya no existen en main_window.py (eliminados, no comentados)
    - Tests parchean sources_mixin_module.db en paralelo a mw.db (18 setUps en 5 ficheros)
    - Suite de tests pasa (393 tests) sin regresiones
  artifacts:
    - app/ui/mixins/sources_mixin.py
    - app/ui/main_window.py (modificado)
    - tests/test_main_window.py (parche db)
    - tests/test_wifi_source.py (parche db)
    - tests/test_source_content.py (parche db)
    - tests/test_session_content_modes.py (parche db)
    - tests/test_e2e.py (parche db)
  key_links:
    - sources_mixin_module.db patch en todos los setUps de test que usan métodos source
    - MainWindow MRO incluye SourcesMixin tras CameraMixin
    - _source_list_event_filter sigue siendo el eventFilter principal (lin 2181-2182)

---

<objective>
Extraer la tabla de fuentes, widgets y menús contextuales de MainWindow a SourcesMixin en app/ui/mixins/sources_mixin.py siguiendo el patrón WifiMixin/CameraMixin: métodos VERBATIM, composición MRO, singleton db patch en tests.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/ui/mixins/wifi_mixin.py
@app/ui/mixins/camera_mixin.py
@app/ui/main_window.py
@tests/test_main_window.py
@tests/test_wifi_source.py
@tests/test_source_content.py
@tests/test_session_content_modes.py
@tests/test_e2e.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear SourcesMixin con 30 métodos source VERBATIM</name>
  <files>
    app/ui/mixins/sources_mixin.py
  </files>
  <action>
    Crear app/ui/mixins/sources_mixin.py copiando VERBATIM los 30 métodos relacionados con la tabla de fuentes, widgets y menús contextuales desde main_window.py. Incluir imports necesarios: os, json, PySide6.QtWidgets (QMessageBox, QFileDialog, QMenu, QCheckBox, QLabel, QWidget, QHBoxLayout, QPushButton, QSizePolicy), PySide6.QtCore (Qt, QDate, QTimer), app.core.db (db, WIFI_DEVICE_ID), app.core.metadata_engine (metadata_engine, _is_system_entry), app.core.ftp (ftp), app.core.mtp (mtp), app.core.shoot_inbox (inboxmod), app.core.translator (translator), app.ui.theme (theme), app.ui.icons (icons). NO añadir __init__, NO super().__init__(). Métodos a extraer (ordenados como aparecen en main_window.py líneas 1747-2248 y 2811-2949):

    Bloque tabla/widgets/menús (1747-2248):
    - _current_source_path (1747-1755)
    - _on_source_double_clicked (1809-1813)
    - _prompt_change_source_path (1815-1848)
    - _refresh_source_list (1851-1888)
    - _update_source_list_height (1890-1898)
    - _build_status_label (1900-1921)
    - _update_source_status_cells (1923-1941)
    - _build_path_widget (1943-1979)
    - _disable_source_in_project (1981-1990)
    - _on_source_widget_check_changed (1991-2054)
    - _build_options_widget (2056-2105)
    - _toggle_device_delicate (2107-2110)
    - _build_remove_file_button (2112-2120)
    - _reconfigure_ftp_source (2128-2132)
    - _reconfigure_mtp_source (2134-2149)
    - _show_source_context_menu (2152-2160)
    - _clear_source_list_selection (2162-2165)
    - _source_list_event_filter (2167-2179)
    - _delete_source_at_row (2184-2204)
    - _remove_source_path (2206-2236)
    - _hide_source_path (2238-2246)
    - _on_source_check_changed (2248-2253)

    Bloque entry helpers (2811-2949):
    - _add_source_entry (2811-2815)
    - _apply_source_choice (2817-2852)
    - _pick_source_entry (2854-2898)
    - _delete_saved_source (2900-2949)

    NOTA: _populate_source_paths_from_sessions (2802-2809) → SessionsMixin (Bloque 2, ya planificado). NO incluirlo aquí.
    NOTA: _register_device_source_from_picker (3045), _assign_folder_source (3063), _warn_managed_source (3142), _pick_ftp_source (3205), _register_device_source (3229) → DevicesMixin (Bloque 6). NO incluirlos aquí.

    Mantener tr() intacto, comillas simples, comentarios en español. self. se queda self. (VERBATIM).
  </action>
  <verify>
    <automated>python -c "from app.ui.mixins.sources_mixin import SourcesMixin; print('SourcesMixin import OK'); methods = [m for m in dir(SourcesMixin) if m.startswith('_') and not m.startswith('__')]; print(f'Métodos: {len(methods)}'); [print(f'  {m}') for m in methods]"</automated>
  </verify>
  <done>
    SourcesMixin creado con 30 métodos source, imports correctos, sin __init__
  </done>
</task>

<task type="auto">
  <name>Task 2: Actualizar MainWindow - heredar SourcesMixin y eliminar métodos extraídos</name>
  <files>
    app/ui/main_window.py
  </files>
  <action>
    Editar app/ui/main_window.py:
    1. Añadir import: from app.ui.mixins.sources_mixin import SourcesMixin
    2. Cambiar declaración de clase (línea 128): class MainWindow(QMainWindow, WifiMixin, CameraMixin, SourcesMixin):
    3. ELIMINAR (no comentar) los 30 métodos source listados en Task 1 (líneas 1747-2248 y 2811-2949 aprox - verificar líneas actuales tras extracciones previas).
    4. El eventFilter principal (líneas 2181-2182 actuales) DEBE mantenerse y seguir delegando a self._source_list_event_filter (que ahora viene del mixin).
    5. Verificar que _populate_source_paths_from_sessions (línea 2802) NO se elimina - va a SessionsMixin.
    6. Verificar que métodos de DevicesMixin (_register_device_source_from_picker, _assign_folder_source, _warn_managed_source, _pick_ftp_source, _register_device_source) NO se tocan - Bloque 6 futuro.
    7. Ajustar números de línea en comentarios si los hay referenciando métodos eliminados.
  </action>
  <verify>
    <automated>python -c "from app.ui.main_window import MainWindow; print('MainWindow import OK'); mro = [c.__name__ for c in MainWindow.__mro__]; print(f'MRO: {mro}'); assert 'SourcesMixin' in mro, 'SourcesMixin no en MRO'; assert 'WifiMixin' in mro; assert 'CameraMixin' in mro; methods = ['_current_source_path','_refresh_source_list','_build_status_label','_build_path_widget','_on_source_widget_check_changed','_delete_source_at_row','_remove_source_path','_hide_source_path','_add_source_entry','_pick_source_entry','_delete_saved_source','_source_list_event_filter']; [print(f'  {m}: {hasattr(MainWindow, m)}') for m in methods]; assert all(hasattr(MainWindow, m) for m in methods), 'Faltan métodos en MainWindow'"
    </automated>
  </verify>
  <done>
    MainWindow hereda SourcesMixin, 30 métodos eliminados, eventFilter principal intacto, MRO correcto
  </done>
</task>

<task type="auto">
  <name>Task 3: Parchear sources_mixin_module.db en 18 setUps de 5 ficheros test</name>
  <files>
    tests/test_main_window.py
    tests/test_wifi_source.py
    tests/test_source_content.py
    tests/test_session_content_modes.py
    tests/test_e2e.py
  </files>
  <action>
    En cada test file que tenga `mw.db = self.db` en setUp, añadir paralela el parche del módulo sources_mixin:
    - Importar: import app.ui.mixins.sources_mixin as sources_mixin_module
    - En setUp: self._orig_sources_db = sources_mixin_module.db; sources_mixin_module.db = self.db
    - En tearDown: sources_mixin_module.db = self._orig_sources_db

    Ficheros y setUps a actualizar (basado en grep actual):
    1. tests/test_main_window.py: 13 setUps (líneas 40, 120, 234, 297, 390, 493, 623, 778, 950, 1079, 1186, 1245, 1293, 1392 aprox)
    2. tests/test_wifi_source.py: 1 setUp (línea 46)
    3. tests/test_source_content.py: 1 setUp (línea 49)
    4. tests/test_session_content_modes.py: 1 setUp (línea 157)
    5. tests/test_e2e.py: 1 setUp (línea 51)
    Total: 17-18 setUps.

    Patrón a seguir (igual que wifi_mixin_module.db y camera_mixin_module.db):
    setUp:
        self._orig_sources_db = sources_mixin_module.db
        sources_mixin_module.db = self.db
    tearDown:
        sources_mixin_module.db = self._orig_sources_db

    NO tocar tests que no parcheen mw.db (no usan métodos source directamente).
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    18 setUps parchean sources_mixin_module.db, suite completa pasa (393 tests)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI ↔ Core DB | MainWindow/SourcesMixin llama a db singleton para CRUD sesiones/orígenes |
| UI ↔ External Dialogs | _pick_source_entry abre AddSourceDialog (modal), _reconfigure_* abren pickers |
| UI ↔ WiFi Server | _build_path_widget crea botón QR que llama a _show_wifi_qr_for_sender (WifiMixin) |
| UI ↔ MTP/FTP | _reconfigure_mtp_source/_reconfigure_ftp_source abren pickers de dispositivo (DevicesMixin futuro) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q4-01 | Tampering | _delete_source_at_row / _remove_source_path | medium | mitigate | Confirmación QMessageBox.Yes/No antes de borrar sesiones y remitentes WiFi |
| T-Q4-02 | Information Disclosure | _build_path_widget muestra ruta completa en tooltip | low | accept | Ruta de origen local, no sensible; usuario es operador local |
| T-Q4-03 | Denial of Service | _refresh_source_list reconstruye tabla completa | low | accept | Tabla típica <20 filas; bloqueo de señales evita flicker |
| T-Q4-SC | Tampering | npm/pip installs | high | mitigate | No nuevos packages en este plan; solo refactor interno |

</threat_model>

<verification>
- [ ] SourcesMixin importable y contiene 30 métodos esperados
- [ ] MainWindow MRO incluye SourcesMixin en posición correcta
- [ ] 30 métodos source eliminados de main_window.py (grep confirma 0 matches)
- [ ] eventFilter principal (lin ~2181) delega a _source_list_event_filter del mixin
- [ ] 18 setUps en 5 ficheros test parchean sources_mixin_module.db
- [ ] pytest tests/ -x pasa completo (393 tests)
- [ ] main_window.py líneas reducidas ~460 (3750 → ~3290)
</verification>

<success_criteria>
SourcesMixin extraído verbatim, MainWindow composición MRO funcionando, tests parcheados y pasando, sin cambios de comportamiento.
</success_criteria>

<output>
Create .planning/quick/260906-sources-mixin/260906-01-SUMMARY.md when done
</output>