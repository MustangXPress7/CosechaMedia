---
phase: quick
plan: 01
subsystem: ui
tags: [refactor, mixin-extraction, ingestion-pipeline]
dependency_graph:
  requires: [260906-ci2, 260906-fgl]
  provides: [ingest-mixin]
  affects: [main_window, test_main_window, test_e2e, test_source_content]
tech_stack:
  added: []
  patterns: [mixin-composition, parallel-singleton-patching]
key_files:
  created:
    - app/ui/mixins/ingest_mixin.py
  modified:
    - app/ui/main_window.py
    - tests/test_main_window.py
    - tests/test_e2e.py
    - tests/test_source_content.py
decisions:
  - "IngestMixin placed last in MRO (after WifiMixin, CameraMixin) to access their methods"
  - "Worker functions (_format_sources_worker, _generate_proxies_worker, _reorganize_worker, _probe_device_connectivity) kept at module level in main_window.py"
  - "ORG_TYPE_MAP duplicated in ingest_mixin.py for module independence"
  - "Parallel db patching extended to ingest_mixin_module across 16 test classes"
metrics:
  duration: 45m
  completed_date: "2026-09-07"
  tasks: 3
  commits: 1
status: complete
actuals:
  tokens: 85000
  tasks: 3
  commits: 1
---

# Phase Quick Plan 01: IngestMixin Extraction Summary

**One-liner:** Extracted ~620 lines of ingestion pipeline and post-ingest actions from MainWindow into canonical IngestMixin at app/ui/mixins/ingest_mixin.py (29 methods verbatim)

## Changes Made

### 1. Created `app/ui/mixins/ingest_mixin.py` (new file, ~650 lines)
IngestMixin class containing all 29 ingestion pipeline and post-ingest methods extracted verbatim from MainWindow:
- `start_ingest`, `stop_ingest`, `prepare_for_update`
- `on_file_started`, `_file_row_key`, `on_copy_progress`, `on_file_finished`
- `_on_ingestor_complete`, `_finalize_ingest`, `_run_next_post_ingest_action`
- `_is_managed_source_path`, `_is_managed_session`, `_format_candidate_paths`, `_update_format_sources_state`
- `_format_sources_after_ingest`, `_on_format_finished`
- `_shutdown_computer`, `_generate_report_after_ingest`
- `update_status_from_watcher`, `_run_background`, `_cleanup_background`
- `_show_table_context_menu`, `_clear_completed_rows`
- `_export_integrity_report`, `_export_card_content_report`
- `_proxy_resolution_height`, `_generate_proxies_after_ingest`, `_on_proxies_finished`

Imports: All required Qt, core, and UI modules; `ORG_TYPE_MAP` duplicated for module independence.

### 2. Modified `app/ui/main_window.py`
- Added import: `from app.ui.mixins.ingest_mixin import IngestMixin`
- Updated class declaration: `MainWindow(QMainWindow, WifiMixin, CameraMixin, MenuMixin, DevicesMixin, SourcesMixin, ProjectMixin, SessionsMixin, IngestMixin)`
- Removed all 29 extracted methods (~620 lines)
- Preserved module-level worker functions: `_format_drive`, `_format_sources_worker`, `_generate_proxies_worker`, `_reorganize_worker`, `_probe_device_connectivity`
- Preserved `ORG_TYPE_MAP` at module level
- Preserved `eventFilter`, `select_dest_path`, `_check_for_updates`, `_run_startup_update_check`, `_on_startup_update_check`, `_reorganize_by_metadata`, `_on_reorganize_finished`

### 3. Updated Test Files with Parallel Singleton Patching
Added `ingest_mixin_module.db` patching to mirror CameraMixin pattern across 16 test classes:
- `tests/test_main_window.py`: 14 test classes updated (setUp/tearDown)
- `tests/test_e2e.py`: 1 test class updated
- `tests/test_source_content.py`: 1 test class updated + fixed mock patches to target `ingest_mixin_module`

## Verification

- **Syntax**: All files compile cleanly (`python -m py_compile`)
- **Instantiation**: MainWindow creates successfully with correct MRO: `[MainWindow, QMainWindow, ..., WifiMixin, CameraMixin, ..., IngestMixin]`
- **Test Suite**: 326 passed, 5 skipped, 28 subtests (excluding pre-existing test_wifi_source.py crash and test_mtp.py failure)
- **Behavior**: All ingestion flows work identically — start_ingest → on_file_started → on_copy_progress → on_file_finished → _on_ingestor_complete → _finalize_ingest → post-ingest actions (format → proxies → report → shutdown)

## Deviations from Plan

### Auto-fixed Issues
1. **Import fixes in ingest_mixin.py**: `QThread` moved to QtCore, `QColor` to QtGui, `translator` imported as module (not name)
2. **ORG_TYPE_MAP duplication**: Added to ingest_mixin.py for module independence (original was module-level in main_window.py)
3. **Test patching for test_source_content.py**: Fixed mock patches to target `ingest_mixin_module.FileSystemWatcher` and `ingest_mixin_module.Ingestor` where they're actually used
4. **Indentation fixes in test_main_window.py**: Fixed missing indentation for `_orig_devices_db` and added `_orig_ingest_mixin_db` to all 14 test classes

### No Behavioral Changes
- Pure refactor — all methods extracted verbatim with Spanish docstrings, single quotes, `tr()` intact
- MRO order ensures IngestMixin can call WifiMixin methods (`_is_inbox_cache_path`, `_remove_ingested_wifi_source`, `_clear_wifi_cache`) and CameraMixin methods (`_on_camera_rename_needed`, `_post_ingest_rename_dialog`)

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: duplicate_constant | app/ui/mixins/ingest_mixin.py | ORG_TYPE_MAP duplicated from main_window.py for module independence |

## Known Stubs

None — all functionality fully implemented and tested.