---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ui/mixins/ingest_mixin.py
  - app/ui/main_window.py
  - tests/test_main_window.py
  - tests/test_e2e.py
autonomous: true
requirements: []
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 42500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "IngestMixin class exists at app/ui/mixins/ingest_mixin.py with all ~620 lines of ingestion pipeline and post-ingest methods verbatim from MainWindow"
    - "MainWindow inherits from IngestMixin (QMainWindow, WifiMixin, CameraMixin, IngestMixin) and all ingestion flows work identically"
    - "All existing tests pass with parallel singleton patching for db across mw, camera_mixin, wifi_mixin, ingest_mixin, ingestor_module, metadata_engine modules"
    - "No behavioral changes: start_ingest, stop_ingest, on_file_started, on_copy_progress, on_file_finished, _finalize_ingest, post-ingest actions (format, proxies, report, shutdown) all function identically"
  artifacts:
    - "app/ui/mixins/ingest_mixin.py"
    - "Modified app/ui/main_window.py (removed extracted methods, added IngestMixin to inheritance)"
    - "Updated tests/test_main_window.py (parallel db patch for ingest_mixin_module)"
    - "Updated tests/test_e2e.py (parallel db patch for ingest_mixin_module)"
  key_links:
    - "IngestMixin methods call WifiMixin methods (_is_inbox_cache_path, _remove_ingested_wifi_source, _clear_wifi_cache) — inheritance order must provide both"
    - "IngestMixin methods call CameraMixin methods (_on_camera_rename_needed, _post_ingest_rename_dialog) — inheritance order must provide both"
    - "Test singleton patching must cover all 6 modules: mw, camera_mixin, wifi_mixin, ingest_mixin, ingestor_module, metadata_engine"
    - "Worker functions (_format_sources_worker, _generate_proxies_worker, _reorganize_worker) stay at module level in main_window.py"

---

<objective>
Extract the ingestion pipeline and post-ingest actions from MainWindow into a canonical IngestMixin at app/ui/mixins/ingest_mixin.py. This is Block 8 (last block) of the MainWindow god-object reduction — the most coupled block with ~620 lines across 27 methods. MainWindow will compose IngestMixin alongside existing WifiMixin and CameraMixin.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/ui/mixins/wifi_mixin.py
@app/ui/mixins/camera_mixin.py
@app/ui/mixins/workers.py
@app/ui/main_window.py
@tests/test_main_window.py
@tests/test_e2e.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IngestMixin with all ingestion pipeline + post-ingest methods verbatim</name>
  <files>app/ui/mixins/ingest_mixin.py</files>
  <action>
Create app/ui/mixins/ingest_mixin.py with the IngestMixin class containing all 27 methods extracted verbatim from MainWindow. The mixin follows the canonical pattern: no __init__, no super().__init__(), Spanish docstrings/comments, single quotes, tr() for all user-facing strings.

Methods to extract (exact line ranges from current main_window.py):
1. start_ingest (1117-1294)
2. stop_ingest (1296-1322)
3. prepare_for_update (1324-1337)
4. on_file_started (1339-1385)
5. _file_row_key staticmethod (1387-1389)
6. on_copy_progress (1391-1400)
7. on_file_finished (1402-1455)
8. _on_ingestor_complete (1457-1461)
9. _finalize_ingest (1464-1535)
10. _run_next_post_ingest_action (1537-1560)
11. _is_managed_source_path (1562-1573)
12. _is_managed_session (1575-1581)
13. _format_candidate_paths (1583-1589)
14. _update_format_sources_state (1591-1599)
15. _format_sources_after_ingest (1601-1639)
16. _on_format_finished (1641-1659)
17. _shutdown_computer (1661-1679)
18. _generate_report_after_ingest (1681-1698)
19. update_status_from_watcher (1700-1701)
20. _run_background (1703-1715)
21. _cleanup_background (1717-1718)
22. _show_table_context_menu (1721-1732)
23. _clear_completed_rows (1734-1745)
24. _current_source_path (1747-1755)
25. _export_integrity_report (1757-1782)
26. _export_card_content_report (1784-1806)
27. _proxy_resolution_height (3710-3711)
28. _generate_proxies_after_ingest (3713-3736)
29. _on_proxies_finished (3738-3743)

Imports needed (copy from MainWindow imports used by these methods):
- os, sys, json, time
- PySide6.QtWidgets: QMessageBox, QFileDialog, QThread, QColor, QTableWidgetItem, QHeaderView, QDialog, QDialogButtonBox
- PySide6.QtCore: Qt, QTimer
- app.core: db, WIFI_DEVICE_ID (for _is_managed_session)
- app.core.ingestor: Ingestor, DumpTarget, generate_integrity_report, generate_card_content_report
- app.core.watcher: FileSystemWatcher
- app.core.metadata_engine: metadata_engine, _is_system_entry
- app.core.notifications: NotificationManager
- app.core.ffmpeg_utils: ffmpeg
- app.core.sd_reader: sd_reader
- app.core.utils: is_removable_drive, resource_path
- app.core.translator: translator, QtString
- app.ui: theme, icons
- app.core.shoot_inbox as inboxmod (for _is_inbox_cache_path, _remove_ingested_wifi_source, _clear_wifi_cache)
- app.ui.mixins.workers: _TaskWorker
- from app.ui.selective_dump import content_summary (for _update_session_dump_switch usage in _export_card_content_report? no - that's in CameraMixin)

Note: _show_table_context_menu calls self._current_source_path() and uses _export_integrity_report, _export_card_content_report — all stay in IngestMixin.
Note: on_file_finished calls self._is_inbox_cache_path() and self._remove_ingested_wifi_source() — these are in WifiMixin, so inheritance order must be (WifiMixin, CameraMixin, IngestMixin) or similar.
Note: _finalize_ingest calls self._post_ingest_rename_dialog() — this is in CameraMixin.
Note: Worker functions (_format_sources_worker, _generate_proxies_worker, _reorganize_worker, _probe_device_connectivity) REMAIN at module level in main_window.py — they are not methods.
  </action>
  <verify>
    <automated>python -c "import app.ui.mixins.ingest_mixin; print('Import OK')" && python -m py_compile app/ui/mixins/ingest_mixin.py && echo "Syntax OK"</automated>
  </verify>
  <done>IngestMixin class created at app/ui/mixins/ingest_mixin.py with all 29 methods verbatim, correct imports, Spanish docstrings, single quotes, tr() intact</done>
</task>

<task type="auto">
  <name>Task 2: Update MainWindow to inherit IngestMixin and remove extracted methods</name>
  <files>app/ui/main_window.py</files>
  <action>
Modify app/ui/main_window.py:
1. Add import: `from app.ui.mixins.ingest_mixin import IngestMixin`
2. Change class declaration from `class MainWindow(QMainWindow, WifiMixin, CameraMixin):` to `class MainWindow(QMainWindow, WifiMixin, CameraMixin, IngestMixin):` — order matters: WifiMixin first (provides _is_inbox_cache_path, _remove_ingested_wifi_source, _clear_wifi_cache, _wifi_ingestors, _wifi_server, _wifi_panel), then CameraMixin (provides _on_camera_rename_needed, _post_ingest_rename_dialog, _detect_camera_for_session, _persist_camera_mapping, _camera_for_path), then IngestMixin.
3. Delete all 29 extracted methods from MainWindow (lines as identified in Task 1). Keep worker functions at module level: _format_sources_worker, _generate_proxies_worker, _reorganize_worker, _probe_device_connectivity.
4. Keep ORG_TYPE_MAP at module level (used by start_ingest via self reference in IngestMixin).
5. Verify no references to deleted methods remain in MainWindow (grep for method names).
6. Ensure MainWindow.__init__ still initializes all attributes used by IngestMixin: self._ingestors, self._ingest_completed, self.watchers, self._file_row_map, self._processed_count, self._total_files, self._unknown_cameras, self._ingested_videos, self._background_tasks, self._source_paths, self.current_project_id, self.dest_root, etc. These are already in __init__.
  </action>
  <verify>
    <automated>python -m py_compile app/ui/main_window.py && echo "Syntax OK" && python -c "import app.ui.main_window; w = app.ui.main_window.MainWindow(); print('Inheritance:', [c.__name__ for c in app.ui.main_window.MainWindow.__mro__])" 2>&1 | head -20</automated>
  </verify>
  <done>MainWindow inherits IngestMixin, all 29 methods removed, worker functions preserved, instantiation works</done>
</task>

<task type="auto">
  <name>Task 3: Update tests with parallel singleton patching for ingest_mixin_module.db</name>
  <files>tests/test_main_window.py, tests/test_e2e.py</files>
  <action>
Update test files to patch the new ingest_mixin_module.db singleton alongside existing patches:

In tests/test_main_window.py:
1. Add import: `import app.ui.mixins.ingest_mixin as ingest_mixin_module`
2. In every setUp() that patches db (TestCameraDetectionToken, TestMetadataUnverifiedMarker, TestIngestTableTerminology, TestCameraPersistence, TestDevicePersistenceAcrossProjects, TestDisconnectedDeviceStatus, TestForcePromptI14, TestRenameDialogPersistence, TestIntegrityReport, TestCardContentReport, TestSessionCRUD, TestProjectWizard):
   - Save original: `self._orig_ingest_mixin_db = ingest_mixin_module.db`
   - Patch: `ingest_mixin_module.db = self.db`
   - Restore in tearDown: `ingest_mixin_module.db = self._orig_ingest_mixin_db`

In tests/test_e2e.py:
1. Add import: `import app.ui.mixins.ingest_mixin as ingest_mixin_module`
2. In setUp():
   - Save original: `self._orig_ingest_mixin_db = ingest_mixin_module.db`
   - Patch: `ingest_mixin_module.db = self.db`
   - Restore in tearDown: `ingest_mixin_module.db = self._orig_ingest_mixin_db`

The pattern matches existing patches for mw.db, camera_mixin_module.db, wifi_mixin_module.db (if any), ingestor_module.db, me_module.db.
  </action>
  <verify>
    <automated>python -m pytest tests/test_main_window.py -x -v 2>&1 | tail -30</automated>
    <automated>python -m pytest tests/test_e2e.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>All tests in test_main_window.py and test_e2e.py pass with parallel db patching for ingest_mixin_module</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MainWindow ↔ IngestMixin | Internal composition — same process, trusted |
| IngestMixin → Ingestor | Core ingestion engine — trusted internal module |
| IngestMixin → DatabaseManager | Persistence layer — trusted internal module |
| IngestMixin → ffmpeg/ffprobe | External CLI subprocess — untrusted input (media files) |
| IngestMixin → format_drive | Windows format command — destructive OS operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QUICK-01 | Tampering | _format_sources_after_ingest | high | mitigate | Confirmation dialog with explicit list of drives to format; only runs on Windows; quick format default |
| T-QUICK-02 | Tampering | _shutdown_computer | medium | mitigate | Confirmation dialog before shutdown; 10s delay on Windows |
| T-QUICK-03 | Denial of Service | start_ingest / stop_ingest | low | mitigate | Stop event + watcher cleanup prevents resource leaks; _reset_ingestors called on project switch |
| T-QUICK-04 | Information Disclosure | _generate_report_after_ingest | low | accept | CSV contains file paths/hashes — user-initiated, local file only |
| T-QUICK-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new dependencies added in this refactor |
</threat_model>

<verification>
- All existing tests pass (test_main_window.py, test_e2e.py, full suite)
- MainWindow instantiates without error with new inheritance chain
- Ingestion flow works: start_ingest → on_file_started → on_copy_progress → on_file_finished → _on_ingestor_complete → _finalize_ingest → post-ingest actions
- Post-ingest actions execute in order: format → proxies → report → shutdown
- WiFi cache clearing works (_clear_wifi_cache called from _finalize_ingest via WifiMixin)
- Camera rename dialog works (_post_ingest_rename_dialog called from CameraMixin)
- Table context menu works (_show_table_context_menu, _clear_completed_rows, _export_integrity_report, _export_card_content_report)
</verification>

<success_criteria>
- app/ui/mixins/ingest_mixin.py exists with IngestMixin class (29 methods)
- MainWindow inherits (QMainWindow, WifiMixin, CameraMixin, IngestMixin)
- All 29 methods removed from MainWindow
- Worker functions remain at module level in main_window.py
- tests/test_main_window.py passes (all 16 test classes)
- tests/test_e2e.py passes (all 3 test methods)
- Full test suite passes (python -m pytest tests/ -x)
- No behavioral changes in ingestion or post-ingest flows
</success_criteria>

<output>
Create .planning/quick/260906-ingest-mixin-extraction/260906-01-SUMMARY.md when done
</output>