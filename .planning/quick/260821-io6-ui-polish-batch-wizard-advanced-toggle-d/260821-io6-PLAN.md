# Plan — Lote de pulido UI (9 cambios)

Quick task: `260821-io6` · Fecha: 2026-08-21
Decisión del operador: los botones verdes pasan a **dependientes del acento** (no azul fijo).

## Cambios

### C1 — Wizard: «Opciones avanzadas» colapsable
`app/ui/project_wizard.py`
- Envolver `config_row` (Duración/Organización) y `config_row2` (Detección/Proxies) en un widget contenedor con su propio layout.
- Añadir `QCheckBox` `chk_advanced` con `self.tr("Opciones avanzadas")`; `toggled` → `advanced_widget.setVisible(checked)`. Colapsado por defecto.
- Reducir `setMinimumHeight(620)` → ~380 (el diálogo encoge al estar colapsado).
- Impacto tests: ninguno (`test_main_window.py:588-594` accede programáticamente; funciona con widgets ocultos).

### C2 — WiFi: eliminar ventana intermedia, QR directo
`app/ui/main_window.py` `_pick_wifi_source()` (~3501)
- Eliminar import local de `WifiMethodDialog` y su `exec()`; llamar directamente `self._open_wifi_panel(force_new_sender=True)`.
- FTP sigue accesible por su propio botón en el lanzador de orígenes.
- Borrar `app/ui/wifi_picker.py` (queda huérfano) y quitarlo de `tools/update_translations.ps1`.
- Tests: reescribir `test_pick_wifi_source_pairdrop_configures_new_qr` (sin mock de diálogo, assertion directa sobre `_open_wifi_panel`); eliminar `test_pick_wifi_source_ftp_opens_ftp_picker` (flujo obsoleto).

### C3 — Orden de botones del lanzador de orígenes
`app/ui/source_picker.py` (~89-95)
- Reordenar a: **Examinar… | USB/MTP | WiFi QR | FTP**.
- Impacto tests: ninguno (`test_source_picker.py:212-215` itera por nombre).

### C4 — Barra de progreso sin texto hasta finalizar
`app/ui/main_window.py`
- `setup_views` (~666): `setFormat("")` en vez de `tr("%v / %m archivos")`.
- En `_finalize_ingest`: establecer `setFormat(self.tr("%v / %m archivos"))` (total conocido al terminar).
- Reset en `start_ingest` vuelve a formato vacío.

### C5 — Combo de sesiones estirable con el splitter
`app/ui/main_window.py` (~493)
- Quitar `setMaximumWidth(200)`; añadir `setSizePolicy(Expanding, Preferred)`.

### C6 — Icono de configuración: llave inglesa
- Nuevo `app/ui/assets/icons/wrench.svg` (SVG con placeholder `#FF00FF`, mismo patrón que gear.svg).
- `main_window.py` (~357): registro de `btn_show_metadata` pasa de `"gear"` a `"wrench"`.
- `tests/test_icons.py`: añadir `"wrench"` a la lista de iconos.

### C7 — Anchos por defecto de la tabla de orígenes
`app/ui/main_window.py` (~444-455)
- Col0 Ruta → `Stretch`; col1 Cámara → Interactive ~70; col2 Opciones → Interactive ~110.
- Mantener `setStretchLastSection(False)`; `closeEvent` intacto (`sourceListWidths` nunca se lee).

### C8 — Flechas de combo visibles como triángulos
`app/ui/theme.py` (~232-238)
- `QComboBox::down-arrow`: añadir `width: 0; height: 0;` (como ya hacen los spinbox) para que se pinte el triángulo border-trick y no un rectángulo.

### C9 — Botones primarios dependientes del acento
`app/ui/theme.py`
- Nuevo helper de mezcla de color + derivación de `primary_bg/primary_hover/primary_pressed` desde el acento efectivo (si acento = Neutro → usar `accent_selection`).
  - Oscuro: bg = mezcla(acento, negro, 30%), hover = acento, pressed = mezcla 55%.
  - Claro: bg = acento, hover = mezcla 18%, pressed = mezcla 38%.
- Sustituir `@success_bg/@success_hover` por las nuevas claves en: `QPushButton#PrimaryAction` (añadiendo regla `:pressed`), `QProgressBar::chunk`, `QCheckBox::indicator:checked`.
- Los verdes semánticos (texto de estado éxito/peligro/aviso) se conservan.

## Ejecución (4 commits atómicos)
1. `theme.py` — C8 + C9.
2. `project_wizard.py` — C1.
3. Flujo WiFi — C2 + C3 + borrado módulo + tests.
4. `main_window.py` + assets — C4 + C5 + C6 + C7 + wrench.svg + test_icons.

## Verificación
- Suite completa offscreen: `python -m unittest discover -s tests` (esperado ~215 tests OK).
- Revisión visual manual por el operador (tema oscuro/claro + cada acento).
