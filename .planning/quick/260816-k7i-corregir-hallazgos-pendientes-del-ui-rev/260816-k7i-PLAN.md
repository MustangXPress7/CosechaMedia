---
phase: 260816-k7i-corregir-hallazgos-pendientes-del-ui-rev
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ui/main_window.py
  - app/ui/source_picker.py
  - tests/test_wifi_source.py
  - tests/test_source_picker.py
autonomous: true
requirements: [UI-04, UI-05]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Crear proyecto pasa por ProjectWizard (600×520 con nombre/descripción/destino/duración/organización/fecha-metadatos); al confirmar se crea el proyecto + sesión inicial y el dashboard lo selecciona y habilita los botones de proyecto (fix 2)."
    - "El diálogo de borrar sesión declara la consecuencia real (registros de ingesta; los archivos en disco se conservan) y el apagado pregunta con default No (fix 3)."
    - "«Volcado selectivo…» vive en Acciones post-ingesta → Operaciones y sigue abriendo el asistente (item 3)."
    - "No existen botones ocultos ni su lógica: `btn_browse_source`, `btn_receive_wifi`, `select_source_path` y `_update_wifi_button_state` desaparecen de main_window.py; el WiFi sigue entrando por Añadir origen… → WiFi QR → cadena WifiMethodDialog (item 4)."
    - "El menú Ingesta conserva solo entradas no duplicadas (destino, toggle Auto-detectar, info tarjeta SD, destinos de volcado, carpeta datos); la gestión de dispositivos guardados se ejerce desde el diálogo Añadir origen (sección Desconectados + menú contextual «Eliminar guardado…») sin perder la capacidad de borrado de dispositivos (item 5)."
    - "test_source_picker, test_wifi_source, test_source_content y test_e2e pasan en Qt offscreen."
  artifacts:
    - app/ui/main_window.py
    - app/ui/source_picker.py
    - tests/test_wifi_source.py
    - tests/test_source_picker.py
  key_links:
    - "_show_create_project → ProjectWizard (on_finished/on_cancel callbacks) → _on_project_wizard_finished: crea sesión inicial, refresca combo y habilita botones de proyecto."
    - "_delete_current_session (copy) ↔ db.delete_session (solo borra registros de BD; los archivos en disco se conservan)."
    - "btn_add_source → _pick_source_entry → SourcePickerDialog → kind 'wifi' → _apply_source_choice → _pick_wifi_source: cadena WiFi intacta tras quitar el botón zombie."
    - "SourcePickerDialog _missing_item role ('device', id) ↔ on_delete → _delete_saved_source kind 'device' → db.delete_device (+ delete_ftp_profile si ftp:): gestión de guardados migrada del menú al diálogo unificado."
    - "build_menu: acciones retiradas (act_pick_source, act_detect_now, act_pick_device, act_devices) sin referencias colgantes en _stage_device_in_background/_on_stage_done."
---

<objective>
Corregir los hallazgos pendientes del UI-REVIEW de la fase 01 en `main_window.py` y su superficie asociada: reactivar el flujo de creación de proyecto a través de `ProjectWizard` (hoy huérfano), corregir las confirmaciones destructivas engañosas, reubicar `btn_selective_dump` al grupo post-ingesta, eliminar los botones zombie ocultos y limpiar el menú Ingesta/Configuración de entradas duplicadas migrando la gestión de dispositivos guardados al diálogo unificado.

**Purpose:** La auditoría UI-REVIEW (2026-08-16) marcó hallazgos pendientes que degradan la experiencia del operador: un flujo de creación degradado a 2 QInputDialog con `ProjectWizard` muerto, una confirmación de borrado con consecuencia falsa, un apagado con default Yes (riesgo real en rodaje: un Enter apaga el equipo), un botón de volcado selectivo fuera de su grupo de contexto, dos botones ocultos con lógica viva (código zombie que confunde refactorizaciones) y un menú con entradas que duplican controles del dashboard. Este plan implementa los fixes 2-3 y los items 3-5 acordados, respetando D-03 («no esconder nada» — toda capacidad retirada del menú se migra al diálogo unificado), la regla de no refactor de `app/core/` y las convenciones i18n (`tr()`).

**Output:**
- `app/ui/main_window.py` modificado: wizard rewire, confirmaciones corregidas, botón movido, zombies eliminados, menú limpio.
- `app/ui/source_picker.py`: ítems «Desconectados» con rol de borrado (gestión de guardados migrada).
- `tests/test_wifi_source.py` y `tests/test_source_picker.py` migrados + tests nuevos.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
# Fuente de decisiones
@C:/Users/JoanRamon/Documents/CosechaMedia/.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-CONTEXT.md (D-03 «no esconder nada» — toda capacidad migrada debe seguir accesible; D-09 patrón de confirmaciones destructivas con default No; D-12 — los guardados viven en el diálogo unificado «Añadir origen»)
@C:/Users/JoanRamon/Documents/CosechaMedia/.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-UI-REVIEW.md (Fix 2: ProjectWizard huérfano; Fix 3: copy falsa + default Yes en apagado; hallazgo 4: botones zombie btn_browse_source/btn_receive_wifi)
@C:/Users/JoanRamon/Documents/CosechaMedia/.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-UI-SPEC.md (§185 ProjectWizard como referencia; §186/198 «Volcado selectivo…» en Operaciones; §131/174 D-12 unificación de orígenes)

# Código relevante (anclas verificadas contra el archivo actual)
@C:/Users/JoanRamon/Documents/CosechaMedia/app/ui/main_window.py — `_show_create_project` :1281-1314 (2 QInputDialog → wizard); `_delete_current_session` :2595-2612 (copy falsa :2601); `_shutdown_computer` :1940-1958 (default Yes :1944); `btn_browse_source` :405-407 (oculto), `btn_receive_wifi` :414-418 (oculto), `btn_selective_dump` :460-463 (fila de escaneo); grupo post-ingesta/Operaciones :602-658 (op_row :641-654); `_update_wifi_button_state` :3152-3155 (8 call sites: :1205, :1249, :2483, :2490, :2508, :2520, :2524, :3577); `select_source_path` :2843-2846 (callers: :406 botón zombie, :2696 menú); `_manage_devices` :985-1039 (caller único: :2754 menú); `_pick_wifi_source` :3141-3150 (SE CONSERVA — la llama `_apply_source_choice` :2877 con kind "wifi"); `_delete_saved_source` :2911-2950 (sin rama "device"); `_disconnected_devices` :2952-2966 (solo MTP); `_stage_device_in_background` :3552-3570 (:3565 act_pick_device.setEnabled(False)), `_on_stage_done` :3575+ (:3576 setEnabled(True)); `build_menu` :2666-2825 (Ingesta :2692-2735, Configuración :2737-2755)
@C:/Users/JoanRamon/Documents/CosechaMedia/app/ui/project_wizard.py — `ProjectWizard(QWidget)` :10, firma `__init__(on_finished_callback, on_cancel_callback=None)` :14, `finish_wizard` :125-171 (INSERT name+root_path, UPDATE description/duration_type/organization_type/use_metadata_date, valida nombre+destino no vacíos, llama on_finished_callback(project_id))
@C:/Users/JoanRamon/Documents/CosechaMedia/app/ui/source_picker.py — `_missing_item` :187-193 (flags ItemIsEnabled, SIN UserRole → no borrable), `_update_ok_state` :113-116, `_accept_current` :118-123, `_show_item_menu` :197-207, `_delete_selected` :209-214
@C:/Users/JoanRamon/Documents/CosechaMedia/app/core/db.py — `delete_session` :544-550 (solo registros de BD), `delete_device` :608-625 (borra sesiones + filas de files), `get_devices` :552-569
@C:/Users/JoanRamon/Documents/CosechaMedia/tests/test_wifi_source.py — setUp con MainWindow + DB tmp + mocks de `_ensure_wifi_server`/`_show_wifi_panel`/`_prompt_wifi_sender` :31-80; tests a migrar `test_wifi_button_enabled_with_project`/`test_wifi_button_disabled_without_project` :94-101; docstring :7; patrón existente: muchos tests llaman `_open_wifi_panel()` :129,155,199,257...
@C:/Users/JoanRamon/Documents/CosechaMedia/tests/test_source_picker.py — helper `_missing_item` :71-76 (asume ítem sin UserRole), `test_missing_item_does_not_enable_ok` :125-136, `test_missing_section_lists_devices` :254-268, `test_ftp_profiles_not_duplicated_in_guardados` :92-98 (comentario menciona «Configuración → Dispositivos guardados»)
@C:/Users/JoanRamon/Documents/CosechaMedia/tests/test_source_content.py — :121-125 `test_selective_dump_button_present` (hasattr btn_selective_dump + click → `_open_selective_dump`): sigue válido, el botón solo cambia de layout, NO se renombra

# Restricciones verificadas
- `QInputDialog` sigue en uso en `_edit_project_description` :1272 — NO eliminar su import.
- `_pick_wifi_source` NO es código muerto: lo llama `_apply_source_choice` (kind "wifi") y los tests :736-753 — conservar intacto.
- El menú contextual de `table` (`_show_table_context_menu` :1985-2000) NO tiene entrada de volcado selectivo: el botón es la única entrada; el movimiento a Operaciones no duplica ni pierde nada (UI-SPEC §198 exige la entrada en Operaciones; el menú contextual se queda como está).
- `_disconnected_devices` :2952-2966 excluye `ftp:` y `wifi:`; al incluir `ftp:` (ver Task 3) la sección Desconectados muestra también perfiles FTP con sesiones — coherente con D-12 y necesario para no perder el borrado de perfiles FTP que hoy vive solo en `_manage_devices`.
- Los tests actuales NO referencian act_pick_source/act_detect_now/act_pick_device/act_devices/select_source_path/btn_browse_source/_manage_devices — solo test_wifi_source (docstring :7 y tests :94-101) toca el código retirado.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reactivar ProjectWizard en creación de proyecto y corregir confirmaciones destructivas (fix 2 y fix 3 del UI-REVIEW)</name>
  <files>app/ui/main_window.py</files>
  <action>En `app/ui/main_window.py` (anclas verificadas: `_show_create_project` :1281-1314, `_delete_current_session` :2595-2612, `_shutdown_computer` :1940-1958):

  FIX 2 — Rewire de `_show_create_project` a `ProjectWizard` (per UI-REVIEW fix 2, D-09, UI-SPEC §185):
  1. En `_show_create_project` (:1281), reemplazar los dos QInputDialog por:
     - Import local: `from app.ui.project_wizard import ProjectWizard` (patrón de import local del archivo; NO eliminar el import `QInputDialog` — sigue usado en `_edit_project_description` :1272).
     - `wizard = ProjectWizard(self._on_project_wizard_finished, on_cancel_callback=lambda: self._close_project_wizard(wizard))`
     - `self._project_wizard = wizard` (referencia persistente — anti-GC; el wizard es `QWidget`, no QDialog).
     - `wizard.setWindowModality(Qt.ApplicationModal)` y `wizard.show()` — NO usar `exec()` (QWidget no es modal por exec; `show()` con WindowModality es el patrón correcto).
  2. Eliminar de `_show_create_project` la creación del proyecto/sesión con `db.create_project` y `db.create_session` que hoy cuelga de los QInputDialog (:1286-1312) — esa lógica pasa al callback.
  3. Añadir el nuevo método `_on_project_wizard_finished(self, project_id)` que replica el final del flujo actual (:1302-1312) usando el `project_id` que devuelve el wizard:
     - Nombre del proyecto: consultar con `db.conn` y row_factory dict (patrón :1171-1174): `SELECT name FROM projects WHERE id = ?` — el ID es el INSERT hecho por `finish_wizard`, válido por construcción (spoofing mitigado: el id solo puede provenir del INSERT propio del wizard).
     - Crear sesión inicial: `db.create_session(project_id=project_id, name="Sesión 1 - " + name, root_path=...destino del proyecto...)` — replicar lo que hoy hace :1302 (mismo nombre/formato de sesión inicial).
     - `self.load_existing_projects()`; buscar con `self.cmb_project.findData(project_id)` y `setCurrentIndex` (el combo usa `findData` con el id, patrón :1162-1185).
     - Habilitar `btn_delete_project`, `btn_rename_project`, `btn_duplicate_project` (hoy :1310-1311).
     - `self.ingest_status_label.setText(translator.tr("Proyecto #%1 creado con sesión inicial.").arg(project_id))` (strings vía `tr()`, patrón del archivo).
     - `self.update_start_button_state()`.
     - Cerrar y limpiar: `wizard.close()`, `wizard.deleteLater()`, `self._project_wizard = None` (en el callback o método helper `_close_project_wizard(self, wizard)` usado también por on_cancel, que solo hace close + deleteLater + None).
  4. Mantener el nombre `_show_create_project` (lo referencian el action-map :299 y `act_new` :2673) y su arranque actual (crear proyecto con `root_path` por defecto si no existe y lanzar el wizard).

  FIX 3a — Copy veraz del borrado de sesión (en `_delete_current_session`, hoy :2601-2602):
  - Reemplazar el texto del QMessageBox.question por: `translator.tr("¿Eliminar la sesión #%1 y sus registros de ingesta?\nLos archivos en disco se conservan.\nEsta acción no se puede deshacer.").arg(session_id)` — el copy debe reflejar que `db.delete_session` :544-550 solo borra filas de BD (registros), nunca archivos en disco.
  - Mantener el título (aprox. "Eliminar sesión") y el par de botones actuales.

  FIX 3b — Apagado con default No (en `_shutdown_computer`, hoy :1944):
  - `QMessageBox.question(self, ...)` con botones `QMessageBox.Yes | QMessageBox.No` y default `QMessageBox.No` (hoy el default es Yes). Verificar que el texto/título siguen existentes y solo cambia el default.

  NO tocar `app/core/` (regla de no refactor del proyecto). Todos los strings nuevos pasan por `translator.tr()`.</action>
  <verify>
    <automated>python -m unittest tests.test_e2e -v; python -c "import io,re; s=io.open(r'app/ui/main_window.py',encoding='utf-8').read(); assert 'from app.ui.project_wizard import ProjectWizard' in s, 'wizard import'; assert 'def _on_project_wizard_finished' in s, 'wizard callback'; assert 'def _close_project_wizard' in s, 'wizard close helper'; assert 'self._project_wizard = wizard' in s, 'wizard ref'; assert 'setWindowModality(Qt.ApplicationModal)' in s; assert 'registros de ingesta' in s and 'todos sus archivos' not in s, 'honest session delete copy'; assert 'QMessageBox.Yes | QMessageBox.No, QMessageBox.No' in s, 'shutdown default No'"</automated>
  </verify>
  <done>El flujo «Crear proyecto» abre ProjectWizard (modal) y al confirmar crea proyecto + sesión inicial, selecciona el proyecto en el combo y habilita los botones de gestión; cancelar cierra el wizard sin efectos. El diálogo de borrar sesión declara «registros de ingesta» (no «todos sus archivos») y el apagado usa default No. test_e2e pasa.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminar botones zombie (btn_browse_source/btn_receive_wifi) y mover btn_selective_dump a Operaciones (hallazgo 4 e item 3)</name>
  <files>app/ui/main_window.py</files>
  <action>En `app/ui/main_window.py` (anclas: `btn_browse_source` :405-407, `btn_receive_wifi` :414-418, `btn_selective_dump` :460-463, fila src_scan_row, grupo Operaciones/op_row :641-654, `_update_wifi_button_state` :3152-3155, `select_source_path` :2843-2846, `_pick_wifi_source` :3141-3150, `_stage_device_in_background` :3565, `_on_stage_done` :3576):

  1. ELIMINAR botones zombie (hallazgo 4 — UI-REVIEW):
     - Quitar `self.btn_browse_source` (def :405-407, oculto, llama `select_source_path`) y `self.btn_receive_wifi` (def :414-418, oculto, llama `_pick_wifi_source`, setEnabled(False) + hide).
     - Quitar TODOS los call sites de `_update_wifi_button_state` (:1205, :1249, :2483, :2490, :2508, :2520, :2524, :3577) y la propia def :3152-3155.
     - Quitar `self.btn_receive_wifi.setEnabled(False)` en `_stage_device_in_background` :3565 y su par `setEnabled(True)` en `_on_stage_done` :3576.
     - Quitar el lambda del click en :407/:418 y las referencias en `_init_ui`/layouts (botón + su línea de layout) si quedan tras quitar las defs.
     - `select_source_path` :2843-2846 NO se elimina aún (la sigue llamando `act_pick_source` del menú; se retira en Task 3 junto al menú).
     - `_pick_wifi_source` :3141-3150 se CONSERVA intacto: es la cadena real del WiFi — `btn_add_source → SourcePickerDialog kind "wifi" → _apply_source_choice :2877 → _pick_wifi_source`. El WiFi sigue accesible por «Añadir origen… → WiFi QR» (D-03: no esconder, no eliminar capacidad).
     - `_open_wifi_panel` (que llamaba btn_receive_wifi.click) se conserva: es el punto de entrada usado por los tests y por `_apply_source_choice` para la vía WiFi.
  2. MOVER `btn_selective_dump` a Operaciones (item 3 — UI-SPEC §186/§198):
     - Quitar de la fila de escaneo (src_scan_row) el bloque del botón (:460-463 con su layout) y añadirlo en el grupo «Acciones post-ingesta → Operaciones»: dentro de `op_row` (:641-654), justo después del botón `btn_clear_completed` (aprox. :651) y ANTES de `op_row.addStretch()` (:653).
     - Conservar EXACTAMENTE el nombre de atributo `self.btn_selective_dump` (lo usan `test_source_content.py` :121-125 `hasattr` + click → `_open_selective_dump`) — solo cambia el layout, no la semántica ni el nombre.
     - Mantener el connect al slot `_open_selective_dump` y el texto actual.
     - El menú contextual de la tabla (`_show_table_context_menu` :1985-2000) se deja como está (no tiene entrada de volcado; el botón en Operaciones es la entrada única del UI-SPEC §198).

  NO tocar `app/core/`. Strings: si algún texto se reutiliza, mantener el `tr()` existente; no añadir strings nuevos salvo que se necesiten (usar los existentes).</action>
  <verify>
    <automated>python -m unittest tests.test_source_content tests.test_e2e -v; python -c "import io; s=io.open(r'app/ui/main_window.py',encoding='utf-8').read(); assert 'btn_receive_wifi' not in s and 'btn_browse_source' not in s and '_update_wifi_button_state' not in s, 'zombie buttons'; assert 'src_scan_row.addWidget(self.btn_selective_dump' not in s, 'removed from scan row'; assert 'op_row.addWidget(self.btn_selective_dump' in s, 'moved to operations'; assert '_pick_wifi_source' in s, 'wifi chain intact'"</automated>
  </verify>
  <done>No quedan `btn_browse_source` ni `btn_receive_wifi` ni `_update_wifi_button_state` en main_window.py; el volcado selectivo aparece en Acciones post-ingesta → Operaciones y `test_source_content.py` (hasattr + click) sigue pasando; la cadena WiFi (Añadir origen → diálogo unificado → `_pick_wifi_source`) queda intacta y test_e2e pasa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Limpiar menú Ingesta/Configuración y migrar la gestión de dispositivos guardados al diálogo unificado (item 5, D-03, D-12)</name>
  <files>app/ui/main_window.py, app/ui/source_picker.py, tests/test_wifi_source.py, tests/test_source_picker.py</files>
  <behavior>
    - Test 1 (test_source_picker): un ítem «Desconectados» con `setData(Qt.UserRole, ("device", dev["id"]))` NO habilita OK en `_update_ok_state` ni se acepta en `_accept_current` (se mantiene la sección informativa, solo cambia el rol). El helper `_missing_item` del módulo de tests migra de «sin UserRole» a `role[0] == "device"`; `test_missing_item_does_not_enable_ok` sigue pasando.
    - Test 2 (test_source_picker / test_wifi_source): `_delete_saved_source("device", dev_id)` con confirmación aprobada borra el dispositivo vía `db.delete_device` y devuelve True; con confirmación rechazada no borra y devuelve False. Para `ftp:<id>` también llama `db.delete_ftp_profile`.
    - Test 3 (test_wifi_source): el gating del WiFi sigue intacto tras quitar el botón zombie: con proyecto activo `_open_wifi_panel()` arranca el servidor/muestra el panel (mocks del setUp existente); sin proyecto no (los tests antiguos :94-101 se reescriben contra `_open_wifi_panel` en vez de `_update_wifi_button_state`).
  </behavior>
  <action>En `app/ui/main_window.py` (anclas: build_menu :2666-2825, `_delete_saved_source` :2911-2950, `_disconnected_devices` :2952-2966, `_manage_devices` :985-1039, `_stage_device_in_background` :3565, `_on_stage_done` :3576, `_pick_source_entry`), `app/ui/source_picker.py` (:113-123, :187-214), y los tests (`test_wifi_source.py` :7 y :94-101; `test_source_picker.py` helper :71-76 y comentario :92-98):

  PASO 1 — Limpiar menú Ingesta (build_menu :2692-2735), quitando SOLO las entradas que duplican controles del dashboard (item 5):
  - Retirar `act_pick_source` (def :2694-2697 + acción del menú) — la selección de origen vive en `btn_add_source`.
  - Retirar `act_detect_now` (def :2715-2717 + acción del menú) — duplica `btn_rescan` del dashboard.
  - Retirar `act_pick_device` (def :2719-2721 + acción del menú) — duplica `btn_scan_device`.
  - CONSERVAR en Ingesta: `act_auto_detect` (toggle QSettings, sin duplicado), `act_detect_sd`, `act_dump_targets`, `act_open_data` y `act_pick_dest`.
  - Retirar en Configuración (:2737-2755): `act_devices` (def :2753-2755 + acción) y su handler `_manage_devices` (def :985-1039, caller único :2754) — la gestión de dispositivos guardados se migra al diálogo unificado (PASO 3), sin perder la capacidad de borrado (D-03).
  - Al retirar `act_pick_device`, eliminar también las dos referencias colgantes: `self.act_pick_device.setEnabled(False)` en `_stage_device_in_background` :3565 y el `setEnabled(True)` en `_on_stage_done` :3576 (el guard del menú ya no existe; la vía diálogo nunca tuvo ese guard).
  - Tras retirar `act_pick_source`, `select_source_path` :2843-2846 queda sin callers → ELIMINAR la def completa (Task 2 dejó la decisión a este paso).

  PASO 2 — Migrar los tests afectados (red → verde antes de tocar producción, ver `<behavior>`):
  - `tests/test_source_picker.py`: migrar helper `_missing_item` :71-76 para que use `role[0] == "device"` (el rol lo setea el código de producción del PASO 3); actualizar el comentario :92-98 («Configuración → Dispositivos guardados» ya no existe — la gestión vive en el diálogo Añadir origen); añadir Test 1 y Test 2 (confirmación con mock de QMessageBox.question).
  - `tests/test_wifi_source.py`: reescribir :94-101 hacia `_open_wifi_panel` (con/sin proyecto, usando los mocks del setUp); actualizar docstring :7; añadir Test 2 (rama device de `_delete_saved_source`, incl. `ftp:` con mock de `db.delete_ftp_profile`) y Test 3 (gating `_open_wifi_panel`).
  - Ejecutar: deben FALLAR los tests nuevos (todavía no hay rol ni rama "device") → commit RED.

  PASO 3 — Implementar la gestión de dispositivos guardados en el diálogo unificado (D-12) para que el verde se alcance:
  - En `app/ui/source_picker.py`:
    - `_missing_item` (:187-193): añadir `item.setData(Qt.UserRole, ("device", dev["id"]))` conservando `flags = Qt.ItemIsEnabled` (sigue sin ser seleccionable/borrable por checkbox; solo porta el rol para `_show_item_menu`/`on_delete`).
    - `_update_ok_state` (:113-116) y `_accept_current` (:118-123): tratar el rol `("device", ...)` como no-accionable — `if role is not None and role[0] == "device": return` (no habilitar OK, no mutar kind/value).
    - `_show_item_menu` (:197-207): el ítem con rol `("device", ...)` muestra «Eliminar guardado…» (usar `translator.tr("Eliminar guardado…")` o el texto que ya exista para borrar) → `_delete_selected`.
  - En `app/ui/main_window.py`:
    - `_delete_saved_source` (:2911-2950): añadir rama `kind == "device"`:
      - Confirmación (patrón D-09, default No): `translator.tr("¿Eliminar este dispositivo guardado y sus sesiones?\nLos archivos en disco se conservan.\nEsta acción no se puede deshacer.")` — coherente con el copy veraz de Task 1 (delete_device :608-625 solo borra filas de BD).
      - Si `value.startswith("ftp:")`: `db.delete_ftp_profile(profile_id)` (extraer id con el patrón de `app/core/ftp.py` `profile_id_from_device_key`, o el helper existente en main_window) antes de `db.delete_device(value)`.
      - `db.delete_device(value)` y return True; si el usuario rechaza, return False sin borrar.
      - El kind "device" llega desde `on_delete` del diálogo (rol del PASO 3) — mantener la firma/firma actual del callback (`on_delete=kind, value` o como esté firmado hoy en `_pick_source_entry`).
    - `_disconnected_devices` (:2952-2966): ampliar para incluir dispositivos `ftp:` (los MTP desconectados hoy; añadir los FTP con sesiones activas/registradas) de modo que la sección «Desconectados» del diálogo unificado muestre también los FTP guardados y se puedan borrar — sustituye la vía `_manage_devices` para perfiles FTP sin perder la capacidad (D-03). Mantener la exclusión de `wifi:` (no tiene borrado de sesión de dispositivo).
    - Añadir los tests nuevos de la rama device (Test 2) si faltan tras la migración.

  PASO 4 — Verificación final y commits:
  - Correr la suite completa de los 4 archivos de test + test_e2e (ver `<verify>`).
  - Git: commit RED (tests), commit GREEN (producción) — convención del repo.

  NO tocar `app/core/` (db.py/ftp.py se consumen, no se modifican). Strings nuevos vía `translator.tr()`. No usar `exec()` ni cambiar la modalidad del diálogo unificado (sigue `QDialog.exec()` como hoy).</action>
  <verify>
    <automated>python -m unittest tests.test_source_picker tests.test_wifi_source tests.test_source_content tests.test_e2e -v; python -c "import io; s=io.open(r'app/ui/main_window.py',encoding='utf-8').read(); assert 'act_pick_source' not in s and 'act_detect_now' not in s and 'act_pick_device' not in s and 'act_devices' not in s and 'select_source_path' not in s and '_manage_devices' not in s, 'menu cleanup'; assert 'def _delete_saved_source' in s and 'kind == \"device\"' in s, 'device branch'; sp=io.open(r'app/ui/source_picker.py',encoding='utf-8').read(); assert '\"device\"' in sp and 'role[0] == \"device\"' in sp, 'picker role'"</automated>
  </verify>
  <done>El menú Ingesta queda sin entradas duplicadas (se conservan las no duplicadas y el toggle auto-detectar); Configuración no tiene «Dispositivos guardados»; el diálogo Añadir origen permite eliminar guardados (MTP y FTP) desde la sección Desconectados/contextual con confirmación veraz y default No; `select_source_path` y `_manage_devices` desaparecen sin referencias colgantes; las 4 suites de tests pasan y el RED/GREEN queda commiteado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Menú → handlers | Las acciones retiradas (act_pick_source/act_detect_now/act_pick_device/act_devices) dejan de existir; los handlers que quedan (act_auto_detect, act_detect_sd, act_dump_targets, act_open_data, act_pick_dest) no cambian de superficie |
| Diálogo Añadir origen → on_delete | El rol `("device", dev_id)` fluye del picker al callback `_delete_saved_source` (kind "device"); un rol corrupto no debe poder borrar fuera de los ids de BD |
| ProjectWizard → _on_project_wizard_finished | El `project_id` del callback proviene del INSERT propio del wizard (no del usuario), validado además con `findData` en el combo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260816-01 | Tampering | `_shutdown_computer` confirmación | medium | mitigate | Default del QMessageBox a `QMessageBox.No` (Task 1 fix 3b): un Enter accidental ya no apaga el equipo durante un rodaje |
| T-260816-02 | Spoofing | `_on_project_wizard_finished(project_id)` | low | mitigate | El id solo puede ser el devuelto por `ProjectWizard.finish_wizard` (INSERT propio); se valida con `cmb_project.findData(project_id)` antes de `setCurrentIndex`; sin input del usuario en el id |
| T-260816-03 | Elevation | `_delete_saved_source` rama "device" | medium | mitigate | La rama solo se alcanza con kind "device" desde el rol del picker; confirmación veraz con default No y `db.delete_device` (nunca borrado de disco); perfil FTP solo vía `db.delete_ftp_profile` cuando `value.startswith("ftp:")` |
| T-260816-04 | DoS | Rol corrupto en `_update_ok_state`/`_accept_current` | low | mitigate | Guard `role is not None and role[0] == "device"` → return: un ítem no-accionable nunca habilita OK ni muta kind/value; el resto de roles siguen el comportamiento actual |
| T-260816-SC | Tampering | pip/npm/cargo installs | low | accept | No se instala ningún paquete en este plan (solo código Python del repo + unittest) — sin superficie de suministro |
</threat_model>

<verification>
1. `python -m unittest tests.test_e2e -v` — arranque del MainWindow sin crashes tras el rewire del wizard y los cambios de layout (Task 1 y 2).
2. `python -m unittest tests.test_source_content -v` — `btn_selective_dump` presente y clickable tras el movimiento a Operaciones (Task 2).
3. `python -m unittest tests.test_source_picker tests.test_wifi_source -v` — migración del helper `_missing_item`, gating de `_open_wifi_panel` sin el botón zombie, y rama "device" de `_delete_saved_source` (Task 3).
4. Gates `python -c` con asserts (definidos en cada `<verify>`): ausencia de símbolos retirados (`btn_receive_wifi`, `btn_browse_source`, `_update_wifi_button_state`, `act_pick_source`, `act_detect_now`, `act_pick_device`, `act_devices`, `select_source_path`, `_manage_devices`), presencia de las nuevas (`ProjectWizard` import, `_on_project_wizard_finished`, `_close_project_wizard`, `kind == "device"`, rol `"device"` en source_picker) y defaults (`QMessageBox.No`).
5. Revisión visual opcional: abrir la app y comprobar que la cadena WiFi sigue operativa («Añadir origen… → WiFi QR») y que el volcado selectivo aparece en Acciones post-ingesta → Operaciones.
</verification>

<success_criteria>
- Crear proyecto abre ProjectWizard y el flujo completo (proyecto + sesión inicial + selección + botones habilitados) funciona sin QInputDialog.
- Las confirmaciones destructivas (borrar sesión, apagar) usan copy veraz y default No.
- `btn_selective_dump` está en Acciones post-ingesta → Operaciones y `test_source_content` pasa.
- No queda ningún botón oculto ni su lógica en main_window.py; la cadena WiFi está intacta.
- El menú Ingesta/Configuración no tiene entradas duplicadas ni referencias colgantes; la gestión de guardados (MTP/FTP) vive en el diálogo unificado.
- Las 4 suites de tests pasan en Qt offscreen.
</success_criteria>

<output>
Create `.planning/quick/260816-k7i-corregir-hallazgos-pendientes-del-ui-rev/260816-k7i-SUMMARY.md` when done
</output>
