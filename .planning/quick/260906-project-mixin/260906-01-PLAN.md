---
phase: quick-260906
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ui/mixins/project_mixin.py
  - app/ui/main_window.py
  - tests/test_main_window.py
  - tests/test_wifi_source.py
  - tests/test_source_content.py
  - tests/test_session_content_modes.py
  - tests/test_e2e.py
autonomous: true
requirements: []
user_setup: []
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - ProjectMixin existe en app/ui/mixins/project_mixin.py con 21 métodos project verbatim
    - MainWindow hereda de ProjectMixin (MRO: QMainWindow, WifiMixin, CameraMixin, SourcesMixin, ProjectMixin)
    - Los 21 métodos project ya no existen en main_window.py (eliminados, no comentados)
    - Tests parchean project_mixin_module.db en paralelo a mw.db (18 setUps en 5 ficheros)
    - Suite de tests pasa (393 tests) sin regresiones
  artifacts:
    - app/ui/mixins/project_mixin.py
    - app/ui/main_window.py (modificado)
    - tests/test_main_window.py (parche db)
    - tests/test_wifi_source.py (parche db)
    - tests/test_source_content.py (parche db)
    - tests/test_session_content_modes.py (parche db)
    - tests/test_e2e.py (parche db)
  key_links:
    - project_mixin_module.db patch en todos los setUps de test que usan métodos project
    - MainWindow MRO incluye ProjectMixin tras SourcesMixin
    - _save_project_root llamado desde select_dest_path (línea 3335) resuelve por MRO
    - load_existing_projects llamado desde menú, _on_project_wizard_finished, _duplicate_current_project, delete_current_project, delete_all_projects, _create_default_project

---

<objective>
Extraer el ciclo de proyecto (crear, cerrar, borrar, cambiar root, renombrar, duplicar) de MainWindow a ProjectMixin en app/ui/mixins/project_mixin.py siguiendo el patrón WifiMixin/CameraMixin/SourcesMixin: métodos VERBATIM, composición MRO, singleton db patch en tests.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/ui/mixins/wifi_mixin.py
@app/ui/mixins/camera_mixin.py
@app/ui/mixins/sources_mixin.py
@app/ui/main_window.py
@tests/test_main_window.py
@tests/test_wifi_source.py
@tests/test_source_content.py
@tests/test_session_content_modes.py
@tests/test_e2e.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear ProjectMixin con 21 métodos project VERBATIM</name>
  <files>
    app/ui/mixins/project_mixin.py
  </files>
  <action>
    Crear app/ui/mixins/project_mixin.py copiando VERBATIM los 21 métodos relacionados con el ciclo de vida del proyecto desde main_window.py. Incluir imports necesarios:
    - stdlib: import os, import shutil, import sys, import json
    - from datetime import datetime
    - from PySide6.QtWidgets import QMessageBox, QInputDialog, QDialog
    - from PySide6.QtCore import Qt, QSettings
    - from app.core.db import db, WIFI_DEVICE_ID
    - from app.core.sd_reader import sd_reader
    - from app.core import translator
    - from app.ui import theme
    - from app.ui.about_dialog import AboutDialog

    NO añadir __init__, NO super().__init__(). Métodos a extraer (ordenados como aparecen en main_window.py líneas 776-952 y 3337-3640):

    Bloque UI proyecto / wizard (776-952):
    - load_existing_projects (776-796)
    - on_project_selected (807-835)
    - _load_project (837-881)
    - _set_project_description (883-897)
    - _edit_project_description (899-910)
    - _show_create_project (913-921)
    - _on_project_wizard_finished (923-945)
    - _close_project_wizard (947-952)

    Bloque rename/duplicate (1028-1113):
    - _rename_current_project (1028-1056)
    - _duplicate_current_project (1058-1113)

    Bloque root / archivos completados (3337-3640):
    - _save_project_root (3337-3360)
    - _completed_files_under_root (3372-3386)
    - _handle_completed_files_on_root_change (3388-3419)
    - _move_completed_files (3421-3460)
    - _delete_completed_file_records (3462-3468)
    - _prune_empty_dirs (3470-3478)
    - open_data_folder (3480-3487)
    - delete_current_project (3489-3556)
    - delete_all_projects (3558-3607)
    - _create_default_project (3611-3637)
    - show_about (3639-3640)

    NOTA: _populate_source_paths_from_sessions (línea ~2802) → SessionsMixin (Bloque 2, ya planificado). NO incluirlo aquí.
    NOTA: _reset_mtp_thread_local, _reset_ingestors, _reset_wifi_ingestors se usan en delete_current_project/delete_all_projects pero NO se mueven — pertenecen a otros mixins/futuro DevicesMixin. Se mantienen como llamadas self. y resuelven por MRO.

    Mantener tr() intacto, comillas simples, comentarios en español. self. se queda self. (VERBATIM).
  </action>
  <verify>
    <automated>python -c "from app.ui.mixins.project_mixin import ProjectMixin; print('ProjectMixin import OK'); methods = [m for m in dir(ProjectMixin) if m.startswith('_') and not m.startswith('__') or m in ('load_existing_projects','on_project_selected','open_data_folder','delete_current_project','delete_all_projects','show_about')]; print(f'Métodos: {len(methods)}'); [print(f'  {m}') for m in sorted(methods)]"</automated>
  </verify>
  <done>
    ProjectMixin creado con 21 métodos project, imports correctos, sin __init__
  </done>
</task>

<task type="auto">
  <name>Task 2: Actualizar MainWindow - heredar ProjectMixin y eliminar métodos extraídos</name>
  <files>
    app/ui/main_window.py
  </files>
  <action>
    Editar app/ui/main_window.py:
    1. Añadir import: from app.ui.mixins.project_mixin import ProjectMixin
    2. Cambiar declaración de clase (línea ~128): class MainWindow(QMainWindow, WifiMixin, CameraMixin, SourcesMixin, ProjectMixin):
    3. ELIMINAR (no comentar) los 21 métodos project listados en Task 1 (verificar líneas actuales tras extracciones previas - bloques aproximados: 776-952, 1028-1113, 3337-3640).
    4. Verificar que _populate_source_paths_from_sessions (línea ~2802) NO se elimina - va a SessionsMixin.
    5. Verificar que llamadas a _reset_mtp_thread_local, _reset_ingestors, _reset_wifi_ingestors dentro de delete_current_project/delete_all_projects se mantienen como self. y resuelven por MRO (no tocar).
    6. Ajustar números de línea en comentarios si los hay referenciando métodos eliminados.
    7. Los imports que queden huérfanos tras el movimiento (QDialog, WIFI_DEVICE_ID si ya no se usan en main_window) se revisan: mantener los que sigan en uso en main_window, retirar solo los verdaderamente huérfanos.
  </action>
  <verify>
    <automated>python -c "
import re, sys
src = open('app/ui/main_window.py', encoding='utf-8').read()
names = ['load_existing_projects','on_project_selected','_load_project','_set_project_description','_edit_project_description','_show_create_project','_on_project_wizard_finished','_close_project_wizard','_rename_current_project','_duplicate_current_project','_save_project_root','_completed_files_under_root','_handle_completed_files_on_root_change','_move_completed_files','_delete_completed_file_records','_prune_empty_dirs','open_data_folder','delete_current_project','delete_all_projects','_create_default_project','show_about']
bad = [n for n in names if re.search(r'\n    def '+n+r'\(', src)]
print('DEFS-REMAINING:', bad if bad else 'NONE')
assert not bad, 'method defs still in main_window'
print('COMPOSITION:', 'PASS' if 'class MainWindow(QMainWindow, WifiMixin, CameraMixin, SourcesMixin, ProjectMixin):' in src else 'FAIL')
assert 'class MainWindow(QMainWindow, WifiMixin, CameraMixin, SourcesMixin, ProjectMixin):' in src
print('MRO-IMPORT:', 'PASS' if 'from app.ui.mixins.project_mixin import ProjectMixin' in src else 'FAIL')
assert 'from app.ui.mixins.project_mixin import ProjectMixin' in src
# todos los usos restantes deben ser self.<name> o .connect(self.<name>)
for n in names:
    for m in re.finditer(r'\b'+n+r'\b', src):
        ctx = src[max(0,m.start()-80):m.start()]
        assert ('self.' in ctx) or ('.connect(self.' in ctx), (n, ctx[-35:])
print('CALLSITES-MRO:', 'PASS')
"; if ($?) { python -m compileall -q app/ui/main_window.py; if ($?) { python -X utf8 -c "import os; os.environ.setdefault('QT_QPA_PLATFORM','offscreen'); import app.ui.main_window; print('IMPORT-MW: PASS')" } }
  </automated>
  </verify>
  <done>
    MainWindow hereda ProjectMixin, 21 métodos eliminados, MRO correcto, import añadido
  </done>
</task>

<task type="auto">
  <name>Task 3: Parchear project_mixin_module.db en 18 setUps de 5 ficheros test</name>
  <files>
    tests/test_main_window.py
    tests/test_wifi_source.py
    tests/test_source_content.py
    tests/test_session_content_modes.py
    tests/test_e2e.py
  </files>
  <action>
    En cada test file que tenga `mw.db = self.db` en setUp, añadir paralela el parche del módulo project_mixin:
    - Importar: import app.ui.mixins.project_mixin as project_mixin_module
    - En setUp: self._orig_project_db = project_mixin_module.db; project_mixin_module.db = self.db
    - En tearDown: project_mixin_module.db = self._orig_project_db

    Ficheros y setUps a actualizar (basado en grep actual):
    1. tests/test_main_window.py: 14 setUps (líneas 45, 125, 234, 297, 390, 493, 623, 778, 950, 1079, 1186, 1245, 1293, 1392 aprox)
    2. tests/test_wifi_source.py: 1 setUp (línea 46)
    3. tests/test_source_content.py: 1 setUp (línea 49)
    4. tests/test_session_content_modes.py: 1 setUp (línea 157) - SOLO el que tiene mw.db = self.db
    5. tests/test_e2e.py: 1 setUp (línea 51)
    Total: 18 setUps.

    Patrón a seguir (igual que wifi_mixin_module.db, camera_mixin_module.db, sources_mixin_module.db):
    setUp:
        self._orig_project_db = project_mixin_module.db
        project_mixin_module.db = self.db
    tearDown:
        project_mixin_module.db = self._orig_project_db

    NO tocar tests que no parcheen mw.db (no usan métodos project directamente).
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    18 setUps parchean project_mixin_module.db, suite completa pasa (393 tests)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI ↔ Core DB | MainWindow/ProjectMixin llama a db singleton para CRUD proyectos/sesiones |
| UI ↔ External Dialogs | _show_create_project abre ProjectWizard (modal), _edit_project_description usa QInputDialog |
| UI ↔ File System | open_data_folder abre explorador de archivos del SO; _move_completed_files mueve archivos físicos |
| UI ↔ SD Reader | delete_current_project usa sd_reader.get_volume_serial para limpiar sd_cards |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q5-01 | Tampering | delete_current_project / delete_all_projects | high | mitigate | Confirmación QMessageBox.Yes/No antes de borrar proyectos y todos sus datos |
| T-Q5-02 | Tampering | _move_completed_files / _delete_completed_file_records | medium | mitigate | Confirmación QMessageBox con tres opciones (mover/borrar/dejar) antes de actuar |
| T-Q5-03 | Information Disclosure | open_data_folder abre carpeta data/ | low | accept | Carpeta local de la app, usuario es operador local |
| T-Q5-04 | Denial of Service | load_existing_projects reconstruye combo completo | low | accept | Proyectos típicos <50; bloqueo de señales evita flicker |
| T-Q5-SC | Tampering | npm/pip installs | high | mitigate | No nuevos packages en este plan; solo refactor interno |

</threat_model>

<verification>
- [ ] ProjectMixin importable y contiene 21 métodos esperados
- [ ] MainWindow MRO incluye ProjectMixin en posición correcta (tras SourcesMixin)
- [ ] 21 métodos project eliminados de main_window.py (grep confirma 0 matches)
- [ ] 18 setUps en 5 ficheros test parchean project_mixin_module.db
- [ ] pytest tests/ -x pasa completo (393 tests)
- [ ] main_window.py líneas reducidas ~450 (3750 → ~3300)
- [ ] Todas las llamadas self.<método> y .connect(self.<método>) resuelven por MRO
</verification>

<success_criteria>
ProjectMixin extraído verbatim, MainWindow composición MRO funcionando, tests parcheados y pasando, sin cambios de comportamiento.
</success_criteria>

<output>
Create .planning/quick/260906-project-mixin/260906-01-SUMMARY.md when done
</output>