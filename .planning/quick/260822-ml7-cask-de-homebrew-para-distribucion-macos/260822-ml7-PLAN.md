---
phase: 260822-ml7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packaging/homebrew/Casks/cosechamedia.rb
  - docs/HOMEBREW.md
  - README.md
autonomous: true
requirements: []

user_setup:
  - service: GitHub — repositorio externo del tap
    why: "Publicar el cask exige un repo público github.com/MustangXPress7/homebrew-tap; crearlo es un paso MANUAL del propietario fuera de este repositorio (per D-01)"
    dashboard_config:
      - task: "Tras ejecutar este plan: crear el repo homebrew-tap y subir Casks/cosechamedia.rb siguiendo docs/HOMEBREW.md"
        location: "github.com/new"

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'Un usuario de macOS con Apple Silicon puede instalar la app con dos comandos brew documentados, y Homebrew resuelve ffmpeg automáticamente como dependencia declarada del cask.'
    - 'El propietario dispone de una receta completa en español para publicar su propio tap (repo homebrew-tap) y para actualizar versión+SHA-256 en cada release futura.'
    - 'El cask declara con honestidad sus limitaciones actuales: solo arm64 (temporal) y hash sin fijar mientras no exista una release estable que hashear.'
    - 'Ambas mitades del README (EN/ES) ofrecen la instalación por Homebrew de forma simétrica y enlazan a docs/HOMEBREW.md.'
    - 'La aplicación no cambia: cero modificaciones en app/core/ y .github/workflows/.'
  artifacts:
    - 'packaging/homebrew/Casks/cosechamedia.rb — cask DSL válido: token cosechamedia, version 1.5.0.b3, url plantilla con #{version}, name/desc/homepage, depends_on arch :arm64 (temporal comentado), dependencia de la fórmula ffmpeg, app CosechaMedia.app, sha256 :no_check documentado'
    - 'docs/HOMEBREW.md — guía en español: estado/limitaciones, instalación de usuario (--no-quarantine), publicación manual del tap homebrew-tap, checklist por release con fijado de SHA-256, cross-ref a SIGNING.md'
    - 'README.md — subsección «Install via Homebrew / Instalar con Homebrew» espejo dentro de Download (EN) y Descarga (ES)'
  key_links:
    - 'cask ↔ asset real de releases: url https://github.com/MustangXPress7/CosechaMedia/releases/download/v#{version}/CosechaMedia-macos.app.zip (el tag v<versión> debe coincidir EXACTO con app/__init__.py; CI lo valida)'
    - 'cask ↔ fórmula ffmpeg: la dependencia declarada resuelve el requisito ffprobe/ffmpeg-en-PATH documentado en el README sin tocar app/core (per D-02)'
    - 'README Download EN ↔ ES: subsecciones Homebrew espejo; cada mitad ↔ docs/HOMEBREW.md (un enlace por mitad)'
    - 'docs/HOMEBREW.md ↔ docs/SIGNING.md: cross-ref de Gatekeeper/--no-quarantine sin duplicar instrucciones (per D-03)'
---

<objective>
Preparar la distribución de CosechaMedia para macOS vía Homebrew: cask propio (`packaging/homebrew/Casks/cosechamedia.rb`) listo para un tap del propietario, guía completa en español para montar ese tap y mantener el cask por release, y anuncio bilingüe simétrico en el README.

Purpose: Hoy los usuarios de macOS solo pueden instalar descargando el zip a mano y peleándose con Gatekeeper. Un cask en un tap propio da instalación/actualización de dos comandos y resuelve ffmpeg automáticamente. La creación del repo externo del tap es un paso manual del propietario — este plan entrega todos los artefactos in-repo y las instrucciones exactas para ese paso (per D-01).

Decisiones bloqueadas (del orquestador — NO NEGOCIABLES):
- D-01: Entregable = cask distribuido por el TAP PROPIO del propietario (NO homebrew/cask upstream). El repo del tap (p. ej. github.com/MustangXPress7/homebrew-tap) NO existe aún — este plan prepara artefactos in-repo + instrucciones de publicación; crear el repo externo de GitHub es paso manual del usuario. NO automatizarlo.
- D-02: FFmpeg — la app invoca `ffmpeg`/`ffprobe` desnudos confiando en el PATH. El propietario construirá su propia UI de detección/instalación in-app más adelante. Este plan NO toca `app/core` — pero el cask SÍ declara `depends_on formula: "ffmpeg"` para que brew la resuelva.
- D-03: Gatekeeper — la app está firmada ad-hoc, SIN notarizar → la documentación recomienda `brew install --cask --no-quarantine` como camino sin fricción, cruzando referencia con docs/SIGNING.md sin duplicarlo.

Output: packaging/homebrew/Casks/cosechamedia.rb (nuevo), docs/HOMEBREW.md (nuevo), README.md (subsecciones espejo EN/ES).
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@app/__init__.py
@README.md
@docs/SIGNING.md

Estado verificado por el orquestador (no volver a investigar):
- Repo: https://github.com/MustangXPress7/CosechaMedia · versión fuente de verdad `app/__init__.py` = `"1.5.0.b3"`; los tags `v*` deben coincidir EXACTAMENTE (CI valida).
- Asset que produce `.github/workflows/build.yml`: `CosechaMedia-macos.app.zip` vía `ditto -c -k --keepParent dist/CosechaMedia.app` → la raíz del zip contiene EXACTAMENTE `CosechaMedia.app`. Patrón de URL: `https://github.com/MustangXPress7/CosechaMedia/releases/download/v{VERSION}/CosechaMedia-macos.app.zip`.
- IMPORTANTE caveat de arquitectura: CI corre en `macos-latest` (Apple Silicon) → assets actuales SOLO arm64; los nombres de asset no llevan sufijo de arquitectura. El cask honesto se guarda con `depends_on arch: :arm64`, documentado como temporal (multi-arquitectura queda FUERA de alcance hoy).
- La release v1.5.0.b3 NO existe todavía (API de GitHub devuelve 404 al planificar, 2026-08-22): no hay valor sha256 obtenible → decisión del planner: `sha256 :no_check` con comentario en el propio cask documentando cómo fijar el hash por release (`shasum -a 256`), y checklist obligatoria en docs/HOMEBREW.md.
- README bilingüe mitad EN luego mitad ES (anclas `#english` / `#espanol`); `## Download` EN ~línea 39 termina con el párrafo que enlaza SIGNING («...how to open the app on macOS the first time.»); `## Descarga` ES ~línea 161 termina igual («...cómo abrir la app en macOS la primera vez.»). Los bloques de código del README usan vallas simples SIN etiqueta de lenguaje. Todo cambio de README debe ser simétrico EN/ES.
- docs/SIGNING.md existe (español): firma ad-hoc desde CI, workaround de primera apertura para Gatekeeper, sidecar `.sha256` por asset — SOLO cruzar referencia, nunca duplicar.
- No existe aún directorio `packaging/`. Licencia GPL-3.0-or-later.

Restricciones duras: NADA en `app/core/`; NADA en `.github/workflows/build.yml` (multi-arquitectura fuera de alcance); comentarios nuevos en español salvo el `desc` del cask (convención del ecosistema Homebrew = descripciones en inglés).

Nota: tracer-first no aplica aquí — no hay arquitectura runtime que probar (todo son artefactos de empaquetado/docs); cada tarea entrega un artefacto real e independiente.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cask de Homebrew en packaging/homebrew/Casks/cosechamedia.rb</name>
  <precondition>Conectividad a github.com SOLO si se intenta la rama de fijar hash real; si no hay red, proceder directamente con la rama por defecto `sha256 :no_check` (la release no existía al planificar).</precondition>
  <read_first>
    app/__init__.py (versión fuente de verdad). .github/workflows/build.yml — paso «Prepare assets (macOS)»: confirmar nombre de asset `CosechaMedia-macos.app.zip` y el `ditto --keepParent dist/CosechaMedia.app` (solo lectura; NO editar el workflow).
  </read_first>
  <files>packaging/homebrew/Casks/cosechamedia.rb</files>
  <action>
Paso A — Comprobación única opcional (per D-01/D-02 contexto): lanzar una petición HEAD a la URL del asset `https://github.com/MustangXPress7/CosechaMedia/releases/download/v1.5.0.b3/CosechaMedia-macos.app.zip`. Si respondiera 200 (release publicada entre planificar y ejecutar), descargarlo y calcular `Get-FileHash -Algorithm SHA256` para fijar el hash REAL en lugar de `:no_check` (ajustando el comentario acorde). Camino esperado/default: 404 → rama B.

Paso B — Crear los directorios `packaging/homebrew/Casks/` y escribir el archivo con EXACTAMENTE esta estructura de estanzas (orden DSL convencional; sangría de 2 espacios; comentarios tal cual):

cask "cosechamedia" do
  version "1.5.0.b3"

  # Sin hash fijado todavía: la release v1.5.0.b3 aún no está publicada en
  # GitHub Releases (comprobado vía API al crear este cask). En cuanto exista,
  # sustituir la línea de abajo por el hash real del asset:
  #   curl -LO https://github.com/MustangXPress7/CosechaMedia/releases/download/v<VERSIÓN>/CosechaMedia-macos.app.zip
  #   shasum -a 256 CosechaMedia-macos.app.zip
  # Checklist completa por release en docs/HOMEBREW.md.
  sha256 :no_check

  url "https://github.com/MustangXPress7/CosechaMedia/releases/download/v#{version}/CosechaMedia-macos.app.zip"
  name "CosechaMedia"
  desc "Verified video ingest tool for film production"
  homepage "https://github.com/MustangXPress7/CosechaMedia"

  # Temporal: CI genera los assets en macos-latest (Apple Silicon), así que hoy
  # solo hay builds arm64. Retirar esta línea cuando existan builds multi-arquitectura.
  depends_on arch: :arm64

  # La app invoca ffmpeg y ffprobe desde el PATH; la fórmula de Homebrew las deja disponibles ahí.
  depends_on formula: "ffmpeg"

  app "CosechaMedia.app"
end

Por qué así (mantener este razonamiento si hace falta):
- Token `cosechamedia`, minúsculas sin guiones: coincide con el nombre del producto y evita ambigüedad en la ruta de instalación `MustangXPress7/tap/cosechamedia`.
- `url` con plantilla `v#{version}`: reproduce el patrón EXACTO de asset verificado (tag `v` + versión literal de `app/__init__.py`, validado por CI); al subir de versión basta cambiar `version`.
- `app "CosechaMedia.app"`: la raíz del zip contiene exactamente ese bundle (verificado: `ditto --keepParent dist/CosechaMedia.app`); homebrew-cask descomprime zips automáticamente, no hace falta stanza extra.
- `depends_on arch: :arm64` TEMPORAL y comentado como tal (evidencia verificada: CI en macos-latest = Apple Silicon; honestidad > prometer x86_64 que no existe).
- `depends_on formula: "ffmpeg"` per D-02: resuelve ffprobe/ffmpeg en PATH vía brew SIN tocar app/core (la futura detección in-app del propietario convivirá con esto).
- `desc` en inglés (convención del estilo homebrew-cask); GPL no va en el desc (el LICENSE del repo manda).
- Si se tomó la rama A (hash real): sustituir `sha256 :no_check` por `sha256 "<hash>"` y reescribir el comentario para decir que el hash corresponde a la release v1.5.0.b3 publicada.

NADA más en el archivo: sin stanzas livecheck/appcast (el versionado beta `1.5.0.b3` rompería las estrategias automáticas), sin `auto_updates` (la app tiene autoactualizador propio pero el cask debe seguir siendo la fuente de verdad de brew), sin `zap` (innecesario: la app guarda todo en data/ relativo).
  </action>
  <verify>
    <automated>$p = 'packaging/homebrew/Casks/cosechamedia.rb'; $c = Get-Content $p -Raw
"exists: $(Test-Path $p)"
"do-open: $($c.Contains('cask "cosechamedia" do'))"
"version: $($c.Contains('version "1.5.0.b3"'))"
"url-template: $($c.Contains('releases/download/v#{version}/CosechaMedia-macos.app.zip'))"
"name-stanza: $($c.Contains('name "CosechaMedia"'))"
"desc-stanza: $($c.Contains('desc "Verified video ingest tool for film production"'))"
"homepage-stanza: $($c.Contains('homepage "https://github.com/MustangXPress7/CosechaMedia"'))"
"dep-formula: $($null -ne (Select-String -Path $p -SimpleMatch -Pattern 'formula: "ffmpeg"'))"
"dep-arch: $($null -ne (Select-String -Path $p -SimpleMatch -Pattern 'arch: :arm64'))"
"app-stanza: $($c.Contains('app "CosechaMedia.app"'))"
"sha256-honest: $(($c -match 'sha256 :no_check') -or ($c -match 'sha256 "[0-9a-f]{64}"'))"
"closes-end: $($c.TrimEnd().EndsWith('end'))"
if (Get-Command ruby -ErrorAction SilentlyContinue) { ruby -c $p } else { 'ruby-syntax: omitida (sin ruby en esta máquina)' }</automated>
  </verify>
  <done>exists: True · do-open/version/url-template/name/desc/homepage/dep-formula/dep-arch/app-stanza: todos True (la URL usa la plantilla v#{version} y reproduce el patrón de asset verificado) · sha256-honest: True (:no_check con comentario de fijado, o hash real hex64 si la rama A prosperó) · closes-end: True · ruby-syntax: ok u omitida.</done>
</task>

<task type="auto">
  <name>Task 2: docs/HOMEBREW.md — guía de tap propio y checklist por release</name>
  <read_first>packaging/homebrew/Casks/cosechamedia.rb (creada en Task 1) y docs/SIGNING.md completos — la guía cruza referencia con SIGNING y explica sus mismos hechos sin repetirlos.</read_first>
  <files>docs/HOMEBREW.md</files>
  <action>
Crear `docs/HOMEBREW.md` en ESPAÑOL con estas seis secciones (tono sobrio del repo; comandos literales tal cual):

1. Título «Distribución vía Homebrew (macOS)» + intro breve: qué aporta (instalar/actualizar la app con dos comandos brew, ffmpeg resuelto solo) y aviso honesto de estado beta (Apple Silicon únicamente, sin notarización, hash pendiente de fijar).

2. «Estado actual y limitaciones»: tres puntos — (a) SOLO Apple Silicon temporalmente: los assets actuales de CI son arm64 y el cask lo refleja con `depends_on arch: :arm64`; retirar esa línea cuando haya builds multi-arquitectura (fuera de alcance hoy). (b) Sin Developer ID ni notarización: por eso se recomienda `--no-quarantine` (per D-03); detalles técnicos de firma y apertura manual en [SIGNING](SIGNING.md). (c) Hash sin fijar mientras no haya release estable: el cask lleva `sha256 :no_check` + comentario; fijar el SHA-256 real desde la primera release siguiendo la sección de abajo.

3. «Instalación (usuarios)»: bloque de código vallado con los DOS comandos exactos:

brew tap MustangXPress7/tap
brew install --cask --no-quarantine MustangXPress7/tap/cosechamedia

Explicar cada uno: el tap registra github.com/MustangXPress7/homebrew-tap (el formato corto funciona SOLO si el repo se llama exactamente `homebrew-tap`); el install descarga el zip de la release, despliega el bundle y mueve CosechaMedia.app a /Applications; ffmpeg/ffprobe quedan instalados como fórmula si faltaban (dependencia declarada del cask, per D-02). Explicar `--no-quarantine` en una frase: evita que brew marque la app con el atributo de cuarentena que Gatekeeper exigiría desbloquear en la primera apertura, dado que la app no está notarizada — remitir a SIGNING.md para la alternativa manual sin duplicar su comando de Terminal (per D-03). Nota breve: la app incluye autoactualizador propio; en instalaciones vía brew conviene actualizar con `brew upgrade --cask cosechamedia` para que brew no se desincronice.

4. «Publicar el tap (propietario, una sola vez)»: pasos numerados marcados como MANUALES fuera de este repo (per D-01) — crear un repo PÚBLICO llamado exactamente `homebrew-tap` bajo tu cuenta de GitHub (nombre obligatorio para el formato corto del tap); copiar `packaging/homebrew/Casks/cosechamedia.rb` de este repo a `Casks/cosechamedia.rb` en el repo del tap; commit y push; verificar desde cualquier Mac ejecutando los dos comandos de la sección de usuarios.

5. «Actualizar el cask en cada release»: checklist numerada — (1) actualizar `__version__` en `app/__init__.py` y crear el tag `v<versión>` EXACTO (CI valida la coincidencia); (2) esperar a que el workflow publique `CosechaMedia-macos.app.zip` (+ su sidecar `.sha256`); (3) descargar el zip y calcular su SHA-256: `shasum -a 256 CosechaMedia-macos.app.zip` en macOS/Linux, o `Get-FileHash -Algorithm SHA256` / `certutil -hashfile <archivo> SHA256` en Windows; (4) actualizar en el cask AMBAS copias — la de este repo (`packaging/homebrew/Casks/`) y la del repo del tap — los campos `version` y `sha256`, sustituyendo `:no_check` por el hash real entrecomillado; (5) push al tap; (6) comprobar con `brew info --cask MustangXPress7/tap/cosechamedia` que muestra la versión nueva.

6. «Ver también»: enlace único [SIGNING](SIGNING.md) para firma, integridad y primera apertura en macOS.

Sin nada más: sin duplicar el contenido de SIGNING.md (solo enlace), sin instrucciones para homebrew/cask upstream (per D-01: SOLO tap propio).
  </action>
  <verify>
    <automated>$d = 'docs/HOMEBREW.md'
"doc-exists: $(Test-Path $d)"
$h = Get-Content $d -Raw
"tap-repo-name: $($h.Contains('homebrew-tap'))"
"cmd-tap: $($null -ne (Select-String -Path $d -SimpleMatch -Pattern 'brew tap MustangXPress7/tap'))"
"cmd-install: $($h.Contains('--no-quarantine MustangXPress7/tap/cosechamedia'))"
"pin-procedure: $(($h -match 'shasum -a 256') -and ($h -match 'Get-FileHash|certutil'))"
"signing-crossref: $($h.Contains('SIGNING.md'))"
"intel-caveat: $($h -match 'Intel')"
"arm64-guard: $($null -ne (Select-String -Path $d -SimpleMatch -Pattern ':arm64'))"
"version-source: $($h.Contains('__version__'))"
"brew-upgrade-note: $($h.Contains('brew upgrade'))"
"no-xattr-dup: $($h -notmatch 'xattr')"</automated>
  </verify>
  <done>doc-exists: True · tap-repo-name/cmd-tap/cmd-install/pin-procedure/signing-crossref/intel-caveat/arm64-guard/version-source/brew-upgrade-note: True (los dos comandos de usuario aparecen LITERALES y correctos; la checklist cubre versión+tag+hash+push) · no-xattr-dup: True (SIGNING.md se ENLAZA sin copiar su workaround; el doc entero en español).</done>
</task>

<task type="auto">
  <name>Task 3: Subsección Homebrew espejo en ambas mitades del README</name>
  <read_first>README.md líneas 39–53 (## Download EN: el párrafo final termina en «...how to open the app on macOS the first time.» seguido de ## Ingest from phones and cameras) y líneas 161–175 (## Descarga ES: párrafo final «...cómo abrir la app en macOS la primera vez.» seguido de ## Ingesta desde móviles y cámaras). Los bloques de código existentes usan vallas simples SIN etiqueta de lenguaje.</read_first>
  <files>README.md</files>
  <action>
Añadir UNA subsección por mitad, simétrica, como cierre de cada sección de descargas — justo DESPUÉS del párrafo que enlaza SIGNING y ANTES del heading siguiente (## Ingest… / ## Ingesta…):

Mitad EN (tras «...how to open the app on macOS the first time.»):

### Install via Homebrew (macOS, Apple Silicon)

(bloque vallado simple, dos líneas:)
brew tap MustangXPress7/tap
brew install --cask --no-quarantine MustangXPress7/tap/cosechamedia

(frase de cierre:) Details, updates and current limitations (Apple Silicon only): see [HOMEBREW](docs/HOMEBREW.md).

Mitad ES (tras «...cómo abrir la app en macOS la primera vez.»), espejo exacto:

### Instalar con Homebrew (macOS, Apple Silicon)

(mismo bloque vallado, mismas dos líneas de comandos)

(frase de cierre:) Detalles, actualizaciones y limitaciones actuales (solo Apple Silicon): consulta [HOMEBREW](docs/HOMEBREW.md).

Reglas: los comandos van DENTRO de un bloque vallado con ``` simple sin etiqueta (estilo ya dominante en el README); las dos mitades comparten idéntico bloque de comandos; el aviso «Apple Silicon only / solo Apple Silicon» es obligatorio en ambas (honestidad sobre el caveat arm64, coherente con el cask y docs/HOMEBREW.md); NO tocar ninguna otra parte del README (ni Créditos ni listados de assets).
  </action>
  <verify>
    <automated>$t = Get-Content README.md -Raw; $i = $t.IndexOf('<a name="espanol">')
"hb-en: $(([regex]::Matches($t.Substring(0,$i),'docs/HOMEBREW\.md')).Count)"
"hb-es: $(([regex]::Matches($t.Substring($i),'docs/HOMEBREW\.md')).Count)"
"h2-en: $($t.Substring(0,$i).Contains('### Install via Homebrew'))"
"h2-es: $($t.Substring($i).Contains('### Instalar con Homebrew'))"
"tap-cmds: $(([regex]::Matches($t,'(?m)^brew tap MustangXPress7/tap$')).Count)"
"install-cmds: $((([regex]::Matches($t,[regex]::Escape('--no-quarantine MustangXPress7/tap/cosechamedia'))).Count))"
"symmetry: (($t.IndexOf('### Install via Homebrew') -ge 0) -and ($t.IndexOf('### Install via Homebrew') -lt $i) -and ($t.IndexOf('### Instalar con Homebrew') -gt $i))"</automated>
  </verify>
  <done>hb-en: 1 · hb-es: 1 (un enlace a docs/HOMEBREW.md por mitad) · h2-en/h2-es: True · tap-cmds: 2 · install-cmds: 2 (idénticos en ambas mitades) · symmetry: True (EN arriba del corte, ES debajo) · el resto del README intacto (revisión visual del diff: solo las dos subsecciones nuevas).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Releases → usuarios finales | Brew descarga y ejecuta un binario de esa fuente; el cask define qué y de dónde |
| docs/HOMEBREW.md → usuarios/mantenedor | Instrucciones que alguien ejecutará literalmente en su máquina |
| Tap externo futuro (homebrew-tap) | Fuente de definiciones de instalación; su repo aún no existe (per D-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ML7-01 | Tampering | Distribución con sha256 :no_check | medium | mitigate | Comentario obligatorio en el propio cask + checklist de docs/HOMEBREW.md que exige fijar el hash real desde la primera release estable; mientras tanto la integridad descansa en TLS + procedencia GitHub Releases |
| T-ML7-02 | Spoofing | Repo del tap mal nombrado o suplantado | low | mitigate | docs exigen el nombre EXACTO `homebrew-tap` bajo la cuenta del propietario; el formato corto `brew tap user/tap` resuelve siempre github.com/<user>/homebrew-tap |
| T-ML7-03 | Tampering | --no-quarantine debilita Gatekeeper | low | mitigate | Recomendación documentada con justificación (app ad-hoc sin notarizar, per D-03) y alternativa manual enlazada a docs/SIGNING.md |
| T-ML7-SC | Tampering | npm/pip/cargo | high | mitigate | N/A — este plan no instala paquetes (Package Legitimacy Gate N/A); sin dependencias nuevas |
</threat_model>

<verification>
Desde la raíz del repo, en orden: (1) gates del Task 1 — todas las stanzas presentes, sha256 honesto, cierra con end; (2) gates del Task 2 — doc creado con comandos literales, checklist de hash y cross-ref a SIGNING sin duplicar; (3) gates del Task 3 — un enlace y una subsección por mitad, comandos idénticos ×2; (4) global —

$st = @(git status --porcelain)
"core-touch: $([bool]($st | Where-Object { $_.Length -ge 3 -and ($_.Substring(3) -like 'app/core*') }))"
"ci-touch: $([bool]($st | Where-Object { $_.Length -ge 3 -and ($_.Substring(3) -like '.github/*') }))"
$st

— core-touch y ci-touch DEBEN ser False (per D-02 y restricciones); el listado solo contiene packaging/homebrew/Casks/cosechamedia.rb, docs/HOMEBREW.md y README.md (más .planning/). Revisión visual del diff del README: mitades espejo intactas, nada colateral.
</verification>

<success_criteria>
- Existe packaging/homebrew/Casks/cosechamedia.rb con DSL válido de homebrew-cask: token/version/url(name/desc/homepage)/depends_on (ffmpeg + arm64 temporal comentado)/app, y sha256 honesto (:no_check documentado, o hash real si la release llegó a existir durante la ejecución).
- Existe docs/HOMEBREW.md en español con: estado/limitaciones honesto, instalación de usuario con los DOS comandos exactos (--no-quarantine incluido, per D-03), pasos manuales de publicación del tap homebrew-tap (per D-01), checklist por release (bump __version__, tag exacto, cálculo de SHA-256 multiplataforma, actualización en AMBAS copias del cask) y cross-ref a SIGNING.md.
- README con subsección «Install via Homebrew / Instalar con Homebrew» simétrica en ambas mitades, enlazando docs/HOMEBREW.md, con el aviso Apple Silicon-only.
- Cero cambios en app/core/ y .github/workflows/; sin dependencias nuevas.
</success_criteria>

<output>
Create `.planning/quick/260822-ml7-cask-de-homebrew-para-distribucion-macos/260822-ml7-SUMMARY.md` when done
</output>
