---
phase: 260816-x3b-reemplazar-los-glifos-emoji-unicode-que-
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ui/icons.py (nuevo)
  - app/ui/assets/icons/ (13 SVGs nuevos)
  - tests/test_icons.py (nuevo)
  - app/ui/main_window.py
  - app/ui/source_picker.py
  - app/i18n/cosechamedia_en.ts
  - tests/test_wifi_source.py
  - tests/test_source_content.py
  - tests/test_source_picker.py
autonomous: true
requirements: [UI-REVIEW-Visuals]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Ningún botón de `main_window.py` ni de `source_picker.py` usa glifos unicode/emoji como icono: los 6 de la header (⟳ + × ✎ ⧉ 📁), ⚙, ✎ de descripción, +/− de sesión, 📁×2 de examinar, 🗑×2 de borrado por fila, y el 📱 de «Desconectados» muestran iconos SVG vectoriales tintados con el color del tema."
    - "Los botones de texto «⟳ Detectar» y «📷 Escanear cámaras» pasan a «Detectar» y «Escanear cámaras» con icono QIcon (refresh/camera) y el catálogo `cosechamedia_en.ts` está sincronizado con los nuevos source strings."
    - "Los prefijos emoji «📶 »/«🌐 »/«📱 » del label de origen de sesión quedan limpios del texto; el texto sigue siendo «Origen automático: %1» con solo el nombre del dispositivo."
    - "Al cambiar de tema o acento, los iconos se re-tintan sin reiniciar: `_switch_theme`/`_switch_accent` llaman a `icons.refresh_all()` que re-aplica los iconos registrados con la paleta vigente."
    - "La suite de tests UI pasa en Qt offscreen; los tests que asertaban glifos (test_source_content:160, test_wifi_source:437/455, test_source_picker:75/328) se actualizaron a aserciones de QIcon no nulo / ausencia de emoji, y `tests/test_icons.py` cubre el módulo nuevo."
    - "PyInstaller empaqueta `app/ui/assets/icons/` sin cambios en `main.spec` (el glob `('app/ui/assets', 'app/ui/assets')` ya cubre el subdirectorio)."
  artifacts:
    - app/ui/icons.py
    - app/ui/assets/icons/refresh.svg, plus.svg, minus.svg, x.svg, pencil.svg, copy.svg, folder.svg, gear.svg, trash.svg, camera.svg, phone.svg, wifi.svg, globe.svg
    - tests/test_icons.py
    - app/ui/main_window.py
    - app/ui/source_picker.py
    - app/i18n/cosechamedia_en.ts
    - tests/test_wifi_source.py, tests/test_source_content.py, tests/test_source_picker.py
  key_links:
    - "`icons.apply(button, name, size)` ↔ `_switch_theme`/`_switch_accent` (`main_window.py:1250,1258`): el registro de weakrefs + `refresh_all()` re-tinta todos los botones al cambiar tema/accento."
    - "`icons.icon(name)` ↔ `resource_path("app/ui/assets/icons/<name>.svg")` ↔ glob de datas en `main.spec:21`: el render lee el SVG vía resource_path (dev y frozen) y el empaquetado ya lo incluye."
    - "`tr("Detectar")`/`tr("Escanear cámaras")` ↔ entradas 461-462 y 471-472 de `cosechamedia_en.ts`: source y translation se limpian juntos."
    - "`SourcePickerDialog._missing_item` ↔ `tests/test_source_picker.py:75,328`: la identificación del ítem desconectado pasa de `text().startswith("📱")` a `not item.icon().isNull()`."
    - "`_build_remove_source_button`/`_build_remove_file_button` (`main_window.py:2104,2113`) ↔ `tests/test_source_content.py:160`: la aserción pasa de `btn.text() == "🗑"` a icono no nulo."
---

<objective>
Reemplazar los glifos emoji/unicode usados como iconos de botones por iconos vectoriales SVG tintables con el tema (hallazgo pendiente de `01-UI-REVIEW.md`, pillar Visuals 3/4: "Iconos emoji en vez de iconos reales — 6 botones de la header y 2 en orígenes usan glifos unicode; en Linux/macOS pueden renderizar como tofu").

**Purpose:** Eliminar el riesgo de renderizado como tofu en Linux/macOS (fuentes sin glifos emoji) y dar iconografía real y consistente, recolorizada con la paleta del tema (mismo patrón que `app/ui/wheat_field.py`: leer SVG, reemplazar color placeholder, cachear QPixmap/QIcon). Sin dependencias nuevas (nada de QtAwesome): SVG propios + QIcon.

**Output:**
- `app/ui/icons.py` — módulo de iconos: lee SVG de `app/ui/assets/icons/`, reemplaza el placeholder `#FF00FF` por el color del tema, renderiza con `QSvgRenderer` a 2× con devicePixelRatio 2.0, cachea por (nombre, hex), registra botones por weakref y re-aplica en `refresh_all()`.
- 13 SVGs stroke-based 24×24 en `app/ui/assets/icons/`: refresh, plus, minus, x, pencil, copy, folder, gear, trash, camera, phone, wifi, globe.
- Sustitución de todos los glifos en `app/ui/main_window.py` y `app/ui/source_picker.py` + hook de re-tinte en `_switch_theme`/`_switch_accent`.
- Limpieza de emojis de strings traducidos + sincronización de `app/i18n/cosechamedia_en.ts`.
- Tests: `tests/test_icons.py` nuevo; actualización de las aserciones de glifos en `test_wifi_source.py`, `test_source_content.py` y `test_source_picker.py`.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
# Fuente del hallazgo
@C:/Users/JoanRamon/Documents/CosechaMedia/.planning/phases/01-auditor-a-ui-y-plan-de-reubicaci-n/01-UI-REVIEW.md (Pillar 2 Visuals: «Iconos emoji en vez de iconos reales» — 6 botones de la header y 2 en orígenes usan glifos unicode; tofu en Linux/macOS sin fuente emoji)

# Patrón a seguir
@C:/Users/JoanRamon/Documents/CosechaMedia/app/ui/wheat_field.py (patrón de recolor de SVG por tema: `_PLACEHOLDER` + replace + QSvgRenderer + cache; `_HAS_SVG` guard de archivo ausente)
@C:/Users/JoanRamon/Documents/CosechaMedia/app/ui/theme.py (`theme.color("text")`/`"text_disabled"` como colores de tinte; `build_qss`/`apply_theme` aplicados en `main.py` antes de construir MainWindow, así que el módulo de iconos lee la paleta ya vigente)

# Código relevante
@C:/Users/JoanRamon/Documents/CosechaMedia/app/ui/main_window.py (puntos de uso: header `:284-330`, descripción `:359`, detectar/escanear `:433/:437`, sesión `:463/:470/:489/:512`, borrado por fila `:2104/:2113`, prefijos de origen `:2464-2470`, hooks `:1250-1264`)
@C:/Users/JoanRamon/Documents/CosechaMedia/app/ui/source_picker.py (`_missing_item` `:194-201`)
@C:/Users/JoanRamon/Documents/CosechaMedia/app/core/utils.py (`resource_path`)
@C:/Users/JoanRamon/Documents/CosechaMedia/main.spec (datas `:19-24`: el glob `('app/ui/assets', 'app/ui/assets')` ya cubre `app/ui/assets/icons/` — sin cambios)

# Tests afectados (aserciones sobre glifos que hay que actualizar)
@C:/Users/JoanRamon/Documents/CosechaMedia/tests/test_wifi_source.py (:437 assertIn "📶"; :455 assertIn "📱")
@C:/Users/JoanRamon/Documents/CosechaMedia/tests/test_source_content.py (:160 assertEqual btn.text() "🗑")
@C:/Users/JoanRamon/Documents/CosechaMedia/tests/test_source_picker.py (:75 startswith "📱"; :328 startswith "📱")
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear el módulo app/ui/icons.py + 13 SVGs + tests/test_icons.py</name>
  <files>app/ui/icons.py, app/ui/assets/icons/refresh.svg, app/ui/assets/icons/plus.svg, app/ui/assets/icons/minus.svg, app/ui/assets/icons/x.svg, app/ui/assets/icons/pencil.svg, app/ui/assets/icons/copy.svg, app/ui/assets/icons/folder.svg, app/ui/assets/icons/gear.svg, app/ui/assets/icons/trash.svg, app/ui/assets/icons/camera.svg, app/ui/assets/icons/phone.svg, app/ui/assets/icons/wifi.svg, app/ui/assets/icons/globe.svg, tests/test_icons.py</files>
  <behavior>
    - Test 1: para cada uno de los 13 nombres (refresh, plus, minus, x, pencil, copy, folder, gear, trash, camera, phone, wifi, globe), `icons.icon(name)` devuelve un QIcon no nulo y `icons.icon(name).pixmap(QSize(16,16)).isNull()` es False.
    - Test 2: `icons._svg_text_for("refresh", "#123456")` no contiene el placeholder `#FF00FF` y contiene `#123456`.
    - Test 3: `icons.apply(btn, "trash", size=16)` fija un icono no nulo en el QPushButton (incluido el pixmap en modo Disabled) y registra el botón en el registro del módulo.
    - Test 4: `icons.refresh_all()` se ejecuta sin error con botones registrados y descarta las weakrefs muertas.
  </behavior>
  <action>
    Crea el módulo `app/ui/icons.py` siguiendo el patrón de `wheat_field.py` (convenciones de CONVENTIONS.md: strings ES, helpers `_` privados, nunca lanzar excepción en runtime — fallback a QIcon nulo con `print(f"...")`):

    - Constantes: `_PLACEHOLDER = "#FF00FF"`, `_ICON_DIR = resource_path(os.path.join("app", "ui", "assets", "icons"))`, `_HAS_ICONS = os.path.isdir(_ICON_DIR)` (mismo guard que `_HAS_SVG`).
    - `_svg_text_for(name, color_hex)`: lee `<name>.svg` del directorio y devuelve el texto con TODAS las ocurrencias de `_PLACEHOLDER` reemplazadas por `color_hex`. Función privada pero parte de la superficie testeable (patrón del proyecto).
    - `pixmap(name, color_hex, size=16) -> QPixmap`: renderiza con `QSvgRenderer(QByteArray(svg.encode("utf-8")))` sobre un QPixmap de `size*2` relleno transparente y `pm.setDevicePixelRatio(2.0)` (nítido en HiDPI; patrón 2× de `wheat_field._get_tile`). Si el SVG no existe, devuelve QPixmap nulo.
    - `icon(name, color_key="text", size=16) -> QIcon`: hex = `theme.color(color_key)`; cache de módulo `_icon_cache[(name, hex)]`; el QIcon lleva dos pixmaps: `QIcon.Mode.Normal` con el hex de `color_key` y `QIcon.Mode.Disabled` con `theme.color("text_disabled")` (paridad con el dimming que el QSS `QPushButton:disabled` aplicaba a los glifos). En cache hit devuelve el QIcon cacheado.
    - `apply(button, name, size=16, color_key="text")`: guarda `weakref.ref(button)` junto con (name, size, color_key) en un registro de módulo `_registry`, luego `button.setIcon(icon(name, color_key, size))` y `button.setIconSize(QSize(size, size))`. Usa weakref: los botones de fila de tabla (🗑) se crean y destruyen continuamente — el registro no debe retenerlos vivos.
    - `refresh_all()`: recorre `_registry`, descarta weakrefs muertas, y re-aplica `icon()` + `setIconSize()` con la paleta vigente a cada botón vivo. Es lo que re-tinta al cambiar de tema/accento.
    - Importa `theme` (de `app.ui`) y `resource_path` (de `app.core.utils`) a nivel de módulo; `QSize`/`QByteArray` de QtCore; `QPixmap`, `QPainter`, `QIcon` de QtGui; `QSvgRenderer` de QtSvg.

    SVGs (`app/ui/assets/icons/`): 13 archivos stroke-based con `viewBox="0 0 24 24"`, `fill="none"`, `stroke="#FF00FF"` (el placeholder), `stroke-width="1.8"`, `stroke-linecap="round"`, `stroke-linejoin="round"`, un solo grupo de trazos por archivo. Formas: refresh (flecha circular con cabeza), plus (dos trazos en cruz), minus (un trazo horizontal), x (dos diagonales), pencil (lápiz inclinado), copy (dos rectángulos superpuestos), folder (silueta de carpeta), gear (círculo + 8 radios cortos), trash (cubo con tapa y asa), camera (cuerpo + lente), phone (auricular), wifi (arco + punto base), globe (círculo + meridianos). Nada de texto ni rellenos: un único color por icono para que el replace funcione.

    TESTS (`tests/test_icons.py`, patrón `unittest.TestCase` de la carpeta tests/): crea `QApplication` offscreen si no existe (`QApplication.instance() or QApplication([])`), igual que los demás tests Qt del repo. Implementa los 5 tests del bloque `<behavior>` (RED primero: el módulo no existe, todo falla; luego crea `icons.py` y los SVGs hasta verde). Para el Test 4 registra un botón real y uno descartado (borra la referencia y llama `gc.collect()` o usa un objeto local fuera de scope) para verificar que las weakrefs muertas se descartan sin error.
  </action>
  <verify>
    <automated>python -m unittest tests.test_icons -v</automated>
  </verify>
  <done>Los 5 tests de `tests/test_icons.py` pasan verde; `icons.py` expone `icon()`, `apply()`, `refresh_all()` y `pixmap()`; los 13 SVGs existen en `app/ui/assets/icons/` con placeholder `#FF00FF` recolorizable.</done>
</task>

<task type="auto">
  <name>Task 2: Sustituir los glifos de main_window.py por QIcon + hook de re-tinte + sincronizar .ts y tests wifi/source_content</name>
  <files>app/ui/main_window.py, app/i18n/cosechamedia_en.ts, tests/test_wifi_source.py, tests/test_source_content.py</files>
  <action>
    Sustituye en `app/ui/main_window.py` todos los glifos usados como iconos (per UI-REVIEW Visuals; sin tocar `app/core/` ni los botones zombie `btn_browse_source`/`btn_receive_wifi` ni las flechas de texto «→» de `project_path_label` — no son iconos de botón):

    1. Import: `from app.ui import icons` junto al resto de imports de `app.ui` (tras `from app.ui import theme`, `:26`).
    2. Header (`:284-305`): el tuple del bucle pasa de `(attr, txt, tip)` a `(attr, icon_name, tip)` con `"⟳"→"refresh"`, `"+"→"plus"`, `"×"→"x"`, `"✎"→"pencil"`, `"⧉"→"copy"`, `"📁"→"folder"`. `b = QPushButton()` (sin texto) y `icons.apply(b, icon_name, size=18)`; conserva `objectName("IconButton")`, `setFixedSize(28, 28)`, tooltip y conexión de clicked idénticos.
    3. Header derecho (`:321-330`): `"⚙"→"gear"`; mismo tratamiento.
    4. `btn_edit_description` (`:359`): `QPushButton()` + `icons.apply(b, "pencil", size=16)`; conserva fixed 24×24, tooltip y clicked.
    5. `btn_detect_drives` (`:433`): texto pasa a `self.tr("Detectar")` + `icons.apply(b, "refresh", size=14)`. `btn_scan_cameras` (`:437`): texto pasa a `self.tr("Escanear cámaras")` + `icons.apply(b, "camera", size=14)`. Tooltips sin cambios.
    6. `btn_new_session` (`:463`): `"+"→"plus"`, size 18. `btn_delete_session` (`:470`): `"−"→"minus"`, size 18.
    7. `_btn_browse_sess_src` (`:489`) y `_btn_browse_sess_dest` (`:512`): `"📁"→"folder"`, size 18.
    8. `_build_remove_source_button` (`:2104`) y `_build_remove_file_button` (`:2113`): `QPushButton()` + `icons.apply(b, "trash", size=16)`; conserva fixed 24×24, tooltip, cursor y conexión (los botones de fila se crean en cada `_refresh_source_list`/refresco — el registro por weakref del módulo gestiona el churn).
    9. `_on_session_selected` (`:2464-2470`): elimina la variable `prefix` y sus 3 asignaciones («📶 », «🌐 », «📱 »); el texto queda `self.tr("Origen automático: %1").arg(name)` (el nombre del dispositivo solo). El string fuente `tr("Origen automático: %1")` no cambia — los prefijos eran concatenación en runtime, no parte del catálogo.
    10. Hooks (`:1250-1264`): en `_switch_theme` y `_switch_accent`, tras `theme.apply_theme()` y antes de `_style_table_viewports()`, añade `icons.refresh_all()`. No cambies el resto de la lógica de los hooks.
    11. Grep final de limpieza: verifica con una búsqueda de `[⟳✎⧉⚙🗑📱📶🌐📷−]` en `main_window.py` que los únicos restos permitidos son los tooltips/comentarios que no contienen glifos (no debe quedar ninguno como texto de botón).

    `app/i18n/cosechamedia_en.ts` (usa Edit con reemplazo puntual, nunca Write): en la entrada `:459-463` cambia `<source>⟳ Detectar</source>` → `<source>Detectar</source>` y `<translation>⟳ Detect</translation>` → `<translation>Detect</translation>`; en la entrada `:469-473` cambia `<source>📷 Escanear cámaras</source>` → `<source>Escanear cámaras</source>` y `<translation>📷 Scan cameras</translation>` → `<translation>Scan cameras</translation>`. El atributo `line` de los `<location>` puede quedarse obsoleto (lupdate lo regenera) o actualizarse al número de línea nuevo del `tr()`.

    TESTS (actualiza las aserciones que dependían de los glifos):
    - `tests/test_wifi_source.py:437`: sustituye `self.assertIn("📶", self.window.session_src_label.toolTip())` (y el comentario de la línea 435) por `self.assertNotIn("📶", self.window.session_src_label.toolTip())` — verifica la limpieza del prefijo. Igual en `:455` con `📱` (mantén las aserciones de "Alice"/"Canon R5" y de "Origen automático:" intactas).
    - `tests/test_source_content.py:160`: sustituye `self.assertEqual(btn.text(), "🗑")` por `self.assertFalse(btn.icon().isNull())` (el icono de borrado está presente aunque el texto del botón sea vacío). El resto del test no cambia.

    Verificación final de no-regresión: `python -m unittest tests.test_wifi_source tests.test_source_content tests.test_e2e -v` en Qt offscreen.
  </action>
  <verify>
    <automated>python -m unittest tests.test_wifi_source tests.test_source_content tests.test_e2e -v</automated>
  </verify>
  <done>Ningún glifo de la lista queda como texto de botón en `main_window.py`; `_switch_theme`/`_switch_accent` llaman a `icons.refresh_all()`; las 2 entradas del .ts están limpias y sincronizadas; los 3 archivos de tests pasan verde.</done>
</task>

<task type="auto">
  <name>Task 3: Icono de teléfono en los ítems «Desconectados» de source_picker.py + actualizar sus tests</name>
  <files>app/ui/source_picker.py, tests/test_source_picker.py</files>
  <action>
    Sustituye el prefijo emoji del ítem «Desconectados» del selector de orígenes (per UI-REVIEW Visuals; sin tocar el resto del diálogo):

    1. Import: `from app.ui import icons` junto al resto de imports de `app.ui` (`:21-23`).
    2. `_missing_item` (`:194-201`): crea `QListWidgetItem(name + " — " + self.tr("desconectado"))` (sin el prefijo «📱 ») y aplica `item.setIcon(icons.icon("phone", size=16))`. Conserva intactos los flags `Qt.ItemIsEnabled`, el `setData(Qt.UserRole, ("device", dev["id"]))` y el tooltip de estado. No registres el ítem vía `icons.apply` (los QListWidgetItem no son botones y el diálogo no cambia de tema abierto): usa `icons.icon()` directamente.
    3. Grep de limpieza: verifica que `source_picker.py` no contiene «📱».

    TESTS (`tests/test_source_picker.py`): las aserciones que identificaban el ítem por `text().startswith("📱")` pasan a identificar por icono no nulo:
    - `:75` (helper de búsqueda del ítem desconectado): `item.text().startswith("📱")` → `not item.icon().isNull()` (conserva la condición `role is not None and role[0] == "device"`).
    - `:328` (`test_missing_section_lists_devices`): `self.assertTrue(any(t.startswith("📱") for t in texts))` → `self.assertTrue(any(not dlg.list_widget.item(i).icon().isNull() for i in range(dlg.list_widget.count())))`; conserva la aserción de `dlg.tr("desconectado")` en el texto.

    Verificación final de no-regresión: `python -m unittest tests.test_source_picker tests.test_wifi_source -v` en Qt offscreen.
  </action>
  <verify>
    <automated>python -m unittest tests.test_source_picker tests.test_wifi_source -v</automated>
  </verify>
  <done>El ítem «Desconectados» muestra el icono de teléfono con texto limpio (sin emoji); `source_picker.py` no contiene glifos; `tests/test_source_picker.py` pasa verde con las aserciones de icono.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Assets empaquetados → render | Los SVGs de `app/ui/assets/icons/` se leen de disco (dev) o de `sys._MEIPASS` (frozen) y se parsean con QSvgRenderer antes de mostrarse |
| Módulo de iconos → botones | `icons.apply`/`icons.refresh_all` mutan el estado visual de botones vivos desde cualquier hilo de la UI (solo main thread, patrón Qt) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-x3b-01 | Denial of Service | `icons.py` render/lectura | low | mitigate | Guard `_HAS_ICONS` + fallback a QIcon nulo con `print(f"Icon not found: ...")` — la app nunca crashea por un SVG ausente o corrupto (patrón `wheat_field._HAS_SVG`, convención de errores del proyecto) |
| T-x3b-02 | Tampering | `cosechamedia_en.ts` | low | mitigate | Actualizar source+translation de las 2 entradas juntas (fuera de sync, Qt cae al source ES — texto correcto pero sin traducir); verificado por la suite de tests EN |
| T-x3b-03 | Tampering | `main_window.py`/`source_picker.py` | low | mitigate | Tests actualizados (test_icons + wifi_source + source_content + source_picker + e2e) verifican iconos no nulos, ausencia de emoji y re-tinte vía `refresh_all`; ningún cambio en `app/core/` |
| T-x3b-SC | Tampering | instala paquetes | n/a | accept | Sin instalación de paquetes en este plan — no aplica gate de legitimidad |
</threat_model>

<verification>
- Ejecutar la suite completa en Qt offscreen: `QT_QPA_PLATFORM=offscreen python -m unittest discover -s tests -v` — debe salir verde (los 4 archivos de tests modificados + el nuevo test_icons).
- Verificar por grep que no quedan glifos como texto de botón en `app/ui/main_window.py` ni `app/ui/source_picker.py` (patrón `[⟳✎⧉⚙🗑📱📶🌐📷−]`; se permiten los tooltips de `tr()` que no contengan glifos — ninguno debe).
- Verificar que `git diff --stat` no toca ningún archivo de `app/core/` y que `main.spec` queda sin cambios.
- Verificar que `app/ui/assets/icons/` contiene exactamente los 13 SVGs y que cada uno usa `stroke="#FF00FF"` como placeholder recolorizable.
</verification>

<success_criteria>
- Todos los botones y ítems que usaban glifos unicode muestran iconos SVG vectoriales tintados con `theme.color("text")` (y dimming `text_disabled` en estado deshabilitado).
- Cambiar de tema o acento re-tinta los iconos al instante (`icons.refresh_all()` en `_switch_theme`/`_switch_accent`).
- El catálogo EN (`cosechamedia_en.ts`) está sincronizado con los nuevos strings «Detectar» / «Escanear cámaras».
- Los ítems «Desconectados» del selector de orígenes muestran icono de teléfono sin emoji en el texto.
- Suite completa de tests en verde, incluyendo `tests/test_icons.py`; sin cambios en `app/core/` ni en `main.spec`.
</success_criteria>

<output>
Create `.planning/quick/260816-x3b-reemplazar-los-glifos-emoji-unicode-que-/260816-x3b-SUMMARY.md` when done, siguiendo `templates/summary.md` (quick: qué se cambió — módulo icons.py + 13 SVGs + sustitución de glifos en main_window/source_picker + hook de re-tinte + .ts; qué tests cubren los iconos; verificación de suite completa y de ausencia de glifos por grep).
</output>
