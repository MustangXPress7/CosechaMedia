---
phase: 260822-gi3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [I-03, I-06, I-11, I-14, I-15, I-18]

estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - A README reader in either language can discover every shipped v1.5 feature area (volcado selectivo por sesión, informes CSV, proyectos en un solo paso, fechas de rodaje flexibles, detección de dispositivos con nombres persistentes, iconos SVG tintables) without reading the changelog.
    - Both language halves carry the same number of bold bullets with parallel labels (16 per half after the edit).
    - Every pre-existing URL, image, license block, build instruction and section heading survives unchanged.
    - The stale WIP claim about camera detection is gone from both halves.
  artifacts:
    - README.md with symmetric EN/ES feature updates
  key_links:
    - EN Features bullets ↔ ES Características bullets (1:1 mirror, same order)
---

<objective>
Update README.md so its bilingual feature lists describe what v1.5 actually ships.

Purpose: The README still describes v1.2-era functionality — it marks camera detection as work-in-progress (it was completed and renamed to "dispositivo") and says nothing about the six feature areas added since (per-session selective dump, CSV reports, one-step project wizard, flexible shoot dates, persistent device naming, tintable SVG icons). A visitor landing on GitHub Releases cannot see what changed.

Output: One edited file — `README.md`, both language halves updated symmetrically, everything else byte-identical.

Decisions locked for this plan:
- No version literal is added anywhere (the README has never carried one; pinning "1.5" would drift). The version story is told by features, not numbers.
- Intro taglines and the `Footage/<Camera>/<Date>` / `Footage/<Cámara>/<Fecha>` folder examples stay as-is: the UI's organization-mode labels are still "Cámara / Fecha", "Fecha / Cámara", "Solo cámara" (verified at app/ui/main_window.py:791), so those claims remain true. Only the device/source concept was renamed to "dispositivo".
- The WiFi QR sources being locked to "all content" mode is deliberately omitted from this bullet list — too granular at README level.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md

Source of truth for what v1.5 shipped (already gathered and verified by the orchestrator; do not re-research):
- Volcado selectivo por sesión (I-15 + I-18): three content modes per session — all content / date interval / days-since-last-dump window (default 1 day) — cycled via an icon-only mode button plus a config button; SelectiveDumpAssistant wizard scans the card, groups by shooting day, lets the operator pick days, dumps verified MD5 copies.
- Informes CSV (I-06): card-content report before dumping and integrity report after ingest, both available as post-ingest actions (app/core/ingestor.py generate_card_content_report / generate_integrity_report).
- Asistente de proyecto en un solo paso (I-11): ProjectWizard collects every project setting at creation; advanced options collapsed by default.
- Fechas de rodaje flexibles (I-03 line of work): project date_mode auto/manual with manual_date, plus per-device date overrides for cards without reliable metadata.
- Dispositivos (I-14): cámara→dispositivo rename across UI; device name forced when registering a source; names persist.
- Estética: tintable SVG icon set that follows theme/accent (app/ui/icons.py); accent-driven primary buttons, progress, hover/focus borders, selections.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Features sections symmetrically in both language halves</name>
  <read_first>
    README.md — English Features section (lines ~15–28, bullets start after `## Features`) and Spanish Características section (lines ~132–145). Note each half currently has 10 feature bullets; the target bullet is the third one in each list (the WIP-marked camera-detection bullet).
  </read_first>
  <files>README.md</files>
  <action>
Make exactly four edits per language half, keeping the two halves mirror-symmetric (same positions, same order).

ENGLISH HALF (`## Features`):

Edit 1 — replace the third bullet (the one marked WIP about camera-detection) with:
`- **Device detection**: automatic source scanning with persistent device names (set when registering the source).`

Edit 2 — insert these three new bullets immediately AFTER the Device detection bullet (so they sit between it and the Multiple dump destinations bullet):
`- **Selective dump per session**: dump all content, a date interval, or only the days since the last dump (configurable window, 1 day by default), session by session — with an assistant that scans the card, groups files by shooting day, and copies verified.`
`- **Flexible shoot dates**: taken automatically from video metadata or set manually per project, with per-device overrides for cards without reliable metadata.`
`- **One-step projects**: the creation wizard collects every project setting up front (advanced options collapsed by default).`

Edit 3 — replace the Post-ingest bullet with:
`- **Post-ingest**: CSV reports (card contents before dumping, integrity after ingest), source formatting (Windows), and scheduled shutdown.`

Edit 4 — replace the Themes and accents bullet with:
`- **Themes and accents**: dark/light theme with color accents, tintable SVG icons that follow the accent, and animated wheat background.`

SPANISH HALF (`## Características`) — exact mirrors:

Edit 1 — replace the third bullet (la viñeta marcada como WIP sobre detección de cámaras) with:
`- **Detección de dispositivos**: escaneo automático de orígenes con nombres de dispositivo persistentes (se fijan al registrar el origen).`

Edit 2 — insert immediately after it:
`- **Volcado selectivo por sesión**: vuelca todo el contenido, un intervalo de días o solo los días transcurridos desde el último volcado (ventana configurable, 1 día por defecto), sesión a sesión, con un asistente que escanea la tarjeta, agrupa por día de rodaje y copia verificado.`
`- **Fechas de rodaje flexibles**: tomadas automáticamente de los metadatos de vídeo o fijadas a mano por proyecto, con excepciones por dispositivo para tarjetas sin metadatos fiables.`
`- **Proyectos en un solo paso**: el asistente de creación recoge todos los ajustes del proyecto desde el principio (opciones avanzadas plegadas por defecto).`

Edit 3 — replace the Post-ingesta bullet with:
`- **Post-ingesta**: informes CSV (contenido de la tarjeta antes de volcar, integridad tras la ingesta), formateo de orígenes (Windows) y apagado programado.`

Edit 4 — replace the Temas y acentos bullet with:
`- **Temas y acentos**: tema oscuro/claro con acentos de color, iconos SVG tintables que siguen al acento y fondo de trigo animado.`

DO NOT touch anything else: intro taglines (line ~13 EN, ~130 ES), the `Footage/<Camera>/<Date>` and `Footage/<Cámara>/<Fecha>` examples inside the Automatic organization / Organización automática bullets, Requirements, Download, the phones/cameras ingestion section, Building, Project structure, License, Credits, all images and links. Match the existing single-space-after-bold-label bullet style exactly. Do not add any heading, version number, or changelog note.
  </action>
  <verify>
    <automated>$t = Get-Content README.md -Raw; $i = $t.IndexOf('<a name="espanol">'); if ($i -lt 0) { throw 'anchor missing' }; $en = $t.Substring(0,$i); $es = $t.Substring($i); "bullets en/es: $(([regex]::Matches($en,'(?m)^- \*\*')).Count)/$(([regex]::Matches($es,'(?m)^- \*\*')).Count)"; "old-en-left: $((Select-String -Path README.md -Pattern 'unknown cameras' | Measure-Object).Count)"; "old-es-left: $((Select-String -Path README.md -Pattern 'desconocidas' | Measure-Object).Count)"</automated>
  </verify>
  <done>Bullet counts report `bullets en/es: 16/16` (each half had 13 bold bullets before: 10 Features + 1 Requirements + 2 FTP apps; +3 new per half, replacements are 1:1), `old-en-left: 0` and `old-es-left: 0`. Each of these strings appears exactly once per half: Selective dump per session · per-device overrides · advanced options collapsed by default · CSV reports · tintable SVG icons · asistente que escanea la tarjeta · excepciones por dispositivo · opciones avanzadas plegadas por defecto · iconos SVG tintables · informes CSV.</done>
</task>

<task type="auto">
  <name>Task 2: Integrity gates — prove nothing outside the feature lists moved</name>
  <files>README.md</files>
  <action>
Read the full diff once (`git diff -- README.md`) and confirm by eye that every changed line sits inside the English Features block or the Spanish Características block, that tone matches the surrounding concise style, and that both halves read as translations of each other. If anything drifted outside the two blocks or the halves diverged, fix both halves together and re-check.

Then run the automated gates below (from repo root):
1. URL-set equality vs HEAD — output must be `url-diff-count: 0`.
2. Language anchors intact — output must be `anchors: 2`.
3. Hunk confinement — every diff hunk must start on an original-file line inside the Features regions (EN lines ~15–30, ES lines ~130–150); output must be `hunks-outside-features: 0`.

If any gate fails, correct README.md (never the gate) and re-run until all pass.
  </action>
  <verify>
    <automated>$a = git show HEAD:README.md | Select-String 'https?://[^\s)\]>]+' -AllMatches | ForEach-Object { $_.Matches.Value } | Sort-Object -Unique; $b = Get-Content README.md | Select-String 'https?://[^\s)\]>]+' -AllMatches | ForEach-Object { $_.Matches.Value } | Sort-Object -Unique; "url-diff-count: $((Compare-Object $a $b | Measure-Object).Count)"; "anchors: $((Select-String -Path README.md -Pattern 'name="(english|espanol)"' | Measure-Object).Count)"; $bad = git diff -U0 -- README.md | Select-String '^@@ -(\d+)' | ForEach-Object { [int]$_.Matches[0].Groups[1].Value } | Where-Object { ($_ -lt 15 -or $_ -gt 30) -and ($_ -lt 130 -or $_ -gt 150) }; "hunks-outside-features: $(@($bad).Count)"</automated>
  </verify>
  <done>All four gates green: bullets en/es 16/16 (re-run Task 1 check if needed), url-diff-count 0, anchors 2, hunks-outside-features 0. Sentinel content untouched: PolyForm-Noncommercial-1.0.0, CosechaMedia-windows-x86_64.exe, .github/workflows/build.yml, Compilar.bat, Primitive FTPd each still present at least once. git status shows only README.md modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced. This plan edits a documentation artifact only — no code paths, no inputs processed, no installs, no network calls beyond what the existing updater already performs.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-GI3-01 | Information Disclosure | README.md (public repo) | low | accept | Content documents already-shipped public features; no secrets, internal URLs, or unreleased roadmap details are introduced |
| T-GI3-02 | Repudiation | Feature claims vs shipped reality | low | mitigate | Every claim traces to a completed initiative ID (I-03/I-06/I-11/I-14/I-15+I-18) or merged commit evidence provided by the orchestrator; no package installs occur (supply-chain gate N/A) |
</threat_model>

<verification>
Run Task 1's symmetry/presence checks and Task 2's four integrity gates back-to-back from repo root; all must report their expected values. Then render-check mentally via the diff: markdown structure unchanged (same headings, same blank-line rhythm), no trailing-whitespace or broken-em-dash artifacts introduced.
</verification>

<success_criteria>
- README.md describes all six v1.5 feature areas in BOTH languages, in the same order, in the established concise bullet style.
- The WIP camera-detection claim is replaced by the completed device-detection description in both halves.
- Zero collateral damage: URLs, images, license, build instructions, requirements, and all other sections byte-identical to HEAD.
- Only README.md is modified in the working tree.
</success_criteria>

<output>
Create `.planning/quick/260822-gi3-actualizar-readme-para-version-1-5-con-l/260822-gi3-SUMMARY.md` when done
</output>
