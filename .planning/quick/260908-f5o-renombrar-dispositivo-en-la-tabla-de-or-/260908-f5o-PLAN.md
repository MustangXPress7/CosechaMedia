---
phase: quick-260908-f5o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ui/mixins/camera_mixin.py
  - app/ui/add_source_dialog.py
autonomous: true
requirements:
  - I-03
  - I-14
  - REQ-09

must_haves:
  truths:
    - Renombrar dispositivo por doble clic persiste el nombre en el registro global (device_settings/known_devices/sd_cards), no solo en la sesión
    - Antes de persistir globalmente, se muestra popup con opciones Sí/No/No volver a preguntar
    - "No volver a preguntar" se persiste en QSettings para no repetir el diálogo
    - Al añadir origen, si el dispositivo tiene un nombre conocido distinto, se auto-rellena en el combo
    - Los combos en Añadir origen muestran todos los nombres conocidos (ya funciona)
  artifacts:
    - app/ui/mixins/camera_mixin.py (modificado: _prompt_rename_camera + QSettings)
    - app/ui/add_source_dialog.py (modificado: auto-fill combo con nombre conocido)
  key_links:
    - _prompt_rename_camera → _persist_camera_mapping → upsert_known_device + save_dispositivo_config
    - AddSourceDialog → db.get_dispositivo_for_device → combo pre-fill
---

<objective>
Persistir el nombre de dispositivo al renombrar por doble clic en la tabla de orígenes (antes solo actualizaba la sesión, no el registro global), con popup de confirmación configurable, y auto-rellenar el combo de cámara en Añadir origen si el dispositivo ya tiene un nombre conocido.

Purpose: El bug precursor es que `_prompt_rename_camera` solo hace `update_session_config`, mientras que `_on_camera_cell_edited` sí persiste vía `_persist_camera_mapping`. El renombrado por doble clic no se propaga al registro global, perdiéndose entre sesiones/proyectos. Además, el combo de cámara en Añadir origen no pre-selecciona el nombre conocido del dispositivo.

Output: camera_mixin.py con rename persistente + popup; add_source_dialog.py con auto-fill de nombre conocido.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@app/ui/mixins/camera_mixin.py
@app/ui/add_source_dialog.py
@app/core/device_registry.py
@app/core/db.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persistir rename en registro global + popup de confirmación</name>
  <files>app/ui/mixins/camera_mixin.py, tests/test_camera_mixin.py</files>
  <behavior>
    - Test: _prompt_rename_camera con popup "Sí" → db.update_session_config llamado Y _persist_camera_mapping llamado con el nuevo nombre
    - Test: _prompt_rename_camera con popup "No" → db.update_session_config llamado Y _persist_camera_mapping NO llamado
    - Test: _prompt_rename_camera con popup "No volver a preguntar" → QSettings "camera/skip_rename_confirm" = True, y la próxima llamada NO muestra popup
    - Test: _prompt_rename_camera con QSettings flag activo → persiste directamente sin popup
    - Test: _prompt_rename_camera con nombre vacío → no persiste nada (early return)
  </behavior>
  <action>
Modificar `_prompt_rename_camera` en `app/ui/mixins/camera_mixin.py` (línea 48):

1. Al inicio, tras obtener `current`, comprobar `QSettings().value("camera/skip_rename_confirm", False, type=bool)`. Si es True, saltar al popup y persistir directamente.

2. Reemplazar el `QInputDialog.getText` actual por un `QMessageBox` con 3 botones:
   - `QMessageBox.Yes` → "Sí, guardar para futuras sesiones"
   - `QMessageBox.No` → "Solo para esta sesión"
   - `QMessageBox.Discard` → "No volver a preguntar"
   El título: `self.tr("Renombrar dispositivo")`
   El texto: `self.tr("¿Guardar '%1' como nombre del dispositivo para futuras sesiones?")` con `.arg(name.strip())` después de obtener `name` del InputDialog.

3. Secuencia de flujo:
   - Mostrar `QInputDialog.getText` para obtener el nombre (ya existente)
   - Si ok y nombre no vacío, mostrar `QMessageBox` con los 3 botones
   - Si Yes: llamar `db.update_session_config(...)` Y `self._persist_camera_mapping(session["id"], path, name.strip())`
   - Si No: llamar solo `db.update_session_config(...)`
   - Si Discard: llamar `db.update_session_config(...)`, `self._persist_camera_mapping(...)`, y `QSettings().setValue("camera/skip_rename_confirm", True)`
   - Si cancel del InputDialog: no hacer nada

4. Añadir `from PySide6.QtWidgets import QInputDialog, QMessageBox` — QMessageBox ya importado. QSettings ya importado.

5. Añadir test en `tests/test_camera_mixin.py`:
   - Crear clase `TestPromptRenameCamera(unittest.TestCase)` con setUp que crea QApplication offscreen, mockea `db`, y configura un MainWindow stub mínimo
   - Test 1 (Yes): mockear `QInputDialog.getText` devolviendo ("Canon R5", True), mockear `QMessageBox` devolviendo `QMessageBox.Yes`, verificar que `db.update_session_config` Y `db.save_dispositivo_config`/`upsert_known_device` son llamados
   - Test 2 (No): mismo input, `QMessageBox.No`, verificar solo `update_session_config`
   - Test 3 (Discard): verificar QSettings se escribe
   - Test 4 (skip flag): setear QSettings, verificar que no se muestra QMessageBox
  </action>
  <verify>
    <automated>python -m pytest tests/test_camera_mixin.py -x -v 2>&amp;1 | head -40</automated>
  </verify>
  <done>
    _prompt_rename_camera persiste en registro global (device_settings + known_devices) cuando el usuario confirma con "Sí" o "No volver a preguntar". El flag QSettings "camera/skip_rename_confirm" suprime el popup en sucesivas llamadas. Tests pasan.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-fill nombre conocido para USB en Añadir origen</name>
  <files>app/ui/add_source_dialog.py, tests/test_add_source_dialog.py</files>
  <behavior>
    - Test: fila USB con dispositivo conocido en DB → combo se pre-selecciona con nombre conocido (no "Sin nombre")
    - Test: fila USB sin dispositivo conocido → combo queda en "Sin nombre"
    - Test: fila MTP con nombre conocido → combo se pre-selecciona (ya funciona, regression test)
  </behavior>
  <action>
Modificar `_refresh_devices` en `app/ui/add_source_dialog.py` (línea ~724-736):

El bug: al detectar unidades USB, se asigna `"camera": self.tr("Sin nombre")` sin consultar `db.get_dispositivo_for_device()`. Para MTP sí se hace (línea 706). Filtro: USB drives should also check for known names.

1. En el bloque de USB (líneas 724-736), antes de crear el source dict, consultar:
   ```
   saved_usb = db.get_dispositivo_for_device(usb_device_id(drive_path))
   usb_name = saved_usb or self.tr("Sin nombre")
   ```
   Usar `usb_name` en lugar de `self.tr("Sin nombre")` en el dict `"camera"`.

2. Necesitar importar `usb_device_id` en el archivo. Verificar si ya está importado (probablemente no en add_source_dialog.py). Añadir `from app.core.db import usb_device_id` al inicio del archivo o usar `db.usb_device_id` si es accesible. Mejor: añadir `from app.core.db import usb_device_id` al bloque de imports existente.

3. Para MTP, la línea 707 ya hace `name = saved_camera or dev.name or device_id` — sin cambios.

4. Añadir tests en `tests/test_add_source_dialog.py`:
   - Test USB known: mockear `db.get_dispositivo_for_device` devolviendo "Sony A7S" para `usb_device_id("E:\\")`, verificar que la fila USB tiene camera="Sony A7S"
   - Test USB unknown: mockear devolviendo None, verificar camera="Sin nombre"
   - Test MTP known: regression test existente
  </action>
  <verify>
    <automated>python -m pytest tests/test_add_source_dialog.py -x -v 2>&amp;1 | head -30</automated>
  </verify>
  <done>
    USB drives en Añadir origen muestran nombre conocido en el combo cuando existe en el registro. Mantiene "Sin nombre" si no se conoce. Tests pasan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → DB | Nombres de dispositivo escritos por usuario se sanitizan antes de persistir |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260908-f5o-01 | Tampering | QSettings camera/skip_rename_confirm | low | accept | Boolean local, no sensibilidad; si se corrompe, solo suprime popup |
| T-260908-f5o-02 | Information Disclosure | device name persistence | low | accept | Nombre de cámara visible solo en UI local; no se reda |
</threat_model>

<verification>
1. `python -m pytest tests/test_camera_mixin.py tests/test_add_source_dialog.py -x -v` pasa
2. Manual: doble clic en cámara en tabla → popup 3 botones → "Sí" → nombre visible en Añadir origen al próximo launch
3. Manual: "No volver a preguntar" → no aparece popup en siguientes renames
</verification>

<success_criteria>
- `_prompt_rename_camera` persiste nombre en device_settings + known_devices (no solo sesión)
- Popup de confirmación con 3 opciones funciona y "No volver a preguntar" se persiste en QSettings
- Combo de cámara en Añadir origen pre-selecciona nombre conocido
- Suite de tests pasa sin regressions
</success_criteria>

<output>
Create `.planning/quick/260908-f5o-renombrar-dispositivo-en-la-tabla-de-or-/260908-f5o-01-SUMMARY.md` when done
</output>
