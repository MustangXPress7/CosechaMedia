---
phase: 260821-f2k-volcado-selectivo-por-sesion-i-15-i-18-s
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - app/core/ingestor.py
  - app/ui/main_window.py
  - app/ui/selective_dump.py
  - app/i18n/cosechamedia_en.ts
  - tests/test_session_content_modes.py
  - tests/test_source_content.py
  - tests/test_wifi_source.py
autonomous: true
requirements: [I-15, I-18]
estimate:
  tokens: 90000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "El volcado selectivo se controla SOLO por sesión desde el área «Sesiones»: nueva fila «Volcado:» con un switch cíclico Todo → Intervalo → Últimos N días → Todo, ligado a la sesión seleccionada (current_session_id)."
    - "Al caer en Intervalo se abre INMEDIATAMENTE el asistente (modo filtro, calendario con todos los días preseleccionados); aceptar persiste content_mode='interval' + content_filter (la selección completa se normaliza a None = «todo», comportamiento existente de _apply_selection); cancelar NO cambia el modo."
    - "Al caer en Últimos N días se abre INMEDIATAMENTE un diálogo numérico (default 7, rango 1–3650); aceptar persiste {\"window_days\": N} SIN cutoff congelado y el switch muestra «Últimos N días»; el cutoff real se calcula AL INICIAR LA INGESTA desde el último volcado de la sesión (db.get_last_dump_date_for_session) con fallback hoy−N si no hay volcado previo."
    - "Sesiones con device_id wifi:/ftp: tienen el switch deshabilitado fijado en «Todo el contenido» (coherente con el forzado a 'all' que ya hace start_ingest en main_window.py:1564-1569)."
    - "El botón global «Volcado selectivo…» (src_scan_row) y su slot ya no existen; el modo 'dump' del asistente queda sin punto de entrada desde MainWindow pero su maquinaria interna (setup page, radios, dump page) está intacta."
    - "La tabla de orígenes tiene 3 columnas («Ruta de origen», «Cámara», «Opciones»); la columna Opciones alberga toggle carpeta/archivo WiFi (si aplica), toggle rápido/delicado y papelera de eliminar origen; ya no existe botón de resumen/filtro por fila."
    - "Todos los strings nuevos pasan por tr() y están en el contexto MainWindow de app/i18n/cosechamedia_en.ts; la suite completa unittest pasa en Qt offscreen."
  artifacts:
    - app/core/ingestor.py
    - app/ui/main_window.py
    - app/ui/selective_dump.py
    - app/i18n/cosechamedia_en.ts
    - tests/test_session_content_modes.py
    - tests/test_source_content.py
    - tests/test_wifi_source.py
  key_links:
    - "btn_session_dump_mode.clicked → _cycle_session_content_mode() → db.update_session_config(sid, content_mode=..., content_filter=...) → _update_session_dump_switch()"
    - "_cycle (caer en interval) → _open_content_filter(session_id) → SelectiveDumpAssistant(mode='filter', session_id=sid, initial_mode='interval') → Accepted persiste modo+filtro; Rejected no persiste"
    - "start_ingest (main_window.py:1564-1603) lee content_mode/content_filter de la sesión → Ingestor(content_mode=..., content_filter=...) → _init_window_filter conserva window_days → _calculate_window_cutoff calcula cutoff al inicio de ingesta vía db.get_last_dump_date_for_session (fallback hoy−N)"
    - "_matches_filter: content_filter None ⇒ True (el «intervalo - todo» normalizado vuelca todo)"
---

<objective>
Volcado selectivo por sesión (I-15 + I-18): sustituir el control global del volcado selectivo por un switch cíclico de 3 modos dentro del área «Sesiones», con persistencia por sesión, bloqueo WiFi/FTP, ventana real de N días desde el último volcado, tabla de orígenes reducida a 3 columnas («Ruta de origen» / «Cámara» / «Opciones») con papelera integrada, y retirada del botón global «Volcado selectivo…».

**Purpose:** Hoy el filtrado de contenido vive en la columna «Contenido» de la tabla de orígenes (por origen, confuso) más un botón global redundante; el modo ventana no calcula N días reales y el modo no se persiste bien (auditoría AUDIT_I15_I18_2026-08-20: veredicto «Parcial» en ambas ideas). El operador necesita decidir QUÉ volcar por sesión, en un solo sitio, con semántica determinista.

**Output:** Switch por sesión persistente en DB (sessions.content_mode/content_filter ya existen — db.py NO cambia), 3 fixes mínimos en app/core/ingestor.py (única excepción permitida al constraint «no tocar core»), asistente reutilizado vía kwarg aditivo initial_mode, tabla de 3 columnas, i18n sincronizado, tests nuevos + adaptados, suite verde.
</objective>

<execution_context>
@C:/Users/JoanRamon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JoanRamon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
# Decisiones BLOQUEADAS del usuario (no reabrir): quick 260821-f2k, decisiones #1–#5 del planning_context
# Auditoría previa (hallazgos a cerrar): @.planning/AUDIT_I15_I18_2026-08-20.md

# Código anclado (leer rangos, NO archivos enteros)
- app/ui/main_window.py — source_list :444-468 (setColumnCount(4) :445, headers :446-447, resize modes :448-457); botón global «Volcado selectivo…» :482-485; QGroupBox «Sesiones» :490-556 (fila Destino :541-555, sess_post_row :557-560); start_ingest lectura content_mode/filter + forzado wifi/ftp :1564-1603; _refresh_source_list :2200-2234 (col2 :2229, col3 :2231); builder col Contenido :2356-2420; _toggle_wifi_folder_mode :2422-2435; _toggle_device_delicate :2437-2440; builder papelera dedicada :2442-2450; _refresh_sessions_combo :2779-2812; _on_session_selected :2814-2850; slot global del asistente :4201-4217; _open_content_filter(row) :4219-4243
- app/ui/selective_dump.py — __init__ :358-426 (carga modo sesión :377-413, self._build_ui() :423, auto_scan :425-426); _apply_selection :830-873 (normalización selección completa → None :838-845); content_summary :70-113
- app/core/ingestor.py — constantes CONTENT_MODE_* :124-126; wiring filtros en __init__ :168-188; _init_interval_filter :206-210; _init_window_filter :212-220; _calculate_window_cutoff :222-236; _matches_filter :344-368; _check_window_filter :370-379
- app/core/db.py — SIN CAMBIOS: columnas sessions.content_mode/content_filter :124,:152-156; update_session_config allowlist incluye ambas :593-601; get_last_dump_date_for_session :573-591
- app/i18n/cosechamedia_en.ts — contexto MainWindow :375; patrón de mensaje: <message><location filename="../ui/main_window.py" line="N"/><source>ES</source><translation>EN</translation></message>

# Tests acoplados
- tests/test_source_content.py — helper _content_button :24-30; tests columna Contenido :79-100; tests botón global :130-152; tests col borrado :163-185; patrón setUp (swap mw.db + DatabaseManager tmp + MainWindow offscreen) :32-77
- tests/test_wifi_source.py — 3 tests del builder :749-785
- tests/test_ingestor.py — patrón para construir Ingestor con db swapeada (ingestor_module.db) y filtros de fechas :172-207
- tests/test_selective_dump.py — DEBE seguir pasando sin cambios (referencia _setup_page :355, construcciones mode="dump" por defecto)

# Convenciones: CLAUDE.md (tr() obligatorio, tema oscuro/claro intacto, print(f"Error ...") en handlers, sin refactor core)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core mínimo — ventana N días real y «filtro None = todo» en ingestor.py</name>
  <files>app/core/ingestor.py, tests/test_session_content_modes.py</files>
  <behavior>
    - Test 1: Ingestor(content_mode="window", content_filter={"window_days": 3}, session_id=sid) donde sid tiene un files row con verified_at '2026-08-10 12:00:00' → ing._content_filter["cutoff_date"] == "2026-08-07" (último volcado − 3 días) y window_days conservado == 3.
    - Test 2: mismo modo, sesión SIN filas en files → cutoff_date == str(date.today() - timedelta(days=3)) (fallback hoy−N).
    - Test 3: content_filter={"window_days": 5} sin clave cutoff_date → ing._content_filter["window_days"] == 5 (NO se resetea al default 7).
    - Test 4: Ingestor(content_mode="interval", content_filter=None) → ing._matches_filter("ruta/cualquiera.mp4") es True (el «intervalo - todo» normalizado a None vuelca todo).
    - Test 5: content_mode="interval", content_filter={"dates": ["2024-01-02"], "include_nodate": False} → solo matchea archivos cuyo date_key sea 2024-01-02 (patrón de resolución de fecha de tests/test_ingestor.py:172-207).
  </behavior>
  <action>ÚNICOS cambios permitidos en app/core/. Tres fixes quirúrgicos, sin renombrar nada ni tocar el resto del pipeline:
  1. En `_matches_filter` (:344-368): añadir como PRIMERA línea `if self._content_filter is None: return True`. Motivo: cuando el usuario acepta el intervalo con todos los días seleccionados, `_apply_selection` de selective_dump.py normaliza el filtro a None pero deja mode="interval"; hoy esa combinación cae al bloque final de `_matches_filter` y devuelve False (¡saltaría TODO!). Con este guard, filtro None = volcar todo, coherente con la normalización existente.
  2. En `_init_window_filter` (:212-220): la rama else (filtro recibido SIN clave cutoff_date — formato nuevo que guardará el switch) debe construir el dict preservando `window_days` e `include_nodate` del filtro recibido (con `.get(..., self._window_days_default)` / `bool(...)`) en lugar de descartarlos y volver al default 7. La rama if (filtros legacy CON cutoff_date) no se toca.
  3. En `_calculate_window_cutoff` (:222-236): cuando `db.get_last_dump_date_for_session(self.session_id)` devuelve None (sin volcado previo), añadir rama else que fije `self._content_filter["cutoff_date"]` a la fecha de HOY menos `window_days` días (usar el patrón de import local `from datetime import datetime, timedelta` ya presente en ese método; formatear "%Y-%m-%d"). Así `_check_window_filter` nunca recibe cutoff None.
  Tests nuevos en tests/test_session_content_modes.py (crear): clase TestWindowCutoffCore con setUp copiado de tests/test_ingestor.py (QT_QPA_PLATFORM=offscreen arriba del archivo, swap de `mw` NO necesario aquí — solo `ingestor_module.db = DatabaseManager(db_path=tmp)` y restauración en tearDown, tmpdir con shutil.rmtree). Para el «último volcado»: INSERT INTO files (session_id, source_path, dest_path, file_size, md5_hash, status, verified_at) VALUES (str(sid), ..., 'completed', '2026-08-10 12:00:00'). Construir Ingestor con los kwargs mínimos (project_id, destination_root=tmp dest, session_id=sid, content_mode=..., content_filter=...). Commit atómico SOLO código+tests.</action>
  <verify>
    <automated>set QT_QPA_PLATFORM=offscreen&& python -m unittest tests.test_session_content_modes tests.test_ingestor -v && python -c "import io; s=io.open(r'app/core/ingestor.py',encoding='utf-8').read(); assert 'if self._content_filter is None:' in s.split('def _matches_filter')[1].split('def ')[0], 'guard None en _matches_filter'; assert s.count('cutoff_date') >= 4, 'ramas cutoff intactas'"</automated>
  </verify>
  <done>Los 5 tests nuevos pasan; test_ingestor completo sin regresiones; los únicos diffs en app/core/ son los 3 fixes descritos (guard None, else de _init_window_filter preservando valores, fallback hoy−N en _calculate_window_cutoff).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Switch cíclico de volcado por sesión en el área «Sesiones» (+ bloqueo WiFi/FTP)</name>
  <files>app/ui/main_window.py, app/ui/selective_dump.py, tests/test_session_content_modes.py</files>
  <behavior>
    - Test 1 (estado inicial): sesión normal seleccionada → btn_session_dump_mode isEnabled() y text == window.tr("Todo el contenido").
    - Test 2 (all→interval aceptado): con mw.SelectiveDumpAssistant parcheado (fake.exec→QDialog.Accepted, content_filter={"dates":["2024-01-05"],"include_nodate":False}, content_mode="interval", content_text="el 5-1-26"), click → db.get_session(sid)["content_mode"]=="interval" y json.loads(content_filter)==filtro; texto del switch contiene "Intervalo:".
    - Test 3 (cancelar intervalo NO persiste): fake.exec→QDialog.Rejected partiendo de modo "all" → sesión sigue content_mode="all"/content_filter None y el switch vuelve a "Todo el contenido".
    - Test 4 (→window aceptado): mw.QInputDialog.getInt parcheado → (9, True) → sesión content_mode=="window", content_filter=={"window_days": 9} (sin clave cutoff_date), switch text == tr("Últimos %1 días").arg(9).
    - Test 5 (cancelar ventana NO persiste): getInt → (9, False) → sesión sin cambios.
    - Test 6 (window→all): click desde modo window → content_mode=="all", content_filter None.
    - Test 7 (bloqueo WiFi): sesión con device_id="wifi:pairdrop" seleccionada → switch NOT enabled y text == tr("Todo el contenido").
    - Test 8 (refresh por selección): dos sesiones con modos distintos → cambiar currentIndex del combo actualiza el texto del switch.
  </behavior>
  <action>En app/ui/main_window.py:
  1. Nueva fila en el QGroupBox «Sesiones» (decisión #1), entre `sess_box_layout.addLayout(sess_dest_row)` (:555) y `sess_post_row = QHBoxLayout()` (:557): QHBoxLayout `sess_dump_row` con QLabel `self.tr("Volcado:")` + `self.btn_session_dump_mode = QPushButton()` (setCursor(Qt.PointingHandCursor), tooltip `self.tr("Cambiar el volcado de esta sesión: todo / intervalo de fechas / últimos N días")`, clicked.connect(self._cycle_session_content_mode)) + addStretch(); `sess_box_layout.addLayout(sess_dump_row)`; llamar `self._update_session_dump_switch()` justo después para estado inicial.
  2. Nuevo método `_session_content_state(self, sid)`: carga db.get_session(sid); devuelve (mode, filt_dict|None, restricted) donde restricted = device_id empieza por "wifi:" o "ftp:"; filt parseado de content_filter JSON con try/except (TypeError, ValueError) → None (mismo patrón que :2359-2366).
  3. Nuevo método `_update_session_dump_switch(self)`: sin current_session_id → disabled + text tr("Todo el contenido"); restricted → disabled + text tr("Todo el contenido") + tooltip tr("WiFi y FTP siempre vuelcan todo el contenido") (decisión #3, coherente con el forzado de start_ingest :1564-1569); mode "all" → text tr("Todo el contenido") enabled; mode "interval" → text tr("Intervalo: %1").arg(content_summary(filt)) si filt else tr("Intervalo de fechas") enabled; mode "window" → days = int(filt.get("window_days", 7)) → text tr("Últimos %1 días").arg(days) enabled.
  4. Nuevo método `_cycle_session_content_mode(self)` (decisión #2, orden cíclico all→interval→window→all): guard sid None o restricted → return; next = {"all":"interval","interval":"window","window":"all"}[modo_actual]; caer en "interval" → `ok = self._open_content_filter(sid)`; si not ok llamar `_update_session_dump_switch()` (revertir visual, NO persistir nada); caer en "window" → leer días actuales del filtro (default 7, ignorando cutoff_date legacy) y `days, ok = QInputDialog.getInt(self, self.tr("Últimos N días"), self.tr("Número de días a volcar:"), days, 1, 3650)`; si ok → `db.update_session_config(sid, content_mode="window", content_filter=json.dumps({"window_days": int(days)}))` SIN cutoff congelado (semántica elegida: cutoff al iniciar ingesta, Task 1); caer en "all" → `db.update_session_config(sid, content_mode="all", content_filter=None)`; terminar SIEMPRE con `self._update_session_dump_switch()`.
  5. Reconvertir `_open_content_filter(self, row)` (:4219-4243) a firma por sesión `_open_content_filter(self, session_id)` (decisión #4): cargar sesión con db.get_session(session_id) (guard None → return False); construir `SelectiveDumpAssistant(self, source_path=session.get("source_path") or "", mode="filter", session_id=session_id, initial_mode="interval")`; en Accepted persistir AMBOS (las líneas :4237-4240 ya guardan content_mode+content_filter — conservarlas), refrescar switch, mantener el mensaje de status existente con la ruta de la sesión, return True; en otro caso return False.
  6. Refrescos: llamar `self._update_session_dump_switch()` al final de `_on_session_selected` (:2814-2850, TAMBIÉN en la rama sin sesión :2816-2823) y al final de `_refresh_sessions_combo` (:2779-2812, cubre el caso «(Sin sesiones)» con señales bloqueadas).
  En app/ui/selective_dump.py (cambio ADITIVO mínimo): añadir kwarg `initial_mode=None` a `SelectiveDumpAssistant.__init__` (:358); tras el bloque de carga del modo desde sesión (:377-413) y ANTES de `self._build_ui()` (:423), si `initial_mode` es uno de los tres modos y NO `self._session_restricted` → `self.content_mode = initial_mode`. NO tocar setup page, radios ni páginas existentes (test_selective_dump.py las usa y debe seguir verde). El flujo queda: switch → asistente arranca directo a escaneo→calendario (auto_scan ya lo hace en mode="filter") con TODOS los días preseleccionados (`_populate_calendar` ya llama `select_all()` :781) → aceptar normaliza selección completa a None («intervalo - todo») vía `_apply_selection` :838-845.
  Tests: clase TestSessionDumpSwitch en tests/test_session_content_modes.py con setUp copiado de tests/test_source_content.py:32-77 (swap mw.db, proyecto+sesión tmp, MainWindow offscreen, refresh combo). Parchear con mock.patch.object(mw, "SelectiveDumpAssistant") y mock.patch.object(mw.QInputDialog, "getInt"). Commit atómico SOLO código+tests.</action>
  <verify>
    <automated>set QT_QPA_PLATFORM=offscreen&& python -m unittest tests.test_session_content_modes tests.test_selective_dump tests.test_e2e -v && python -c "import io; s=io.open(r'app/ui/main_window.py',encoding='utf-8').read(); assert 'def _cycle_session_content_mode' in s and 'def _update_session_dump_switch' in s and '_open_content_filter(self, session_id)' in s, 'metodos switch'; assert 'sess_box_layout.addLayout(sess_dump_row)' in s, 'fila Volcado en Sesiones'; d=io.open(r'app/ui/selective_dump.py',encoding='utf-8').read(); assert 'initial_mode=None' in d, 'kwarg initial_mode'"</automated>
  </verify>
  <done>Los 8 tests del switch pasan; test_selective_dump y test_e2e sin regresiones; cancelar cualquier diálogo deja la sesión EXACTAMENTE como estaba; WiFi/FTP deshabilitados; cada cambio aceptado persiste content_mode+content_filter en la sesión.</done>
</task>

<task type="auto">
  <name>Task 3: Tabla 3 columnas «Opciones» con papelera integrada + retirada del botón global + i18n</name>
  <files>app/ui/main_window.py, tests/test_source_content.py, tests/test_wifi_source.py, app/i18n/cosechamedia_en.ts</files>
  <behavior>
    - Test A (test_source_content): tras _refresh_source_list(), source_list.columnCount() == 3; horizontalHeaderItem(2).text() == window.tr("Opciones"); cellWidget(0, 2) es un QWidget con botones pero NINGUNO con text "Todo" (ya no hay botón de resumen/filtro); cellWidget(0, 3) is None.
    - Test B (test_source_content): el botón global ya no existe: assertFalse(hasattr(self.window, "btn_selective_dump")) y assertFalse(hasattr(mw.MainWindow, "_open_selective_dump")).
    - Test C (test_source_content): la papelera vive dentro del wrapper de Opciones (findChildren(QPushButton) cuyo toolTip() == window.tr("Eliminar este origen…")); click con QMessageBox.question→Yes elimina el origen de _source_paths manteniendo la sesión; con →No no cambia nada (adaptación de :172-185).
    - Test D (test_wifi_source): _build_options_widget(0, alice_wifi) devuelve wrapper con toggle carpeta/archivo (botón con icono no nulo) y SIN ningún botón de texto "Todo"; FTP y origen normal → wrapper con delicate + papelera, sin botón de resumen (adaptación de :749-785).
  </behavior>
  <action>En app/ui/main_window.py:
  1. RETIRAR el bloque del botón global «Volcado selectivo…» de src_scan_row (:482-485, definición+tooltip+connect+addWidget — decisión #4) y su slot completo (:4201-4217). El modo "dump" del asistente queda sin punto de entrada desde MainWindow; NO borrar maquinaria interna de selective_dump.py (setup page/radios/dump page se quedan — decisión #4 explícita).
  2. Tabla de orígenes 4→3 columnas (decisión #5): en :445 `setColumnCount(3)`; headers :446-447 → `[self.tr("Ruta de origen"), self.tr("Cámara"), self.tr("Opciones")]`; resize modes :450-453 → col0 Interactive, col1 Interactive, col2 Stretch, eliminando el modo Fixed de la antigua 4ª columna y su `resizeSection(3, 36)` (:453,:457); conservar resizeSection(0,100)/(1,60) y setMinimumSectionSize(32).
  3. En `_refresh_source_list` (:2228-2231): col 2 pasa a `self.source_list.setCellWidget(row, 2, self._build_options_widget(row, sess))`; ELIMINAR la línea de la antigua col 3 (:2231).
  4. Reconvertir el builder de la columna Contenido (:2356-2420) en `_build_options_widget(self, row, session)`: misma estructura wrapper QWidget + QHBoxLayout margins(0,0,0,0) spacing 2; ELIMINAR por completo el botón de resumen/filtro (todo el bloque del QPushButton con content_summary/texto/QSS :2374-2389, incluida la rama sin sesión); CONSERVAR tal cual el toggle carpeta/archivo WiFi (:2391-2404) y el toggle rápido/delicado (:2406-2418); AÑADIR al final la papelera movida desde el builder dedicado (:2442-2450): QPushButton IconButton 24×24, icons.apply(btn, "trash", size=16), tooltip self.tr("Eliminar este origen…"), cursor pointing hand, clicked → self._delete_source_at_row(row) — activa también sin sesión (hoy funciona por fila independientemente). Orden final en la fila: [toggle WiFi] [delicate] [papelera]. ELIMINAR el método del builder dedicado de la papelera (:2442-2450) — su lógica vive ahora dentro de _build_options_widget. El import de content_summary SIGUE usándose (label del switch de Task 2) — no quitarlo.
  5. Adaptar tests/test_source_content.py: eliminar el helper `_content_button` (:24-30); reescribir :79-100 según behavior A; sustituir :130-152 por el test de ausencia B; adaptar :163-185 según C (localizar la papelera dentro del wrapper col 2). Adaptar tests/test_wifi_source.py :749-785 según D (llamar a _build_options_widget; los docstrings actualizados a «columna Opciones»).
  6. i18n (Edit puntual siguiendo el patrón de entradas vecinas del contexto MainWindow :375; NO tocar entradas existentes aunque queden obsoletas — las podará pylupdate en el futuro; alternativa válida si el executor prefiere: regenerar con tools/update_translations.ps1): añadir estas 10 entradas source ES → translation EN: "Volcado:" → "Dump:"; "Todo el contenido" → "All content"; "Intervalo de fechas" → "Date range"; "Intervalo: %1" → "Range: %1"; "Últimos %1 días" → "Last %1 days"; "Últimos N días" → "Last N days"; "Número de días a volcar:" → "Number of days to dump:"; "Cambiar el volcado de esta sesión: todo / intervalo de fechas / últimos N días" → "Change this session's dump: all / date range / last N days"; "WiFi y FTP siempre vuelcan todo el contenido" → "WiFi and FTP always dump all content"; "Opciones" → "Options".
  7. Suite COMPLETA verde (incluye test_db, test_updater, test_wifi_source, test_mtp…). Estética intacta: solo widgets existentes reordenados + theme por defecto, sin nuevos colores. Commits atómicos SOLO código+tests+i18n (los docs los commitea el orquestador).</action>
  <verify>
    <automated>set QT_QPA_PLATFORM=offscreen&& python -m unittest discover -s tests -v && python -c "import io; s=io.open(r'app/ui/main_window.py',encoding='utf-8').read(); assert 'btn_selective_dump' not in s, 'boton global retirado'; assert '_open_selective_dump' not in s, 'slot global retirado'; assert '_build_content_button' not in s, 'builder Contenido reconvertido'; assert '_build_remove_source_button' not in s, 'builder papelera dedicado integrado'; assert 'setColumnCount(3)' in s and '_build_options_widget' in s, 'tabla 3 columnas Opciones'; t=io.open(r'app/i18n/cosechamedia_en.ts',encoding='utf-8').read(); assert '&lt;source&gt;Opciones&lt;/source&gt;' in t and '&lt;source&gt;Últimos %1 días&lt;/source&gt;' in t, 'entradas i18n nuevas'"</automated>
  </verify>
  <done>Suite completa unittest discover pasa en offscreen; la tabla tiene exactamente 3 columnas con la papelera dentro de Opciones; no queda ninguna referencia al botón/slot globales ni a los builders sustituidos; las 10 entradas nuevas están en el .ts bajo el contexto MainWindow; el modo "dump" del asistente permanece definido en selective_dump.py sin entry point desde MainWindow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → UI | content_filter JSON leído de SQLite (puede estar corrupto/legacy) alimenta labels y el Ingestor |
| Usuario → QInputDialog | entero libre introducido por el operador llega al cálculo de cutoff |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-F2K-01 | Tampering | parseo de sessions.content_filter (switch + start_ingest) | medium | mitigate | Mantener try/except (TypeError, ValueError) → None = «todo»; un valor corrupto degrada a comportamiento seguro, nunca crash (patrón existente :2359-2366/:1571-1577) |
| T-F2K-02 | DoS | QInputDialog.getInt de N días | low | mitigate | Clamp estricto min=1, max=3650 antes de persistir; int(days) al guardar |
| T-F2K-03 | Repudiation | cutoff de ventana calculado al iniciar ingesta (no congelado) | low | accept | Semántica decidida por el usuario (ventana relativa al último volcado en el momento de volcar); documentada en must_haves y SUMMARY |
</threat_model>

<verification>
1. `python -m unittest discover -s tests -v` con QT_QPA_PLATFORM=offscreen — suite completa verde (incluye test_source_content, test_wifi_source, test_selective_dump, test_e2e, test_ingestor, test_db adaptados o sin cambios).
2. Flujo manual offscreen opcional (checkpoint implícito del executor): crear proyecto+sesión → seleccionarla → click ×3 en el switch verifica ciclo y textos; con sesión wifi: el switch aparece deshabilitado.
3. Greps estructurales incluidos en el `<automated>` de cada tarea (metodos nuevos presentes, identificadores retirados ausentes, setColumnCount(3), entradas i18n).
</verification>

<success_criteria>
- [ ] Switch cíclico por sesión operativo con persistencia inmediata en sessions.content_mode/content_filter (decisiones #1 y #2)
- [ ] Cancelar cualquier diálogo no altera el modo persistido
- [ ] WiFi/FTP bloqueados a «Todo el contenido» en UI (decisión #3)
- [ ] Ventana N días: {\"window_days\": N} sin cutoff congelado; cutoff real al iniciar ingesta desde último volcado, fallback hoy−N (Task 1)
- [ ] Botón global y su slot eliminados; maquinaria interna del asistente intacta (decisión #4)
- [ ] Tabla 3 columnas Ruta/Cámara/Opciones con papelera integrada y sin botón de filtro (decisión #5)
- [ ] i18n .ts sincronizado con las 10 entradas nuevas; strings nuevos vía tr()
- [ ] Sin cambios en app/core/db.py; app/core/ingestor.py limitado a los 3 fixes de Task 1
- [ ] Suite completa verde en offscreen
</success_criteria>

<output>
Create `.planning/quick/260821-f2k-volcado-selectivo-por-sesion-i-15-i-18-s/260821-f2k-SUMMARY.md` when done
</output>
